/**
 * Vitest global setup — registers the `@testing-library/jest-dom` custom
 * matchers (`.toBeInTheDocument()`, `.toHaveAttribute()`, …) for the
 * component tests added in 07-08.
 */
import "@testing-library/jest-dom/vitest";
