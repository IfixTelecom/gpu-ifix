# Phase 06.7: Primary Pod TTS — swap embed for Kani-TTS-2-pt GPU + move embed to 24/7 CPU - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-19
**Phase:** 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
**Areas discussed:** Embed 24/7 CPU host, TTS server shim, Voice cloning workflow, TTS tier-0 routing

---

## Embed 24/7 CPU host

### Host
| Option | Description | Selected |
|--------|-------------|----------|
| vps-ifix-vm | Co-located w/ gateway+redis+postgres, busiest host | |
| worker-vm | Isolated, RabbitMQ+workers, NAT hop | |
| **n8n-ia-vm (free-text)** | VMID 101, 10.10.10.20, future converseai prod host; operator frees resources or spins dedicated VM | ✓ |

### Embed model scope (factual challenge from user)
| Option | Description | Selected |
|--------|-------------|----------|
| Serving engine only (keep bge-m3) | Respect ROADMAP lock | |
| Evaluate switching the model | Bigger scope | ✓ |

**User's challenge:** "todo o RAG atual no converseai usa uma LLM do OpenAI, valide." → **VALIDATED TRUE.** converseai-v4 RAG uses OpenAI text-embedding-3-small (1536-dim), called directly, NOT bge-m3. ROADMAP premise corrected.

### Strategy after correction
| Option | Description | Selected |
|--------|-------------|----------|
| Re-embed with bge-m3 self-hosted | Drop OpenAI dependency | |
| Keep OpenAI for RAG, bge-m3 only for new apps | No re-embed, two vector spaces | |
| Research best self-host model + re-embed | Research-informed model choice | ✓ |

### Scope split
| Option | Description | Selected |
|--------|-------------|----------|
| 06.7 delivers upstream + model choice; re-embed in Phase 8 | Respects repo/phase boundary | ✓ |
| 06.7 does everything incl. re-embed | Cross-repo, riskier | |

### Wiring
| Option | Description | Selected |
|--------|-------------|----------|
| Static tier-0 upstream | Always-on, leaves override map | ✓ |
| Keep in pod override map | No benefit | |

**Notes:** Operator added `mistral-embed` to the candidate list mid-discussion (validate self-host vs API-only). Embed-model decision left OPEN for researcher.

---

## TTS server shim

| Question | Selected |
|----------|----------|
| Expose Kani | **Own thin FastAPI shim (~150 LoC)** (vs community wrapper) |
| API contract | **OpenAI /v1/audio/speech** (vs Kani-native) |
| Piper fallback | **Piper assumes off-peak + pod failure**, degraded to default voice (no cloning) (vs no TTS fallback) |

---

## Voice cloning workflow

| Question | Selected |
|----------|----------|
| Clone management | **Managed voices (voice_id + S3)** (vs inline ref audio per request) |
| Voice catalog store | **Postgres ai_gateway + S3** (vs decide later) |
| Multi-tenant | **Isolated per tenant (API key)** (vs global shared) |

---

## TTS tier-0 routing

| Question | Selected |
|----------|----------|
| TTS into tier-0 | **Add 'tts' to OverrideTier0** (embed→static) (vs TTS static — rejected, pod IP changes) |
| Gateway changes | **New route + proxy + probe + quota class** (vs reuse STT pipeline — rejected) |
| Pitfall #11 | **Researcher/planner fix spanning llm/stt/tts** (vs ignore — rejected) |

---

## Claude's Discretion

- VRAM headroom on 5090 with Kani as 4th GPU model
- Output audio format/sample-rate defaults
- TTS latency SLO + Piper fallback trigger threshold
- supervisord directives for Kani child
- Exact env-var names for embed static upstream + TTS config

## Deferred Ideas

- Corpus re-embed + converseai OpenAI→gateway embed cutover + pgvector dim migration → **Phase 8 (INT-01)**
- Embed model switch as a corpus-wide decision folded into the Phase 8 re-embed
