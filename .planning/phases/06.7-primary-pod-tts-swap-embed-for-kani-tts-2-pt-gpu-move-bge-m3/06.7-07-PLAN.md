---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 07
type: execute
wave: 2
depends_on: ["06.7-03", "06.7-04", "06.7-05"]
files_modified:
  - gateway/internal/proxy/tts.go
  - gateway/internal/proxy/voices.go
  - gateway/cmd/gateway/main.go
  - gateway/internal/config/config.go
autonomous: true
requirements: [D-06, D-08, D-09, D-10, D-12]
must_haves:
  truths:
    - "POST /v1/audio/speech is served through the dispatcher (tier-0 dynamic override -> tier-1 Piper fallback per GATE 3), passing JSON in and binary audio out"
    - "POST/GET/DELETE /v1/audio/voices manage voice clones: upload persists ONLY a reference WAV to MinIO + a catalog row (no .pt push — pod regenerates lazily per the Plan 05 contract), listing/deletion are tenant-scoped"
    - "DELETE removes the catalog row AND the S3 object with defined S3-delete-failure behavior; upload is idempotent on client retry"
    - "A voice handler NEVER reads tenant from the request body — always from auth.MustFromContext"
    - "Config exposes UpstreamTTSURL + UpstreamTTSPiperURL + tts limits (max text, max upload bytes) as explicit env vars"
    - "The tier-1 Piper fallback is implemented per the GATE 3 contract (adapter conversion | voice-api route | controlled 503)"
  artifacts:
    - path: "gateway/internal/proxy/tts.go"
      provides: "JSON->binary TTS reverse proxy + GATE-3 Piper fallback"
      contains: "NewTTSProxy"
    - path: "gateway/internal/proxy/voices.go"
      provides: "voice CRUD handlers (MinIO + sqlc)"
      contains: "MustFromContext"
    - path: "gateway/cmd/gateway/main.go"
      provides: "/v1/audio/speech + /v1/audio/voices mounts"
      contains: "/v1/audio/speech"
  key_links:
    - from: "/v1/audio/speech mount"
      to: "dispatcher tier-0(tts)->tier-1(piper per GATE 3)"
      via: "same dispatcher wrap as chat/audio"
      pattern: "/v1/audio/speech"
    - from: "voices upload"
      to: "MinIO bucket ai-gateway + ai_gateway.voices (WAV only, no .pt)"
      via: "S3 PutObject keyed by UUID + CreateVoice; pod regenerates .pt lazily"
      pattern: "PutObject"
---

<objective>
Gateway TTS request pipeline: the JSON->binary `/v1/audio/speech` reverse proxy (mirrors the structure of audio.go but the opposite data direction per D-12), the `/v1/audio/voices` CRUD handlers (first Go S3 consumer — MinIO + sqlc, tenant-isolated per D-10), config fields, and main.go mounting. TTS routes go through the dispatcher so tier-0(pod)->tier-1(Piper) fallback works like chat.

OWNED Plan 02 stubs (must become non-skipped + asserting before this plan completes — REVIEWS action #3): `TestTTSProxy_JSONToBinaryAudio`, `TestTTSProxy_ErrorEnvelope`, `TestTTSProxy_PiperFallback_AdapterConverts` (tts_test.go); `TestVoices_CreateUploadsToS3AndReturnsVoiceID`, `TestVoices_TenantIsolation_ListOnlyOwn`, `TestVoices_RejectsOversizeWAV`, `TestVoices_S3KeyFromUUIDNotInput`, `TestVoices_DeleteRemovesRowAndS3Object`, `TestVoices_UnsupportedResponseFormatReturns400` (voices_test.go / tts_test.go).

VOICE PERSISTENCE CONTRACT (REVIEWS action #1): upload persists the reference WAV to S3 at key `<S3_VOICE_PREFIX>/<voice_id>.wav` + writes the catalog row. It does NOT push a `.pt`. The pod shim (Plan 05) regenerates the speaker embedding lazily on first speech for an unknown voice_id (cache miss -> fetch WAV from S3 -> regenerate -> cache). This handler's only durable-storage job is WAV + row.

PIPER FALLBACK CONTRACT (REVIEWS action #2): implement the tier-1 Piper path EXACTLY per GATE 3 (Plan 01) — one of adapter (ulaw 8kHz -> target format conversion) / voice-api route / controlled 503. No re-deciding here; read the GATE 3 contract and implement it verbatim.

Purpose: Expose the OpenAI TTS + voice-clone surface to clients.
Output: tts.go + voices.go + config fields + main.go mounts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-RESEARCH.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-PATTERNS.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-WAVE0-GATES.md

<config_surface>
## Gateway TTS config env (REVIEWS action #6 — explicit, enumerated)
Add to config.Config (mirror UpstreamEmbedURL loading at config.go:150-203):
- UpstreamTTSURL          <- UPSTREAM_TTS_URL          (tier-0 placeholder, overwritten by reconciler override)
- UpstreamTTSPiperURL     <- UPSTREAM_TTS_PIPER_URL    (tier-1 Piper fallback target per GATE 3)
- TTSMaxInputChars        <- TTS_MAX_INPUT_CHARS        (synth text DoS cap; default e.g. 4000)
- VoiceMaxUploadBytes     <- VOICE_MAX_UPLOAD_BYTES     (reference WAV upload DoS cap; default e.g. 10485760)
- S3VoicePrefix           <- S3_VOICE_PREFIX            (S3 key prefix for reference WAVs; default "voices"; MUST match the pod shim KANI_S3_VOICE_PREFIX)
MinIO creds already loaded: MinioEndpoint/MinioBucket/MinioAccessKey/MinioSecretKey (config.go:199-202).
</config_surface>

<interfaces>
From gateway/internal/proxy/audio.go:19-40 — NewAudioProxy(upstreamURL, log, interceptors...) shape (Director=BuildDirector(u), Transport with ResponseHeaderTimeout, ErrorHandler, ModifyResponse=ComposeInterceptors).
From gateway/internal/proxy/director.go:39-62 — BuildDirector(u) strips Authorization/X-API-Key/Cookie/Proxy-Authorization, sets authoritative X-Request-ID. REUSE.
From gateway/internal/proxy/errors.go:35-48 — ErrorHandler(name, log) -> httpx.WriteOpenAIError 502.
From gateway/internal/auth/context.go:13-39 — AuthContext{TenantID,APIKeyID,DataClass,KeyPrefix}; MustFromContext(ctx).
From gateway/internal/config/config.go:199-202 — MinioEndpoint/MinioBucket/MinioAccessKey/MinioSecretKey already loaded.
From gateway/internal/db/gen/voices.sql.go (Plan 04) — CreateVoice/ListVoicesByTenant/GetVoiceForTenant/DeleteVoiceForTenant.
From gateway/cmd/gateway/main.go — px struct (chat/embed/audio handlers) :78-91; proxy build :554-561; mounts :1136-1149; http.MaxBytesHandler :985.
Dispatcher wrap (chatHandler pattern) in gateway/internal/proxy/dispatcher.go — produces a handler with tier-0->tier-1 breaker fallback. TTS uses the same wrap (NOT the raw single-upstream proxy).
GATE 3 contract: 06.7-WAVE0-GATES.md "## GATE 3 — Piper fallback contract" — read the named option + (if adapter) the 5 sub-fields before implementing the tier-1 path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tts.go JSON->binary proxy + GATE-3 Piper fallback + config TTS fields</name>
  <read_first>
    - gateway/internal/proxy/tts_test.go (Plan 02: TestTTSProxy_JSONToBinaryAudio, TestTTSProxy_ErrorEnvelope, TestTTSProxy_PiperFallback_AdapterConverts, TestVoices_UnsupportedResponseFormatReturns400 if placed here)
    - gateway/internal/proxy/audio.go (:19-40), director.go (:39-62), errors.go (:35-48)
    - gateway/internal/proxy/dispatcher.go (tier-0->tier-1 wrap)
    - gateway/internal/config/config.go (:150-203 env loading)
    - 06.7-WAVE0-GATES.md GATE 3 (the named Piper contract + adapter sub-fields)
    - 06.7-RESEARCH.md §Pattern 3 (TTS proxy, ResponseHeaderTimeout) + §Anti-Patterns (do NOT reuse audio.go body) + §Pitfall 5 (Piper ulaw)
    - 06.7-PATTERNS.md tts.go section
    - <config_surface> above
  </read_first>
  <behavior>
    - NewTTSProxy(upstreamURL, log, interceptors...) returns a ReverseProxy that POSTs JSON to upstream /v1/audio/speech and streams the binary audio response back with its Content-Type preserved
    - client Authorization/X-API-Key are stripped (BuildDirector); X-Request-ID is the gateway's
    - upstream unreachable -> OpenAI-shaped 502 via ErrorHandler("tts", log)
    - tier-0 breaker OPEN -> tier-1 Piper path runs per GATE 3 (if adapter: ulaw 8kHz converted to the GATE-3 target format with the GATE-3 Content-Type; if voice-api route: proxy to that URL; if 503: deterministic OpenAI-shaped 503)
    - response_format not in the supported set -> clean 400 (not a 5xx)
  </behavior>
  <action>
    Create `gateway/internal/proxy/tts.go` with `NewTTSProxy` mirroring the CONSTRUCTOR structure of `NewAudioProxy` (parse/validate URL, Director=BuildDirector(u), ErrorHandler("tts",log), ModifyResponse=ComposeInterceptors) but set `ResponseHeaderTimeout` to ~60s for long synth (RESEARCH §Pattern 3) and default (buffered) FlushInterval for non-streaming; if `stream_format:sse` is supported later, FlushInterval=-1. Do NOT copy audio.go's multipart body handling — the data direction is opposite. Implement the tier-1 Piper fallback EXACTLY per GATE 3: if `adapter`, add a ModifyResponse/interceptor on the tier-1 path doing the GATE-3 conversion (ulaw 8kHz -> target, set the GATE-3 Content-Type, JSON {input,voice} -> Piper form {text,voice}); if `voice-api route`, point tier-1 at UpstreamTTSPiperURL with no conversion; if `503`, the tier-1 handler emits a deterministic OpenAI-shaped 503. Reject unsupported `response_format` with a clean 400. Add the <config_surface> fields to config.Config (mirror UpstreamEmbedURL loading at config.go:150-203). UNSKIP the owned tts_test stubs and make them assert real behavior.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/proxy/ -run TTSProxy -count=1 -v 2>&1 | grep -q -- '--- PASS' && grep -q 'UpstreamTTSURL' internal/config/config.go && grep -q 'UPSTREAM_TTS_PIPER_URL' internal/config/config.go && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func NewTTSProxy' gateway/internal/proxy/tts.go` and it uses `BuildDirector` + `ErrorHandler("tts"`
    - `grep -Eq 'UpstreamTTSURL|UpstreamTTSPiperURL|TTSMaxInputChars|VoiceMaxUploadBytes|S3VoicePrefix' gateway/internal/config/config.go` (all 5 config fields present)
    - the tier-1 Piper path matches the GATE 3 named option (grep for the conversion/route/503 implementation referencing the GATE-3 decision)
    - HARD (stub enforcement, REVIEWS #3): `TestTTSProxy_JSONToBinaryAudio`, `TestTTSProxy_ErrorEnvelope`, `TestTTSProxy_PiperFallback_AdapterConverts` show `--- PASS`, none `--- SKIP`
    - `cd gateway && go build ./...` exits 0
  </acceptance_criteria>
  <done>TTS JSON->binary proxy + GATE-3 Piper fallback + 5 config fields exist; TTSProxy + fallback stubs unskipped and PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: voices.go CRUD (MinIO + sqlc, tenant-isolated) + cleanup + idempotency + S3 client wiring</name>
  <read_first>
    - gateway/internal/proxy/voices_test.go (Plan 02: Create, TenantIsolation, RejectsOversizeWAV, S3KeyFromUUIDNotInput, DeleteRemovesRowAndS3Object, UnsupportedResponseFormatReturns400)
    - gateway/internal/auth/context.go (:13-39 MustFromContext)
    - gateway/internal/proxy/embeddings.go (handler wiring style) + httpx.WriteOpenAIError
    - gateway/internal/db/gen/voices.sql.go (Plan 04 generated CRUD)
    - 06.7-RESEARCH.md §Security Domain (V4/V5/V12) + §Voice Cloning Workflow + 06.7-PATTERNS.md voices.go section
    - <config_surface> (S3VoicePrefix, VoiceMaxUploadBytes) + the <voice_persistence_contract> in Plan 05
  </read_first>
  <behavior>
    - POST /v1/audio/voices (multipart WAV + label) -> generate UUID voice_id, PutObject the WAV to MinIO at key <S3VoicePrefix>/<voice_id>.wav, CreateVoice row with tenant from context, returns {voice_id, label}. NO .pt is generated or pushed (pod regenerates lazily — REVIEWS #1).
    - upload idempotency: if a client retries (same idempotency key / same content for same tenant+label), do not create a duplicate orphan S3 object + row — define the idempotency strategy (e.g. accept an Idempotency-Key header, or upsert on (tenant_id,label)); document it.
    - GET /v1/audio/voices -> ListVoicesByTenant(ctx tenant) only; never another tenant's rows
    - DELETE /v1/audio/voices/{id} -> GetVoiceForTenant then delete the S3 object AND DeleteVoiceForTenant(id, ctx tenant); deleting another tenant's id is a no-op/404. S3-delete-failure behavior: log + return 200 if the row deleted but S3 delete failed (orphan object reaped later) OR return 502 — pick one, document it (recommend: delete row only after S3 delete succeeds; on S3 failure return 502 and keep the row so retry is safe).
    - oversize WAV upload rejected (MaxBytes = VoiceMaxUploadBytes); voice label/id validated (regex, anti path-traversal)
    - unsupported response_format on speech -> 400 (assert here if the stub lives in voices_test)
  </behavior>
  <action>
    Create `gateway/internal/proxy/voices.go` — the first Go S3 consumer (use `github.com/aws/aws-sdk-go-v2` S3 client, or MinIO Go SDK; build the client at boot from config.Minio* creds). Handlers parse multipart/JSON, derive the S3 object key from a freshly generated UUID + S3VoicePrefix (NEVER from user input — Security V12 / path-traversal), call `CreateVoice/ListVoicesByTenant/GetVoiceForTenant/DeleteVoiceForTenant` always passing `auth.MustFromContext(r.Context()).TenantID` (D-10 — NEVER from body), and write errors via `httpx.WriteOpenAIError`. Apply `http.MaxBytesHandler` (VoiceMaxUploadBytes) to the upload route. Validate `label`/`voice_id` with the regex `^[A-Za-z0-9_\-\.]+$` and reject `..`. Implement DELETE cleanup semantics (delete S3 object + row, with the documented S3-delete-failure behavior) and upload idempotency (documented strategy) per the behavior block. The upload persists ONLY the reference WAV + row — the pod shim regenerates the speaker embedding lazily (Plan 05 contract); document this handoff in a comment (no .pt is pushed by the gateway). UNSKIP the owned voices_test stubs and make them assert real behavior (mock S3 via an interface).
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/proxy/ -run Voices -count=1 -v 2>&1 | grep -q -- '--- PASS' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'MustFromContext' gateway/internal/proxy/voices.go` and no occurrence of reading tenant from request body
    - `grep -Eq 'NewString|uuid|UUID' gateway/internal/proxy/voices.go` (UUID-derived S3 key) and the key uses S3VoicePrefix
    - DELETE deletes both S3 object + row with a documented failure mode; upload has a documented idempotency strategy (grep for an Idempotency or upsert comment)
    - HARD (stub enforcement, REVIEWS #3): `TestVoices_TenantIsolation_ListOnlyOwn`, `TestVoices_S3KeyFromUUIDNotInput`, `TestVoices_RejectsOversizeWAV`, `TestVoices_DeleteRemovesRowAndS3Object` show `--- PASS`, none `--- SKIP`
    - `cd gateway && go test ./internal/proxy/ -run Voices -count=1` PASS
  </acceptance_criteria>
  <done>Voice CRUD persists WAV-only to MinIO + tenant-scoped catalog with delete-cleanup + idempotency; isolation/path-traversal/DoS/delete tests unskipped and PASS.</done>
</task>

<task type="auto">
  <name>Task 3: main.go wiring — build TTS dispatcher + voices handlers + mount routes + fallback integration test</name>
  <read_first>
    - gateway/cmd/gateway/main.go (px struct :78-91, proxy build :554-561, mounts :1136-1149, MaxBytes :985)
    - gateway/internal/proxy/dispatcher.go (chatHandler tier-0->tier-1 wrap)
    - gateway/internal/integration_test/ (existing dispatcher fallback test pattern, if present)
    - 06.7-PATTERNS.md main.go section
    - 06.7-WAVE0-GATES.md GATE 3 (Piper contract — the fallback integration test asserts this path)
  </read_first>
  <action>
    Add `tts http.Handler` + voices handlers (create/list/delete) to the `px` struct. Build the TTS dispatcher handler the SAME way chat/audio are built (tier-0 dynamic override -> tier-1 Piper fallback via the dispatcher — NOT the raw single-upstream proxy; wrap NewTTSProxy with the dispatcher, tier-1 = UpstreamTTSPiperURL per GATE 3). Build the S3 client + voices handlers from config.Minio* + the voices queries. Mount: `POST /v1/audio/speech` -> tts dispatcher handler; `POST /v1/audio/voices` -> voicesCreate; `GET /v1/audio/voices` -> voicesList; `DELETE /v1/audio/voices/{id}` -> voicesDelete — all behind the existing auth middleware so MustFromContext is populated, with MaxBytes (VoiceMaxUploadBytes) on the upload route. Add ONE integration test (REVIEWS lower-priority) for the fallback path: tier-0 TTS fails / breaker opens -> tier-1 Piper receives the request (assert the request reaches the Piper upstream / the GATE-3 path runs). Plan 09 covers the live version; this is the local fake-upstream version.
  </action>
  <verify>
    <automated>cd gateway && grep -q '/v1/audio/speech' cmd/gateway/main.go && grep -q '/v1/audio/voices' cmd/gateway/main.go && go build ./... && go test ./internal/proxy/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '/v1/audio/speech' gateway/cmd/gateway/main.go` and `grep -q '/v1/audio/voices' gateway/cmd/gateway/main.go`
    - TTS route uses the dispatcher (tier-0->tier-1), not the raw proxy
    - voices routes mounted behind auth middleware with MaxBytes on upload
    - a fallback integration test exists asserting tier-0 fail -> tier-1 Piper receives the request
    - `cd gateway && go build ./...` exits 0
  </acceptance_criteria>
  <done>TTS speech + voices routes mounted and wired through dispatcher/auth; fallback path covered by an integration test; gateway builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /v1/audio/speech | untrusted JSON (input, voice) crosses into the gateway then the pod |
| client -> /v1/audio/voices | untrusted multipart WAV upload -> MinIO + catalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-13 | Info Disclosure | cross-tenant voice access | mitigate | every voices handler uses auth.MustFromContext tenant; sqlc queries filter tenant_id; body tenant ignored (ASVS V4 / D-10) |
| T-06.7-14 | Tampering | S3 key path traversal via voice_id/label | mitigate | S3 key derived from generated UUID + S3VoicePrefix; label/voice_id regex `^[A-Za-z0-9_\-\.]+$` + reject `..` (ASVS V5/V12) |
| T-06.7-15 | Denial of Service | oversize WAV upload | mitigate | http.MaxBytesHandler (VoiceMaxUploadBytes) on /v1/audio/voices + TTSMaxInputChars cap on /v1/audio/speech |
| T-06.7-16 | Spoofing | client auth header forwarded upstream | mitigate | BuildDirector strips Authorization/X-API-Key/Cookie before proxying to the pod |
| T-06.7-SC | Tampering | aws-sdk-go-v2 / minio-go install | mitigate | mainstream Go module (AWS/MinIO official); pin via go.mod; matches CLAUDE.md @aws-sdk/client-s3 ecosystem standard |
</threat_model>

<verification>
- `cd gateway && go test ./internal/proxy/... -count=1` green; owned stubs unskipped and PASS
- `cd gateway && go build ./...` exits 0
- routes mounted; tenant isolation + path-traversal + DoS + delete-cleanup + fallback covered by tests
</verification>

<success_criteria>
Clients can POST /v1/audio/speech (tier-0 pod -> tier-1 Piper fallback per GATE 3) and manage their own voices via /v1/audio/voices with full tenant isolation, WAV-only persistence, delete cleanup, and idempotent upload.
</success_criteria>

<output>
Create `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-07-SUMMARY.md` when done.
</output>
