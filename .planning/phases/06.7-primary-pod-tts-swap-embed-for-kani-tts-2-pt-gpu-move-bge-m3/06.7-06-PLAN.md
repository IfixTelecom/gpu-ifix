---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 06
type: execute
wave: 1
depends_on: ["06.7-01"]
files_modified:
  - deploy/embed/docker-compose.yml
  - deploy/embed/README.md
autonomous: false
requirements: [D-01, D-02, D-03]
user_setup:
  - service: n8n-ia-vm Infinity embed
    why: "24/7 CPU embed so RAG survives off-peak; static tier-0 upstream"
    dashboard_config:
      - task: "Deploy Infinity embed Compose stack on the chosen host (n8n-ia-vm or dedicated VM per GATE 4) via Portainer"
        location: "Portainer -> stacks"
      - task: "Set gateway env UPSTREAM_EMBED_URL -> http://<embed-host>:<port> in the ai-gateway-dev Portainer stack"
        location: "Portainer -> ai-gateway-dev stack env"
must_haves:
  truths:
    - "An Infinity service serving intfloat/multilingual-e5-large (1024-dim) runs 24/7 on CPU at the GATE-4-chosen host and answers POST /embeddings"
    - "UPSTREAM_EMBED_URL points at that host so the gateway resolves embed to a static tier-0 upstream (no FSM/override involvement)"
    - "The gateway's existing /v1/embeddings route reaches the Infinity service (which serves /embeddings) — path rewrite verified, not only a direct /embeddings curl"
    - "The embed service survives the primary pod sleeping (off-peak) — RAG keeps working"
  artifacts:
    - path: "deploy/embed/docker-compose.yml"
      provides: "Infinity CPU embed stack"
      contains: "michaelf34/infinity"
  key_links:
    - from: "Infinity /embeddings on embed host"
      to: "gateway Resolve(embed,0) via UPSTREAM_EMBED_URL"
      via: "static tier-0 row local-embed (seed 0008); gateway /v1/embeddings -> upstream /embeddings"
      pattern: "UPSTREAM_EMBED_URL"
---

<objective>
Stand up the 24/7 CPU embed service so RAG survives the peak-only pod sleeping, and wire it as a static tier-0 upstream (D-03). Uses Infinity (same engine already known from the pod) serving the GATE-4-locked model `intfloat/multilingual-e5-large` (1024-dim) on the GATE-4-chosen host.

GATE: This plan MUST NOT start until Plan 01 GATE 4 (embed host + model) is recorded. The host (co-located n8n-ia-vm vs dedicated VM) comes from GATE 4.

NOTE (D-04 scope): This plan delivers the SERVING service + static wiring ONLY. The converseai corpus re-embed, the OpenAI->gateway cutover, and the pgvector dimension migration are Phase 8 (INT-01) — explicitly NOT planned here.

PATH MISMATCH (REVIEWS consensus action #5 — Codex): Infinity serves `/embeddings`, but the gateway's client-facing route is `/v1/embeddings`. The gateway's embed proxy must reach the service through the existing `/v1/embeddings` route — a path rewrite from `/v1/embeddings` to `/embeddings` may be required at the embed upstream. This plan VERIFIES the gateway path actually reaches Infinity, not only a bare `/embeddings` curl against the service.

Purpose: Decouple embed from the GPU pod schedule.
Output: Infinity embed Compose stack + deploy README + UPSTREAM_EMBED_URL wiring + gateway-path verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-RESEARCH.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-PATTERNS.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-WAVE0-GATES.md

<interfaces>
Infinity child pattern (pod): pod/primary/supervisord.conf [program:infinity] — same engine michaelf34/infinity 0.0.77, SHA-pinned in 06.6-WAVE0-GATES.md.
Command shape: infinity_emb v2 --port <p> --model-id intfloat/multilingual-e5-large (+ ONNX int8 per RESEARCH §Standard Stack).
Infinity serves POST /embeddings (NOT /v1/embeddings — verified UAT 14: OpenAPI confirms /embeddings).
Gateway embed route: /v1/embeddings (client-facing). The embed upstream proxy may need to rewrite /v1/embeddings -> /embeddings; check gateway/internal/proxy/embeddings.go director/path handling for whether path is preserved 1:1 (it is, via BuildDirector) — if so, either (a) configure Infinity to serve /v1/embeddings, or (b) add a path rewrite for the embed upstream. Pick one and document it.
Static embed row already seeded: 0008_seed_upstreams.sql local-embed (embed,0,UPSTREAM_EMBED_URL).
Host candidates: n8n-ia-vm (10.10.10.20, VMID 101) or dedicated VM per GATE 4.
Deploy flow: Portainer stack + GitHub repo (CLAUDE.md Dev Environment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infinity embed Compose stack + deploy README (+ path-rewrite decision)</name>
  <read_first>
    - pod/primary/supervisord.conf [program:infinity] (engine + flags analog)
    - gateway/internal/proxy/embeddings.go + gateway/internal/proxy/director.go (does the embed proxy preserve path 1:1? — determines whether a rewrite is needed)
    - 06.7-RESEARCH.md §Standard Stack (embed) + §Architecture Patterns Pattern 4 + §Environment Availability (n8n-ia-vm sizing) + 06.7-WAVE0-GATES.md GATE 4 (host + model lock + served path note)
    - 06.7-PATTERNS.md "embed service — Docker Compose" section
    - CLAUDE.md Dev Environment (Portainer + ghcr deploy flow)
  </read_first>
  <action>
    Create `deploy/embed/docker-compose.yml` running `michaelf34/infinity:0.0.77` (SHA-pin referenced from 06.6-WAVE0-GATES.md) on CPU with `--model-id intfloat/multilingual-e5-large` and ONNX int8 (RESEARCH §Standard Stack), `restart: unless-stopped` so it stays 24/7, a published port, healthcheck on `/health`, and a model-cache volume to avoid re-download on restart. Resolve the path mismatch (REVIEWS #5): determine whether the gateway embed proxy preserves the request path 1:1 (it does via BuildDirector) and therefore whether the service must answer `/v1/embeddings` or the upstream needs a `/v1/embeddings -> /embeddings` rewrite. Pick the approach (configure Infinity routing if it supports a path prefix, else document the gateway-side rewrite needed) and write it into the README. Write `deploy/embed/README.md` documenting: target host (from GATE 4), Portainer stack deploy steps, the served endpoint (`/embeddings`, 1024-dim) AND how the gateway `/v1/embeddings` route reaches it (the rewrite decision), and the exact `UPSTREAM_EMBED_URL` value the gateway stack must use. Do NOT use a GPU device reservation — this is CPU 24/7 (D-03). Per D-04, README explicitly states corpus re-embed / OpenAI cutover / pgvector dim migration are Phase 8, not here.
  </action>
  <verify>
    <automated>grep -q 'michaelf34/infinity' deploy/embed/docker-compose.yml && grep -q 'multilingual-e5-large' deploy/embed/docker-compose.yml && grep -q 'restart:' deploy/embed/docker-compose.yml && grep -q 'UPSTREAM_EMBED_URL' deploy/embed/README.md && grep -iq 'phase 8' deploy/embed/README.md && grep -Eq '/v1/embeddings|path rewrite|rewrite' deploy/embed/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/embed/docker-compose.yml` runs SHA-pinned Infinity 0.0.77 serving multilingual-e5-large on CPU with restart policy + healthcheck + cache volume; no GPU reservation
    - `deploy/embed/README.md` documents host (GATE 4), Portainer deploy, the `/embeddings` 1024-dim endpoint, the `/v1/embeddings` -> `/embeddings` reachability decision, and the UPSTREAM_EMBED_URL value
    - README states the Phase 8 deferral (D-04) explicitly
  </acceptance_criteria>
  <done>Embed Compose stack + deploy README exist, targeting the GATE-4 host with the locked model, and document how the gateway /v1/embeddings route reaches the service.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy embed stack + verify 24/7 + verify gateway /v1/embeddings reaches it + wire UPSTREAM_EMBED_URL</name>
  <read_first>
    - deploy/embed/README.md (from Task 1)
    - 06.7-WAVE0-GATES.md GATE 4 (host decision)
    - CLAUDE.md (ssh n8n-ia-vm, Portainer)
  </read_first>
  <action>
    Operator deploys the `deploy/embed/docker-compose.yml` stack on the GATE-4 host via Portainer (or `ssh n8n-ia-vm docker compose up -d` for the dev cycle). Confirm the service answers `curl -s http://<embed-host>:<port>/embeddings` with a 1024-dim vector for a sample input, and that it stays up (restart policy). THEN — the key REVIEWS #5 check — confirm the gateway reaches it through the CLIENT-FACING route: `curl -s -H 'X-API-Key: <key>' https://<gateway>/v1/embeddings -d '{"input":"ping","model":"e5"}'` returns a 1024-dim vector (this exercises the static tier-0 row + any path rewrite, NOT a direct hit on the service). Set `UPSTREAM_EMBED_URL` in the ai-gateway-dev Portainer stack to that host and confirm `gatewayctl upstreams` (or the gateway probe) resolves embed tier-0 to the new host — NOT a pod IP. This is a human-action gate: it requires Portainer access + a live host the planner/CI cannot reach autonomously.
  </action>
  <verify>
    <automated>MISSING — operator runs the direct curl + the gateway /v1/embeddings curl + gatewayctl checks on the live host; record output in 06.7-06-SUMMARY.md</automated>
    <human-check>Direct curl http://&lt;embed-host&gt;:&lt;port&gt;/embeddings returns 1024-dim; gateway curl https://&lt;gateway&gt;/v1/embeddings ALSO returns 1024-dim (path rewrite works); service persists across a restart; gateway embed tier-0 resolves to the embed host</human-check>
  </verify>
  <acceptance_criteria>
    - embed service answers `/embeddings` directly with a 1024-dim vector on the chosen host
    - the gateway `/v1/embeddings` client route returns a 1024-dim vector (path rewrite / reachability verified end-to-end, not just direct)
    - `UPSTREAM_EMBED_URL` in the gateway stack points to the embed host (not a pod IP)
    - service confirmed 24/7 (restart policy survives a container restart)
  </acceptance_criteria>
  <done>Embed service live on CPU host; gateway resolves embed statically AND reaches it through /v1/embeddings; verified output recorded in summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> embed host | static tier-0 embed requests cross to the CPU host |
| embed host on shared Swarm | Infinity co-located with n8n/rabbit/redis if not dedicated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-11 | Denial of Service | Infinity RAM pressure on shared host | mitigate | GATE 4 sized footprint; dedicated VM authorized (D-01) if peak RAM exceeds headroom; restart policy bounds crash impact |
| T-06.7-12 | Tampering | embed endpoint exposed | accept | embed host on internal NAT 10.10.10.0/24; not public; gateway is the only consumer |
| T-06.7-SC | Tampering | michaelf34/infinity image | mitigate | SHA-pinned digest reused from 06.6-WAVE0-GATES.md (already vetted in Phase 6.6) |
</threat_model>

<verification>
- embed `/embeddings` returns 1024-dim vectors on the chosen host
- the gateway `/v1/embeddings` route returns 1024-dim (rewrite verified)
- UPSTREAM_EMBED_URL wired; gateway resolves embed to the static host
- service survives a restart (24/7)
</verification>

<success_criteria>
Embed runs 24/7 on CPU as a static tier-0 upstream reachable through the gateway's /v1/embeddings route; the gateway no longer depends on the pod for embed. Re-embed remains Phase 8.
</success_criteria>

<output>
Create `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-06-SUMMARY.md` when done.
</output>
