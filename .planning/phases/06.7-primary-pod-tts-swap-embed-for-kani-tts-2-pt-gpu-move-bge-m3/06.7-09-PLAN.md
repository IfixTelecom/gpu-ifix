---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 09
type: execute
wave: 3
depends_on: ["06.7-06", "06.7-07", "06.7-08"]
files_modified:
  - docs/RUNBOOK-PRIMARY-POD-TTS.md
  - .planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md
autonomous: false
requirements: [D-05, D-06, D-07, D-08, D-10, D-11, D-13, D-03]
must_haves:
  truths:
    - "Operator provisioned a live Vast 5090 primary pod and confirmed DCGM shows Kani fits VRAM with headroom (embed out, kani in)"
    - "A voice-clone round-trip works end-to-end: upload reference WAV -> voice_id -> /v1/audio/speech returns audio in the cloned voice"
    - "A voice survives pod replacement: after destroying + re-provisioning the pod, the same voice_id still synthesizes the cloned voice (cache miss -> refetch WAV from S3 -> regenerate)"
    - "Off-peak fallback works: with the pod drained/asleep, /v1/audio/speech is served by voice-api Piper (degraded default voice) per GATE 3"
    - "Embed retrieval works 24/7: /v1/embeddings (1024-dim) answers while the pod is asleep"
    - "Pitfall #11 verified: emerg cutback then primary Ready-tick re-asserts the tts slot within ~1s"
    - "Cleanup verified: all MinIO voice objects from the UAT are removed and all Vast instances destroyed"
  artifacts:
    - path: ".planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md"
      provides: "live UAT scenario sheet + sign-off"
      contains: "Scenario"
    - path: "docs/RUNBOOK-PRIMARY-POD-TTS.md"
      provides: "operator runbook for the TTS+embed topology"
      contains: "TTS"
  key_links:
    - from: "live /v1/audio/speech voice-clone"
      to: "Kani shim on pod 8003 via tts tier-0 override"
      via: "end-to-end gateway -> pod"
      pattern: "voice_id"
---

<objective>
Phase gate: live UAT on a real Vast 5090 plus an operator runbook. Validates the integrated phase against the success criteria that only live GPU + live hosts can prove (VRAM fit, voice-clone round-trip, voice-survives-pod-replacement, off-peak Piper fallback, 24/7 embed, Pitfall #11 re-assert). Requires real Vast spend -> autonomous mode cannot satisfy it.

Depends on the embed service (Plan 06), the gateway TTS pipeline (Plan 07), and the reconciler routing (Plan 08) all merged.

Purpose: Prove "TTS on GPU + embed 24/7 on CPU + invisible fallback + durable voices" works end-to-end.
Output: 06.7-HUMAN-UAT.md sign-off + RUNBOOK-PRIMARY-POD-TTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-CONTEXT.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-RESEARCH.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-WAVE0-GATES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RUNBOOK-PRIMARY-POD-TTS.md + 06.7-HUMAN-UAT.md scenario sheet</name>
  <read_first>
    - docs/RUNBOOK-PRIMARY-POD.md (Phase 6.6 runbook — extend, do not duplicate)
    - 06.7-RESEARCH.md §Validation Architecture (Phase gate items) + §Architecture Patterns diagram
    - 06.7-CONTEXT.md (D-03/D-05/D-07/D-08/D-11/D-13)
    - Plan 05 <voice_persistence_contract> (the pod-replacement survival mechanism the new scenario tests)
    - 06.7-WAVE0-GATES.md GATE 3 (Piper fallback contract — S4 asserts this exact format)
  </read_first>
  <action>
    Write `docs/RUNBOOK-PRIMARY-POD-TTS.md` covering the new topology: pod serves llm/stt/tts (embed gone), embed runs 24/7 on the CPU host, Piper is tier-1 TTS fallback (per GATE 3), voice CRUD via /v1/audio/voices, the lazy S3 voice-regen contract, and the Pitfall #11 re-assert behavior. Write `06.7-HUMAN-UAT.md` with 6 numbered scenarios mirroring RESEARCH §Validation Architecture Phase gate + the REVIEWS additions:
      (S1) provision 5090 pod + DCGM VRAM scrape proves Kani fits;
      (S2) voice-clone round-trip (upload WAV -> voice_id -> /v1/audio/speech cloned voice);
      (S3) VOICE SURVIVES POD REPLACEMENT (REVIEWS #1): with the voice_id from S2, force-down + re-provision the pod (fresh ephemeral storage), then /v1/audio/speech with the SAME voice_id still returns the cloned voice — confirm gateway/pod logs show a cache miss -> S3 WAV refetch -> embedding regen;
      (S4) off-peak Piper fallback (drain pod -> /v1/audio/speech served by Piper, format EXACTLY per GATE 3);
      (S5) 24/7 embed (pod asleep -> /v1/embeddings 1024-dim answers through the gateway route);
      (S6) Pitfall #11 (force emerg cutback while primary Ready -> tts slot re-asserted <=~1s).
    Each scenario lists exact curl/gatewayctl commands + expected output + a sign-off line. Add a final CLEANUP section (REVIEWS lower-priority): explicit steps to delete the UAT MinIO voice objects (`mc rm ifix/ai-gateway/voices/<voice_id>.wav`) and destroy all Vast instances, with a verification line (mc ls shows the test objects gone; Vast instance count = 0).
  </action>
  <verify>
    <automated>grep -q 'TTS' docs/RUNBOOK-PRIMARY-POD-TTS.md && grep -c 'Scenario' .planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md | grep -qE '[6-9]|[1-9][0-9]' && grep -iq 'cleanup' .planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/RUNBOOK-PRIMARY-POD-TTS.md` documents the llm/stt/tts pod + 24/7 CPU embed + Piper fallback (GATE 3) + voice CRUD + lazy S3 voice-regen topology
    - `06.7-HUMAN-UAT.md` has 6 scenarios (S1-S6) each with concrete commands, expected output, and a sign-off line — including the voice-survives-pod-replacement scenario
    - a CLEANUP section lists MinIO voice-object deletion + Vast destroy + verification
  </acceptance_criteria>
  <done>Runbook + UAT scenario sheet exist with the 6 phase-gate scenarios (incl. pod-replacement survival) + cleanup section fully specified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live Vast 5090 UAT — run 6 scenarios + cleanup + sign-off</name>
  <read_first>
    - .planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md (from Task 1)
    - STATE.md Phase 6.6 UAT 18 (5090 provisioning, DCGM, force-down/emerg coexist technique)
    - CLAUDE.md (VAST_AI_API_KEY, gatewayctl primary/emerg, vps-ifix-vm deploy, MinIO mc creds)
  </read_first>
  <what-built>
    The full phase integrated on dev: pod image with Kani:8003 (embed removed) + lazy S3 voice regen, gateway /v1/audio/speech + /v1/audio/voices routes, reconciler driving llm/stt/tts tier-0 with Ready-tick re-assert, and the 24/7 CPU embed service.
  </what-built>
  <how-to-verify>
    1. Deploy the rebuilt gateway + pod image to ai-gateway-dev (Portainer pull). Set UPSTREAM_TTS_URL/UPSTREAM_TTS_PIPER_URL/UPSTREAM_EMBED_URL + the TTS limit/prefix env (TTS_MAX_INPUT_CHARS, VOICE_MAX_UPLOAD_BYTES, S3_VOICE_PREFIX).
    2. `gatewayctl primary force-up` -> wait Ready. Run S1 (DCGM VRAM scrape: confirm Kani resident, headroom > 0; record GB used/free).
    3. S2: POST /v1/audio/voices with a reference WAV -> get voice_id; POST /v1/audio/speech with that voice_id -> save + listen to the cloned-voice audio.
    4. S3: `gatewayctl primary force-down` then `force-up` (fresh pod) -> POST /v1/audio/speech with the SAME voice_id -> cloned voice still works; confirm logs show cache miss -> S3 WAV refetch -> embedding regen.
    5. S4: `gatewayctl primary force-down` -> drain; POST /v1/audio/speech -> confirm served by Piper (format EXACTLY per GATE 3), no client error.
    6. S5: with pod asleep, POST /v1/embeddings through the gateway -> 1024-dim vector (embed 24/7 alive).
    7. S6: provision emerg pod, publish primary_ready (UAT 18 technique), confirm emerg cutback clears then primary Ready-tick re-asserts the tts slot <=~1s (gateway logs `primary re-asserted tier-0 override`).
    8. CLEANUP: delete the UAT MinIO voice objects (`mc rm ifix/ai-gateway/voices/<voice_id>.wav`), destroy all Vast instances; verify mc ls shows them gone + Vast instance count = 0; record spend. Fill each scenario's sign-off line PASS/FAIL.
  </how-to-verify>
  <verify>
    <automated>MISSING — live Vast UAT requires GPU spend; results recorded in 06.7-HUMAN-UAT.md + 06.7-09-SUMMARY.md</automated>
    <human-check>Operator runs S1-S6 on a live 5090 + cleanup and signs each PASS/FAIL</human-check>
  </verify>
  <acceptance_criteria>
    - 06.7-HUMAN-UAT.md has a PASS/FAIL verdict + evidence (DCGM numbers, audio files, log lines) for each of S1-S6 (incl. voice-survives-pod-replacement)
    - CLEANUP verified: UAT MinIO voice objects removed (mc ls confirms) AND all Vast instances destroyed (count 0); spend recorded
    - any FAIL routed to gap closure (`/gsd:plan-phase 06.7 --gaps`)
  </acceptance_criteria>
  <resume-signal>Type "approved" when all 6 scenarios + cleanup are signed, or list failing scenarios.</resume-signal>
  <done>Live UAT executed on a 5090; 6 scenarios signed; MinIO voice objects + Vast instances cleaned up; spend recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live Vast pods | provisioning with full-account VAST_AI_API_KEY |
| live client -> /v1/audio/* | real synth + upload traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-20 | Denial of Service | orphan Vast pods burning budget | mitigate | force-destroy + manual Vast DELETE verification after UAT (STATE.md UAT-9 orphan lesson); BestEffortDestroy 429 retry already in place; CLEANUP step asserts instance count 0 |
| T-06.7-21 | Info Disclosure | uploaded reference WAV (biometric) | accept | private MinIO bucket; flagged for Phase 9 LGPD review; CLEANUP deletes UAT voice objects |
| T-06.7-SC | Tampering | n/a (no installs) | accept | UAT runs prebuilt images |
</threat_model>

<verification>
- All 6 UAT scenarios signed PASS (or gaps filed)
- DCGM proves Kani VRAM fit; voice-clone + pod-replacement survival + Piper fallback + 24/7 embed + Pitfall #11 re-assert all demonstrated live
- CLEANUP verified (MinIO voice objects gone + Vast instances destroyed)
- 06.7-VERIFICATION.md can be written from the signed UAT
</verification>

<success_criteria>
Live evidence that the pod serves voice-cloned TTS during peak, voices survive pod replacement, falls back to Piper off-peak per GATE 3, embed serves RAG 24/7, and the tier-0 slots survive emerg cutback — with all UAT artifacts cleaned up.
</success_criteria>

<output>
Create `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-09-SUMMARY.md` when done.
Fill `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md` sign-offs.
</output>
