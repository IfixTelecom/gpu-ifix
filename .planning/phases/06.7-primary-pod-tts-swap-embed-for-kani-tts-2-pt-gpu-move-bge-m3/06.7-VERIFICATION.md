---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
verified: 2026-05-23T02:15:00Z
status: passed_partial
score: 9/9 plans GREEN on develop; 5/6 live UAT scenarios PASS (S6 deferred to SEED-002 emerg redesign)
overrides_applied: 0
human_verification:
  - test: "S1 — Provision RTX 5090 + DCGM Chatterbox VRAM headroom"
    result: PASS
    evidence: "nvidia-smi 21972 MiB used / 10138 MiB free / 32607 MiB total on RTX 5090 (Chatterbox ≈ +4 GB over LLM+STT baseline 18 GB); supervisorctl shows 4 children RUNNING (llama + speaches + chatterbox + dcgm; infinity GONE); pod /health:8003 returns {\"status\":\"ok\",\"model_ready\":true}; gateway log shows tts tier-0 override asserted. UAT operator sign-off: PASS."
  - test: "S2 — Zero-shot voice-clone round-trip (24kHz cloned voice)"
    result: PASS
    evidence: "POST /v1/audio/voices with reference WAV → voice_id c8b6aa6c… returned; POST /v1/audio/speech with that voice_id → HTTP 200 + WAV 24000 Hz mono 5.8s; `file` confirms 24kHz; operator listen-test confirms clone audibility match to reference; mc ls ifix/ai-gateway/voices/c8b6aa6c….wav (250 KiB) present. UAT operator sign-off: PASS."
  - test: "S3 — Voice survives pod replacement (S3 WAV refetch, NO .pt)"
    result: PASS
    evidence: "force-down + force-up cycle → fresh Vast pod 37226253 with wiped ephemeral storage. POST /v1/audio/speech with the SAME voice_id from S2 → WAV 24kHz 4.5s, cloned voice intact. Fresh pod log shows /opt/chatterbox-data/voices/c8b6aa6c….wav (256078 B) cached at synth time (= S3 refetch) — no embedding regeneration. Inspection of /opt/chatterbox-data shows only base-model `.pt` (conds/ve/s3gen) — NO per-voice `.pt`. Zero-shot S3-WAV-refetch contract verified end-to-end. UAT operator sign-off: PASS."
  - test: "S4 — Off-peak Piper fallback (WAV 16kHz, GATE 3 Option A)"
    result: PASS
    evidence: "pod force-down → drained → asleep. POST /v1/audio/speech (no voice, default) → HTTP 200, WAV 16000 Hz mono 16-bit (GATE 3 Option A adapter contract). Gateway log: breaker `local-tts` OPEN → failover to `voice-api-piper`. UAT operator sign-off: PASS."
  - test: "S5 — Embed 24/7 (1024-dim, pod asleep) [GATE 4]"
    result: PASS
    evidence: "pod state=asleep (S4 carry). POST /v1/embeddings via gateway → dim=1024, model=multilingual-e5-large, served by CPU Infinity on n8n-ia-vm:7997. RAG path decoupled from pod schedule. UAT operator sign-off: PASS."
  - test: "S6 — Pitfall #11 re-assert (emerg cutback → tts re-asserted ≤~1s)"
    result: DEFERRED
    evidence: "Live emerg-cutback test deferred — emergency subsystem being redesigned to a full primary-pod hot-standby (see SEED-002-emergency-full-pod-hot-standby-and-embed-on-pod.md), making the LLM-only emerg test moot. Pitfall #11 re-assert mechanic holds: (a) Plan 06.7-08 reconciler_test.go unit coverage (TestEvaluateReady_ReassertsTier0WhenCleared + TestMarkReady_OverridesTTSNotEmbed verify the {llm,stt,tts} loop fires on cleared slot); (b) empirical re-assert observed after gateway restart during UAT 14/15 carry-over; (c) S6 scenario re-emerges inside the SEED-002 hot-standby phase with the same RestoreTier0 → re-assert assertion against the new emerg architecture. Routing to /gsd:plan-phase 06.7 --gaps would have implemented a fix against a deprecated subsystem (waste)."
  - test: "CLEANUP — MinIO voice objects gone + Vast count 0"
    result: PASS
    evidence: "mc rm ifix/ai-gateway/voices/c8b6aa6c…wav + e4ea7729…wav; mc ls voices/ returns empty; `curl Vast /api/v0/instances/` returns `.instances|length = 0`. No orphan pods this round (UAT-9 lesson held). Total UAT spend bounded ~$2-3. UAT operator sign-off: PASS."
gaps_remaining:
  - "S6 live emerg-cutback Pitfall #11 — deferred to SEED-002 hot-standby phase; unit + empirical coverage holds for the re-assert mechanic"
  - "Latent observability gap: tts breaker state + voice-cache miss rate not surfaced on dashboard — Phase 07 owns"
  - "Voice biometric WAV (uploaded reference) needs LGPD review — Phase 09 owns (per Plan 09 T-06.7-21 disposition)"
regressions: []
---

# Phase 06.7: Primary Pod TTS — swap embed for Chatterbox GPU + move embed to 24/7 CPU — Verification Report

**Phase Goal:** Extend the peak-only primary pod (RTX 5090, supervisord PID 1 with 4 children from Phase 6.6: llama/speaches/infinity/dcgm) to add **GPU TTS via Chatterbox Multilingual** (PT-BR zero-shot voice-cloning, MIT, ~4 GB VRAM, 24kHz) as a 5th supervisord child, **remove infinity from the pod**, and **relocate the embed service to 24/7 CPU on n8n-ia-vm** (multilingual-e5-large via Infinity:7997) so RAG survives off-peak. Wire `/v1/audio/speech` + `/v1/audio/voices` gateway routes + tts tier-0 role + Piper tier-1 fallback (WAV 16kHz, GATE 3 Option A). Net topology: pod = llm/stt/tts on GPU peak-only; embed = CPU 24/7 static tier-0; Piper = tts tier-1 fallback.

**Verified:** 2026-05-23T02:15:00Z
**Status:** passed_partial (5/6 live UAT PASS + CLEANUP PASS; S6 deferred to SEED-002 architectural redesign, not gap-closure)
**Score:** 9/9 plans GREEN on develop; 5/6 live UAT scenarios PASS

## Resumo Executivo

Phase 06.7 entrega o pivot de arquitetura completo: **TTS na GPU + embed 24/7 fora da GPU + voice cloning zero-shot durável via S3 WAV** — sem `.pt` por voz, sem regeneração de embedding, com Piper degradado como fallback off-peak. Live UAT em Vast RTX 5090 em 2026-05-21 cobriu 5/6 scenarios (S1-S5 + CLEANUP) com PASS direto. S6 (Pitfall #11 live emerg cutback) **não falhou — foi tornado moot por uma decisão de redesign simultânea (SEED-002)**: o emerg LLM-only está sendo substituído por um full-pod hot-standby; rotear S6 para `/gsd:plan-phase 06.7 --gaps` implementaria fix em subsistema deprecado.

### O que foi validado live no 5090

1. **Chatterbox fits o orçamento VRAM** com headroom explícito (21.972 MiB used / 10.138 MiB free / 32.607 MiB total — ~4 GB para Chatterbox sobre os 18 GB de LLM+STT).
2. **Voice-clone zero-shot funciona end-to-end** em 24kHz (upload WAV → voice_id → /v1/audio/speech retorna WAV no timbre clonado).
3. **Voz sobrevive substituição de pod** (fresh pod, storage ephemeral wiped → mesma voice_id → mesma voz clonada via lazy S3 WAV refetch como `audio_prompt_path`). Confirmado: NO `.pt` por voz, NO embedding regen.
4. **Piper fallback** entrega exatamente WAV 16kHz mono 16-bit per GATE 3 Option A com pod drenado.
5. **Embed 24/7** funciona com pod asleep (dim=1024 multilingual-e5-large via CPU Infinity n8n-ia-vm:7997).
6. **CLEANUP rigoroso** (MinIO voice objects deletados; Vast instance count = 0; sem orphan pods — lição UAT-9 sustentada).

### Patches de produção que vieram do UAT (8 bugs, todos shipped develop)

1. CI: sqlc gen out of sync; gofmt unclean main.go
2. Piper adapter sent form-encoded; voice-api Piper expects JSON `/tts`
3. torch cu124 → **cu128** (RTX 5090 Blackwell sm_120 — no kernel image)
4. **CUPTI**: cu128 torch needs nvidia-cuda-*-cu12 12.8 runtime (dropped `--no-deps`)
5. **setuptools<81**: resemble-perth uses `pkg_resources` removed in setuptools 81+ → PerthImplicitWatermarker None
6. audit middleware truncated multipart voice upload (`isAudioRoute` only matched `/transcriptions`) → fix `c5acccc`
7. **TTS routing**: loader override resolves `emergency_pod_tts` mas dispatcher não tinha proxy registrado → 503; fix `12f7479` adds `NewDynamicTTSProxy`
8. Host selection: removed `verified` + EU-`geolocation` filters (`6e66841`); lowered `PRIMARY_VAST_PRICE_CAP_DPH` 2.20→1.00; added `PRIMARY_VAST_MACHINE_BLOCKLIST` + machine_id logging (`96e823e`) — broken-CDI hosts catalogados: 36773, 53128, 38389

**Gap latente fechado eagerly:** llm/stt tinham o mesmo `emergency_pod_<role>` no-registered-proxy issue que TTS — fix `30f90e7` registra dynamic emergency_pod_{llm,stt} proxies. Live-verificado posteriormente em Phase 06.6 td #4 closeout (2026-05-22) com regression test `bc26cc8`.

## Goal Achievement

### Live UAT Scenario Scoreboard

| # | Scenario | Status | Wall time / metric | Pass criterion |
|---|----------|--------|---------------------|----------------|
| S1 | Primary force-up → Chatterbox loaded on 5090 + DCGM VRAM scrape | PASS | ~30-40s Chatterbox cold load on top of pod cold-start | 4-of-4 children RUNNING + free > 0 GB + tts tier-0 override asserted |
| S2 | Voice-clone round-trip (upload + synth) | PASS | WAV 5.8s @ 24000 Hz | 24kHz cloned WAV + voice_id + MinIO object |
| S3 | Voice survives pod replacement (S3 refetch) | PASS | WAV 4.5s on fresh pod | Same voice_id → cloned voice + log cache-miss/refetch + NO `.pt` |
| S4 | Off-peak Piper fallback | PASS | WAV @ 16000 Hz | EXACTLY WAV 16kHz 16-bit mono (Option A adapter) |
| S5 | Embed 24/7 (pod asleep) | PASS | dim=1024 | multilingual-e5-large served by CPU Infinity 24/7 |
| S6 | Pitfall #11 emerg cutback re-assert | DEFERRED | n/a | Deprecated by SEED-002 (full-pod hot-standby supersedes LLM-only emerg) |
| CLEANUP | MinIO voice objs gone + Vast count 0 | PASS | spend ~$2-3 | mc ls empty + Vast `.instances | length = 0` |

**Live PASS: 5 + CLEANUP. Deferred: 1. Failures: 0. Regressions: 0.**

### Requirements Coverage

| Requirement | Validated by | Status |
|------------|--------------|--------|
| D-03 (embed off pod, static tier-0) | S5 live + Plan 06 ship | ✓ |
| D-05 (zero-shot voice via S3 WAV refetch, no .pt) | S3 live (cache miss → S3 refetch + no per-voice .pt) | ✓ |
| D-06 (Piper tier-1 fallback) | S4 live (WAV 16kHz GATE 3 Option A) | ✓ |
| D-07 (24kHz Chatterbox output) | S2 + S3 live (`file` confirms 24000 Hz) | ✓ |
| D-08 (Chatterbox engine swap from Kani) | S1 + S2 + S3 live (supervisord chatterbox RUNNING, voice-clone works) | ✓ |
| D-10 (gateway /v1/audio/voices + /v1/audio/speech routes) | S2 live (both routes HTTP 200) | ✓ |
| D-11 (tts tier-0 dynamic-override roster) | S1 live (tts tier-0 override asserted in log) | ✓ |
| D-13 (Pitfall #11 re-assert on cleared slot) | Plan 08 unit tests + empirical post-restart obs; live deferred to SEED-002 | ⏸ partial (unit/empirical, not live) |

## Acknowledged Gaps

1. **S6 live deferred to SEED-002.** Pitfall #11 re-assert against LLM-only emerg subsystem is moot; SEED-002 replaces emerg with a full primary-pod hot-standby on 5090 where the equivalent assertion re-emerges. Unit + empirical coverage holds in the interim.

2. **TTS observability gap (Phase 07 owns).** Dashboard needs to surface `local-tts` breaker state + Chatterbox voice-cache miss rate. The S3 "cache miss → S3 refetch" log line was only visible by grepping pod supervisord logs — not surfaced anywhere queryable.

3. **Voice biometric LGPD (Phase 09 owns).** Uploaded reference WAVs are biometric data; private MinIO bucket policy holds for now (Plan 09 T-06.7-21 disposition) but Phase 09 LGPD review must cover voice-clone uploads explicitly before sensitive tenants get voice access.

## Decisions Locked Live

- **Engine = Chatterbox Multilingual (not Kani)** — Wave 0 GATE 1 swap proven end-to-end via S2/S3 live PASS.
- **Embed = multilingual-e5-large on n8n-ia-vm CPU Infinity** — S5 live PASS via 24/7 path.
- **Voice persistence = S3 WAV reference only, NO .pt per voice** — S3 live PASS (no per-voice .pt found anywhere; fresh pod refetched WAV at synth time).
- **Piper fallback = WAV 16kHz mono 16-bit (Option A adapter)** — S4 live PASS.
- **SEED-002 supersedes current emerg architecture** — S6 deferral routed there.

## Spend

| Run | Hardware | Wall time | Cost |
|-----|----------|-----------|------|
| Multiple 5090 short runs | RTX 5090 EU $0.33–0.61/h | ~3-4 hours total (S1+S2+S3 pod cycles + Piper test + redeploy iterations) | ~$1.80-2.40 |
| Brief emerg 4090 (S6 prep, aborted) | RTX 4090 $0.37/h | ~25 min | ~$0.14 |
| **Total Phase 06.7 UAT spend** | | | **~$2-3** |

No orphan pods; all instances destroyed; MinIO voice objects deleted. UAT-9 ($2.17 orphan, ~3h30) lesson held this round.

## Conclusion

Phase 06.7 entrega o pivot de TTS+embed completo com 5/6 cenários live PASS + CLEANUP PASS + 8 patches de produção. S6 não bloqueia o phase — é absorbed por SEED-002 que reverses D-03 (embed volta pra pod GPU como tier-0) e unifica emerg com primary architecture. Phase 06.7 → status `passed_partial`. Phase 07 (observability) destrava com tts breaker + voice-cache visibility como novo requisito imediato.
