---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 02
subsystem: test-scaffolding
tags: [tdd, nyquist, wave0, tts, chatterbox, voices, probe, tier0]
requires:
  - 06.7-WAVE0-GATES.md (engine = Chatterbox Multilingual)
provides:
  - RED/skip test stubs binding every Wave 1/2 behavior to an owning plan
affects:
  - Plans 06.7-03 (probe/loader/quota), 06.7-05 (shim), 06.7-07 (proxy/voices), 06.7-08 (reconciler reassert)
tech-stack:
  added: []
  patterns: [t.Skip with OWNER annotation, pytest.mark.skip with OWNER reason]
key-files:
  created:
    - gateway/internal/upstreams/probe_test.go
    - gateway/internal/proxy/tts_test.go
    - gateway/internal/proxy/voices_test.go
    - pod/primary/test_chatterbox_shim.py
  modified:
    - gateway/internal/upstreams/loader_test.go
    - gateway/internal/quota/enforcer_test.go
    - gateway/internal/primary/reconciler_test.go
decisions:
  - Stubs are ENGINE-AGNOSTIC on the Go side (assert tts ROLE plumbing), engine-specific only in the pod pytest (Chatterbox 24kHz, zero-shot audio_prompt_path)
  - Every stub carries an OWNER comment + skip reason naming the downstream plan that must turn it green
metrics:
  duration: ~10m
  completed: 2026-05-20
---

# Phase 06.7 Plan 02: Wave 0 Test Scaffolding (Nyquist Gate) Summary

RED/skip test stubs for every Chatterbox-era Wave 1/2 behavior — 6 Go test files + 1 pytest file — each annotated with the downstream plan (`OWNER`) that must unskip it and assert real behavior before that plan completes.

## What Was Built

Test-first companion to gate plan 01. Two atomic commits, zero file overlap with implementation plans (touches only `*_test.go` + a new pytest file).

### Task 1 — Go stubs: upstreams + quota + primary reconciler (commit 16d9f81)
- `probe_test.go` (NEW): `TestProbe_TTS_PostsAudioSpeech` (OWNER 03) — engine-agnostic `/v1/audio/speech` probe path + 200=success / 5xx=breaker-failure.
- `loader_test.go` (appended): `TestTier0_TTSKeyPresentEmbedAbsent` + `TestTier0OverrideURL_Getter` (OWNER 03) — D-11 tier-0 map swap {llm,stt,embed}→{llm,stt,tts} + D-13 getter.
- `enforcer_test.go` (appended): `TestClassifyRoute_TTS` (OWNER 03) — `RouteClassTTS` wire contract.
- `reconciler_test.go` (appended): `TestEvaluateReady_ReassertsTier0WhenCleared` + `TestMarkReady_OverridesTTSNotEmbed` (OWNER 08) — Pitfall #11 / D-13 re-assert for llm/stt/tts, never embed.

### Task 2 — proxy + shim stubs (commit 22b2929)
- `tts_test.go` (NEW): `TestTTSProxy_JSONToBinaryAudio`, `TestTTSProxy_ErrorEnvelope`, `TestTTSProxy_PiperFallback_AdapterConverts` (OWNER 07) — JSON→binary 1:1, OpenAI 502 envelope, GATE 3 Option A ulaw 8kHz→WAV 16kHz adapter.
- `voices_test.go` (NEW): create/list/delete CRUD + tenant isolation (D-10), path-traversal UUID key (T-06.7-03), DoS cap, unsupported-format 400 (OWNER 07). Asserts reference-WAV + catalog-row persistence ONLY — NO `.pt` (Chatterbox zero-shot, D-08).
- `test_chatterbox_shim.py` (NEW): `test_speech_default_voice_returns_wav_24000`, `test_voice_clone_roundtrip_upload_then_speak`, `test_voice_id_regex_rejects_path_traversal`, `test_health_returns_200_when_model_loaded`, `test_voice_cache_miss_refetches_from_s3` (OWNER 05). 24000 Hz (not 22050), zero-shot `audio_prompt_path`, pod-replacement S3 WAV refetch (no `.pt`).

## Verification

- `cd gateway && go build ./...` → exit 0 (FULL_BUILD_OK)
- `go vet` + `go test` on `./internal/upstreams/... ./internal/quota/... ./internal/primary/...` → all `ok` (stubs SKIP)
- `go test ./internal/proxy/...` → `ok` (stubs SKIP)
- `python3 -m py_compile pod/primary/test_chatterbox_shim.py` → exit 0
- All per-task acceptance greps confirmed: TestTTSProxy/PiperFallback/TenantIsolation/UUID present; 5 `def test_`; 24000 + audio_prompt_path present; OWNER annotations 16 (Task 1) + 31 (Task 2).
- Pod-replacement-survival stub asserts an S3 WAV refetch (zero-shot, no `.pt`) per plan verification.

## Deviations from Plan

None — plan executed exactly as written. The plan's `files_modified` listed `loader_test.go`/`enforcer_test.go`/`reconciler_test.go` which already existed; stubs were appended to those (matching their existing package decls `upstreams` / `quota_test` / `primary`) and the four `*_test.go`/`.py` files were created new. This matches the plan's intent (no new behavior; compile-clean skip stubs).

## Authentication Gates

None.

## Stub Ownership Map (binding — mirrored in each owning plan)

| Stub | File | OWNER |
|------|------|-------|
| TestProbe_TTS_PostsAudioSpeech | probe_test.go | 06.7-03 |
| TestTier0_TTSKeyPresentEmbedAbsent | loader_test.go | 06.7-03 |
| TestTier0OverrideURL_Getter | loader_test.go | 06.7-03 |
| TestClassifyRoute_TTS | enforcer_test.go | 06.7-03 |
| TestEvaluateReady_ReassertsTier0WhenCleared | reconciler_test.go | 06.7-08 |
| TestMarkReady_OverridesTTSNotEmbed | reconciler_test.go | 06.7-08 |
| TestTTSProxy_JSONToBinaryAudio / _ErrorEnvelope / _PiperFallback_AdapterConverts | tts_test.go | 06.7-07 |
| TestVoices_* (6) | voices_test.go | 06.7-07 |
| test_* (5: synth/clone/regex/health/cache-miss) | test_chatterbox_shim.py | 06.7-05 |

## Known Stubs

All stubs in this plan are INTENTIONAL Wave 0 scaffolding — they are skipped (`t.Skip` / `pytest.mark.skip`) and bound to the downstream plan that must unskip + implement them (see ownership map). This plan is test-only by design; no production behavior was added, so no goal-blocking stub exists.

## Self-Check: PASSED

- All 5 created/modified target files FOUND on disk.
- Both task commits (16d9f81, 22b2929) FOUND in git log.
