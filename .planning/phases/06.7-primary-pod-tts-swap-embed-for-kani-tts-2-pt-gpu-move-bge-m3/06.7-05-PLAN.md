---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 05
type: execute
wave: 1
depends_on: ["06.7-01", "06.7-02"]
files_modified:
  - pod/primary/kani_shim.py
  - pod/primary/Dockerfile
  - pod/primary/supervisord.conf
autonomous: true
requirements: [D-05, D-06, D-08]
must_haves:
  truths:
    - "A FastAPI shim serves POST /v1/audio/speech (OpenAI contract) backed by Kani-TTS-2-pt, returning 22050 Hz 16-bit mono WAV/PCM"
    - "The shim supports default voice and cloned voice via voice_id, extracting a speaker embedding from a reference WAV and persisting it"
    - "The primary pod image builds Kani into an isolated /opt/kani-venv and supervisord runs [program:kani] on port 8003 with model warmup"
    - "The infinity embed child is removed from supervisord (embed left the pod per D-03)"
  artifacts:
    - path: "pod/primary/kani_shim.py"
      provides: "OpenAI /v1/audio/speech shim"
      contains: "/v1/audio/speech"
      min_lines: 80
    - path: "pod/primary/supervisord.conf"
      provides: "[program:kani] child, no [program:infinity]"
      contains: "[program:kani]"
  key_links:
    - from: "kani_shim /v1/audio/speech"
      to: "gateway tts proxy + probe (Plans 03/07)"
      via: "OpenAI-compat contract on pod port 8003"
      pattern: "8003"
    - from: "voice_id → speaker embedding .pt"
      to: "voices CRUD upload (Plan 07)"
      via: "shim extracts embedding from reference WAV"
      pattern: "embed_audio_file"
---

<objective>
Build the GPU TTS service on the primary pod: a thin FastAPI shim (~150 LoC) serving the OpenAI `/v1/audio/speech` contract via Kani-TTS-2-pt, packaged into the pod image and wired as the 5th supervisord child on port 8003. The infinity embed child leaves the pod (embed relocates to CPU per D-03).

GATE: This plan MUST NOT start until Plan 01 GATE 1 (Kani PT-BR quality) = PASS and GATE 2 (CUDA/image) confirms base-image compatibility. If GATE 1 = FAIL, this plan is void.

Purpose: Deliver the engine that the gateway routes `/v1/audio/speech` to during peak.
Output: kani_shim.py + Dockerfile Kani stage + supervisord swap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-RESEARCH.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-PATTERNS.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-WAVE0-GATES.md

<interfaces>
OpenAI /v1/audio/speech (RESEARCH §Code Examples): request {model, input, voice, response_format ("wav"|"pcm"), stream_format (null|"sse")} → binary WAV/PCM 22050 Hz 16-bit mono.
Kani loading (model card): KaniTTS('nineninesix/kani-tts-2-pt') ~3 GB VRAM; SpeakerEmbedder() 128-dim; embedder.embed_audio_file(wav) → speaker_emb; model(text, speaker_emb=spk); voice="random" skips embedding.
Reference server (port from): github.com/nineninesix-ai/kani-tts-2-openai-server — voices are .pt tensors in a speakers/ dir; voice="speaker_x" selects.
Dockerfile venv-per-service pattern: pod/primary/Dockerfile :97-107.
supervisord children: pod/primary/supervisord.conf :35-67 (speaches, infinity to remove).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kani_shim.py — FastAPI /v1/audio/speech + voice clone + warmup</name>
  <read_first>
    - pod/primary/test_kani_shim.py (Plan 02 stubs: speech_default_voice, voice_clone_roundtrip, voice_id regex)
    - 06.7-RESEARCH.md §Pattern 2 (loading/inference) + §Code Examples (contract) + §Security Domain (voice_id regex, WAV size cap)
    - 06.7-PATTERNS.md kani_shim.py section
    - pod/smoke/smoke.py (in-repo Python style)
    - 06.7-WAVE0-GATES.md GATE 1 (PT-BR PASS) + GATE 2 (CUDA decision)
  </read_first>
  <behavior>
    - POST /v1/audio/speech with {model, input, voice:"random", response_format:"wav"} → 200 + WAV bytes 22050 Hz 16-bit mono
    - POST /v1/audio/speech with a known voice_id whose .pt exists → synth uses the cloned speaker embedding
    - voice/voice_id not matching regex ^[A-Za-z0-9_\-\.]+$ or containing ".." → 400 (path-traversal guard, mirror Piper)
    - startup event loads model + runs one short synth (warmup) so first real request is not cold
  </behavior>
  <action>
    Port the shim from the official `nineninesix-ai/kani-tts-2-openai-server` (D-05 — we own ~150 LoC, no wrapper dependency). Load `KaniTTS('nineninesix/kani-tts-2-pt')` + `SpeakerEmbedder()` in a FastAPI `@app.on_event("startup")` with a warmup synth. Implement `POST /v1/audio/speech` accepting the OpenAI fields (D-06); `voice=="random"` → default voice (no embedding), otherwise resolve `voice` / `voice_id` to a `.pt` speaker-embedding file in a local `speakers/` dir and pass `speaker_emb`. Provide an internal helper used by the voices upload path (Plan 07) that runs `embedder.embed_audio_file(reference.wav)` and saves the `.pt` under a UUID-derived name (Security V12 — name from UUID, not input). Validate `voice`/`voice_id` with regex `^[A-Za-z0-9_\-\.]+$` and reject `..` (Security V5). Cap `input` length (DoS) and return audio at 22050 Hz 16-bit mono; honor `response_format` wav|pcm. Keep it ~150 LoC.
  </action>
  <verify>
    <automated>python3 -m py_compile pod/primary/kani_shim.py && (cd pod/primary && python3 -m pytest test_kani_shim.py -q 2>&1 | grep -Eq 'passed|skipped|deselected' || true)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '/v1/audio/speech' pod/primary/kani_shim.py`
    - `grep -q 'embed_audio_file' pod/primary/kani_shim.py` (voice clone path)
    - `grep -Eq 'A-Za-z0-9_' pod/primary/kani_shim.py` (voice_id regex guard) and a `..` rejection
    - `grep -q 'on_event("startup")\|lifespan' pod/primary/kani_shim.py` (warmup)
    - `python3 -m py_compile pod/primary/kani_shim.py` exits 0
  </acceptance_criteria>
  <done>Shim serves /v1/audio/speech with default + cloned voices, warmup on startup, input/voice validation; pytest stubs unskipped where runnable.</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile Kani stage + supervisord swap (remove infinity, add kani:8003)</name>
  <read_first>
    - pod/primary/Dockerfile (:97-107 venv pip stages; COPY supervisord.conf ~:155)
    - pod/primary/supervisord.conf (:35-67 children; :61-67 [program:infinity] to remove)
    - 06.7-RESEARCH.md §Pattern 1 ([program:kani] block) + §Standard Stack (pip install line) + §Runtime State Inventory + 06.7-WAVE0-GATES.md GATE 2 (base-image decision, disk budget)
    - 06.7-PATTERNS.md Dockerfile + supervisord.conf sections
  </read_first>
  <action>
    In `pod/primary/Dockerfile`, add an isolated `/opt/kani-venv` stage (RESEARCH Pitfall 2 recommends a dedicated venv): `python3 -m venv /opt/kani-venv && /opt/kani-venv/bin/pip install --no-cache-dir kani-tts-2 "nemo-toolkit[tts]" "transformers==4.57.1" fastapi uvicorn scipy` (versions per RESEARCH §Standard Stack), then `COPY pod/primary/kani_shim.py /opt/kani-data/kani_shim.py`. If GATE 2 required a CUDA 12.8 base bump, apply it; otherwise keep `llama.cpp:server-cuda-b9191`. The infinity pip stage + weights may be removed (embed left the pod) or kept only for rollback per GATE 2 note. In `pod/primary/supervisord.conf`, REMOVE `[program:infinity]` (:61-67) and ADD `[program:kani]` exactly per RESEARCH §Pattern 1 (command=/opt/kani-venv/bin/uvicorn kani_shim:app --host 0.0.0.0 --port 8003, directory=/opt/kani-data, autorestart=true, stopwaitsecs=10, redirect_stderr=true, stdout_logfile=/var/log/kani.log, priority=250).
  </action>
  <verify>
    <automated>grep -q '/opt/kani-venv' pod/primary/Dockerfile && grep -q 'kani-tts-2' pod/primary/Dockerfile && grep -q '\[program:kani\]' pod/primary/supervisord.conf && grep -q 'port 8003' pod/primary/supervisord.conf && ! grep -q '\[program:infinity\]' pod/primary/supervisord.conf</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile installs the Kani stack into `/opt/kani-venv` and COPYs kani_shim.py to /opt/kani-data
    - supervisord.conf has `[program:kani]` on port 8003 with autorestart + the priority/log directives matching the other children
    - supervisord.conf no longer has `[program:infinity]`
    - base-image line reflects the GATE 2 decision (b9191 kept or 12.8 bump)
  </acceptance_criteria>
  <done>Image builds Kani into an isolated venv; supervisord runs kani:8003 and no longer runs infinity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → shim :8003 | synth requests with client-supplied input/voice cross into the pod |
| shim → MinIO/local speakers dir | reference WAV → speaker embedding file persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-08 | Tampering | voice/voice_id path traversal | mitigate | regex `^[A-Za-z0-9_\-\.]+$` + reject `..` in kani_shim (mirrors Piper); .pt filename derived from UUID not input (ASVS V5/V12) |
| T-06.7-09 | Denial of Service | huge input text synth | mitigate | cap input length in shim before synth (RESEARCH §Security Domain) |
| T-06.7-10 | Info Disclosure (LGPD) | speaker embedding = biometric | accept | reference WAV stored in private MinIO bucket (already private); flagged for Phase 9 LGPD review |
| T-06.7-SC | Tampering | pip install kani-tts-2/nemo-toolkit | mitigate | RESEARCH §Package Legitimacy Audit 7/7 [OK]; versions pinned (transformers==4.57.1) |
</threat_model>

<verification>
- `python3 -m py_compile pod/primary/kani_shim.py` exits 0
- supervisord has kani:8003, lacks infinity
- Dockerfile installs the pinned Kani stack; image-size impact recorded against GATE 2 disk budget
- (Live synth verified in Plan 09 UAT on the 5090)
</verification>

<success_criteria>
The pod image serves /v1/audio/speech via Kani on 8003 with default + cloned voices and warmup; embed no longer runs on the pod.
</success_criteria>

<output>
Create `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-05-SUMMARY.md` when done.
</output>
