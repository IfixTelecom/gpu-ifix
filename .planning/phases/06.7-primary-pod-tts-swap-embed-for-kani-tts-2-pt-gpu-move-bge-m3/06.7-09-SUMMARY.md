---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 09
subsystem: phase-gate-live-uat
tags: [uat, vast, 5090, chatterbox, tts, piper, embed, pitfall-11, voice-clone]
requires:
  - "06.7-05: pod serves Chatterbox tts on 8003 (zero-shot, audio_prompt_path)"
  - "06.7-06: 24/7 CPU embed (multilingual-e5-large, Infinity n8n-ia-vm:7997)"
  - "06.7-07: gateway /v1/audio/speech + /v1/audio/voices + Piper tier-1 adapter (WAV 16kHz)"
  - "06.7-08: reconciler drives {llm,stt,tts} tier-0 + Ready-tick re-assert"
provides:
  - "live UAT sign-off (5/6 PASS, S6 deferred) on Vast 5090"
  - "docs/RUNBOOK-PRIMARY-POD-TTS.md operator runbook"
  - "8 live-fix patches landed on develop (CI sqlc, Piper JSON contract, torch cu128, CUPTI 12.8, setuptools<81, audit /v1/audio/* skip, TTS dispatcher dynamic proxy, Vast host selection)"
  - "Vast host blocklist seed (36773 / 53128 / 38389 broken-CDI) + machine_id logging"
  - "SEED-002 emerg redesign (LLM-only emerg deprecated → full-pod hot-standby)"
affects:
  - "Phase 07 — observability dashboard must surface tts breaker + voice-cache miss rate"
  - "Phase 09 — sensitive tenants LGPD review must cover voice biometric WAV in MinIO"
  - "SEED-002 — emerg redesign now blocking S6 closure"
tech-stack:
  added: []
  patterns:
    - "Vast host blocklist via PRIMARY_VAST_MACHINE_BLOCKLIST env (catalog broken-CDI hosts after live failure)"
    - "machine_id logged on every provision attempt for forensic catalog"
key-files:
  created:
    - "docs/RUNBOOK-PRIMARY-POD-TTS.md (Task 1, commit 41039b4)"
  modified:
    - ".planning/phases/06.7-.../06.7-HUMAN-UAT.md (Task 1 scaffold + Task 2 sign-off, commit 91f0d62)"
    - ".planning/seeds/SEED-002-emergency-full-pod-hot-standby-and-embed-on-pod.md (planted)"
decisions:
  - "S6 (Pitfall #11 live emerg cutback) deferred — emergency subsystem is being redesigned to a full primary-pod hot-standby (SEED-002), making the current LLM-only emerg test moot. Pitfall #11 re-assert holds unit coverage (Plan 06.7-08 reconciler_test) + empirical post-restart observation."
  - "Host blocklist via env (not DB) — fastest to ship, operator-curated; revisit if Vast host fleet churns or list grows past ~20 entries."
  - "Removed `verified` + EU `geolocation` filters from Vast SearchOffers + lowered PRIMARY_VAST_PRICE_CAP_DPH 2.20→1.00 — Phase 06.6 UAT 18 had assumed $2.00/h floor; 06.7 discovered EU 5090 routinely available at $0.33–0.61/h."
  - "Latent gap acknowledged: llm/stt have the same `emergency_pod_<role>` no-registered-proxy issue that TTS had (commit 12f7479 fixed TTS; commit 30f90e7 then fixed llm/stt explicitly during Phase 06.6 td #4 closure on 2026-05-22)."
requirements-completed: [D-03, D-05, D-06, D-07, D-08, D-10, D-11, D-13]
metrics:
  duration: "~3 days wall (2026-05-19 → 2026-05-21 live UAT + 2026-05-21 → 2026-05-22 follow-up patches)"
  tasks: 2
  files: 3
  completed: "2026-05-21"
  vast-spend: "~$2-3 (multiple 5090 short runs $0.33–0.61/h + emerg 4090 ~$0.14)"
---

# Phase 06.7 Plan 09: Live HUMAN-UAT — TTS on GPU + Embed 24/7 on CPU

**5/6 scenarios live PASS on Vast RTX 5090 (S1 VRAM/DCGM + S2 zero-shot voice-clone 24kHz + S3 voice-survives-pod-replacement via S3 WAV refetch + S4 Piper fallback WAV 16kHz + S5 embed 24/7 + CLEANUP). S6 Pitfall #11 live-fire deferred — emerg redesign (SEED-002) supersedes the LLM-only emerg path.**

## Performance

- **Started:** 2026-05-19 (Task 1 runbook + scenario sheet)
- **Live UAT:** 2026-05-21
- **Completed:** 2026-05-21 (HUMAN-UAT sign-off committed `91f0d62`)
- **Tasks:** 2 (Task 1 autonomous + Task 2 human-verify)
- **Files modified:** 3
- **Vast spend:** ~$2-3 (5090 multiple short runs at $0.33–0.61/h + brief emerg 4090 ~$0.14; orphan-free this round)

## Accomplishments

- **Live phase gate PASS on real GPU:** every behavior that ONLY live hosts can prove was demonstrated end-to-end — Chatterbox fits the 5090 (~4 GB peak + headroom alongside LLM+STT), zero-shot voice-clone works (24kHz cloned WAV), a voice survives a pod replacement via lazy S3 reference-WAV refetch (NO `.pt`, NO embedding regen), off-peak Piper fallback returns WAV 16kHz per GATE 3 Option A, embed serves RAG 24/7 from CPU while the pod is asleep.
- **8 production bugs caught + fixed live, all on `develop`:**
  1. CI: sqlc gen out of sync + gofmt unclean main.go
  2. Piper adapter sent form-encoded; voice-api Piper expects JSON `/tts`
  3. torch cu124 → **cu128** (RTX 5090 Blackwell sm_120 — no kernel image)
  4. **CUPTI**: cu128 torch needs nvidia-cuda-*-cu12 12.8 runtime (dropped `--no-deps`)
  5. **setuptools<81**: resemble-perth uses `pkg_resources` (removed in setuptools 81+) → PerthImplicitWatermarker None
  6. audit middleware truncated the multipart voice upload (isAudioRoute only matched `/transcriptions`) — fix `c5acccc fix(06.7-07): audit must skip body capture for all /v1/audio/* routes`
  7. **TTS routing**: loader override resolves name `emergency_pod_tts`, but dispatcher had no proxy registered → 503; fix `12f7479 fix(06.7-07): register dynamic emergency_pod_tts proxy for pod routing` adds `NewDynamicTTSProxy`
  8. Host selection: removed `verified` + EU-`geolocation` filters (commit `6e66841`); lowered `PRIMARY_VAST_PRICE_CAP_DPH` 2.20→1.00; added `PRIMARY_VAST_MACHINE_BLOCKLIST` + machine_id logging (commit `96e823e`) — broken-CDI hosts catalogued: 36773, 53128, 38389
- **CLEANUP verified end-to-end:** MinIO voice objects deleted (`mc rm` two `voice_id` WAVs); Vast instance count = 0; total UAT spend bounded at ~$2-3 (no orphan pods this round — the UAT-9 lesson held).
- **Strategic discovery → SEED-002:** S6 deferral isn't a gap, it's the trigger for a phase redesign. The LLM-only emerg pod (~$0.37/h 4090, 28-minute cold-start) is being deprecated in favor of a full primary-pod hot-standby on 5090 (same image/weights/onstart/GPU/cap/blocklist/filter as primary). Embed also returns to the pod as tier-0 (GPU) with the 24/7 CPU Infinity as tier-1 — reversing 06.7 D-03. Target topology: every role tier-0=pod-GPU, tier-1=always-on fallback (LLM→OpenRouter, STT→OpenAI Whisper, TTS→Piper, embed→CPU 24/7). Seed planted at `.planning/seeds/SEED-002-emergency-full-pod-hot-standby-and-embed-on-pod.md`.

## Live UAT Scoreboard

| # | Scenario | Verdict | Evidence |
|---|----------|---------|----------|
| S1 | 5090 + DCGM Chatterbox ~4GB headroom | **PASS** | nvidia-smi 21972 MiB used / 10138 MiB free / 32607 MiB total on RTX 5090 (Chatterbox ≈ +4GB over LLM+STT 18GB); pod `/health:8003 {"status":"ok","model_ready":true}`; gateway `primary state=ready` + `tts tier-0 override` asserted |
| S2 | Zero-shot voice-clone round-trip (24kHz) | **PASS** | Upload → voice_id `c8b6aa6c…`; `/v1/audio/speech` HTTP 200, WAV **24000 Hz mono 5.8s**; MinIO `voices/c8b6aa6c….wav` (250 KiB) |
| S3 | Voice survives pod replacement (S3 refetch) | **PASS** | force-down + fresh pod (Vast 37226253, ephemeral storage wiped) → same voice_id → WAV 24kHz 4.5s; fresh pod cached `/opt/chatterbox-data/voices/c8b6aa6c….wav` (256078 B) at synth time = S3 refetch; only base-model `.pt` present (conds/ve/s3gen), **NO per-voice `.pt`** |
| S4 | Off-peak Piper fallback (WAV 16kHz) | **PASS** | pod down → `/v1/audio/speech` (no voice) → tier-1 Piper → WAV **16000 Hz mono 16-bit** (GATE 3 Option A); breaker `local-tts` OPEN → failover to `voice-api-piper` |
| S5 | Embed 24/7 (1024-dim, pod asleep) | **PASS** | pod asleep → `/v1/embeddings` → `dim=1024 model=multilingual-e5-large` (CPU Infinity n8n-ia-vm:7997) |
| S6 | Pitfall #11 tts re-assert ≤~1s | **DEFERRED** | Live emerg-cutback test deferred — emergency subsystem being redesigned to full primary-pod hot-standby (SEED-002), making LLM-only emerg test moot. Pitfall #11 re-assert holds Plan 06.7-08 unit coverage + empirical observation after gateway restart. |
| CLEANUP | MinIO voice objects gone + Vast count 0 | **PASS** | `mc rm` c8b6aa6c + e4ea7729; `mc ls voices/` empty; Vast instance count = 0 |

**Total live PASS: 5 + CLEANUP. Deferred: 1. Failures: 0.**

## Task Commits

1. **Task 1: RUNBOOK-PRIMARY-POD-TTS.md + 06.7-HUMAN-UAT.md scenario sheet** — `41039b4` (docs)
2. **Task 2: Live Vast 5090 UAT — 6 scenarios + cleanup + sign-off** — `91f0d62` (docs)

Live-fix patches landed during UAT:
- `6e66841` feat(06.7): drop EU-only geolocation filter from primary provisioning
- `96e823e` feat(06.7): catalog Vast hosts + machine_id blocklist for primary provisioning
- `f34506f` fix(06.7-05): pin setuptools<81 in chatterbox venv (perth pkg_resources)
- `c5acccc` fix(06.7-07): audit must skip body capture for all /v1/audio/* routes
- `12f7479` fix(06.7-07): register dynamic emergency_pod_tts proxy for pod routing
- `30f90e7` fix(06.7): register dynamic emergency_pod_{llm,stt} proxies (gateway→pod routing) — latent llm/stt gap closure
- `44e7d7f` docs(06.7): session handoff — UAT 5/6 PASS, STT pod bug, cost analysis, SEED-002

## Files Created/Modified

- `docs/RUNBOOK-PRIMARY-POD-TTS.md` — operator runbook for the TTS+embed topology (created by Task 1)
- `.planning/phases/06.7-…/06.7-HUMAN-UAT.md` — 6-scenario sheet + CLEANUP + full Verdict table with PASS/evidence + execution notes (Task 1 + Task 2)
- `.planning/seeds/SEED-002-emergency-full-pod-hot-standby-and-embed-on-pod.md` — emerg redesign seed that supersedes S6

## Decisions Made

- **S6 deferred via SEED-002, not via gap-closure plan.** Routing S6 to `/gsd:plan-phase 06.7 --gaps` would have meant implementing a Pitfall #11 fix against the LLM-only emerg subsystem that is on the chopping block. SEED-002 reverses D-03 (embed off-pod) and unifies emerg with primary architecture; once SEED-002 ships, S6 re-emerges as a scenario inside the new emerg-as-primary phase with the same `RestoreTier0 → re-assert` mechanic to verify.
- **Host blocklist is operator-curated env, not DB-backed.** Three broken-CDI hosts catalogued during UAT (36773, 53128, 38389); env knob keeps the loop fast and observable. Re-evaluate if Vast host churn renders the list stale within 60 days.
- **Price cap collapsed 2.20→1.00 after EU 5090 inventory discovery.** Phase 06.6 had assumed $2.00/h floor for 5090; 06.7 observed routine $0.33–0.61/h interruptible offers. Cap kept conservative at $1.00 to allow burst.
- **Latent llm/stt dispatcher gap eagerly closed.** The same `emergency_pod_<role>` 503 that bit TTS was waiting to bite llm/stt (only ever exercised pod-direct in prior UATs). Fixed proactively in `30f90e7` rather than waiting for the next phase to surface it — Phase 06.6 td #4 (2026-05-22) then verified live with regression test `bc26cc8`.

## Deviations from Plan

**1. S6 not signed PASS — deferred to SEED-002**
- **Found during:** Task 2 (live UAT)
- **Issue:** Plan acceptance criterion required all 6 scenarios signed PASS/FAIL with any FAIL routed to `/gsd:plan-phase 06.7 --gaps`. S6 was neither PASS nor FAIL — it was made moot by a concurrent architectural decision (emerg redesign).
- **Decision:** Document as DEFERRED with explicit pointer to SEED-002 + unit-coverage evidence + empirical post-restart observation. Plan completes as `passed_partial`.
- **Rationale:** A gap-closure plan against a deprecated subsystem is waste; SEED-002 carries forward the equivalent assertion in a more useful form.

**2. UAT consumed 3 days wall, not single session**
- **Found during:** Task 2
- **Issue:** Live UAT exposed 8 production bugs that needed fix-and-redeploy cycles (CI, Piper contract, torch cu128, CUPTI, setuptools, audit middleware, TTS dispatcher, host filters). Each fix required a CI build + Portainer pull + fresh pod cold-start.
- **Decision:** Iterated patches on develop rather than batching to a "UAT 2" cycle — kept latency low, spend bounded.

## Carry-Forward / Open Items

- **SEED-002 promotion:** Emerg redesign as full primary-pod hot-standby needs to be promoted from seed to a planned phase (likely Phase 11 or successor). S6 re-tested inside that phase.
- **Voice biometric LGPD review:** uploaded reference WAVs are biometric; flagged for Phase 09 LGPD review (per Plan 09 T-06.7-21 disposition).
- **Phase 07 observability:** dashboard must surface tts breaker state + voice-cache miss rate (Chatterbox `cache miss → S3 refetch` line — would have been invisible without grepping pod logs in S3).
