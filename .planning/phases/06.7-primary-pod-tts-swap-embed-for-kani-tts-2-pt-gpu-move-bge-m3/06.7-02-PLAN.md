---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - gateway/internal/proxy/tts_test.go
  - gateway/internal/proxy/voices_test.go
  - gateway/internal/upstreams/probe_test.go
  - gateway/internal/upstreams/loader_test.go
  - gateway/internal/quota/enforcer_test.go
  - gateway/internal/primary/reconciler_test.go
  - pod/primary/test_kani_shim.py
autonomous: true
requirements: [D-11, D-12, D-13, D-08, D-10, D-05, D-06]
must_haves:
  truths:
    - "Failing (RED) test stubs exist for every Wave 1/Wave 2 behavior so executors implement against contracts, not exploration"
    - "go test compiles for all touched packages (stubs reference real package symbols or t.Skip with TODO)"
    - "Each skipped stub names its OWNING downstream plan (the plan that MUST unskip it + make it assert real behavior before that plan completes)"
  artifacts:
    - path: "gateway/internal/proxy/tts_test.go"
      provides: "JSON→binary TTS proxy test stub"
      contains: "func TestTTS"
    - path: "gateway/internal/proxy/voices_test.go"
      provides: "voice CRUD + tenant-isolation test stub"
      contains: "func TestVoices"
    - path: "pod/primary/test_kani_shim.py"
      provides: "shim synth + voice-clone round-trip smoke stub"
      contains: "def test_"
  key_links:
    - from: "test stubs"
      to: "Wave 1/2 implementation plans 03/05/06/07/08"
      via: "Nyquist — each impl task references its test by name; owning plan unskips it"
      pattern: "go test .*-run"
---

<objective>
Wave 0 test scaffolding (Nyquist gate). Create RED/skip test stubs for every behavior added in Waves 1-2 so that no implementation task ships without an automated verify target. This is the test-first companion to the gate plan (01) and runs in parallel — it touches only `*_test.go` and a new pytest file, so it has zero file overlap with implementation plans.

CRITICAL ENFORCEMENT (REVIEWS consensus action #3 — Codex): a skipped stub satisfies compile-time scaffolding but does NOT fail if the implementation never happens. To prevent false confidence, EVERY stub created here carries a `// OWNER: Plan 0X` comment naming the downstream plan that MUST unskip it and make it assert real behavior. The Stub Ownership Map (below) is authoritative and is mirrored as a hard acceptance criterion in each owning plan.

Purpose: Satisfy the Nyquist rule (every `<verify>` has an `<automated>` target that exists) AND bind each stub to an owner that must turn it green.
Output: 6 Go test stub files + 1 pytest stub, each annotated with its owning plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-RESEARCH.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-PATTERNS.md

<stub_ownership_map>
Authoritative map of each stub → the plan that MUST unskip it + assert real behavior before that plan is COMPLETE.
Each owning plan repeats the relevant row as a hard acceptance criterion.

| Stub (test name) | File | OWNER plan |
|------------------|------|-----------|
| TestTier0_TTSKeyPresentEmbedAbsent | loader_test.go | 06.7-03 |
| TestTier0OverrideURL_Getter | loader_test.go | 06.7-03 |
| TestProbe_TTS_PostsAudioSpeech | probe_test.go | 06.7-03 |
| TestClassifyRoute_TTS | enforcer_test.go | 06.7-03 |
| TestEvaluateReady_ReassertsTier0WhenCleared | reconciler_test.go | 06.7-08 |
| TestMarkReady_OverridesTTSNotEmbed | reconciler_test.go | 06.7-08 |
| TestTTSProxy_JSONToBinaryAudio | tts_test.go | 06.7-07 |
| TestTTSProxy_ErrorEnvelope | tts_test.go | 06.7-07 |
| TestTTSProxy_PiperFallback_AdapterConverts (fallback path) | tts_test.go | 06.7-07 |
| TestVoices_CreateUploadsToS3AndReturnsVoiceID | voices_test.go | 06.7-07 |
| TestVoices_TenantIsolation_ListOnlyOwn | voices_test.go | 06.7-07 |
| TestVoices_RejectsOversizeWAV | voices_test.go | 06.7-07 |
| TestVoices_S3KeyFromUUIDNotInput | voices_test.go | 06.7-07 |
| TestVoices_DeleteRemovesRowAndS3Object | voices_test.go | 06.7-07 |
| TestVoices_UnsupportedResponseFormatReturns400 | voices_test.go / tts_test.go | 06.7-07 |
| test_speech_default_voice_returns_wav_22050 | test_kani_shim.py | 06.7-05 |
| test_voice_clone_roundtrip_upload_then_speak | test_kani_shim.py | 06.7-05 |
| test_voice_id_regex_rejects_path_traversal | test_kani_shim.py | 06.7-05 |
| test_health_returns_200_when_model_loaded | test_kani_shim.py | 06.7-05 |
| test_voice_cache_miss_refetches_from_s3 (pod-replacement survival) | test_kani_shim.py | 06.7-05 |

Note: embed live verification (Plan 06) is operator-curl, not a Go stub — it has no row here.
</stub_ownership_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Go test stubs for upstreams + quota + primary reconciler</name>
  <read_first>
    - gateway/internal/upstreams/probe.go (switch :228-266, existing case "embed"/"llm"/"stt")
    - gateway/internal/upstreams/loader.go (:87-93 map, :245-275 Override/Restore, :200-232 Resolve)
    - gateway/internal/quota/enforcer.go (:294-304 classifyRoute), gateway/internal/quota/bucket.go (:12-19)
    - gateway/internal/primary/reconciler.go (markReady, evaluateReady :394) + existing reconciler_test.go patterns
    - 06.7-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map) + §Code Examples
    - 06.7-PATTERNS.md (probe/loader/quota/reconciler sections)
    - <stub_ownership_map> above (annotate each stub with its OWNER)
  </read_first>
  <action>
    Add table-driven RED test stubs (use `t.Skip("OWNER Plan 0X — implement <X>")` where the symbol does not yet exist, so the package still compiles). Each stub gets a `// OWNER: Plan 0X` comment matching the Stub Ownership Map.
    (1) `probe_test.go` — `TestProbe_TTS_PostsAudioSpeech` (OWNER 03) asserting the tts probe POSTs `/v1/audio/speech` with body `{"model":"tts-1","input":"ping","voice":"random","response_format":"pcm"}` and treats 200+bytes as success, 5xx as breaker failure (mirror the embed-case assertion).
    (2) `loader_test.go` — `TestTier0_TTSKeyPresentEmbedAbsent` (OWNER 03; map has "tts", lacks "embed" per D-03/D-11) and `TestTier0OverrideURL_Getter` (OWNER 03) for the new `Tier0OverrideURL(role)(string,bool)` getter (D-13).
    (3) `enforcer_test.go` — `TestClassifyRoute_TTS` (OWNER 03) asserting `/v1/audio/speech` → `RouteClassTTS`.
    (4) `reconciler_test.go` — `TestEvaluateReady_ReassertsTier0WhenCleared` (OWNER 08; Pitfall #11 / D-13: when slot empty + FSM Ready, re-call OverrideTier0 for llm/stt/tts, NOT embed) and `TestMarkReady_OverridesTTSNotEmbed` (OWNER 08).
    Each stub names the exact assertion in a comment so the owning executor implements to it.
  </action>
  <verify>
    <automated>cd gateway && go vet ./internal/upstreams/... ./internal/quota/... ./internal/primary/... && go test ./internal/upstreams/... ./internal/quota/... ./internal/primary/... 2>&1 | grep -v '^#' | grep -Eq '(ok|SKIP|PASS|no test files)'</automated>
  </verify>
  <acceptance_criteria>
    - `cd gateway && go build ./...` exits 0 (stubs compile; skipped where symbol absent)
    - `grep -c 'func Test' gateway/internal/upstreams/probe_test.go` ≥ 1 and references `tts`
    - `grep -q 'RouteClassTTS' gateway/internal/quota/enforcer_test.go`
    - `grep -q 'Tier0OverrideURL' gateway/internal/upstreams/loader_test.go`
    - `grep -q 'Reasserts\|reassert\|Reassert' gateway/internal/primary/reconciler_test.go`
    - `grep -c 'OWNER' gateway/internal/upstreams/loader_test.go gateway/internal/upstreams/probe_test.go gateway/internal/quota/enforcer_test.go gateway/internal/primary/reconciler_test.go` ≥ 6 (every stub annotated)
  </acceptance_criteria>
  <done>RED/skip stubs exist for tts probe, tier-0 map+getter, quota classify, and Pitfall #11 re-assert; packages compile; each stub annotated with its OWNER plan.</done>
</task>

<task type="auto">
  <name>Task 2: Proxy + shim test stubs (TTS proxy + Piper fallback, voices CRUD, kani shim + health + pod-replacement)</name>
  <read_first>
    - gateway/internal/proxy/audio.go (constructor shape :19-40), gateway/internal/proxy/embeddings.go (JSON-in handler), gateway/internal/proxy/director.go (BuildDirector :39-62)
    - gateway/internal/proxy/dispatcher.go (tier-0→tier-1 fallback wrap — for the Piper-fallback stub)
    - gateway/internal/auth/context.go (:13-39 MustFromContext / AuthContext — tenant isolation)
    - pod/smoke/smoke.py (only in-repo Python style reference)
    - 06.7-RESEARCH.md §Code Examples (/v1/audio/speech contract), §Security Domain (tenant isolation, path traversal, WAV size cap), §Pitfall 5 (Piper fallback)
    - 06.7-PATTERNS.md (tts.go, voices.go, kani_shim.py sections)
    - <stub_ownership_map> above
  </read_first>
  <action>
    (1) `gateway/internal/proxy/tts_test.go` — `TestTTSProxy_JSONToBinaryAudio` (OWNER 07; fake upstream returns audio bytes for POST /v1/audio/speech; proxy preserves Content-Type + path 1:1; strips client Authorization via BuildDirector), `TestTTSProxy_ErrorEnvelope` (OWNER 07; upstream unreachable → OpenAI-shaped 502), and `TestTTSProxy_PiperFallback_AdapterConverts` (OWNER 07; tier-0 breaker OPEN → tier-1 Piper receives the request; if GATE 3 = adapter, the response is converted to the GATE-3 target format with `audio/wav` Content-Type). Use `t.Skip("OWNER Plan 07: implement NewTTSProxy / dispatcher fallback")`.
    (2) `gateway/internal/proxy/voices_test.go` — `TestVoices_CreateUploadsToS3AndReturnsVoiceID`, `TestVoices_TenantIsolation_ListOnlyOwn` (D-10), `TestVoices_RejectsOversizeWAV` (DoS cap), `TestVoices_S3KeyFromUUIDNotInput` (path-traversal mitigation), `TestVoices_DeleteRemovesRowAndS3Object` (cleanup semantics), `TestVoices_UnsupportedResponseFormatReturns400` (clean 400) — all OWNER 07. Mock S3 via an interface. `t.Skip` until handlers exist.
    (3) `pod/primary/test_kani_shim.py` — pytest stubs (all OWNER 05, `pytest.mark.skip(reason="OWNER Plan 05: implement kani_shim")`): `test_speech_default_voice_returns_wav_22050`, `test_voice_clone_roundtrip_upload_then_speak`, `test_voice_id_regex_rejects_path_traversal`, `test_health_returns_200_when_model_loaded` (GET /health probe target — REVIEWS action #4), `test_voice_cache_miss_refetches_from_s3` (pod-replacement survival: unknown voice_id with no local .pt → shim fetches reference WAV from MinIO, regenerates embedding, caches — REVIEWS action #1).
  </action>
  <verify>
    <automated>cd gateway && go build ./internal/proxy/... && go test ./internal/proxy/... 2>&1 | grep -v '^#' | grep -Eq '(ok|SKIP|PASS|no test files)' && python3 -m py_compile pod/primary/test_kani_shim.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func TestTTSProxy' gateway/internal/proxy/tts_test.go` and `grep -q 'PiperFallback' gateway/internal/proxy/tts_test.go`
    - `grep -q 'TenantIsolation' gateway/internal/proxy/voices_test.go`
    - `grep -Eq 'path.?traversal|UUID' gateway/internal/proxy/voices_test.go`
    - `grep -c 'def test_' pod/primary/test_kani_shim.py` ≥ 5 (includes health + cache-miss survival)
    - `grep -q 'test_health' pod/primary/test_kani_shim.py` and `grep -q 'cache_miss_refetches_from_s3' pod/primary/test_kani_shim.py`
    - `grep -c 'OWNER' gateway/internal/proxy/tts_test.go gateway/internal/proxy/voices_test.go pod/primary/test_kani_shim.py` ≥ 5
    - `cd gateway && go build ./internal/proxy/...` exits 0
  </acceptance_criteria>
  <done>RED/skip stubs exist for TTS proxy (JSON→binary + Piper fallback), voices CRUD with tenant isolation + path-traversal + DoS + cleanup + 400, and kani shim synth/clone/health/pod-replacement smoke; each annotated with its OWNER plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures only | no runtime trust boundary crossed; tests assert mitigations exist downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-02 | Information Disclosure | voices_test cross-tenant assertion | mitigate | TestVoices_TenantIsolation encodes the V4 access-control requirement so the Wave 2 handler cannot ship without it |
| T-06.7-03 | Tampering | voices_test path-traversal assertion | mitigate | TestVoices_S3KeyFromUUIDNotInput + shim regex test encode V5/V12 mitigations as RED tests |
</threat_model>

<verification>
- `cd gateway && go build ./...` exits 0
- `python3 -m py_compile pod/primary/test_kani_shim.py` exits 0
- Every Wave 1/2 behavior in RESEARCH §Validation Architecture Test Map has a named stub + an OWNER annotation
</verification>

<success_criteria>
All test stubs compile and are runnable (skipped). Each downstream implementation task has a concrete `go test -run` / `pytest` target naming its assertion, and each stub names the plan that must turn it green.
</success_criteria>

<output>
Create `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-02-SUMMARY.md` when done.
</output>
