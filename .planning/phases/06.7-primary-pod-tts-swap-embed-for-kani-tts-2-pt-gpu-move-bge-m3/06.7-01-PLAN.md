---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-WAVE0-GATES.md
autonomous: false
requirements: [D-01, D-02, D-05, D-07]
must_haves:
  truths:
    - "Operator has listened to a Kani-TTS-2-pt PT-BR synthesis sample and signed off that pronunciation/prosody is acceptable (or rejected → engine reverts)"
    - "torch+nemo from the Kani stack run on GPU inside the primary pod base image (CUDA compat confirmed) and the resulting image size fits the Vast disk budget"
    - "GATE 3 emits a CONCRETE Piper fallback implementation contract (gateway adapter | voice-api route | controlled 503) — including exact conversion + content-type + response_format behavior if adapter — that Plan 07 implements verbatim"
    - "Embed host capacity on n8n-ia-vm is measured against multilingual-e5-large int8 ONNX footprint; co-located vs dedicated-VM decision is recorded"
  artifacts:
    - path: ".planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-WAVE0-GATES.md"
      provides: "Operator-locked sign-off doc for the 4 blocking gates"
      contains: "GATE 1"
  key_links:
    - from: "06.7-WAVE0-GATES.md GATE 1 (Kani PT-BR)"
      to: "downstream Wave 1 Plan 05 (TTS shim + Dockerfile)"
      via: "blocking sign-off — no production TTS code until GATE 1 = PASS"
      pattern: "GATE 1.*(PASS|FAIL)"
    - from: "06.7-WAVE0-GATES.md GATE 3 (Piper fallback contract)"
      to: "Plan 07 tier-1 Piper adapter/route"
      via: "named contract (adapter|route|503) implemented verbatim downstream"
      pattern: "GATE 3.*(adapter|voice-api route|503)"
---

<objective>
Front-loaded BLOCKING gate plan. Validate the three uncertain premises of this phase BEFORE any production TTS/embed code is committed, and record operator sign-off in a WAVE0-GATES lock document.

The phase engine (Kani-TTS-2-pt) is LOCKED by CONTEXT, but its PT-BR quality is UNCERTAIN (RESEARCH Open Q1 / Assumption A1 — model card metadata lists `en/es/ky` and "optimized primarily for English"). If PT-BR quality fails, the engine decision reverts and downstream waves MUST NOT proceed.

Purpose: Prevent burning Vast spend + build time on an engine/host that does not meet requirements.
Output: `06.7-WAVE0-GATES.md` with 4 operator-locked gate decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-CONTEXT.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: GATE 1 — Kani-TTS-2-pt PT-BR quality spike + sign-off</name>
  <read_first>
    - .planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-RESEARCH.md (§Pitfall 1, §Open Questions Q1, §Assumptions Log A1, §Pattern 2 loading excerpt)
    - 06.7-CONTEXT.md D-05/D-06 (shim contract)
    - HuggingFace card huggingface.co/nineninesix/kani-tts-2-pt (loading + voice-clone API)
  </read_first>
  <action>
    Spin a short-lived Vast.ai RTX 5090 (or 4090) instance using the existing Vast tooling (VAST_AI_API_KEY in ~/.claude/CLAUDE.md; pattern from pod/scripts/vast-ai.sh). Inside it, install the Kani stack per RESEARCH §Standard Stack (`pip install kani-tts-2 "nemo-toolkit[tts]" "transformers==4.57.1" fastapi uvicorn scipy`) and load `nineninesix/kani-tts-2-pt` via `KaniTTS` + `SpeakerEmbedder` (RESEARCH §Pattern 2). Synthesize at least 3 PT-BR phrases including the cobranças-style sentence "Olá, seu boleto venceu ontem" and one with a clone reference WAV. Save outputs as 22050 Hz 16-bit mono WAV. Present the audio files to the operator for listening sign-off. Record verdict (PASS = proceed with Kani; FAIL = engine reverts, halt phase) plus measured RTF and VRAM-at-load in GATE 1 of the WAVE0-GATES doc. This is a blocking-human gate per RESEARCH Pitfall 1 — autonomous mode CANNOT auto-approve.
  </action>
  <verify>
    <automated>MISSING — operator listening sign-off required; record outcome in GATE 1 section of 06.7-WAVE0-GATES.md</automated>
    <human-check>Operator listens to the 3 PT-BR WAV samples and types PASS or FAIL with notes on pronunciation/prosody</human-check>
  </verify>
  <acceptance_criteria>
    - `06.7-WAVE0-GATES.md` exists and section `## GATE 1 — Kani PT-BR quality` contains `PASS` or `FAIL` plus operator notes
    - At least 3 PT-BR sample WAV files were generated and presented (paths recorded in GATE 1)
    - If FAIL: a recorded fallback decision (kani-tts-2-en + PT text degraded / fine-tune / reconsider engine) and an explicit instruction to HALT downstream waves
  </acceptance_criteria>
  <done>Operator has signed PASS or FAIL for Kani PT-BR quality; verdict recorded in WAVE0-GATES GATE 1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: GATE 2 — Kani CUDA/image compat spike + GATE 3 Piper fallback CONTRACT</name>
  <read_first>
    - 06.7-RESEARCH.md (§Pitfall 2 CUDA compat, §Assumptions A2, §Pitfall 5 Piper, §Environment Availability)
    - pod/primary/Dockerfile (current base image llama.cpp:server-cuda-b9191, venv pip stages :97-107)
    - voice-api Piper on vps-ifix-vm: `voice-api-piper-1` container, POST /tts → ulaw 8kHz, port 5100 (RESEARCH §Pitfall 5)
  </read_first>
  <action>
    GATE 2 (CUDA/image): On the same spike instance from Task 1, confirm torch+nemo from the Kani venv run inference on GPU on the pod base CUDA runtime (RESEARCH Pitfall 2 — 5090 is Blackwell, Ampere+ OK). Record whether the base `llama.cpp:server-cuda-b9191` CUDA version suffices or whether a CUDA 12.8 base bump is needed (A2 fallback). Build/estimate the added image-layer size of `/opt/kani-venv` + NanoCodec weights and confirm it fits the Vast disk filter (RESEARCH Pitfall 2 warns 40 GB may be tight).

    GATE 3 (Piper fallback — emit a CONCRETE IMPLEMENTATION CONTRACT, not just PASS/FAIL): via `ssh vps-ifix-vm`, exercise `voice-api-piper-1` POST /tts (port 5100) and capture the actual response format (ulaw 8kHz `audio/basic` per RESEARCH §Pitfall 5). Then DECIDE and RECORD one of three named downstream contracts that Plan 07 implements verbatim — there is no "TBD":
      • Option A `gateway adapter` — Plan 07's tier-1 path includes a ModifyResponse interceptor converting ulaw 8kHz → the client's requested format. Record EXACTLY: (1) conversion target (recommend WAV 16kHz PCM — a stable contract for off-peak clients, not raw telephony ulaw); (2) conversion tool/library (e.g. ffmpeg subprocess `ffmpeg -f mulaw -ar 8000 -i - -ar 16000 -f wav -`, or a Go pure mu-law decode + RIFF/WAV header writer — name the chosen one); (3) the JSON→form translation `/v1/audio/speech {input,voice}` → Piper `POST /tts {text,voice}`; (4) Content-Type negotiation (set `audio/wav` on the converted response); (5) `response_format` behavior off-peak — which values the adapter supports (`wav`/`pcm`) and that unsupported values return a clean 400.
      • Option B `voice-api route` — voice-api (Bun) gains its own `POST /v1/audio/speech` that internally proxies Piper + converts; the gateway tier-1 row points straight at it (no Go adapter). Record the endpoint URL + delivered format.
      • Option C `controlled 503` — off-peak TTS returns a deterministic 503 with an OpenAI-shaped error body; record that Plan 07 wires the tier-1 row to a handler emitting 503 (no audio).

    Record GATE 2 + GATE 3 in WAVE0-GATES. GATE 3 MUST name exactly one option and (if Option A) fill all 5 sub-fields so Plan 07 has zero ambiguity.
  </action>
  <verify>
    <automated>MISSING — operator-verified spike measurements; record in GATE 2 + GATE 3 sections of 06.7-WAVE0-GATES.md</automated>
    <human-check>Operator confirms CUDA compat result + image-size budget + the named Piper fallback contract (and the 5 sub-fields if Option A)</human-check>
  </verify>
  <acceptance_criteria>
    - `06.7-WAVE0-GATES.md` section `## GATE 2 — Kani CUDA/image` records: CUDA-compat verdict, base-image decision (keep b9191 / bump 12.8), measured added image size vs disk budget
    - section `## GATE 3 — Piper fallback contract` names exactly ONE of `adapter` / `voice-api route` / `503` (no "TBD")
    - if `adapter` chosen: all 5 sub-fields recorded (conversion target, tool/library, JSON→form translation, Content-Type, response_format support + 400-on-unsupported)
    - the recorded contract is concrete enough that Plan 05 (shim/Dockerfile) and Plan 07 (TTS proxy/fallback) build deterministically with no further decision
  </acceptance_criteria>
  <done>GATE 2 verdict + GATE 3 named contract (with adapter sub-fields if applicable) recorded and operator-confirmed in WAVE0-GATES.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: GATE 4 — embed host capacity on n8n-ia-vm + model lock</name>
  <read_first>
    - 06.7-RESEARCH.md (§Standard Stack embed, §Open Q2, §Assumptions A4, §Environment Availability, §Alternatives Considered)
    - 06.7-CONTEXT.md D-01 (host n8n-ia-vm), D-02 (model open — researcher recommends multilingual-e5-large via Infinity)
    - CLAUDE.md infra (n8n-ia-vm = VMID 101, 10.10.10.20, 6c/16G)
  </read_first>
  <action>
    Via `ssh n8n-ia-vm`, measure current free RAM/disk under its live Swarm load (`free -h`, `df -h`, `docker ps`) — RESEARCH §Open Q2 notes ~3.7 GB free + 8.9 cache. Estimate the steady-state + peak RAM footprint of `michaelf34/infinity:0.0.77` serving `intfloat/multilingual-e5-large` int8 ONNX on CPU (RESEARCH: ~600 MB model + ~1-2 GB runtime). Confirm the embed model lock = multilingual-e5-large served via Infinity (mistral-embed is API-only → disqualified per D-02 self-host; jina-v3 CC-BY-NC → disqualified). Decide co-located on n8n-ia-vm vs dedicated VM (D-01 authorizes a dedicated VM if footprint exceeds headroom). Record the host decision + locked embed model + Infinity image SHA-pin reference (from 06.6-WAVE0-GATES.md) in GATE 4. Also record the Infinity-served path (`/embeddings`, NOT `/v1/embeddings` — verified UAT 14) so Plan 06 knows a gateway-side path rewrite may be required.
  </action>
  <verify>
    <automated>MISSING — operator capacity decision; record in GATE 4 section of 06.7-WAVE0-GATES.md</automated>
    <human-check>Operator approves co-located-vs-dedicated host decision and embed model lock</human-check>
  </verify>
  <acceptance_criteria>
    - `06.7-WAVE0-GATES.md` section `## GATE 4 — embed host + model` records measured n8n-ia-vm free RAM/disk, estimated Infinity+e5-large footprint, and the host decision (co-located | dedicated VM)
    - embed model is LOCKED to `intfloat/multilingual-e5-large` served by Infinity (1024-dim) with rationale citing self-host (D-02) disqualification of mistral-embed/jina-v3
    - GATE 4 notes the Infinity-served path is `/embeddings` (so Plan 06 verifies the gateway `/v1/embeddings` route reaches it, rewriting if needed)
    - decision is concrete so Plan 06 (embed deploy) can target the right host
  </acceptance_criteria>
  <done>GATE 4 host + embed-model decision recorded and operator-approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator laptop → Vast.ai pod | spike instance provisioning with VAST_AI_API_KEY (full-account credential) |
| ops-claude → n8n-ia-vm (SSH) | capacity probe on a production Swarm host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-01 | Denial of Service | spike Vast instance left running | mitigate | spike is short-lived; destroy instance immediately after sign-off via Vast DELETE (pod/scripts/vast-ai.sh destroy); record spend in WAVE0-GATES |
| T-06.7-02 | Information Disclosure | VAST_AI_API_KEY exposure in logs | accept | key already in ~/.claude/CLAUDE.md local-only; do not echo into committed artifacts |
| T-06.7-SC | Tampering | pip install of kani-tts-2 / nemo-toolkit on spike | mitigate | RESEARCH §Package Legitimacy Audit verified 7/7 [OK] via slopcheck 0.6.1; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- 06.7-WAVE0-GATES.md exists with 4 GATE sections, each carrying an explicit operator decision (no "TBD")
- GATE 1 verdict gates all downstream TTS work; if FAIL, phase halts and re-plans the engine
- GATE 3 names a concrete contract Plan 07 implements verbatim
- Spike Vast instance destroyed; spend recorded
</verification>

<success_criteria>
All 4 gates resolved with operator sign-off. GATE 1 = PASS unblocks Wave 1 TTS plans; GATE 3 hands Plan 07 a deterministic Piper contract; GATE 4 locks embed host/model for Plan 06.
</success_criteria>

<output>
Create `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-01-SUMMARY.md` when done.
Create/populate `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-WAVE0-GATES.md`.
</output>
