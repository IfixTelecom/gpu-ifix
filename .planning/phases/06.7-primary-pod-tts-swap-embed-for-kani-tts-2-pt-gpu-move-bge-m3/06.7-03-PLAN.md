---
phase: 06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3
plan: 03
type: execute
wave: 1
depends_on: ["06.7-02"]
files_modified:
  - gateway/internal/upstreams/loader.go
  - gateway/internal/upstreams/probe.go
  - gateway/internal/upstreams/types.go
  - gateway/internal/quota/bucket.go
  - gateway/internal/quota/enforcer.go
autonomous: true
requirements: [D-11, D-12]
must_haves:
  truths:
    - "The tier-0 override map contains a 'tts' key and no longer contains 'embed' (embed is now static)"
    - "Loader exposes Tier0OverrideURL(role)(string,bool) so the primary reconciler can detect a cleared slot"
    - "The upstream probe issues a real /v1/audio/speech POST for role 'tts' and treats 200+audio as healthy, 5xx as a breaker failure"
    - "Quota classifies POST /v1/audio/speech as RouteClassTTS"
  artifacts:
    - path: "gateway/internal/upstreams/loader.go"
      provides: "tts override slot + Tier0OverrideURL getter"
      contains: "Tier0OverrideURL"
    - path: "gateway/internal/upstreams/probe.go"
      provides: "tts probe case"
      contains: "/v1/audio/speech"
    - path: "gateway/internal/quota/bucket.go"
      provides: "RouteClassTTS constant"
      contains: "RouteClassTTS"
  key_links:
    - from: "loader.Tier0OverrideURL"
      to: "primary reconciler evaluateReady (Plan 08)"
      via: "Pitfall #11 re-assert reads this getter"
      pattern: "Tier0OverrideURL"
    - from: "probe case tts"
      to: "dispatcher tier-0/tier-1 fallback"
      via: "breaker state drives Piper fallback"
      pattern: "case \"tts\""
---

<objective>
Gateway tier-0 routing core for TTS. Surgical edits to the role-agnostic upstream machinery so `tts` becomes a first-class dynamic-override role and `embed` leaves the override map (becomes static per D-03). No new subsystem — the map, probe, and quota classifier already exist and are role-keyed.

Purpose: Wire the routing primitives that the TTS proxy (Plan 07) and primary reconciler (Plan 08) depend on.
Output: tts in the override map + new getter; tts probe; RouteClassTTS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-RESEARCH.md
@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-PATTERNS.md

<interfaces>
From gateway/internal/upstreams/loader.go (current):
- newTier0OverrideMap() returns map{"llm","stt","embed"} of *atomic.Pointer[string] (loader.go:87-93; comment :82-86 confirms "só o map precisa crescer")
- OverrideTier0(role, url string) / RestoreTier0(role string) — role-agnostic, signature unchanged (:245-275)
- Resolve(role string, tier int) reads tier0Override[role] atomically (:200-232)

From gateway/internal/upstreams/probe.go:
- dispatch() switch with case "llm" (:229), case "embed" (:236), case "stt" (:243); success/failure logic role-agnostic (:279-288, 5xx→*breaker.HTTPError)

From gateway/internal/quota/bucket.go (:12-19): RouteClass consts (string wire-contract for Redis — do not rename existing)
From gateway/internal/quota/enforcer.go (:294-304): classifyRoute(path string) RouteClass
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tier-0 map (add tts, remove embed) + Tier0OverrideURL getter + Role comment</name>
  <read_first>
    - gateway/internal/upstreams/loader.go (:82-93 map+comment, :200-232 Resolve hot-path, :245-275 Override/Restore)
    - gateway/internal/upstreams/types.go (:24-42 Role field)
    - gateway/internal/upstreams/loader_test.go (stubs from Plan 02: TestTier0_TTSKeyPresentEmbedAbsent, TestTier0OverrideURL_Getter)
    - 06.7-PATTERNS.md §loader.go section + 06.7-RESEARCH.md §Pattern 4 (embed static) + §Pitfall 3 (getter design)
  </read_first>
  <behavior>
    - newTier0OverrideMap() returns a map with keys exactly {"llm","stt","tts"} — "embed" removed (D-03), "tts" added (D-11)
    - Tier0OverrideURL("tts") returns ("",false) before any override, (url,true) after OverrideTier0("tts",url)
    - Tier0OverrideURL("embed") returns ("",false) always (no slot)
    - Resolve("embed",0) still resolves the static row UPSTREAM_EMBED_URL unchanged (no override path)
  </behavior>
  <action>
    In `newTier0OverrideMap()` (loader.go:87-93) remove the `"embed"` entry and add `"tts": new(atomic.Pointer[string])` per D-03/D-11 — the file comment at :82-86 already anticipates this. Add a new method `Tier0OverrideURL(role string) (string, bool)` mirroring the atomic read in `Resolve` (:206-208): look up `l.tier0Override[role]`, return ("",false) if key absent or pointer nil/empty, else (*ptr,true). Required by the Pitfall #11 fix in Plan 08 (D-13). Update the `Role` field comment in types.go:27 from `"llm"|"stt"|"embed"` to `"llm"|"stt"|"embed"|"tts"`. Do NOT change OverrideTier0/RestoreTier0 signatures.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/upstreams/ -run 'Tier0' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A6 'func newTier0OverrideMap' gateway/internal/upstreams/loader.go | grep -q '"tts"'` and `grep -A6 'func newTier0OverrideMap' gateway/internal/upstreams/loader.go | grep -vq '"embed"'`
    - `grep -q 'func (l \*Loader) Tier0OverrideURL(role string) (string, bool)' gateway/internal/upstreams/loader.go`
    - `cd gateway && go test ./internal/upstreams/ -run Tier0 -count=1` PASS (Plan 02 stubs now green)
    - `cd gateway && go build ./...` exits 0
  </acceptance_criteria>
  <done>tts in override map, embed removed, Tier0OverrideURL getter present, Role comment updated; Tier0 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tts probe case + RouteClassTTS quota classify</name>
  <read_first>
    - gateway/internal/upstreams/probe.go (dispatch switch :228-288)
    - gateway/internal/quota/bucket.go (:12-19), gateway/internal/quota/enforcer.go (classifyRoute :294-304)
    - probe_test.go + enforcer_test.go stubs from Plan 02
    - 06.7-RESEARCH.md §Code Examples (probe body + RouteClassTTS) + 06.7-PATTERNS.md probe/quota sections
  </read_first>
  <behavior>
    - probe role "tts" POSTs upstreamURL+"/v1/audio/speech" with Content-Type application/json and body {"model":"tts-1","input":"ping","voice":"random","response_format":"pcm"}; 200→healthy, 5xx→*breaker.HTTPError
    - classifyRoute("/v1/audio/speech") == RouteClassTTS; unknown paths still default to RouteClassChat
  </behavior>
  <action>
    Add `case "tts":` to the probe `dispatch()` switch (probe.go) mirroring `case "embed":` but POSTing `/v1/audio/speech` with the exact body from RESEARCH §Code Examples; reuse the shared success/failure logic at :279-288 (no change). Add `RouteClassTTS RouteClass = "tts"` to bucket.go const block (the string value is a Redis wire-contract — fixed). Add `case "/v1/audio/speech": return RouteClassTTS` to classifyRoute in enforcer.go.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/upstreams/ -run Probe -count=1 && go test ./internal/quota/ -run Classify -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'case "tts"' gateway/internal/upstreams/probe.go` and `grep -q '/v1/audio/speech' gateway/internal/upstreams/probe.go`
    - `grep -q 'RouteClassTTS' gateway/internal/quota/bucket.go`
    - `grep -q '"/v1/audio/speech"' gateway/internal/quota/enforcer.go`
    - `cd gateway && go test ./internal/upstreams/ -run Probe -count=1` and `go test ./internal/quota/ -run Classify -count=1` PASS
  </acceptance_criteria>
  <done>tts probe POSTs /v1/audio/speech; /v1/audio/speech classified RouteClassTTS; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → tts upstream | probe POST crosses into the pod/Piper inference service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-04 | Denial of Service | tts probe body | mitigate | probe input is a fixed 4-char "ping" with response_format pcm — bounded synth cost; reuses existing probe timeout/breaker |
| T-06.7-05 | Tampering | Resolve("embed") regression | mitigate | Task 1 keeps embed resolving the static row; loader_test asserts embed has no override slot |
</threat_model>

<verification>
- `cd gateway && go test ./internal/upstreams/... ./internal/quota/... -count=1` green
- `cd gateway && go build ./...` exits 0
</verification>

<success_criteria>
tts is a dynamic-override role with a probe and quota class; embed is removed from the override map; Tier0OverrideURL getter exists for Plan 08.
</success_criteria>

<output>
Create `.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-03-SUMMARY.md` when done.
</output>
