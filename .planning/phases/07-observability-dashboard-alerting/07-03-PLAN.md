---
phase: 07-observability-dashboard-alerting
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - gateway/db/queries/audit.sql
  - gateway/internal/admin/metrics.go
  - gateway/internal/admin/metrics_test.go
  - gateway/internal/admin/audit.go
  - gateway/internal/admin/audit_test.go
autonomous: true
requirements: [OBS-01, OBS-07]
must_haves:
  truths:
    - "GET /admin/metrics returns JSON with per-tenant P50/P95/P99 + error rate, per-route + per-upstream latency, inflight, and current FSM state"
    - "GET /admin/audit returns a paginated JSON list of audit_log state-change rows ordered newest-first"
    - "Both handlers reject bad query params with the OpenAI error envelope and a 4xx admin-metric increment"
  artifacts:
    - path: "gateway/internal/admin/metrics.go"
      provides: "GET /admin/metrics JSON handler with injected query interface + *emerg.FSM"
      contains: "func NewMetricsHandler"
    - path: "gateway/internal/admin/audit.go"
      provides: "GET /admin/audit paginated JSON handler"
      contains: "func NewAuditHandler"
    - path: "gateway/db/queries/audit.sql"
      provides: "TenantLatencyPercentiles :many query (percentile_cont over latency_ms)"
      contains: "percentile_cont"
  key_links:
    - from: "gateway/internal/admin/metrics.go"
      to: "gateway/internal/emerg/fsm.go"
      via: "injected *emerg.FSM, calls .State()"
      pattern: "\\.State\\(\\)"
    - from: "gateway/internal/admin/metrics.go"
      to: "ai_gateway.audit_log"
      via: "TenantLatencyPercentiles sqlc query"
      pattern: "TenantLatencyPercentiles"
    - from: "gateway/internal/admin/audit.go"
      to: "ai_gateway.audit_log"
      via: "ListAuditStateChanges sqlc query"
      pattern: "ListAuditStateChanges"
---

<objective>
Two new admin HTTP handlers under the existing `/admin` sub-router (admin-key bcrypt middleware from Phase 4): `GET /admin/metrics` (OBS-01 — aggregated JSON the dashboard polls) and `GET /admin/audit` (OBS-07 — paginated audit_log state-change history). Both clone the `UsageHandler` shape in `gateway/internal/admin/usage.go` exactly.

Purpose: this is the gateway's read API for the dashboard. The dashboard never touches Postgres or Prometheus directly — it polls these two JSON endpoints (plus the existing `/admin/usage`).
Output: `TenantLatencyPercentiles` query, `admin/metrics.go`, `admin/audit.go`, and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-dashboard-alerting/07-CONTEXT.md
@.planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md
@.planning/phases/07-observability-dashboard-alerting/07-PATTERNS.md
@.planning/phases/07-observability-dashboard-alerting/07-01-SUMMARY.md

<interfaces>
From gateway/internal/admin/usage.go — the handler shape to clone EXACTLY:
  type usageQueries interface { /* only the sqlc methods this handler calls */ }
  type UsageHandler struct { q usageQueries; log *slog.Logger }
  func NewUsageHandler(q *gen.Queries, log *slog.Logger) *UsageHandler
  func newUsageHandlerWithQueries(q usageQueries, log *slog.Logger) *UsageHandler  // test ctor
  func (h *UsageHandler) ServeHTTP(w http.ResponseWriter, r *http.Request)
  // every bad-input branch: httpx.WriteOpenAIError(...) + obs.GatewayAdminRequests.WithLabelValues("/admin/<route>","4xx").Inc()
  // success: Content-Type json, WriteHeader(200), json.NewEncoder(w).Encode(resp), "...2xx").Inc()

From gateway/internal/db/gen (after 07-01 sqlc generate):
  ListAuditStateChanges(ctx, arg) — paginated audit_log read (event_kind IS NOT NULL, ORDER BY ts DESC, LIMIT/OFFSET)

From gateway/internal/emerg/fsm.go:
  func (f *FSM) State() State   // lockless atomic read, line ~194

From gateway/internal/obs/metrics.go:
  var GatewayInflight *prometheus.GaugeVec   // read current inflight per upstream
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TenantLatencyPercentiles query</name>
  <read_first>
    - gateway/db/queries/audit.sql (existing annotation style + the ListAuditStateChanges query added by 07-01)
    - gateway/db/queries/emergency_lifecycles.sql (the `:many` query style)
    - .planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md (Code Examples → "Per-tenant percentiles from audit_log" — the exact percentile_cont SQL)
  </read_first>
  <action>
    Add `-- name: TenantLatencyPercentiles :many` to `gateway/db/queries/audit.sql`: `SELECT tenant_id, route, percentile_cont(0.50) WITHIN GROUP (ORDER BY latency_ms) AS p50, percentile_cont(0.95) WITHIN GROUP (ORDER BY latency_ms) AS p95, percentile_cont(0.99) WITHIN GROUP (ORDER BY latency_ms) AS p99, count(*) AS requests, count(*) FILTER (WHERE status_code >= 500)::float / NULLIF(count(*),0) AS error_rate FROM ai_gateway.audit_log WHERE ts >= $1 GROUP BY tenant_id, route;` — the `$1` is the window-start timestamp (the handler passes `NOW() - INTERVAL '5 minutes'` equivalent as a `time.Time`). Run `sqlc generate` from `gateway/`.
  </action>
  <verify>
    <automated>cd gateway && grep -q "percentile_cont" db/queries/audit.sql && grep -rq "TenantLatencyPercentiles" internal/db/gen/ && go build ./internal/db/...</automated>
  </verify>
  <acceptance_criteria>
    - `gateway/db/queries/audit.sql` contains `-- name: TenantLatencyPercentiles :many` and `percentile_cont(0.95)`
    - After `sqlc generate`, `grep -rl TenantLatencyPercentiles gateway/internal/db/gen/` returns at least one file
    - The generated row type exposes `P50`, `P95`, `P99`, `Requests`, `ErrorRate` fields
    - `cd gateway && go build ./internal/db/...` exits 0
  </acceptance_criteria>
  <done>TenantLatencyPercentiles is a sqlc `:many` query computing per-tenant/route percentiles natively in Postgres; db package builds.</done>
</task>

<task type="auto">
  <name>Task 2: GET /admin/metrics JSON handler</name>
  <read_first>
    - gateway/internal/admin/usage.go (clone this entire handler shape — package doc, imports, query-interface isolation, dual constructor, ServeHTTP validation + error envelope + metric increments, typed JSON response structs)
    - gateway/internal/admin/middleware.go (the admin-key middleware that gates the sub-router)
    - gateway/internal/emerg/fsm.go (the FSM.State() method + State type's String())
    - gateway/internal/obs/metrics.go (GatewayInflight gauge — how to read a gauge value in-process)
    - gateway/internal/httpx/ (WriteOpenAIError signature)
    - .planning/phases/07-observability-dashboard-alerting/07-PATTERNS.md (`gateway/internal/admin/metrics.go` section — exact package doc, imports, dual ctor, validation, JSON response pattern + the Phase 7-specific data sources)
  </read_first>
  <action>
    Create `gateway/internal/admin/metrics.go`. Clone `UsageHandler`: a `metricsQueries interface` listing only the sqlc methods used (`TenantLatencyPercentiles`), a `MetricsHandler` struct holding `q metricsQueries`, `fsm *emerg.FSM`, `log *slog.Logger`; public `NewMetricsHandler(q *gen.Queries, fsm *emerg.FSM, log *slog.Logger)` + private `newMetricsHandlerWithQueries(q metricsQueries, fsm *emerg.FSM, log *slog.Logger)` test ctor. `ServeHTTP`: optional `window` query param (default 5m) — validate like `usage.go` validates `from`/`to`, bad input → `httpx.WriteOpenAIError(w, 400, "invalid_request_error", "invalid_query_param", ...)` + `obs.GatewayAdminRequests.WithLabelValues("/admin/metrics","4xx").Inc()`. Success path: build a typed response struct with `json:` tags containing: `tenants` (slice from `TenantLatencyPercentiles` rows — tenant_id, route, p50/p95/p99, requests, error_rate), `fsm_state` (`h.fsm.State().String()`), `inflight` (read from `obs.GatewayInflight` per upstream via `prometheus` gather or a metric read helper). Encode JSON, `WriteHeader(200)`, `obs.GatewayAdminRequests.WithLabelValues("/admin/metrics","2xx").Inc()`. Add `metrics_test.go` using `newMetricsHandlerWithQueries` with a fake `metricsQueries` + a real or stub `*emerg.FSM`, asserting: 200 + JSON shape for a valid request; 400 + OpenAI envelope for a bad `window` param.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/admin/ -count=1 -run TestMetrics</automated>
  </verify>
  <acceptance_criteria>
    - `func NewMetricsHandler(` and `func newMetricsHandlerWithQueries(` both exist in `gateway/internal/admin/metrics.go`
    - The handler struct holds a `metricsQueries` interface field (not concrete `*gen.Queries`)
    - `grep -c 'GatewayAdminRequests.WithLabelValues("/admin/metrics"' gateway/internal/admin/metrics.go` returns ≥ 2 (a 4xx branch + a 2xx branch)
    - `grep -q '\.State()' gateway/internal/admin/metrics.go` (FSM state read)
    - `metrics_test.go` asserts both a 200 JSON path and a 400 OpenAI-envelope path
    - `cd gateway && go test ./internal/admin/ -count=1 -run TestMetrics` green
  </acceptance_criteria>
  <done>`/admin/metrics` handler clones the UsageHandler shape, returns aggregated JSON (per-tenant percentiles + FSM state + inflight), validates input with the OpenAI envelope, increments admin metrics; tests cover success + bad-input.</done>
</task>

<task type="auto">
  <name>Task 3: GET /admin/audit paginated JSON handler</name>
  <read_first>
    - gateway/internal/admin/usage.go (same handler shape; the limit/offset validation mirrors usage.go's from/to validation)
    - gateway/internal/admin/metrics.go (the sibling handler just written — keep the two consistent)
    - gateway/db/queries/audit.sql (the ListAuditStateChanges query from 07-01)
    - .planning/phases/07-observability-dashboard-alerting/07-PATTERNS.md (`gateway/internal/admin/audit.go` section)
  </read_first>
  <action>
    Create `gateway/internal/admin/audit.go`. Same handler shape as `metrics.go`: `auditQueries interface` (only `ListAuditStateChanges`), `AuditHandler` struct, public `NewAuditHandler(q *gen.Queries, log *slog.Logger)` + private `newAuditHandlerWithQueries` test ctor. `ServeHTTP`: parse `limit` (default 50, cap at 200) and `offset` (default 0, reject negative) query params — bad input → `httpx.WriteOpenAIError` + `GatewayAdminRequests.WithLabelValues("/admin/audit","4xx").Inc()`. Success: call `ListAuditStateChanges`, marshal a typed `{ items: [...], limit, offset }` response with `json:` tags, `WriteHeader(200)`, `...("/admin/audit","2xx").Inc()`. Add `audit_test.go` with a fake `auditQueries`: assert 200 + the rows are ordered as the query returns them; assert 400 for `limit=-1`.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/admin/ -count=1 -run TestAudit</automated>
  </verify>
  <acceptance_criteria>
    - `func NewAuditHandler(` and `func newAuditHandlerWithQueries(` both exist in `gateway/internal/admin/audit.go`
    - The handler caps `limit` at 200 and rejects negative `offset` — verified by reading the validation branches
    - `grep -c 'GatewayAdminRequests.WithLabelValues("/admin/audit"' gateway/internal/admin/audit.go` returns ≥ 2
    - `audit_test.go` asserts a 200 paginated response and a 400 for an invalid `limit`
    - `cd gateway && go test ./internal/admin/ -count=1 -run TestAudit` green
  </acceptance_criteria>
  <done>`/admin/audit` handler returns paginated newest-first audit_log state-change rows as JSON, validates limit/offset with the OpenAI envelope, increments admin metrics; tests cover success + bad-input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard (or operator curl) → /admin/* | Untrusted HTTP crosses into the gateway's admin surface |
| /admin handlers → Postgres `audit_log` | Handlers issue read queries against tenant + audit data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Spoofing | /admin/metrics, /admin/audit | mitigate | Both handlers mount only inside the `if px.adminVerifier != nil` block under `admin.Middleware(px.adminVerifier, log)` — the existing Phase 4 `X-Admin-Key` bcrypt gate. No new auth path; an unauthenticated request never reaches the handler. (Wiring done in plan 07-06.) |
| T-07-08 | Tampering | query param parsing | mitigate | `window`, `limit`, `offset` are validated before use — bad input returns `httpx.WriteOpenAIError` 400, never reaches the SQL layer with an unbounded value. `limit` is capped at 200 so a hostile caller cannot request an unbounded result set. |
| T-07-09 | Information Disclosure | /admin/audit response body | mitigate | `ListAuditStateChanges` selects only `audit_log` *metadata* columns (ts, route, status, latency, event_kind) — never `audit_log_content` (prompts/responses). Sensitive tenant content stays out of the response by construction. |
| T-07-10 | Denial of Service | /admin/metrics percentile query | accept | `TenantLatencyPercentiles` scans a 5-minute `ts >= $1` window backed by `idx_audit_log_tenant_ts`; bounded by the window + the ~6-tenant cardinality. Low-volume internal admin endpoint (~4 admins, 5-10s poll). |
</threat_model>

<verification>
- `cd gateway && go build ./...` exits 0.
- `cd gateway && go test ./internal/admin/ ./internal/db/... -count=1 -race` green.
- Both handlers follow the `usage.go` contract: query-interface isolation, dual constructor, OpenAI error envelope on bad input, admin-metric increment on every branch.
</verification>

<success_criteria>
- `GET /admin/metrics` returns per-tenant P50/P95/P99 + error rate + FSM state + inflight as JSON.
- `GET /admin/audit` returns paginated newest-first audit_log state-change rows as JSON.
- Both reject bad query params with the OpenAI error envelope + a 4xx admin-metric increment; tests cover success + bad-input.
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-dashboard-alerting/07-03-SUMMARY.md`
</output>
