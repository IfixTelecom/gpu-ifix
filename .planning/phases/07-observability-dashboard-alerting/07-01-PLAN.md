---
phase: 07-observability-dashboard-alerting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/internal/config/config.go
  - gateway/internal/config/config_test.go
  - gateway/db/migrations/0020_audit_log_event_kind.sql
  - gateway/db/queries/audit.sql
  - gateway/internal/obs/metrics.go
  - gateway/internal/alert/testsupport.go
  - gateway/internal/alert/doc.go
autonomous: true
requirements: [OBS-01, OBS-02, OBS-04, OBS-05, OBS-07]
user_setup:
  - service: chatwoot
    why: "Critical-tier WhatsApp alert delivery (OBS-05)"
    env_vars:
      - name: CHATWOOT_API_URL
        source: "Ifix Chatwoot host — https://crm.ifixtelecom.com.br"
      - name: CHATWOOT_API_TOKEN
        source: "Chatwoot admin → Profile → Access Token (api_access_token)"
      - name: CHATWOOT_ONCALL_ACCOUNT_ID
        source: "Chatwoot admin → account id for the on-call workspace"
      - name: CHATWOOT_ONCALL_INBOX_ID
        source: "Chatwoot admin → inbox id for the on-call WhatsApp inbox"
      - name: CHATWOOT_ONCALL_CONTACT_ID
        source: "Chatwoot admin → contact id representing the on-call operator"
  - service: clickup
    why: "Critical + warning alerts open a ClickUp task (OBS-04)"
    env_vars:
      - name: CLICKUP_API_TOKEN
        source: "ClickUp → Settings → Apps → API Token (raw token, not Bearer)"
      - name: CLICKUP_ALERT_LIST_ID
        source: "ClickUp → target list for alert tasks → list id from URL"
  - service: brevo
    why: "Critical + warning alert email (OBS-05)"
    env_vars:
      - name: BREVO_SMTP_HOST
        source: "Brevo → SMTP & API → smtp-relay.brevo.com"
      - name: BREVO_SMTP_PORT
        source: "Brevo SMTP port — 587"
      - name: BREVO_SMTP_USER
        source: "Brevo → SMTP & API → SMTP login"
      - name: BREVO_SMTP_PASS
        source: "Brevo → SMTP & API → SMTP key/password"
      - name: ALERT_EMAIL_TO
        source: "Comma-separated list of the ~4 Ifix operator emails"
      - name: ALERT_EMAIL_FROM
        source: "Verified Brevo sender identity"

must_haves:
  truths:
    - "Gateway boots with all 12 new alert env vars unset and logs a WARN per disabled channel — never fails boot"
    - "Migration 0020 adds nullable audit_log.event_kind; existing rows are unaffected"
    - "obs/metrics.go declares the two new latency histograms and gateway_alert_dropped_total counter"
  artifacts:
    - path: "gateway/db/migrations/0020_audit_log_event_kind.sql"
      provides: "audit_log.event_kind nullable column"
      contains: "ADD COLUMN IF NOT EXISTS event_kind"
    - path: "gateway/internal/config/config.go"
      provides: "12 alert env vars parsed as optional config fields"
      contains: "ChatwootAPIURL"
    - path: "gateway/internal/obs/metrics.go"
      provides: "RequestDurationByRoute, RequestDurationByUpstream, AlertDroppedTotal collectors"
      contains: "gateway_request_duration_ms_by_route"
    - path: "gateway/db/queries/audit.sql"
      provides: "ListAuditStateChanges :many paginated read query"
      contains: "ListAuditStateChanges"
    - path: "gateway/internal/alert/testsupport.go"
      provides: "shared fake Chatwoot/ClickUp/Brevo clients for alert package tests"
      contains: "FakeChatwoot"
  key_links:
    - from: "gateway/internal/config/config.go"
      to: "os.Getenv"
      via: "Load() parses CHATWOOT_/CLICKUP_/BREVO_/ALERT_EMAIL_ env vars"
      pattern: "os.Getenv\\(\"CHATWOOT_API_URL\"\\)"
    - from: "gateway/db/queries/audit.sql"
      to: "gateway/internal/db/gen"
      via: "sqlc generate produces ListAuditStateChanges method"
      pattern: "ListAuditStateChanges"
---

<objective>
Wave 0 scaffolding for Phase 7. Lay every contract that downstream plans (07-02..07-06) consume: the 12 optional alert env vars in config, the additive `0020_audit_log_event_kind.sql` migration, the `ListAuditStateChanges` sqlc query, the two new bounded latency histograms + `gateway_alert_dropped_total` counter in `obs/metrics.go`, and shared Go test fakes for the new `alert` package.

Purpose: downstream plans run in parallel against fixed contracts — no scavenger hunt. The alert env vars exist before any client reads them; the migration + query exist before the admin handler consumes them; the metrics collectors exist before the middleware records them.
Output: config extension, migration 0020, audit.sql extension, obs/metrics.go extension, alert package test support + doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-dashboard-alerting/07-CONTEXT.md
@.planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md
@.planning/phases/07-observability-dashboard-alerting/07-PATTERNS.md

<interfaces>
From gateway/internal/config/config.go — the optional-env-var pattern (Sentry is the canonical example):
  SentryDSN string // SENTRY_DSN (optional; empty = Sentry disabled)
  ... parsed in Load() as: SentryDSN: os.Getenv("SENTRY_DSN")
  Helpers available in this file: atoiOr(s, default), csvOr(s, default), boolOr(s, default).

From gateway/internal/obs/metrics.go — the bounded-label collector pattern:
  var ProbeDurationMs = promauto.NewHistogramVec(prometheus.HistogramOpts{
    Name: "gateway_probe_duration_ms", Buckets: []float64{50,100,250,500,1000,2500,5000},
  }, []string{"upstream"})
  var AuditDroppedTotal = promauto.NewCounter(prometheus.CounterOpts{...})  // plain counter shape

From gateway/db/migrations/0019_emergency_lifecycles.sql — goose file structure:
  -- +goose Up / -- +goose StatementBegin ... -- +goose StatementEnd, matching Down block.
  SET search_path = ai_gateway, public;

From gateway/db/queries/emergency_lifecycles.sql — the `:many` paginated read style (sqlc annotation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config — add 12 optional alert env vars</name>
  <read_first>
    - gateway/internal/config/config.go (the Config struct + Load() + atoiOr/csvOr/boolOr helpers; SentryDSN at line 84 is the optional-var precedent)
    - gateway/internal/config/config_test.go (existing table-driven config tests)
    - .planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md (Runtime State Inventory — Secrets/env vars row lists all 12 names)
    - .planning/phases/07-observability-dashboard-alerting/07-PATTERNS.md (Shared Patterns → Additive struct extension — "empty = feature disabled with a WARN, never fail-boot")
  </read_first>
  <action>
    Add to the `Config` struct, grouped under a `// Phase 7 — alerting (all optional; empty = channel disabled with WARN)` comment block: `ChatwootAPIURL`, `ChatwootAPIToken`, `ChatwootOncallAccountID`, `ChatwootOncallInboxID`, `ChatwootOncallContactID` (string), `ClickUpAPIToken`, `ClickUpAlertListID` (string), `BrevoSMTPHost` (string), `BrevoSMTPPort` (int, default 587 via `atoiOr`), `BrevoSMTPUser`, `BrevoSMTPPass` (string), `AlertEmailTo` ([]string via `csvOr` with empty default), `AlertEmailFrom` (string). Parse each in `Load()` from the env var names in the frontmatter `user_setup` block (`CHATWOOT_API_URL`, `CHATWOOT_API_TOKEN`, `CHATWOOT_ONCALL_ACCOUNT_ID`, `CHATWOOT_ONCALL_INBOX_ID`, `CHATWOOT_ONCALL_CONTACT_ID`, `CLICKUP_API_TOKEN`, `CLICKUP_ALERT_LIST_ID`, `BREVO_SMTP_HOST`, `BREVO_SMTP_PORT`, `BREVO_SMTP_USER`, `BREVO_SMTP_PASS`, `ALERT_EMAIL_TO`, `ALERT_EMAIL_FROM`). All optional — do NOT add them to any required-var validation list. Mirror `SentryDSN`: no boot failure when empty.
  </action>
  <verify>
    <automated>cd gateway && go build ./internal/config/ && go test ./internal/config/ -count=1 -run TestLoad</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ChatwootAPIURL\|ChatwootAPIToken\|ChatwootOncallAccountID\|ChatwootOncallInboxID\|ChatwootOncallContactID\|ClickUpAPIToken\|ClickUpAlertListID\|BrevoSMTPHost\|BrevoSMTPPort\|BrevoSMTPUser\|BrevoSMTPPass\|AlertEmailTo\|AlertEmailFrom' gateway/internal/config/config.go` returns ≥ 26 (13 struct fields + 13 Load assignments)
    - `cd gateway && go build ./internal/config/` exits 0
    - No new entry added to any required-env validation slice — `git diff` shows the new fields are only in the struct + Load(), not in a fail-boot check
    - `config_test.go` still passes with all 12 vars unset (zero values are valid)
  </acceptance_criteria>
  <done>All 12 alert env vars are typed Config fields parsed in Load(); gateway builds; config tests green with the vars unset.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0020 + ListAuditStateChanges query</name>
  <read_first>
    - gateway/db/migrations/0019_emergency_lifecycles.sql (goose up/down DDL structure)
    - gateway/db/migrations/0003_create_audit_log_partitioned.sql (audit_log column list + month-partition seeding — confirms the partition window)
    - gateway/db/queries/audit.sql (existing annotation style)
    - gateway/db/queries/emergency_lifecycles.sql (the `:many` paginated-read query style)
    - .planning/phases/07-observability-dashboard-alerting/07-PATTERNS.md (the `0020_audit_log_event_kind.sql` section — exact goose block + Pitfall 8 partition note)
  </read_first>
  <action>
    Create `gateway/db/migrations/0020_audit_log_event_kind.sql` with `-- +goose Up` / `-- +goose StatementBegin` blocks: `SET search_path = ai_gateway, public;` then `ALTER TABLE ai_gateway.audit_log ADD COLUMN IF NOT EXISTS event_kind TEXT;` and a matching `-- +goose Down` block with `ALTER TABLE ai_gateway.audit_log DROP COLUMN IF EXISTS event_kind;`. Nullable, no backfill — existing rows get NULL. Add a header comment noting Pitfall 8: migration 0003 seeded partitions for the current month + next 2 (May/Jun/Jul 2026), which covers the Phase 7 test/ship horizon; the partition-roll automation (deferred 02-09) remains a tracked limitation, not a Phase 7 blocker.
    Then extend `gateway/db/queries/audit.sql` with `-- name: ListAuditStateChanges :many` — `SELECT ts, request_id, tenant_id, route, method, upstream, status_code, latency_ms, error_code, event_kind FROM ai_gateway.audit_log WHERE event_kind IS NOT NULL ORDER BY ts DESC LIMIT $1 OFFSET $2;`. Run `sqlc generate` (from `gateway/`) so `internal/db/gen` gains the `ListAuditStateChanges` method and its row type.
  </action>
  <verify>
    <automated>cd gateway && grep -q "ADD COLUMN IF NOT EXISTS event_kind" db/migrations/0020_audit_log_event_kind.sql && grep -q "ListAuditStateChanges" internal/db/gen/*.go && go build ./internal/db/...</automated>
  </verify>
  <acceptance_criteria>
    - `gateway/db/migrations/0020_audit_log_event_kind.sql` exists with both `-- +goose Up` and `-- +goose Down` StatementBegin/End blocks
    - `grep -c 'event_kind' gateway/db/migrations/0020_audit_log_event_kind.sql` returns ≥ 2 (up + down)
    - `gateway/db/queries/audit.sql` contains `-- name: ListAuditStateChanges :many`
    - After `sqlc generate`, `grep -rl ListAuditStateChanges gateway/internal/db/gen/` returns at least one file
    - `cd gateway && go build ./internal/db/...` exits 0
  </acceptance_criteria>
  <done>Migration 0020 is an additive nullable column with up/down; ListAuditStateChanges query exists and sqlc-generates a method; db package builds.</done>
</task>

<task type="auto">
  <name>Task 3: obs/metrics.go collectors + alert package test support</name>
  <read_first>
    - gateway/internal/obs/metrics.go (ProbeDurationMs HistogramVec at lines 84-91; AuditDroppedTotal plain counter at lines 25-32; the Phase 5/6 cardinality-budget header comments)
    - gateway/internal/emerg/vast/client.go (the Client struct + NewClientWithBaseURL test ctor shape — the fakes mirror this surface)
    - .planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md (Code Examples → "Latency histogram with bounded labels"; Pitfall 1 + Pitfall 5)
  </read_first>
  <action>
    In `gateway/internal/obs/metrics.go` add three collectors with cardinality-budget doc comments matching the Phase 5/6 header style: `RequestDurationByRoute` = `promauto.NewHistogramVec` named `gateway_request_duration_ms_by_route`, buckets `{25,50,100,250,500,1000,2500,5000,10000}`, label `[]string{"route"}` (~4 routes × 9 buckets ≈ 44 series); `RequestDurationByUpstream` = same buckets, name `gateway_request_duration_ms_by_upstream`, label `[]string{"upstream"}` (~6 × 9 ≈ 60 series); `AlertDroppedTotal` = `promauto.NewCounter` named `gateway_alert_dropped_total` (plain counter, follows the `AuditDroppedTotal` shape — incremented when the alerter drops an event because a per-channel worker queue is full). Do NOT cross `tenant × route × upstream` — per-tenant percentiles come from audit_log SQL in plan 07-03, not a label.
    Create `gateway/internal/alert/doc.go` (package doc: `// Package alert: the in-gateway alerting goroutine ...`) and `gateway/internal/alert/testsupport.go` with exported fakes: `FakeChatwoot`, `FakeClickUp`, `FakeBrevo` — each a struct recording the calls it received (e.g. `FakeChatwoot{ Sent []string; Err error }`) plus a method matching the client interface that 07-04 will define (record args, return `Err`). Keep `testsupport.go` build-tag-free (it is shared production-adjacent test support, like other in-repo `__test-helpers__` analogs) so both 07-04 and 07-05 import it.
  </action>
  <verify>
    <automated>cd gateway && go build ./internal/obs/ ./internal/alert/ && grep -q "gateway_request_duration_ms_by_route" internal/obs/metrics.go && grep -q "gateway_alert_dropped_total" internal/obs/metrics.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RequestDurationByRoute\|RequestDurationByUpstream\|AlertDroppedTotal' gateway/internal/obs/metrics.go` returns ≥ 3
    - Each new histogram has exactly one label in its `[]string{...}` declaration (no `tenant` label) — verified by reading the declarations
    - `gateway/internal/alert/doc.go` declares `package alert`
    - `gateway/internal/alert/testsupport.go` exports `FakeChatwoot`, `FakeClickUp`, `FakeBrevo`
    - `cd gateway && go build ./internal/obs/ ./internal/alert/` exits 0
  </acceptance_criteria>
  <done>Two bounded latency histograms + one drop counter declared with budget comments; alert package exists with shared fakes that build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy config → gateway process | Alert credentials (Chatwoot/ClickUp/Brevo) enter as env vars from the Portainer stack |
| gateway → Postgres `ai_gateway` schema | Migration 0020 alters a shared partitioned table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | config.go alert credentials | mitigate | Credentials are plain string Config fields; they are NEVER logged. The optional-var WARN log (plan 07-04/05) logs only the channel name + "disabled", never the token value. config.go itself does not log. |
| T-07-02 | Tampering | migration 0020 on shared `audit_log` | accept | Additive nullable column with `IF NOT EXISTS` / `IF EXISTS` guards; goose is idempotent across restarts (per 02-08 boot-migration decision). No data loss path. |
| T-07-03 | Denial of Service | audit_log month partitions | accept | Pitfall 8 — migration 0003 seeded May/Jun/Jul 2026 partitions; the Phase 7 test/ship horizon (2026-05) is covered. Partition-roll automation stays a tracked limitation in Open Todos, not a Phase 7 blocker. |
</threat_model>

<verification>
- `cd gateway && go build ./...` exits 0 (whole gateway still compiles with the new config fields, collectors, alert package).
- `cd gateway && go test ./internal/config/ ./internal/obs/ -count=1` green.
- Migration 0020 is reviewed as additive-only: `git diff` shows no DROP/RENAME of existing columns.
</verification>

<success_criteria>
- 12 alert env vars are optional Config fields; gateway boots with all unset.
- Migration 0020 adds nullable `audit_log.event_kind`; `ListAuditStateChanges` sqlc query generates.
- `obs/metrics.go` has the two bounded latency histograms + `gateway_alert_dropped_total`, each ≤ one label.
- `gateway/internal/alert/` exists with shared `Fake{Chatwoot,ClickUp,Brevo}` test support.
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-dashboard-alerting/07-01-SUMMARY.md`
</output>
