---
phase: 07-observability-dashboard-alerting
plan: 09
type: execute
wave: 5
depends_on: [06, 08]
files_modified:
  - .planning/phases/07-observability-dashboard-alerting/07-HUMAN-UAT.md
  - docs/RUNBOOK-OBSERVABILITY-ALERTING.md
autonomous: false
requirements: [OBS-02, OBS-04, OBS-05, OBS-08]
must_haves:
  truths:
    - "An operator can run the documented Prometheus cardinality audit and confirm /metrics stays under 10k active series"
    - "A real critical event delivers a WhatsApp message (Chatwoot) + an email (Brevo) within 60s and opens a ClickUp task"
    - "A repeated warning-tier event within 5 minutes produces exactly one notification (dedup verified live)"
    - "Sentry shows a captured event with authorization / x-api-key / payload bodies redacted"
    - "An operator has a runbook covering detection → diagnosis → channel-degradation for the observability + alerting subsystem"
  artifacts:
    - path: ".planning/phases/07-observability-dashboard-alerting/07-HUMAN-UAT.md"
      provides: "the live-verification scenario sheet + sign-off table for SC-2/SC-3/SC-5/SC-6"
      contains: "Sign-off"
    - path: "docs/RUNBOOK-OBSERVABILITY-ALERTING.md"
      provides: "operator runbook for the dashboard + alerting + metrics + Sentry subsystem"
      contains: "RUNBOOK"
  key_links:
    - from: ".planning/phases/07-observability-dashboard-alerting/07-HUMAN-UAT.md"
      to: "the gateway alerting + /metrics + Sentry"
      via: "live UAT scenarios exercise the deployed gateway"
      pattern: "Chatwoot|Brevo|ClickUp|Sentry"
---

<objective>
The phase-closing HUMAN-UAT plan — the live-credential and live-delivery verification that cannot run autonomously, mirroring 03-08 / 04-09 / 06-11. The autonomous plans (07-01..07-08) build the whole subsystem and degrade gracefully to "log + dashboard banner only" when alert credentials are absent — so the build is NOT blocked. This plan documents and gates the success criteria that need real Chatwoot/ClickUp/Brevo credentials, a real on-call routing target, a deployed gateway, and Sentry: SC-2 (WhatsApp + email within 60s), SC-3 (live dedup), SC-5 (Prometheus cardinality under 10k), SC-6 (Sentry redaction).

Purpose: close Phase 7 with an operator-executed evidence sheet + a runbook, the same pattern every prior phase used for its live-UAT deferral.
Output: `07-HUMAN-UAT.md` (scenario sheet + sign-off), `docs/RUNBOOK-OBSERVABILITY-ALERTING.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-dashboard-alerting/07-CONTEXT.md
@.planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md
@.planning/phases/07-observability-dashboard-alerting/07-VALIDATION.md
@.planning/phases/07-observability-dashboard-alerting/07-06-SUMMARY.md
@.planning/phases/07-observability-dashboard-alerting/07-08-SUMMARY.md

<interfaces>
Prior-phase HUMAN-UAT analogs to mirror (structure + sign-off table format):
  .planning/phases/06-*/06-HUMAN-UAT.md  (the closest — Vast.ai live scenarios + sign-off)
  docs/RUNBOOK-EMERGENCY-POD.md          (the runbook structure to mirror — detection → diagnosis → action)
  docs/RUNBOOK-FAILOVER.md / docs/RUNBOOK-QUOTAS-BILLING.md

Success criteria from ROADMAP Phase 7 needing live verification:
  SC-2: critical GPU-down ≥30s → on-call gets WhatsApp + email within 60s; same event raises a dashboard banner
  SC-3: a warning-tier event repeating within 5 min is deduplicated (one notification, not ten)
  SC-5: Prometheus scrape of /metrics stays under 10k active series, consumable by standard Prometheus tooling
  SC-6: Sentry captures panics/circuit trips/provisioning failures with authorization, x-api-key, payload bodies redacted

Env vars that must be set on the deployed gateway for the alert channels to fire (from 07-01 / 07-RESEARCH.md Runtime State Inventory):
  CHATWOOT_API_URL / CHATWOOT_API_TOKEN / CHATWOOT_ONCALL_ACCOUNT_ID / CHATWOOT_ONCALL_INBOX_ID / CHATWOOT_ONCALL_CONTACT_ID
  CLICKUP_API_TOKEN / CLICKUP_ALERT_LIST_ID
  BREVO_SMTP_HOST / BREVO_SMTP_PORT / BREVO_SMTP_USER / BREVO_SMTP_PASS / ALERT_EMAIL_TO / ALERT_EMAIL_FROM
  SENTRY_DSN (already a Phase 2 var)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/RUNBOOK-OBSERVABILITY-ALERTING.md</name>
  <read_first>
    - docs/RUNBOOK-EMERGENCY-POD.md (the runbook structure — detection → diagnosis → action → escalation)
    - docs/RUNBOOK-FAILOVER.md (the structure of a gateway-subsystem runbook)
    - .planning/phases/07-observability-dashboard-alerting/07-CONTEXT.md (the alerting channel matrix + the dashboard data source + the audit-log coverage)
    - .planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md (the Common Pitfalls — these become runbook "known failure modes"; the env var inventory)
    - .planning/phases/07-observability-dashboard-alerting/07-06-SUMMARY.md + 07-08-SUMMARY.md (what was actually built)
  </read_first>
  <action>
    Create `docs/RUNBOOK-OBSERVABILITY-ALERTING.md` mirroring the `RUNBOOK-EMERGENCY-POD.md` structure. Cover: (1) the dashboard — URL, Better Auth login, what each view shows, how to read the FSM panel + the critical banner; (2) the alerting subsystem — the severity→channel matrix, where the alerter goroutine logs, the `gw:alert:dedup:` 5-min dedup behavior, and the graceful-degradation rule (empty alert env var → channel disabled with a WARN, "log + dashboard banner only"); (3) `/metrics` — how to run the cardinality audit (`curl` the endpoint + `promtool check metrics` or a `count by (__name__)` query) and the ≤10k-series budget; (4) `/admin/metrics` + `/admin/audit` — the admin-key-gated JSON endpoints, how to curl them; (5) Sentry — what gets captured, what gets redacted (`authorization`, `x-api-key`, request/response bodies); (6) known failure modes drawn from the 07-RESEARCH.md Pitfalls — boot-window lost events (Pitfall 4), alerter stall on a dead external API (Pitfall 5 → check `gateway_alert_dropped_total`), ClickUp 401 (Pitfall 6), `audit_log` partition-window limitation (Pitfall 8); (7) detection → diagnosis → action for each: "alerts not arriving", "dashboard shows stale data", "/metrics series climbing", "Sentry leaking a secret".
  </action>
  <verify>
    <automated>test -f docs/RUNBOOK-OBSERVABILITY-ALERTING.md && grep -qi "cardinality\|dedup\|severity\|Sentry\|gateway_alert_dropped_total" docs/RUNBOOK-OBSERVABILITY-ALERTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/RUNBOOK-OBSERVABILITY-ALERTING.md` exists and follows the `RUNBOOK-EMERGENCY-POD.md` section structure
    - It documents the severity→channel matrix, the graceful-degradation rule, the `/metrics` cardinality audit procedure, and the Sentry redaction guarantee
    - It includes a "known failure modes" section covering at least Pitfalls 4, 5, 6, 8 from 07-RESEARCH.md
    - It includes detection → diagnosis → action for "alerts not arriving", "stale dashboard", "/metrics series climbing", "Sentry leaking a secret"
    - `grep -qi 'cardinality.*dedup.*severity' ` style check: the verify command's keyword grep passes
  </acceptance_criteria>
  <done>The observability + alerting runbook exists, mirrors the prior runbook structure, and covers the dashboard, the alerting matrix + degradation rule, the cardinality audit, Sentry redaction, and the RESEARCH.md pitfalls as known failure modes with detection→diagnosis→action.</done>
</task>

<task type="auto">
  <name>Task 2: Write 07-HUMAN-UAT.md scenario sheet + sign-off table</name>
  <read_first>
    - .planning/phases/06-*/06-HUMAN-UAT.md (the closest analog — scenario list + sign-off table format + the cost/time-box framing)
    - .planning/phases/07-observability-dashboard-alerting/07-VALIDATION.md (the Manual-Only Verifications table — WhatsApp+email within 60s, ClickUp task; "channels degrade gracefully... build is not blocked")
    - .planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md (Open Questions 1-4 — the Chatwoot/ClickUp/Brevo/Better-Auth provisioning unknowns the operator must resolve first; Assumptions A6 highest-risk)
    - ROADMAP.md (Phase 7 Success Criteria SC-1..SC-6)
  </read_first>
  <action>
    Create `.planning/phases/07-observability-dashboard-alerting/07-HUMAN-UAT.md` mirroring `06-HUMAN-UAT.md`. Structure: (a) a **Prerequisites** section — the operator must first resolve 07-RESEARCH.md Open Questions 1-4 and set the 12 alert env vars + `SENTRY_DSN` on the deployed gateway, deploy the `dashboard` service, and create the Better Auth operator accounts; note A6 (the Chatwoot on-call routing target) as the highest-risk unknown. (b) **Scenarios** — numbered, each with setup / action / expected / pass-fail: **S1 (SC-2)** induce a critical event (kill the local LLM or `gatewayctl` force a breaker open) → confirm a WhatsApp message arrives via Chatwoot AND an email arrives via Brevo within 60s AND the dashboard critical banner appears; **S2 (SC-2 cont.)** confirm the same critical event opened a ClickUp task in the target list; **S3 (SC-3)** trigger the same warning-tier event repeatedly within 5 minutes → confirm exactly one notification per channel (dedup); **S4 (SC-5)** `curl https://<gateway>/metrics` and run `promtool check metrics` + a `count by (__name__)` series count → confirm under 10k active series and standard-tooling-consumable; **S5 (SC-6)** trigger a captured Sentry event (a panic or a circuit trip) → in the Sentry UI confirm `authorization` + `x-api-key` headers and any request/response body show `***REDACTED***`; **S6 (SC-1 live)** open the deployed dashboard, sign in, confirm live per-tenant latency + cost + FSM state polling. (c) a **Sign-off table** — one row per scenario with Result / Date / Operator / Notes columns, plus an overall phase-status line (`passed` / `passed_partial` / `human_needed`). (d) a note that, per the gateway optional-feature pattern, scenarios whose credentials are unavailable mark `passed_partial` — the autonomous build is already green and is NOT blocked.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-observability-dashboard-alerting/07-HUMAN-UAT.md && grep -qi "Sign-off" .planning/phases/07-observability-dashboard-alerting/07-HUMAN-UAT.md && grep -c "SC-" .planning/phases/07-observability-dashboard-alerting/07-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-observability-dashboard-alerting/07-HUMAN-UAT.md` exists with a Prerequisites section referencing 07-RESEARCH.md Open Questions 1-4
    - It has at least 6 numbered scenarios mapped to SC-1/SC-2/SC-3/SC-5/SC-6, each with setup/action/expected/pass-fail
    - It has a Sign-off table with Result / Date / Operator / Notes columns and an overall phase-status line
    - It documents the `passed_partial` path for unavailable credentials (build not blocked)
    - `grep -c "SC-" ...07-HUMAN-UAT.md` returns ≥ 6
  </acceptance_criteria>
  <done>The HUMAN-UAT scenario sheet exists, mirrors 06-HUMAN-UAT.md, covers SC-2/SC-3/SC-5/SC-6 + the live dashboard, lists the credential prerequisites, and has a sign-off table with the passed_partial fallback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute the live UAT scenarios + record sign-off</name>
  <what-built>
    Phase 7's full observability + alerting subsystem is built and the autonomous test suites (07-01..07-08) are green: `/admin/metrics` + `/admin/audit` JSON, the two bounded latency histograms + the `gateway_alert_dropped_total` counter, the alerting goroutine (Chatwoot/ClickUp/Brevo clients + severity tiers + 5-min dedup), the extended Sentry body redaction, the FSM-transition audit rows, and the `dashboard/` Next.js app. This task is the live-credential verification that cannot run autonomously.
  </what-built>
  <how-to-verify>
    1. Complete the **Prerequisites** in `07-HUMAN-UAT.md`: resolve 07-RESEARCH.md Open Questions 1-4 (the Chatwoot on-call `account_id`/`inbox_id`/`contact_id`, the ClickUp `list_id` + token, the Brevo SMTP creds + `ALERT_EMAIL_TO`, the Better Auth storage), set all 12 alert env vars + `SENTRY_DSN` on the deployed gateway via the Portainer stack, deploy the `dashboard` service, and create the Better Auth operator accounts.
    2. Execute scenarios S1-S6 in `07-HUMAN-UAT.md` in order. For each, record Result / Date / Operator / Notes in the sign-off table.
    3. If a credential is genuinely unavailable, mark that scenario `passed_partial` and note the blocker — the autonomous build is already green; live delivery is the deferred-UAT pattern every prior phase used.
    4. Fill the overall phase-status line (`passed` if all live scenarios pass; `passed_partial` if some are credential-blocked; `human_needed` if a real defect is found).
    5. If a real defect is found (not a missing credential), describe it precisely so a `/gsd-plan-phase --gaps` pass can close it.
  </how-to-verify>
  <resume-signal>Type "approved" with the sign-off table filled, or describe the defects found for a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → deployed gateway + dashboard | Live UAT exercises the production-dev deployment with real credentials |
| gateway → Chatwoot / ClickUp / Brevo / Sentry | Real outbound delivery with real tokens during the UAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-33 | Information Disclosure | live alert credentials during UAT | mitigate | The 12 alert env vars + `SENTRY_DSN` are set via the Portainer stack UI (CLAUDE.md deploy pattern) — never committed to git. `07-HUMAN-UAT.md` and the runbook reference the env var NAMES, never values. The S5 scenario actively verifies Sentry redaction, closing the loop on OBS-08. |
| T-07-34 | Repudiation | UAT result provenance | mitigate | The sign-off table records Operator + Date per scenario — the live verification is attributable, mirroring the 06-HUMAN-UAT.md evidence pattern. |
| T-07-35 | Spoofing | dashboard admin access during UAT | accept | S6 uses real Better Auth operator accounts on the deployed dashboard; the `middleware.ts` session gate (07-07) + the server-side admin-key proxy are exercised as built. No new surface introduced by the UAT itself. |
</threat_model>

<verification>
- `docs/RUNBOOK-OBSERVABILITY-ALERTING.md` and `.planning/phases/07-observability-dashboard-alerting/07-HUMAN-UAT.md` both exist and are committed.
- The Task 3 blocking checkpoint produces a filled sign-off table with an overall phase-status line.
- This plan is `autonomous: false` — it cannot reach COMPLETE without operator action, consistent with 03-08 / 04-09 / 06-11.
</verification>

<success_criteria>
- The observability + alerting runbook exists and covers the dashboard, the alerting matrix + degradation rule, the cardinality audit, Sentry redaction, and the known failure modes.
- The HUMAN-UAT scenario sheet covers SC-2 (WhatsApp + email + ClickUp + banner), SC-3 (live dedup), SC-5 (cardinality under 10k), SC-6 (Sentry redaction), and the live dashboard.
- An operator executes the scenarios and records a sign-off table with an overall phase-status line; `passed_partial` is the documented path when credentials are unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-dashboard-alerting/07-09-SUMMARY.md` and `.planning/phases/07-observability-dashboard-alerting/07-VERIFICATION.md`
</output>
