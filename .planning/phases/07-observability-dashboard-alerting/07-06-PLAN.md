---
phase: 07-observability-dashboard-alerting
plan: 06
type: execute
wave: 4
depends_on: [07-02, 07-03, 07-05]
files_modified:
  - gateway/cmd/gateway/main.go
  - gateway/cmd/gateway/main_test.go
  - gateway/internal/emerg/fsm.go
  - gateway/internal/emerg/reconciler.go
autonomous: true
requirements: [OBS-01, OBS-04, OBS-05, OBS-07]
must_haves:
  truths:
    - "The gateway boots the alerter goroutine EARLY — before the breaker/shed/emerg subsystems publish"
    - "GET /admin/metrics and GET /admin/audit are mounted under the admin-key-gated /admin sub-router"
    - "Every FSM transition writes an audit_log row with event_kind = fsm_transition"
    - "When all 12 alert env vars are unset the gateway boots, logs a WARN per disabled channel, and runs normally (no external alerts)"
  artifacts:
    - path: "gateway/cmd/gateway/main.go"
      provides: "alerter goroutine spawn + ReconcileBoot + 2 admin route registrations + alert client construction from config"
      contains: "alerter.Run"
    - path: "gateway/internal/emerg/fsm.go"
      provides: "FSM transitions emit a WriteStateChange(fsm_transition) audit row"
      contains: "WriteStateChange"
  key_links:
    - from: "gateway/cmd/gateway/main.go"
      to: "gateway/internal/alert/alerter.go"
      via: "go alerter.Run(ctx) spawned before breakerSet.Subscribe / emergReconciler.Run"
      pattern: "go .*[Aa]lerter"
    - from: "gateway/cmd/gateway/main.go"
      to: "gateway/internal/admin/metrics.go + audit.go"
      via: "adminRouter.Method(GET, /metrics|/audit, ...) inside the adminVerifier block"
      pattern: "adminRouter.Method"
    - from: "gateway/internal/emerg/fsm.go"
      to: "gateway/internal/audit/writer.go"
      via: "FSM onChange calls WriteStateChange(\"fsm_transition\", ...)"
      pattern: "WriteStateChange"
---

<objective>
The composition-root wiring — `main.go` plus the two state-change audit call-sites that emit `audit_log` rows. This plan turns the isolated pieces from 07-02..07-05 into a running system: construct the three alert clients from config (empty env var = channel disabled with a WARN), build the `Alerter`, spawn `go alerter.Run(ctx)` EARLY (before the publishers — Pitfall 4), run `ReconcileBoot`, mount `/admin/metrics` + `/admin/audit` under the existing admin-key sub-router, and wire `audit.Writer.WriteStateChange("fsm_transition", ...)` into the FSM `onChange` path.

Purpose: this is the only plan that touches `main.go` — single composition root, no parallel writers. After this plan the gateway boots with full Phase 7 gateway-side observability + alerting.
Output: main.go wiring, FSM transition audit, build + boot tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-dashboard-alerting/07-CONTEXT.md
@.planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md
@.planning/phases/07-observability-dashboard-alerting/07-PATTERNS.md
@.planning/phases/07-observability-dashboard-alerting/07-02-SUMMARY.md
@.planning/phases/07-observability-dashboard-alerting/07-03-SUMMARY.md
@.planning/phases/07-observability-dashboard-alerting/07-05-SUMMARY.md

<interfaces>
From gateway/cmd/gateway/main.go — existing wiring points:
  main.go:88   adminUsageHandler http.Handler  / adminVerifier *admin.Verifier  (proxy struct fields)
  main.go:278  go breakerSet.Subscribe(ctx)                      (existing goroutine spawn)
  main.go:542  adminVerifier := admin.NewVerifier(gen.New(pool), rdb, log)
  main.go:682  go emergReconciler.Run(ctx)                       (existing goroutine spawn)
  main.go:748  adminUsageHandler := admin.NewUsageHandler(gen.New(pool), log)
  main.go:842  r.Handle("/metrics", obs.Handler())               (Prometheus — UNAUTHENTICATED, distinct from /admin/metrics)
  main.go:946  if px.adminVerifier != nil { adminRouter := chi.NewRouter(); adminRouter.Use(admin.Middleware(...)); adminRouter.Method(GET, "/usage", px.adminUsageHandler); r.Mount("/admin", adminRouter) }

From gateway/internal/alert/ (07-04 + 07-05):
  alert.NewChatwootClient(cfg) / NewClickUpClient(cfg) / NewBrevoClient(cfg)  — return a Channel
  alert.NewAlerter(rdb *redis.Client, channels []alert.Channel, log *slog.Logger) *Alerter
  func (a *Alerter) Run(ctx context.Context)
  func (a *Alerter) ReconcileBoot(ctx context.Context)

From gateway/internal/admin/ (07-03):
  admin.NewMetricsHandler(q *gen.Queries, fsm *emerg.FSM, log *slog.Logger) *MetricsHandler
  admin.NewAuditHandler(q *gen.Queries, log *slog.Logger) *AuditHandler

From gateway/internal/audit/writer.go (07-02):
  func (w *Writer) WriteStateChange(kind string, ev Event)

From gateway/internal/emerg/fsm.go:
  the FSM has an onChange hook (already emits a Sentry breadcrumb on transition) — line ~194 State()

From gateway/internal/config/config.go (07-01): the 12 alert Config fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construct alert clients + Alerter from config; spawn the goroutine early</name>
  <read_first>
    - gateway/cmd/gateway/main.go (the boot sequence — where pool/rdb/log are created, where breakerSet.Subscribe and emergReconciler.Run are spawned at lines 278/682, the proxy struct at line 88)
    - gateway/internal/config/config.go (the 12 alert Config fields from 07-01)
    - gateway/internal/alert/alerter.go + client.go (NewAlerter, Run, ReconcileBoot, the Channel constructors)
    - .planning/phases/07-observability-dashboard-alerting/07-RESEARCH.md (Pitfall 4 — spawn EARLY; Runtime State Inventory — "empty var = channel disabled with WARN, never fail-boot")
    - .planning/phases/07-observability-dashboard-alerting/07-PATTERNS.md (`gateway/cmd/gateway/main.go (EXTEND)` section — goroutine spawn ordering)
  </read_first>
  <action>
    In `main.go`, after `rdb` + `log` are available and BEFORE the breaker/shed/emerg subsystems are started (Pitfall 4 — Pub/Sub is at-most-once): build the channel list — for each of Chatwoot/ClickUp/Brevo, if its required config fields are non-empty construct the client and append to `[]alert.Channel`, else `log.Warn` (`"chatwoot alert channel disabled — CHATWOOT_API_TOKEN unset"` etc.) and skip it. Construct `alerter := alert.NewAlerter(rdb, channels, log)`. Call `alerter.ReconcileBoot(ctx)` then `go alerter.Run(ctx)` — this `go` statement MUST appear textually before `go breakerSet.Subscribe(ctx)` (main.go:278) and `go emergReconciler.Run(ctx)` (main.go:682). If the channel list is empty (all 12 vars unset), the alerter still runs (it logs + raises dashboard banners via the events; external fan-out is just a no-op set) — the gateway boots normally. Extend `main_test.go` (or the existing boot test) to assert the gateway constructs without panic when all alert env vars are unset.
  </action>
  <verify>
    <automated>cd gateway && go build ./cmd/gateway/ && go test ./cmd/gateway/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'go .*[Aa]lerter' gateway/cmd/gateway/main.go` — the alerter `go` spawn line number is LESS than the line numbers of `go breakerSet.Subscribe` and `go emergReconciler.Run`
    - `grep -q 'ReconcileBoot' gateway/cmd/gateway/main.go`
    - `grep -c 'log.Warn' gateway/cmd/gateway/main.go` increased by ≥ 3 (one disabled-channel WARN per client) vs the pre-plan baseline
    - The alert client construction is guarded by config non-emptiness — `git diff` shows no path where an empty token fails boot
    - `cd gateway && go build ./cmd/gateway/ && go test ./cmd/gateway/ -count=1` green
  </acceptance_criteria>
  <done>The three alert clients are constructed from config (empty var → WARN + skip), the Alerter is built, ReconcileBoot runs, and `go alerter.Run(ctx)` is spawned textually before all three event-publishing subsystems; the gateway builds and boots with all alert vars unset.</done>
</task>

<task type="auto">
  <name>Task 2: Mount /admin/metrics + /admin/audit under the admin sub-router</name>
  <read_first>
    - gateway/cmd/gateway/main.go (the proxy struct at line 88; the adminUsageHandler construction at line 748; the `if px.adminVerifier != nil` admin-router block at line 946)
    - gateway/internal/admin/metrics.go + audit.go (the NewMetricsHandler / NewAuditHandler constructors from 07-03)
    - gateway/internal/emerg/reconciler.go or fsm.go (where the `*emerg.FSM` instance lives — NewMetricsHandler needs it injected)
    - .planning/phases/07-observability-dashboard-alerting/07-PATTERNS.md (`gateway/cmd/gateway/main.go (EXTEND)` — Admin route registration block + the `/metrics` vs `/admin/metrics` distinction)
  </read_first>
  <action>
    In `main.go`: add `adminMetricsHandler http.Handler` and `adminAuditHandler http.Handler` fields to the proxy struct next to `adminUsageHandler` (line 88). Construct them next to `adminUsageHandler := admin.NewUsageHandler(...)` (line 748): `adminMetricsHandler := admin.NewMetricsHandler(gen.New(pool), emergFSM, log)` (pass the existing `*emerg.FSM` instance) and `adminAuditHandler := admin.NewAuditHandler(gen.New(pool), log)`; assign both into the proxy struct. In the `if px.adminVerifier != nil` block (line 946), after the existing `adminRouter.Method(http.MethodGet, "/usage", px.adminUsageHandler)`, add `adminRouter.Method(http.MethodGet, "/metrics", px.adminMetricsHandler)` and `adminRouter.Method(http.MethodGet, "/audit", px.adminAuditHandler)` — both guarded by the same `px.adminXHandler != nil` nil-check style the usage handler uses. Do NOT touch the unauthenticated `r.Handle("/metrics", obs.Handler())` at line 842 — that is the Prometheus exposition endpoint and stays distinct from the admin-key-gated `/admin/metrics`.
  </action>
  <verify>
    <automated>cd gateway && go build ./cmd/gateway/ && go test ./cmd/gateway/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'adminMetricsHandler\|adminAuditHandler' gateway/cmd/gateway/main.go` returns ≥ 6 (2 struct fields + 2 constructions + 2 struct assignments)
    - `grep -c 'adminRouter.Method' gateway/cmd/gateway/main.go` returns ≥ 3 (usage + metrics + audit)
    - Both new `adminRouter.Method` lines are inside the `if px.adminVerifier != nil` block — verified by reading
    - The line `r.Handle("/metrics", obs.Handler())` (line ~842) is unchanged — `git diff` shows no edit to it
    - `cd gateway && go build ./cmd/gateway/ && go test ./cmd/gateway/ -count=1` green
  </acceptance_criteria>
  <done>`/admin/metrics` and `/admin/audit` are mounted inside the admin-key-gated `/admin` sub-router; the unauthenticated Prometheus `/metrics` is untouched; the gateway builds.</done>
</task>

<task type="auto">
  <name>Task 3: FSM transitions emit fsm_transition audit rows</name>
  <read_first>
    - gateway/internal/emerg/fsm.go (the FSM struct + its onChange hook — it already emits a Sentry breadcrumb on transition; the State type)
    - gateway/internal/emerg/reconciler.go (how the FSM is constructed + whether the audit Writer is reachable from it)
    - gateway/internal/audit/writer.go (the WriteStateChange helper from 07-02 + the Event struct's relevant fields)
    - gateway/cmd/gateway/main.go (where the audit Writer and the FSM are both constructed — to thread the Writer into the FSM)
    - .planning/phases/07-observability-dashboard-alerting/07-CONTEXT.md (OBS-07 — FSM transitions must leave an audit_log entry)
  </read_first>
  <action>
    Thread the existing `*audit.Writer` into the FSM (constructor parameter or a setter, mirroring how the Sentry-breadcrumb dependency is already passed). In the FSM `onChange` path — the same place the Sentry breadcrumb is emitted — call `writer.WriteStateChange("fsm_transition", audit.Event{...})` populating the from-state, to-state, and a `Route`/`ErrorCode` carrying the transition reason (use whatever fields the `Event` struct exposes; `EventKind` is set by `WriteStateChange`). The write goes through the existing async batch writer — non-blocking, never stalls the FSM. If the FSM is constructed before the audit Writer in `main.go`, reorder so the Writer exists first (the Writer has no dependency on the FSM). Guard for a nil Writer (tests construct the FSM without one) — nil Writer → skip the audit call, no panic. Add/extend an `emerg` test asserting a transition triggers exactly one `WriteStateChange("fsm_transition", ...)` call against an injected fake writer.
  </action>
  <verify>
    <automated>cd gateway && go test ./internal/emerg/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'WriteStateChange("fsm_transition"' gateway/internal/emerg/fsm.go`
    - The FSM `onChange` path calls `WriteStateChange` next to the existing Sentry-breadcrumb emission — verified by reading
    - A nil audit Writer does NOT panic the FSM — there is an explicit nil guard
    - An `emerg` test asserts a transition produces exactly one `fsm_transition` audit call against a fake writer
    - `cd gateway && go test ./internal/emerg/ -count=1` green
  </acceptance_criteria>
  <done>Every FSM transition emits one `WriteStateChange("fsm_transition", ...)` audit row through the existing async writer, with a nil-Writer guard; emerg tests prove it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot config → gateway process | Alert credentials decide which channels activate |
| HTTP client → /admin/* | The admin sub-router is the only authenticated read surface for the dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Spoofing | /admin/metrics, /admin/audit mounting | mitigate | Both routes are registered ONLY inside the `if px.adminVerifier != nil` block, under `admin.Middleware(px.adminVerifier, log)` (Phase 4 `X-Admin-Key` bcrypt gate). The unauthenticated Prometheus `/metrics` (main.go:842) is deliberately left untouched and serves only cardinality-bounded counters — no per-tenant cost or audit content. |
| T-07-21 | Repudiation | FSM transition audit | mitigate | OBS-07 — every FSM transition now writes an append-only `audit_log` row with `event_kind = fsm_transition` via the existing async writer. The trail is immutable (audit_log has no UPDATE path). |
| T-07-22 | Denial of Service | missed boot-window alerts | mitigate | Pitfall 4 — `go alerter.Run(ctx)` is spawned textually before `go breakerSet.Subscribe` and `go emergReconciler.Run`; `ReconcileBoot` reads current incident state on startup. An acceptance criterion asserts the spawn-line ordering. |
| T-07-23 | Information Disclosure | alert credentials at boot | mitigate | The disabled-channel WARN logs only the channel name + the missing env var NAME (`"CHATWOOT_API_TOKEN unset"`) — never a token value. config.go holds the credentials as plain strings and never logs them. |
</threat_model>

<verification>
- `cd gateway && go build ./...` exits 0.
- `cd gateway && go test ./... -count=1 -race` green (full gateway suite, race-clean) — this is the end-of-wave full-suite gate.
- `grep -n 'go .*[Aa]lerter\|go breakerSet.Subscribe\|go emergReconciler.Run' gateway/cmd/gateway/main.go` confirms the alerter spawn precedes both publishers.
- A gateway boot with all 12 alert env vars unset logs three disabled-channel WARNs and serves traffic normally.
</verification>

<success_criteria>
- The alerter goroutine is spawned before every event-publishing subsystem; `ReconcileBoot` runs at startup.
- `/admin/metrics` + `/admin/audit` are mounted under the admin-key sub-router; Prometheus `/metrics` untouched.
- Every FSM transition writes a `fsm_transition` audit_log row.
- The gateway boots and runs with all alert env vars unset (channels disabled with WARN, no fail-boot).
- Full gateway test suite is green and race-clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-dashboard-alerting/07-06-SUMMARY.md`
</output>
