---
phase: 06.9-openrouter-model-rewrite-per-upstream
plan: 05
type: execute
wave: 3
depends_on: [06.9-03, 06.9-04]
files_modified:
  - gateway/internal/integration_test/resilience_helpers_test.go
  - gateway/internal/integration_test/openrouter_model_rewrite_test.go
  - gateway/internal/integration_test/openai_whisper_model_rewrite_test.go
  - gateway/internal/integration_test/openai_embed_model_rewrite_test.go
  - gateway/internal/integration_test/migration_0026_test.go
  - .planning/PROJECT.md
autonomous: true
requirements: [OR-FIX, STT-OAI-FIX, EMBED-OAI-FIX]
tags: [integration-tests, body-capture, mock, cascade-docs, project-md]

must_haves:
  truths:
    - "newSuccessMock (or a new newSuccessMockCapturing variant) exposes captured request body via mock.LastBody() []byte"
    - "TestIntegration_OpenRouterModelRewrite: trips local-llm breaker, posts {model:qwen}, asserts tier-1 mock saw forwarded body with model=qwen/qwen3.5-27b (not qwen)"
    - "TestIntegration_OpenAIWhisperModelRewrite: trips local-stt breaker, posts multipart with model=whisper + WAV, asserts tier-1 mock saw multipart with model=whisper-1 AND audio bytes byte-identical"
    - "TestIntegration_OpenAIEmbedModelRewrite: trips local-embed breaker, posts {model:bge-m3,input:[x]}, asserts tier-1 mock saw forwarded body with model=text-embedding-3-small AND dimensions=1024"
    - "TestIntegration_Migration0026_UpDownUp: applies migration Up, asserts 6 rows + composite PK; applies Down, asserts PK back on (alias) + tier-1 rows removed AND column preserved AND tier-0 backfilled values preserved; applies Up again, asserts 6 rows again (idempotent)"
    - "PROJECT.md tier-1 stack documentation lists per-upstream model targets correctly (qwen/qwen3.5-27b, whisper-1, text-embedding-3-small)"
  artifacts:
    - path: "gateway/internal/integration_test/resilience_helpers_test.go"
      provides: "Extended upstreamMock with body-capture + assertion API"
      contains: "LastBody"
    - path: "gateway/internal/integration_test/openrouter_model_rewrite_test.go"
      provides: "End-to-end OR-FIX integration test (body capture proves rewrite)"
      contains: "TestIntegration_OpenRouterModelRewrite"
    - path: "gateway/internal/integration_test/openai_whisper_model_rewrite_test.go"
      provides: "End-to-end STT-OAI-FIX with binary audio preservation"
      contains: "TestIntegration_OpenAIWhisperModelRewrite"
    - path: "gateway/internal/integration_test/openai_embed_model_rewrite_test.go"
      provides: "End-to-end EMBED-OAI-FIX consistency check"
      contains: "TestIntegration_OpenAIEmbedModelRewrite"
    - path: "gateway/internal/integration_test/migration_0026_test.go"
      provides: "Schema migration round-trip test against testcontainers Postgres"
      contains: "TestIntegration_Migration0026"
  key_links:
    - from: "gateway/internal/integration_test/openrouter_model_rewrite_test.go"
      to: "gateway/internal/integration_test/resilience_helpers_test.go"
      via: "newSuccessMockCapturing(t) + mock.LastBody()"
      pattern: "mock\\.LastBody\\(\\)"
    - from: ".planning/PROJECT.md"
      to: ".planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md"
      via: "tier-1 model targets table"
      pattern: "qwen/qwen3.5-27b"
---

<objective>
Wave 3: Integration test coverage + project doc updates. With Plan 03 (director rewrites) and Plan 04 (config + CLI) shipped, this plan closes the test-shaped gap that hid the bug for months: the existing `newSuccessMock` returns 200 regardless of forwarded body, so tier-1 routing tests passed while production OpenRouter 404'd.

This plan:
1. Extends `newSuccessMock` (or adds a new `newSuccessMockCapturing` variant) so tests can assert on the FORWARDED body, not just on which upstream was called.
2. Adds 3 new integration tests — one per requirement (OR-FIX, STT-OAI-FIX, EMBED-OAI-FIX) — that trip the tier-0 breaker, POST through the gateway, and assert the tier-1 mock saw the rewritten model slug.
3. Adds a migration 0026 round-trip integration test (Up → Down → Up) against testcontainers Postgres so future PK / column / seed drift is caught at PR time.
4. Updates `PROJECT.md` with the corrected tier-1 model targets table (qwen/qwen3.5-27b, whisper-1, text-embedding-3-small) so the project's source-of-truth doc stays accurate.

Purpose: After this plan, the entire OR-FIX → STT-OAI-FIX → EMBED-OAI-FIX chain has CI-enforced regression coverage. The "fake upstream accepts any model name" anti-pattern is closed.

Output: 1 helper file extended + 3 new integration test files + 1 new migration test file + 1 doc update. `cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=300s` PASS including 4 new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-PATTERNS.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-03-SUMMARY.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-04-SUMMARY.md

@gateway/internal/integration_test/resilience_helpers_test.go
@gateway/internal/integration_test/fallback_routing_test.go
@gateway/internal/upstreams/testdata/probe.wav
@.planning/PROJECT.md

<interfaces>
<!-- Current upstreamMock shape (gateway/internal/integration_test/resilience_helpers_test.go:29-66) — to EXTEND: -->
```go
type upstreamMock struct {
    server *httptest.Server
    hits   *atomic.Int64
}

func newSuccessMock(t *testing.T) *upstreamMock { /* returns 200 always; body discarded */ }
```

<!-- Phase 06.9 extension (this plan adds): -->
```go
type upstreamMock struct {
    server   *httptest.Server
    hits     *atomic.Int64
    lastBody atomic.Value // []byte — captured forwarded body
    lastCT   atomic.Value // string — captured Content-Type
}

// NEW: capturing variant for body-shape assertions
func newSuccessMockCapturing(t *testing.T) *upstreamMock
func (m *upstreamMock) LastBody() []byte
func (m *upstreamMock) LastContentType() string
```

<!-- Existing integration test pattern (fallback_routing_test.go:30-92): -->
<!-- - freshSchema(t, ctx) for DB; spawns testcontainers Postgres + Redis -->
<!-- - newFailMock + newSuccessMock for upstreams -->
<!-- - resilienceLoader("llm", "local-llm", url0, "openrouter-chat", url1) -->
<!-- - newClassifyingProxy(t, url, bs, name) for breaker integration -->
<!-- - proxy.NewDispatcher(...) with Proxies map keyed by upstream name -->
<!-- - driveBreaker(t, bs, "local-llm", 500, 3) to trip the breaker -->

<!-- For Phase 06.9 tests, ALSO instantiate a real models.Resolver via models.NewResolver(pool, log) -->
<!-- and wire it into each Build*Director (per Plan 03 wiring). -->

<!-- Migration round-trip test pattern (use goose programmatic API + testcontainers): -->
<!-- - Spin up Postgres container -->
<!-- - Run all migrations up to 0025 (preceding) -->
<!-- - Apply 0026 Up; assert 6 rows + new PK structure -->
<!-- - Apply 0026 Down; assert 3 rows + old PK structure -->
<!-- - Apply 0026 Up again; assert 6 rows (idempotency) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend upstreamMock with body capture + add 3 integration tests for model rewrite</name>
  <files>gateway/internal/integration_test/resilience_helpers_test.go, gateway/internal/integration_test/openrouter_model_rewrite_test.go, gateway/internal/integration_test/openai_whisper_model_rewrite_test.go, gateway/internal/integration_test/openai_embed_model_rewrite_test.go</files>
  <read_first>
    - gateway/internal/integration_test/resilience_helpers_test.go (full file — extend, don't replace; preserve newFailMock + newSlowMock + newClassifyingProxy + resilienceLoader + driveBreaker)
    - gateway/internal/integration_test/fallback_routing_test.go (full file — copy the freshSchema/breaker/dispatcher boilerplate verbatim into new test files)
    - gateway/internal/models/resolver.go (NewResolver signature; Start + Refresh)
    - gateway/internal/proxy/openrouter_director.go (post-Plan-03 signature with resolver+upstreamName)
    - gateway/internal/proxy/openai_whisper_director.go (post-Plan-03 signature)
    - gateway/internal/proxy/openai_embed_director.go (post-Plan-03 signature)
    - gateway/internal/upstreams/testdata/probe.wav (file path + size for binary fixture)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-PATTERNS.md (lines 480-528 — integration test additions)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md (lines 604-636 — capturing mock pattern with verbatim shape)
  </read_first>
  <acceptance_criteria>
    - `resilience_helpers_test.go` extends `upstreamMock` with `lastBody atomic.Value` + `lastCT atomic.Value` + accessor methods `LastBody() []byte` + `LastContentType() string`
    - New constructor `newSuccessMockCapturing(t *testing.T) *upstreamMock` returns 200 + captures body bytes via `io.ReadAll(r.Body)` BEFORE writing the response
    - Existing `newSuccessMock` callers in other integration tests UNCHANGED (the new capturing variant is opt-in to avoid touching unrelated tests)
    - New file `openrouter_model_rewrite_test.go` with `//go:build integration` tag; defines `TestIntegration_OpenRouterModelRewrite` that:
      1. `freshSchema(t, ctx)` to bootstrap DB with migration 0026 applied (the migration runner will pick up the new file naturally if Plan 01 added it to migrate_test.go's list; verify)
      2. Instantiates `models.NewResolver(pool, log)` + Refresh
      3. Wires `tier0 := newFailMock(t)`, `tier1 := newSuccessMockCapturing(t)`
      4. Builds OpenRouter director against `tier1.server.URL` with the resolver + `"openrouter-chat"` upstream name
      5. Constructs dispatcher with proxies map (Plan 03 wiring shape)
      6. Trips `driveBreaker(t, bs, "local-llm", 500, 3)`
      7. Sends `POST /v1/chat/completions {"model":"qwen","messages":[{"role":"user","content":"ping"}]}`
      8. Assert: `tier1.hits.Load() >= 1`, `var body map[string]any; json.Unmarshal(tier1.LastBody(), &body); body["model"] == "qwen/qwen3.5-27b"`
    - New file `openai_whisper_model_rewrite_test.go` with `TestIntegration_OpenAIWhisperModelRewrite`:
      1. Same DB + resolver bootstrap
      2. tier0 = newFailMock, tier1 = newSuccessMockCapturing for whisper
      3. Build Whisper director with resolver + `"openai-whisper"` upstream name
      4. Trip `local-stt` breaker
      5. POST multipart `/v1/audio/transcriptions` with `model=whisper` form field + `file=<probe.wav bytes>`
      6. Assert: parse `tier1.LastBody()` as multipart (using `mime/multipart.NewReader` keyed on `tier1.LastContentType()`'s boundary); `model` form field == `"whisper-1"`; file part bytes == original probe.wav bytes (`bytes.Equal`)
    - New file `openai_embed_model_rewrite_test.go` with `TestIntegration_OpenAIEmbedModelRewrite`:
      1. Same setup
      2. tier1 = newSuccessMockCapturing for embed
      3. Build Embed director with resolver + `"openai-embed"`
      4. Trip `local-embed` breaker
      5. POST `/v1/embeddings {"model":"bge-m3","input":["test"]}`
      6. Assert: forwarded body has `"model":"text-embedding-3-small"` AND `"dimensions":1024` AND `"input":["test"]`
    - All 3 tests pass: `cd gateway && go test -tags=integration ./internal/integration_test/... -run 'TestIntegration_(OpenRouter|OpenAIWhisper|OpenAIEmbed)_ModelRewrite' -count=1 -timeout=300s`
  </acceptance_criteria>
  <action>
    First, extend `resilience_helpers_test.go`: add `lastBody atomic.Value` + `lastCT atomic.Value` fields to `upstreamMock`; add `LastBody() []byte` + `LastContentType() string` getters; add `newSuccessMockCapturing(t *testing.T) *upstreamMock` constructor that reads request body via `io.ReadAll(r.Body)` + stores via `Store(body)` BEFORE responding. Reference RESEARCH.md lines 604-636 for the verbatim shape.

    Then create the 3 new test files (one per requirement). Each file:
    - Uses `//go:build integration` build tag
    - Imports the testcontainers/Redis/Postgres helpers from `resilience_helpers_test.go`
    - Copies the `fallback_routing_test.go:30-92` skeleton (freshSchema + loader + breaker + proxies + dispatcher) and adapts to wire the appropriate Director with resolver + upstream-name
    - Asserts on the captured body via `mock.LastBody()`

    For the Whisper test: construct multipart body via `mime/multipart.NewWriter` in the test setup; embed `gateway/internal/upstreams/testdata/probe.wav` bytes via `os.ReadFile`. Parse the captured body back as multipart in the assertion using `mime.ParseMediaType` on `LastContentType()` to extract the new boundary.

    Run tests — all must PASS. If any test fails because of fixture wiring (resolver not seeded), bootstrap the resolver via SQL INSERT in the test setup directly OR rely on the migration 0026 having seeded the rows.
  </action>
  <verify>
    <automated>cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=300s -run 'TestIntegration_(OpenRouter|OpenAIWhisper|OpenAIEmbed)_?ModelRewrite' -v</automated>
  </verify>
  <done>3 new integration tests + body-capture mock variant; all PASS against testcontainers Postgres+Redis; bug-hiding "fake mock" anti-pattern eliminated.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0026 round-trip integration test</name>
  <files>gateway/internal/integration_test/migration_0026_test.go</files>
  <read_first>
    - gateway/internal/integration_test/resilience_helpers_test.go (freshSchema helper to understand the testcontainers Postgres lifecycle)
    - gateway/internal/db/migrate_test.go (the existing migration ordering test — see how it loads/applies migrations)
    - gateway/db/migrations/ (find the goose entry point + how to run Up/Down programmatically — likely via `github.com/pressly/goose/v3`)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md (lines 67-71 — Wave 0 gap list, this test is item 4)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md (lines 240-246 — Down rationale: column PRESERVED for re-Up idempotency + tier-0 data safety)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-01-PLAN.md (line 137 — Plan 01 Down acceptance: DELETEs only tier-1 seed rows; does NOT drop upstream_name column)
  </read_first>
  <acceptance_criteria>
    - File `gateway/internal/integration_test/migration_0026_test.go` exists with `//go:build integration` tag
    - Test name: `TestIntegration_Migration0026_UpDownUp`
    - Test bootstraps a fresh testcontainers Postgres
    - Applies all migrations up to 0025 (i.e., the migration BEFORE 0026)
    - Asserts initial state: `SELECT count(*) FROM ai_gateway.model_aliases` returns 3
    - Applies 0026 Up via goose programmatic API
    - Asserts post-Up: `SELECT count(*) FROM ai_gateway.model_aliases` returns 6; verify a specific tier-1 row exists: `SELECT target FROM ai_gateway.model_aliases WHERE alias='qwen' AND upstream_name='openrouter-chat'` returns `'qwen/qwen3.5-27b'`; verify the composite PK: `pg_constraint` lookup or `\d model_aliases` style introspection shows PK is `(alias, upstream_name)`
    - Applies 0026 Down
    - Asserts post-Down: `SELECT count(*) FROM ai_gateway.model_aliases` returns 3; PK back on `(alias)`; **upstream_name column STILL PRESENT** after Down (Plan 01 Down preserves column for re-Up idempotency per RESEARCH A6); assert presence via `SELECT column_name FROM information_schema.columns WHERE table_schema='ai_gateway' AND table_name='model_aliases' AND column_name='upstream_name'` returns 1 row
    - Applies 0026 Up again
    - Asserts idempotent: 6 rows again (ON CONFLICT DO NOTHING handled re-insert)
    - Test PASSES: `cd gateway && go test -tags=integration ./internal/integration_test/... -run TestIntegration_Migration0026 -count=1 -timeout=180s`
  </acceptance_criteria>
  <action>
    Inspect how `migrate_test.go` runs migrations (likely uses `goose.SetBaseFS` + `goose.UpTo` or similar). Clone that pattern for the integration test, but use testcontainers Postgres instead of an in-memory DB.

    Test flow:
    1. Spin up testcontainers Postgres + create schema `ai_gateway`
    2. Use `goose.UpTo(db, "...", 25)` to apply migrations 0001-0025
    3. Query: expect 3 rows
    4. `goose.UpTo(db, "...", 26)` (or `goose.Up(db, "...")` to advance one)
    5. Query: expect 6 rows + PK shape + specific tier-1 target value
    6. `goose.Down(db, "...")` (single step back)
    7. Query post-Down: expect 3 rows total (tier-0 backfilled rows preserved: local-llm/local-stt/local-embed) + PK back on (alias) + upstream_name column PRESERVED (per Plan 01 Down rationale); also assert: rows with `upstream_name IN ('openrouter-chat','openai-whisper','openai-embed')` returns 0 rows (tier-1 seed rows deleted); rows with `upstream_name IN ('local-llm','local-stt','local-embed')` returns 3 rows (tier-0 backfilled rows preserved)
    8. `goose.Up(db, "...")` again
    9. Query: expect 6 rows (idempotent re-insert)

    Each assertion uses `require.Equal` from testify (if used in other integration tests; check `freshSchema` for the assert lib in use).

    Reference Plan 01 migration filename if pattern-matching helpers need the exact name (e.g., `0026_evolve_model_aliases_per_upstream.sql` per the planned name; if Plan 01 bumped to 0027, adjust the goose step count).

    Run test — must PASS.
  </action>
  <verify>
    <automated>cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=180s -run TestIntegration_Migration0026 -v</automated>
  </verify>
  <done>Migration 0026 round-trip Up→Down→Up tested end-to-end; schema PK + seeds + Down rollback verified.</done>
</task>

<task type="auto">
  <name>Task 3: PROJECT.md tier-1 stack confirmation update</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full file — find the tier-1 / fallback / model-targets section; likely has a table with current OpenRouter/OpenAI model targets)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md (D-01..D-04 for the canonical targets)
    - .planning/phases/03-resilience-fallback-chain/03-WAVE0-GATES.md (already updated by Plan 01 — cross-reference the corrected URL convention)
  </read_first>
  <acceptance_criteria>
    - PROJECT.md tier-1 stack documentation reflects:
      - LLM tier-1: OpenRouter, model `qwen/qwen3.5-27b`, provider Novita preferred (DeepInfra fallback)
      - STT tier-1: OpenAI, model `whisper-1`
      - Embed tier-1: OpenAI, model `text-embedding-3-small` + dimensions=1024 (BGE-M3 parity)
      - URL convention: base URL ends at `/api` for OpenRouter (NOT `/api/v1`); explicit note that Phase 06.9 added boot fail-fast for this
      - Resolver lookup: schema-driven via `model_aliases` composite PK `(alias, upstream_name)` — schema is the source of truth; deprecated `UPSTREAM_LLM_OPENROUTER_MODEL` env var noted as no-op
    - Add a Phase 06.9 closure note (1-2 sentences) at the bottom of the tier-1 stack section: "Phase 06.9 (2026-05-24) closed the per-upstream model rewrite gap end-to-end; SC-1 cascade closes Phase 02/03/05 deferrals."
    - Document does NOT regress other tier-1 facts (read existing content carefully; surgical edit only — do NOT rewrite unrelated sections)
  </acceptance_criteria>
  <action>
    Read PROJECT.md fully. Locate the tier-1 / external-provider section (search for `OpenRouter`, `Whisper`, `tier-1`, `fallback`).

    Make a SURGICAL update: correct any outdated model slugs, URL conventions, or env var references to match the post-Phase-06.9 reality. Add the Phase 06.9 closure note. Do NOT touch other sections.

    If the file does NOT have a tier-1 stack section (or is highly abbreviated), add a new "Phase 06.9: Tier-1 Stack Final" subsection that lists the 3 upstreams + their targets + URL convention + resolver behavior, with a single-line cross-reference to `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md` for full decision history.

    Verify the file remains valid markdown (no broken headers, no orphan list markers).
  </action>
  <verify>
    <automated>grep -q "qwen/qwen3.5-27b" .planning/PROJECT.md && grep -q "whisper-1" .planning/PROJECT.md && grep -q "text-embedding-3-small" .planning/PROJECT.md && grep -q "Phase 06.9" .planning/PROJECT.md && ! grep -q "openrouter.ai/api/v1" .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md tier-1 stack section reflects post-06.9 reality; Phase 06.9 closure note added; no broken markdown.</done>
</task>

</tasks>

<verification>
- `cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=300s` ALL PASS (3 new model-rewrite tests + 1 new migration test + existing tests)
- `cd gateway && go test ./... -count=1 -race -timeout=120s` (full unit suite) still PASS
- `grep -c "lastBody\|LastBody" gateway/internal/integration_test/resilience_helpers_test.go` returns at least 2
- `ls gateway/internal/integration_test/*model_rewrite_test.go` shows 3 files
- `grep -c "qwen/qwen3.5-27b\|whisper-1\|text-embedding-3-small" .planning/PROJECT.md` returns at least 3
</verification>

<success_criteria>
- Body-capturing mock variant in place; bug-hiding "fake mock returns 200 regardless of body" anti-pattern closed
- 3 new integration tests prove per-upstream model rewriting end-to-end with body assertion
- Migration 0026 round-trip integration test catches future schema drift at PR time
- PROJECT.md reflects the corrected post-Phase-06.9 tier-1 stack reality
- All 3 Phase 06.9 unit requirements (OR-FIX, STT-OAI-FIX, EMBED-OAI-FIX) have BOTH unit + integration coverage
- Plan 06 (live UAT) can proceed knowing the code-side is fully proven
</success_criteria>

<output>
Create `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-05-SUMMARY.md` when done. Capture: diff of `resilience_helpers_test.go` body-capture extension; full source of each new integration test (4 files); PROJECT.md diff snippet for the tier-1 section update.
</output>
