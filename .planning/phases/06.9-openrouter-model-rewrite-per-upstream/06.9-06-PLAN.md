---
phase: 06.9-openrouter-model-rewrite-per-upstream
plan: 06
type: execute
wave: 5
depends_on: [06.9-05]
files_modified:
  - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md
  - .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md
  - .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md
  - .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md
autonomous: false
requirements: [SC1-CLOSE]
user_setup:
  - service: openai
    why: "S2 (Whisper) + S3 (Embed) live UAT scenarios require a real OpenAI API key"
    env_vars:
      - name: UPSTREAM_STT_OPENAI_AUTH_BEARER
        source: "OpenAI Platform > API keys (https://platform.openai.com/api-keys); create a project-scoped secret key"
      - name: UPSTREAM_EMBED_OPENAI_AUTH_BEARER
        source: "Same OpenAI key (one key serves both whisper + embed scopes)"
    dashboard_config:
      - task: "Apply both env vars to /opt/ai-gateway-dev/.env on vps-ifix-vm"
        location: "SSH vps-ifix-vm; edit /opt/ai-gateway-dev/.env; docker compose -f /opt/ai-gateway-dev/docker-compose.yml up -d --force-recreate gateway"
  - service: openrouter
    why: "S1 (Chat) live UAT scenario; key already applied this session 2026-05-24 — re-verify before UAT"
    env_vars:
      - name: UPSTREAM_LLM_OPENROUTER_AUTH_BEARER
        source: "OpenRouter > Keys; existing key in /opt/ai-gateway-dev/.env per memory entry openrouter-token-and-stack-location.md"
    dashboard_config:
      - task: "Re-verify key still valid by direct curl ops-claude → openrouter.ai/api/v1/chat/completions before S1"
        location: "Same key + verify mid-UAT-day"

tags: [live-uat, human-checkpoint, cascade-close, sc1, phase-02, phase-03, phase-05]

must_haves:
  truths:
    - "06.9-HUMAN-UAT.md authored with 6 scenarios + sign-off lines + Wave 0 pre-req (OpenAI key) + cleanup section"
    - "S1 (Chat OpenRouter), S2 (Whisper OpenAI), S3 (Embed OpenAI) operator-signed PASS with HTTP 200 + real tier-1 completion/transcription/embedding"
    - "S4 (Phase 03 SC-1 cascade), S5 (Phase 02 step 7 cascade), S6 (Phase 05 SC-1 vegeta cascade) operator-signed PASS"
    - "Phase 02 02-VERIFICATION.md SC-5 step 7 flipped from passed_partial → passed with cross-reference to 06.9-HUMAN-UAT.md S5 evidence"
    - "Phase 03 03-VERIFICATION.md SC-1 flipped from human_needed → passed with cross-reference to 06.9-HUMAN-UAT.md S4 evidence"
    - "Phase 05 05-VERIFICATION.md SC-1 flipped from PARTIAL → passed with cross-reference to 06.9-HUMAN-UAT.md S6 evidence"
  artifacts:
    - path: ".planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md"
      provides: "6-scenario operator scoresheet + signed evidence"
      contains: "S1 — Chat tier-1"
    - path: ".planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md"
      provides: "SC-5 step 7 closure"
      contains: "06.9-HUMAN-UAT.md S5"
    - path: ".planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md"
      provides: "SC-1 closure"
      contains: "06.9-HUMAN-UAT.md S4"
    - path: ".planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md"
      provides: "SC-1 closure"
      contains: "06.9-HUMAN-UAT.md S6"
  key_links:
    - from: ".planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md"
      to: ".planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md"
      via: "S4 cascade evidence reference"
      pattern: "06.9-HUMAN-UAT.md"
---

<objective>
Wave 5 (final): Author the live HUMAN-UAT scenario sheet, drive operator through 6 scenarios on the live dev stack, capture sign-off evidence, and cascade-close 3 prior phase VERIFICATIONs whose deferrals this phase resolves.

This is the FINAL plan in Phase 06.9. After this plan signs:
- Phase 06.9 status: complete (SC1-CLOSE met, all 4 requirement IDs verified end-to-end)
- Phase 02 SC-5 step 7 chat E2E deferral CLOSED (was waiting for tier-1 to actually return 200 instead of 404)
- Phase 03 SC-1 live UAT deferral CLOSED (was waiting for full failover chain to function)
- Phase 05 SC-1 deferral CLOSED (was waiting for tier-1 to actually serve overflow traffic instead of 502/404)

**This plan is autonomous: false.** Task 2 is a BLOCKING human-verify checkpoint requiring real spend (OpenRouter + OpenAI API calls, no GPU/Vast spend). Autonomous mode CANNOT satisfy it. Operator must run S1-S6 on the live ai-gateway-dev stack on vps-ifix-vm, sign each scenario PASS/FAIL, and record evidence.

Estimated cost: ~$0.05 in OpenRouter + OpenAI usage (a few completions + 1 short transcription + 1 embedding). ZERO Vast / GPU spend (no pod is provisioned during this UAT — the WHOLE POINT of Phase 06.9 is that the tier-1 chain works WITHOUT the primary pod being up).

Estimated wall: 45-60 minutes operator-driven, including pre-UAT env-key application (Task 1.5 prereq).

Output: 6 signed scenarios + 3 cascade-closure VERIFICATION.md edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-CONTEXT.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-VALIDATION.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-04-SUMMARY.md
@.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-05-SUMMARY.md

@.planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md
@.planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md
@.planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md
@.planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md
@.planning/phases/02-gateway-core-multi-tenant-auth/02-UAT-2026-05-23.md
@.planning/phases/05-load-shedding-saturation-aware-routing/05-UAT-2026-05-13.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 06.9-HUMAN-UAT.md scoresheet with 6 scenarios + Wave 0 prereq + cleanup</name>
  <files>.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06.7-primary-pod-tts-swap-embed-for-kani-tts-2-pt-gpu-move-bge-m3/06.7-HUMAN-UAT.md (style template — 6-scenario sign-off shape; copy headers + sign-off line conventions)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-VALIDATION.md (S1-S6 already drafted at the bottom; rewrite as operator-facing scenarios with exact curl commands + expected responses)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-04-SUMMARY.md (read Plan 04's breaker (a)/(b) decision — informs whether operator uses `gatewayctl breaker force-open` OR `gatewayctl primary force-down` to drive S1-S3)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-RESEARCH.md (lines 488-538 — curl recipes for S1/S2/S3 verbatim)
    - .planning/phases/02-gateway-core-multi-tenant-auth/02-UAT-2026-05-23.md (step 7 chat E2E format for S5 re-run)
    - .planning/phases/05-load-shedding-saturation-aware-routing/05-UAT-2026-05-13.md (vegeta recipe for S6 re-run)
  </read_first>
  <acceptance_criteria>
    - File `06.9-HUMAN-UAT.md` exists with the following sections, in order:
      1. Header (Phase 06.9, date, status=in_progress, operator, expected wall time, expected $ spend)
      2. Pre-UAT checklist (Wave 0 prereqs): (a) verify OpenAI keys applied to `/opt/ai-gateway-dev/.env` for `UPSTREAM_STT_OPENAI_AUTH_BEARER` + `UPSTREAM_EMBED_OPENAI_AUTH_BEARER`; (b) re-verify OpenRouter key still valid via direct curl; (c) confirm `docker compose ps` shows ai-gateway-dev container healthy; (d) confirm migration 0026 (or whichever number Plan 01 chose) applied via `gatewayctl migrate status` (or psql); (e) confirm gateway image deployed matches develop tip after Phase 06.9 plans 01-05 merged; (f) note which method operator will use to drive tier-0 breaker OPEN (`gatewayctl breaker force-open` per Plan 04 decision (a), OR `gatewayctl primary force-down` per fallback (b))
      3. S1 — Chat tier-1 (OpenRouter): exact curl command, expected response shape (HTTP 200 + Qwen content + provider Novita or DeepInfra), PASS/FAIL sign-off line + evidence box (paste response + timestamp + operator initials)
      4. S2 — STT tier-1 (OpenAI Whisper): exact curl command using `/tmp/probe.wav` (operator scp's from local `gateway/internal/upstreams/testdata/probe.wav`), expected response (HTTP 200 + `{"text":"..."}`), sign-off + evidence box
      5. S3 — Embed tier-1 (OpenAI): exact curl command, expected response (HTTP 200 + 1024-dim embedding array), sign-off + evidence box
      6. S4 — Phase 03 SC-1 cascade re-run: cross-reference to `03-HUMAN-UAT.md` SC-1 scenario steps (re-run identically); operator confirms PASS; sign-off + evidence
      7. S5 — Phase 02 SC-5 step 7 cascade: re-run the step 7 chat E2E from `02-UAT-2026-05-23.md`; sign-off + evidence
      8. S6 — Phase 05 SC-1 cascade: re-run the vegeta burst from `05-UAT-2026-05-13.md` (5 RPS × 30s); expected: tier-1 200s (NOT 502/404); sign-off + evidence
      9. Cleanup section: (a) release `gatewayctl breaker force-close <upstream>` for any breaker still forced open; (b) restore primary pod state via `gatewayctl primary force-up` if S1-S3 used force-down; (c) operator confirms `docker compose ps` healthy + no orphan resources
      10. Final sign-off block: "All 6 scenarios PASS — Phase 06.9 ready for cascade closure" + operator initials + final timestamp
    - Each scenario includes:
      - Exact curl command(s) ready to copy-paste (`ssh vps-ifix-vm 'curl ...'` shape per RESEARCH.md)
      - Exact expected HTTP status + response shape
      - "Common failure modes" callout (e.g., S1: if HTTP 404 received, verify migration 0026 applied + resolver refreshed; S2: if 400 invalid_request_error, verify OpenAI key applied)
      - Sign-off line: `**Status:** [ ] PASS [ ] FAIL    **Operator:** ___    **Date:** ___    **Evidence:** [paste response or attach screenshot path]`
    - Estimated time per scenario in the header (target: S1=5min, S2=10min including key application if not done, S3=5min, S4=5min cascade-confirm, S5=5min cascade-confirm, S6=10min vegeta run)
    - Total target wall: 45-60min
  </acceptance_criteria>
  <action>
    Use `06.7-HUMAN-UAT.md` as the styling template (header/footer/sign-off shape). Substitute the 6 Phase 06.9 scenarios using the recipes from `06.9-VALIDATION.md` (bottom section S1-S6) and `06.9-RESEARCH.md` (lines 488-538 for curl recipes).

    For S1, the exact recipe is in RESEARCH.md gap evidence section (verified live 2026-05-24 — the failing case). Adapt: operator drives `local-llm` to OPEN via the chosen breaker driver, then runs the same curl; expected result CHANGES from HTTP 404 HTML to HTTP 200 + Qwen JSON.

    For S2, use `gateway/internal/upstreams/testdata/probe.wav` (50KB existing fixture). Instruct operator to scp it from the dev box: `scp ./gateway/internal/upstreams/testdata/probe.wav vps-ifix-vm:/tmp/probe.wav` BEFORE running the curl.

    For S3, send 2-input batch to verify list-response shape; assert `data[].embedding` length == 1024.

    For S4/S5/S6 (cascade scenarios), the operator simply re-runs the previously-failing scenario from each prior phase's UAT doc and confirms PASS this time. Each cascade scenario links to the original UAT doc — operator copies the exact prior failing-curl from the prior doc and runs it now.

    The breaker driver decision (from Plan 04 Task 2's (a)/(b) outcome) lives in the Pre-UAT checklist item (f) AND each S1/S2/S3 scenario. If (a) is live: `gatewayctl breaker force-open openrouter-chat` style commands. If (b) is the fallback: `gatewayctl primary force-down --reason uat69_s1` to put primary to sleep + observe breaker trip naturally (~30s wait).

    Cleanup section MUST include the inverse of every state change S1-S6 made. Operator confirms `docker compose ps` + `gatewayctl primary state` + `gatewayctl breaker list` (if Plan 04 (a)) return clean baseline state.
  </action>
  <verify>
    <automated>test -f .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S1 — Chat tier-1" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S2 — STT tier-1" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S3 — Embed tier-1" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S4 — Phase 03 SC-1" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S5 — Phase 02" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "S6 — Phase 05" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "Cleanup" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md && grep -q "Pre-UAT checklist" .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md</automated>
  </verify>
  <done>06.9-HUMAN-UAT.md authored with 6 scenarios + Wave 0 pre-req + cleanup + sign-off blocks; ready for operator execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING — operator runs 06.9-HUMAN-UAT.md S1-S6 + cleanup + signs evidence</name>
  <what-built>
    - Phase 06.9 Plans 01-05 have shipped:
      - Migration 0026 PK widening + tier-1 seeds (Plan 01)
      - Resolver per-upstream-name lookup (Plan 02)
      - 3 Directors rewrite model via resolver (Plan 03)
      - Config fail-fast + gatewayctl breaker + model-alias subcommands (Plan 04)
      - 3 integration tests + migration round-trip test + PROJECT.md update (Plan 05)
    - 06.9-HUMAN-UAT.md authored by Task 1 with 6 scenarios + Wave 0 pre-req + cleanup section
    - All unit + integration tests GREEN; develop tip carries the full Phase 06.9 fix
    - Dev stack at /opt/ai-gateway-dev/ on vps-ifix-vm received the new gateway image (operator confirms `docker compose ps` healthy after pull/recreate)
  </what-built>
  <how-to-verify>
    Operator follows `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md` end-to-end:

    1. **Pre-UAT (Task 1 checklist items a-f)**: apply OpenAI key to /opt/ai-gateway-dev/.env (`UPSTREAM_STT_OPENAI_AUTH_BEARER` + `UPSTREAM_EMBED_OPENAI_AUTH_BEARER`); recreate gateway container; re-verify OpenRouter key via direct curl ops-claude → openrouter; confirm migration applied via psql or gatewayctl migrate; confirm latest develop image deployed.

    2. **S1 (Chat OpenRouter, ~5min)**: drive local-llm breaker OPEN per Plan 04 decision; curl POST /v1/chat/completions with model=qwen + ping message; expect HTTP 200 + Qwen JSON with provider Novita preferred (DeepInfra acceptable); paste response + sign PASS.

    3. **S2 (Whisper OpenAI, ~10min)**: scp probe.wav to vps-ifix-vm; drive local-stt breaker OPEN; curl POST /v1/audio/transcriptions with file=@/tmp/probe.wav + model=whisper; expect HTTP 200 + {"text":"..."}; paste response + sign PASS.

    4. **S3 (Embed OpenAI, ~5min)**: drive local-embed breaker OPEN; curl POST /v1/embeddings with bge-m3 + 2 inputs; expect HTTP 200 + 2 embeddings × 1024 dims; sign PASS.

    5. **S4 (Phase 03 SC-1 cascade, ~5min)**: re-run 03-HUMAN-UAT.md SC-1; sign PASS.

    6. **S5 (Phase 02 SC-5 step 7 cascade, ~5min)**: re-run 02-UAT-2026-05-23.md step 7; sign PASS.

    7. **S6 (Phase 05 SC-1 cascade, ~10min)**: re-run 05-UAT-2026-05-13.md vegeta burst; assert tier-1 200s instead of prior 502/404; sign PASS.

    8. **Cleanup**: release breaker force-open OR restore primary force-up per Plan 04 (a)/(b) decision; confirm `gatewayctl primary state` + `gatewayctl breaker list` baseline; confirm `docker compose ps` healthy.

    9. **Final sign-off** in 06.9-HUMAN-UAT.md: "All 6 scenarios PASS — Phase 06.9 ready for cascade closure" + operator initials + date.

    **Spend cap:** if total OpenRouter + OpenAI usage exceeds $1.00 by S3 completion, STOP and triage (something is mis-configured; tier-1 should be cheap because we're not GPU-bound).

    **Failure handling:**
    - If S1 returns HTTP 404 + HTML body: migration 0026 didn't apply OR resolver didn't refresh OR director constructor didn't get the resolver+name. Inspect: `gatewayctl model-alias list` (should show 6 rows); `docker logs ai-gateway-dev | grep 'model aliases refreshed'` (should show count=6); `docker logs ai-gateway-dev | grep 'dispatching upstream=openrouter-chat'` (should appear after the request). Report finding back via `/gsd:plan-phase 06.9 --gaps`.
    - If S2 returns HTTP 400 from OpenAI: model field rewrite failed OR audio bytes corrupted. Run TestIntegration_OpenAIWhisperModelRewrite locally to isolate.
    - If S6 vegeta shows tier-1 errors but S1 PASSes: load-shedding interaction; capture vegeta detailed report + open follow-up.
  </how-to-verify>
  <resume-signal>
    Operator types "approved" to acknowledge sign-off OR describes a specific FAIL with the affected scenario(s). If FAIL: do NOT proceed to Task 3 (cascade closure); instead run `/gsd:plan-phase 06.9 --gaps` to plan the gap-closure fix.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Cascade-close Phase 02, 03, 05 VERIFICATION.md to flip deferred SCs to PASS</name>
  <files>.planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md, .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md, .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md (find the SC-5 step 7 section currently in passed_partial state)
    - .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md (find the SC-1 section currently human_needed)
    - .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md (find the SC-1 section currently PARTIAL)
    - .planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md (post-Task-2: read the signed S4/S5/S6 evidence blocks for verbatim quotation in the cascade closures)
  </read_first>
  <acceptance_criteria>
    - **Phase 02 02-VERIFICATION.md SC-5 step 7**: status changes from `passed_partial` (or whatever the current marker is) to `passed`; add a Phase 06.9 closure note quoting S5 evidence from 06.9-HUMAN-UAT.md: "Closed 2026-MM-DD via Phase 06.9 — chat E2E gateway-path returned HTTP 200 with real Qwen completion after OR-FIX shipped. Evidence: see `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md` S5 sign-off."
    - **Phase 03 03-VERIFICATION.md SC-1**: status changes from `human_needed` to `passed`; add a Phase 06.9 closure note quoting S4 evidence: "Closed 2026-MM-DD via Phase 06.9 — full failover chain (local-llm breaker OPEN → dispatcher picks openrouter-chat → director rewrites model → OpenRouter returns 200 Qwen) verified end-to-end on dev stack. Evidence: see `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md` S4 sign-off."
    - **Phase 05 05-VERIFICATION.md SC-1**: status changes from `PARTIAL` to `passed`; add a Phase 06.9 closure note quoting S6 evidence: "Closed 2026-MM-DD via Phase 06.9 — vegeta 5 RPS × 30s burst now returns tier-1 200s (was 502/404 pre-OR-FIX because model name was forwarded as 'qwen' literal). Evidence: see `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md` S6 sign-off."
    - Each VERIFICATION.md edit is SURGICAL: change ONLY the SC status line + add the closure paragraph. Do NOT touch other SCs in those docs.
    - Each edit committed in a separate small commit (3 commits total) so cascade attribution is clean:
      - `docs(02): close SC-5 step 7 via Phase 06.9 cascade`
      - `docs(03): close SC-1 via Phase 06.9 cascade`
      - `docs(05): close SC-1 via Phase 06.9 cascade`
  </acceptance_criteria>
  <action>
    For each VERIFICATION.md:
    1. Locate the relevant SC section (SC-5 step 7 for Phase 02; SC-1 for Phase 03; SC-1 for Phase 05).
    2. Update the status marker (status: line OR table cell — match existing convention in each file).
    3. Insert a closure note paragraph referencing the Task 2 signed evidence and the date of operator sign-off (read from 06.9-HUMAN-UAT.md).
    4. Stage + commit with the scoped message above.

    Use `gsd-sdk query commit` for each commit:
    ```
    gsd-sdk query commit "docs(02): close SC-5 step 7 via Phase 06.9 cascade" --files .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md
    ```
    (and similar for 03/05)

    After all 3 commits land, write `06.9-06-SUMMARY.md` AND `06.9-VERIFICATION.md` (phase-level VERIFICATION) capturing the full Phase 06.9 status: all 4 requirement IDs verified (OR-FIX + STT-OAI-FIX + EMBED-OAI-FIX + SC1-CLOSE), 6 scenarios PASS in 06.9-HUMAN-UAT.md, 3 cascade closures landed, no open Tech Debt from this phase.
  </action>
  <verify>
    <automated>grep -q "Phase 06.9" .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md && grep -q "Phase 06.9" .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md && grep -q "Phase 06.9" .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md && grep -L "passed_partial\|human_needed\|PARTIAL" .planning/phases/02-gateway-core-multi-tenant-auth/02-VERIFICATION.md .planning/phases/03-resilience-fallback-chain/03-VERIFICATION.md .planning/phases/05-load-shedding-saturation-aware-routing/05-VERIFICATION.md | wc -l | grep -q '^3$' || echo "WARN: at least one VERIFICATION.md still has pending markers — confirm only OTHER SCs hold them (not the cascade-closed ones)"</automated>
  </verify>
  <done>Phase 02/03/05 VERIFICATION.md cascade-closed for the 3 SCs Phase 06.9 was promised to resolve; 3 small docs commits land; phase-level 06.9-VERIFICATION.md authored.</done>
</task>

</tasks>

<verification>
- `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-HUMAN-UAT.md` exists with 6 signed scenarios (each Status: [x] PASS line filled in by operator)
- Operator total spend < $1.00 (OpenRouter + OpenAI combined; zero Vast/GPU spend)
- 3 cascade VERIFICATION.md edits landed in 3 distinct commits
- Phase-level `06.9-VERIFICATION.md` exists with status=passed
- `06.9-06-SUMMARY.md` exists and references all 6 PASS scenarios + cascade evidence
- Phase 06.9 STATE.md update reflects completion (handled by orchestrator post-plan)
</verification>

<success_criteria>
- All 4 phase requirement IDs verified end-to-end:
  - OR-FIX: S1 PASS + integration TestIntegration_OpenRouterModelRewrite PASS
  - STT-OAI-FIX: S2 PASS + integration TestIntegration_OpenAIWhisperModelRewrite PASS
  - EMBED-OAI-FIX: S3 PASS + integration TestIntegration_OpenAIEmbedModelRewrite PASS
  - SC1-CLOSE: S4 (Phase 03 SC-1) + S5 (Phase 02 step 7) + S6 (Phase 05 SC-1) all PASS
- 3 prior phase deferrals cascade-closed in their VERIFICATION.md files
- Phase 06.9 marked complete; ready for Phase 07 (Observability) to begin
- Net result: gateway returns real tier-1 completions when primary is down; the original gap from Phase 03 Wave 0 Gate A (2026-04-20) is closed
</success_criteria>

<output>
Create `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-06-SUMMARY.md` AND `.planning/phases/06.9-openrouter-model-rewrite-per-upstream/06.9-VERIFICATION.md` when done. Both files reference the operator sign-off evidence in 06.9-HUMAN-UAT.md.

SUMMARY captures: operator sign-off dates per scenario; final spend tally; any tech debt surfaced during UAT (e.g., if S5 cleanup needs a manual workaround); the 3 cascade-close commit SHAs.

VERIFICATION captures: status=passed for all 4 requirement IDs; cross-references to 06.9-HUMAN-UAT.md scenario evidence per ID; cross-references to the 3 cascade-closed VERIFICATION.md files.
</output>
