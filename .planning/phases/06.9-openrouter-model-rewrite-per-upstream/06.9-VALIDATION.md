---
phase: 06.9
slug: openrouter-model-rewrite-per-upstream
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-24
---

# Phase 06.9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Source: 06.9-RESEARCH.md §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go 1.23 `go test` (unit + `-tags=integration` for testcontainers) |
| **Config file** | none (gateway/go.mod root); per-package `*_test.go` |
| **Quick run command** | `cd gateway && go test ./internal/models/... ./internal/proxy/... ./internal/config/... -count=1` |
| **Full suite command** | `cd gateway && go test ./... -count=1 -race -timeout=5m && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=10m` |
| **Live UAT command** | `ssh vps-ifix-vm 'cd /opt/ai-gateway-dev && ./run-06.9-uat.sh'` (operator-side; 6 scenarios from 06.9-HUMAN-UAT.md) |
| **Estimated runtime** | ~30s unit, ~5min integration |

---

## Sampling Rate

- **After every task commit:** Run `{quick run command}` for the package(s) touched
- **After every plan wave:** Run `{full suite command}` (unit + integration)
- **Before `/gsd:verify-work`:** Full suite must be green AND live UAT signed
- **Max feedback latency:** ~30s unit / ~5min integration

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD-01 | 06.9-01 | 0 | OR-FIX | T-06.9-01 (model-name leak via wrong upstream) | Forwarded body.model = upstream's registered slug, NOT client's alias | Unit | `cd gateway && go test ./internal/models/... -run TestResolver_PerUpstreamLookup -count=1` | TBD | pending |
| TBD-02 | 06.9-02 | 1 | OR-FIX | T-06.9-02 (URL convention drift) | Boot fails fast if UPSTREAM_*_URL ends in /v1 | Unit | `cd gateway && go test ./internal/config/... -run TestLoad_RejectsTier1URLEndingInV1 -count=1` | TBD | pending |
| TBD-03 | 06.9-03 | 2 | OR-FIX | T-06.9-03 (director-level model wrong on route swap) | openrouter_director rewrites body.model from resolved-upstream alias | Unit | `cd gateway && go test ./internal/proxy/... -run TestOpenRouterDirector_RewritesModelFromResolver -count=1` | TBD | pending |
| TBD-04 | 06.9-04 | 2 | STT-OAI-FIX | T-06.9-04 (multipart audio body corrupted) | openai_whisper_director rewrites model field in multipart form data without breaking audio bytes | Unit | `cd gateway && go test ./internal/proxy/... -run TestOpenAIWhisperDirector_RewritesModelInMultipart -count=1` | TBD | pending |
| TBD-05 | 06.9-05 | 2 | EMBED-OAI-FIX | T-06.9-05 (embed model mismatch under refactor) | openai_embed_director sources model from resolver (not hard-coded) | Unit | `cd gateway && go test ./internal/proxy/... -run TestOpenAIEmbedDirector_RewritesModelFromResolver -count=1` | TBD | pending |
| TBD-06 | 06.9-06 | 3 | OR-FIX | T-06.9-06 (integration mock false-positive) | upstreamMock captures forwarded body + asserts model slug per upstream | Integration | `cd gateway && go test -tags=integration ./internal/integration_test/... -run TestIntegration_OpenRouterModelRewrite -count=1 -timeout=2m` | TBD | pending |
| TBD-07 | 06.9-07 | 4 | SC1-CLOSE | T-06.9-07 (live UAT regression) | 6 live scenarios PASS on ai-gateway-dev: S1 chat OR / S2 whisper OAI / S3 embed OAI / S4 Phase 03 SC-1 cascade / S5 Phase 02 step 7 / S6 Phase 05 SC-1 vegeta | Live UAT | operator runs 06.9-HUMAN-UAT.md scenarios; signs PASS/FAIL per scenario | TBD | pending |

> Task IDs are placeholders (TBD-NN) — planner replaces with real `{phase-plan}-{task}` IDs (e.g., `06.9-01-T1`) when PLAN.md files are produced.

---

## Coverage Dimensions (Nyquist 8-D matrix)

| # | Dimension | How Phase 06.9 Validates |
|---|-----------|---------------------------|
| 1 | Functional correctness | TBD-01..05 unit tests assert model rewriting per resolved-upstream + multipart audio preservation |
| 2 | Boundary conditions | TBD-02 fail-fast on `UPSTREAM_*_URL` ending in `/v1`; TBD-04 multipart with 0-byte audio + 100MB audio |
| 3 | Error paths | Resolver miss → fall-back to alias-as-target (preserve current behavior); director parse error → forward body unchanged (existing pattern) |
| 4 | Concurrency / race | Resolver cache refresh under load (LISTEN/NOTIFY hot-reload not in scope per RESEARCH O.Q. #3); breaker race during container recreate covered in TBD-06 integration test |
| 5 | Security | T-06.9-01 prevents model-name leak to wrong upstream (could route sensitive tenant audio to wrong provider); SECURITY.md gate runs after Wave 3 |
| 6 | Observability | Audit log records upstream + final-resolved model (not client alias); Prometheus counter gateway_model_rewrite_total{upstream,from,to} (new) |
| 7 | Documentation | 06.9-09-PLAN updates 03-WAVE0-GATES.md (URL convention + model env retirement); PROJECT.md tier-1 stack confirmation; cascade VERIFICATION.md updates on Phase 02/03/05 |
| 8 | Validation strategy | THIS FILE — covers each task with type + command + assertion target |

---

## Live UAT Acceptance (operator-side)

6 scenarios from 06.9-HUMAN-UAT.md must sign PASS to close phase:

- **S1 — Chat tier-1 (OpenRouter)**: Force `local-llm` breaker OPEN. `POST /v1/chat/completions {"model":"qwen"}` → HTTP 200 + real Qwen completion (provider Novita preferred, DeepInfra acceptable).
- **S2 — STT tier-1 (OpenAI Whisper)**: Force `local-stt` breaker OPEN. `POST /v1/audio/transcriptions` with WAV + `model=whisper` → HTTP 200 + real Whisper transcription (requires OpenAI API key in `/opt/ai-gateway-dev/.env`).
- **S3 — Embed tier-1 (OpenAI)**: Force `local-embed` breaker OPEN. `POST /v1/embeddings {"model":"bge-m3","input":["test"]}` → HTTP 200 + real OpenAI text-embedding-3-small.
- **S4 — Phase 03 SC-1 cascade**: Re-run `03-HUMAN-UAT.md` SC-1 scenario, mark PASS.
- **S5 — Phase 02 SC-5 step 7 cascade**: Re-run `02-UAT-2026-05-23.md` step 7, mark PASS.
- **S6 — Phase 05 SC-1 cascade**: Re-run vegeta burst from `05-UAT-2026-05-13.md`, assert tier-1 200s instead of 502s.

All 3 cascade closures happen automatically when S1-S3 PASS (the underlying bugs are identical).

---

## Wave 0 Sign-off

Wave 0 (Wave 0 Gate A revision + URL convention enforcement) is a PRE-WAVE-1 gate. Wave 0 complete = `wave_0_complete: true` after:

- `03-WAVE0-GATES.md` updated with corrected URL convention (`/api` not `/api/v1`)
- `UPSTREAM_LLM_OPENROUTER_MODEL` env var marked deprecated (kept as override per D-06)
- Operator confirms OpenAI API key path (decision: mint new key vs reuse existing) — pre-req for S2/S3 live UAT
