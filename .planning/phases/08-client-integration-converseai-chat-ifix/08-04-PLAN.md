---
phase: 08-client-integration-converseai-chat-ifix
plan: 04
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md
  - .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md
autonomous: false
requirements: [INT-01, INT-02]
must_haves:
  truths:
    - "An operator has a documented rollback procedure with a per-app env-var diff + redeploy command + verify step, drilled to under 5 minutes"
    - "An operator has a live-verification scenario sheet covering SC1 (converseai smoke), SC2 (chat-ifix transcription +/-10%), SC3 (rollback drill), SC4 (dashboard per-tenant)"
    - "The runbook documents the env-var contract: base_url -> gateway, api_key -> tenant key, for both converseai-v4 consumers + the campanhas-chatifix backend"
    - "The autonomous Phase 8 artifacts (08-01..08-03) ship the gpu-ifix-side scripts + fixtures and are NOT blocked by this plan or the un-deployed gateway"
  artifacts:
    - path: "gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md"
      provides: "operator runbook for the converseai + chat-ifix gateway integration, with the load-bearing <5-min rollback procedure"
      contains: "ROLLBACK"
    - path: ".planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md"
      provides: "live-verification scenario sheet + sign-off table for SC1/SC2/SC3/SC4"
      contains: "Sign-off"
  key_links:
    - from: ".planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md"
      to: "scripts/integration-smoke/provision-tenants.sh + smoke-converseai.py + smoke-chat-ifix.py"
      via: "the UAT scenarios run the seed script + both smoke scripts against the deployed gateway"
      pattern: "provision-tenants|smoke-converseai|smoke-chat-ifix"
    - from: "gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md"
      to: "converseai-v4 + campanhas-chatifix client repos"
      via: "the rollback procedure documents the per-app env-var revert + redeploy"
      pattern: "base_url|BASE_URL|Portainer"
---

<objective>
The phase-closing HUMAN-UAT plan — the live-credential, live-traffic verification that cannot run autonomously, mirroring 03-08 / 04-09 / 06-11 / 07-09. The autonomous Phase 8 plans (08-01..08-03) ship only the gpu-ifix-side artifacts — the idempotent tenant seed script, both smoke scripts, the WhatsApp audio fixture + baseline — and stay green; they are NOT blocked. This plan writes the operator runbook (with the load-bearing <5-min rollback procedure) and the HUMAN-UAT scenario sheet, then gates the live integration behind a blocking human-verify checkpoint.

Note the additional gate: per 08-CONTEXT.md `## Deferred Ideas`, the gateway itself is not deployed yet — build-gateway is currently blocked on Phase 6 emergency-pod integration tests (a separate debug session). So the live UAT is doubly deferred: it needs both the gateway deployed AND the operator-run env-var switch in each client repo.

Purpose: close Phase 8 with an operator-executed evidence sheet + a runbook — the same pattern every prior phase used for its live-UAT deferral.
Output: `gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md`, `.planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-client-integration-converseai-chat-ifix/08-CONTEXT.md
@.planning/phases/08-client-integration-converseai-chat-ifix/08-PATTERNS.md
@.planning/phases/08-client-integration-converseai-chat-ifix/08-01-SUMMARY.md
@.planning/phases/08-client-integration-converseai-chat-ifix/08-02-SUMMARY.md
@.planning/phases/08-client-integration-converseai-chat-ifix/08-03-SUMMARY.md

<interfaces>
Runbook structure analog — gateway/docs/RUNBOOK-FAILOVER.md (read it; the section skeleton to mirror):
  Title + "Read this when:" trigger bullets
  ## Mental Model (30 seconds) — compact diagram + moving parts
  ## Quick Diagnosis (~2 minutes) — ordered curl / psql / docker exec commands
  ## Incident Response by Symptom — `### Symptom N` blocks (Likely cause / Diagnose / Mitigate / Recovery)
  the numbered "To rotate a bearer secret" procedure at lines 376-384 — THE shape to mirror for the rollback procedure
  ## Required Env Vars (Portainer stack) — the `| Var | Required | Purpose |` table at lines 387-413
  ## Escalation — lines 483-488
  ## Related Docs — lines 491-499
Also: gateway/docs/RUNBOOK-EMERGENCY-POD.md (referenced as structural template by every prior phase's HUMAN-UAT plan).
> Location decision (from 08-PATTERNS.md): write to `gateway/docs/` — the dominant convention (4 of 5 runbooks live there).

HUMAN-UAT plan analog — .planning/phases/07-observability-dashboard-alerting/07-09-PLAN.md (the plan structure: objective explaining the autonomous-build-not-blocked framing, two `type="auto"` write tasks + one `type="checkpoint:human-verify" gate="blocking"` task).
Produced-artifact analog — .planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-HUMAN-UAT.md (the `08-HUMAN-UAT.md` shape: YAML frontmatter with `status`/`operator`/`date_executed`/`final_status: pending`, `## Prerequisites` checkbox list, numbered `### N. UAT-N — <name> (SC-N)` blocks with Pre-conditions/Steps/Expected/pass-fail, a Sign-off table, the `passed_partial` fallback note).

Phase 8 SC (from ROADMAP Phase 8):
  SC1: ConverseAI v4 (api Elysia + agents Python) points to gateway via env-var changes only; chat / streaming / tool calls / embeddings work in staging, validated against dev traffic
  SC2: Chat Ifix transcribes real WhatsApp audios via gateway Whisper; quality + latency within +/-10% of the prior direct integration
  SC3: a documented rollback plan (revert env vars, redeploy) is tested end-to-end and measured at <5 minutes from decision to fully-rolled-back
  SC4: dashboard shows both apps' traffic as separate tenants with independent latency + cost panels

Deploy facts (from CLAUDE.md `## Dev Environment`):
  converseai-v4 -> Portainer stack `converseai-v4-dev` + GitHub webhook on `develop`; env vars set via Portainer stack UI, never committed
  campanhas-chatifix -> its own deploy flow (Bun monorepo backend — the Chat Ifix external backend)
  the two client repos are SIBLINGS, not part of gpu-ifix — they are NOT edited by Phase 8 plans; the base_url switch is an operator action

Phase 8 artifacts the UAT consumes (from 08-01/08-02/08-03):
  scripts/integration-smoke/provision-tenants.sh   — seed the `converseai` + `chat-ifix` tenants, mint keys with `--mint-keys`
  scripts/integration-smoke/smoke-converseai.py    — INT-01 chat/streaming/tool-call/embedding smoke
  scripts/integration-smoke/smoke-chat-ifix.py     — INT-02 transcription smoke with +/-10% gates
  scripts/integration-smoke/fixtures/whatsapp-sample.ogg + .baseline.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md</name>
  <read_first>
    - gateway/docs/RUNBOOK-FAILOVER.md (the full section skeleton — Read-this-when -> Mental Model -> Quick Diagnosis -> Symptom blocks -> the numbered bearer-rotation procedure at lines 376-384 -> Required Env Vars table -> Escalation -> Related Docs)
    - gateway/docs/RUNBOOK-EMERGENCY-POD.md (the prior-phase runbook structure template)
    - .planning/phases/08-client-integration-converseai-chat-ifix/08-CONTEXT.md (the env-var contract, the two-consumers-one-tenant model, the rollback-must-be-drilled requirement, the deploy flow per app)
    - .planning/phases/08-client-integration-converseai-chat-ifix/08-01-SUMMARY.md + 08-02-SUMMARY.md + 08-03-SUMMARY.md (what the seed script + smoke scripts actually do — the runbook cross-links them)
    - .planning/phases/08-client-integration-converseai-chat-ifix/08-PATTERNS.md (the "RUNBOOK section skeleton" Shared Pattern + the location decision: gateway/docs/)
  </read_first>
  <action>
    Create `gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md` mirroring the `RUNBOOK-FAILOVER.md` structure:

    (1) **Title + "Read this when:"** — trigger bullets: a client app is being pointed at the gateway, a client app integration is misbehaving, the integration needs to be rolled back.

    (2) **## Mental Model (30 seconds)** — the env-var contract: `base_url` -> `https://gateway.ifix.com.br/v1` (or the dev gateway URL), `api_key` -> the per-tenant key. State that converseai-v4 has TWO consumers — `apps/api` (Elysia, OpenAI SDK) and `agents/` (Python, LangChain) — both on the ONE `converseai` tenant; chat-ifix's transcription backend lives in the `campanhas-chatifix` sibling repo on the `chat-ifix` tenant. A compact diagram of client app -> gateway -> upstream.

    (3) **## Quick Diagnosis (~2 minutes)** — ordered commands: `curl` the gateway `/v1/chat/completions` and `/v1/audio/transcriptions` with each tenant key (reference the smoke scripts as the fuller check); `psql` the gateway `audit_log` to confirm rows appear per tenant slug; check the Phase 7 dashboard tenant-table shows `converseai` + `chat-ifix`.

    (4) **## Incident Response by Symptom** — `### Symptom N` blocks (Likely cause / Diagnose / Mitigate / Recovery) for: "client app gets 401 from the gateway" (wrong/unset api key), "client app gets 429" (tenant quota/rate-limit — Phase 4), "streaming responses arrive non-incrementally" (FlushInterval / proxy buffering), "transcription latency or quality regressed" (point at `smoke-chat-ifix.py` and the +/-10% gates).

    (5) **## ROLLBACK procedure** — THE load-bearing section (SC3, <5 min). Mirror the numbered `RUNBOOK-FAILOVER.md` lines 376-384 shape. One numbered procedure per app:
        **To roll back ConverseAI v4:** 1. In Portainer stack `converseai-v4-dev`, revert env vars — `OPENAI_BASE_URL` / `ANTHROPIC_BASE_URL` -> the direct provider URL, `OPENAI_API_KEY` -> the direct provider key, and the `agents/` LangChain base_url env var -> direct (name the exact env vars by inspecting what converseai-v4 actually reads — if the executor cannot confirm the exact names from the sibling repo, use the documented OpenAI-SDK-standard names and add a `> CONFIRM:` note that the operator must verify them against the converseai-v4 repo). 2. Redeploy: the exact Portainer redeploy / webhook trigger. 3. Verify: an exact `curl` / health check confirming traffic no longer flows through the gateway.
        **To roll back Chat Ifix:** the same 3-step shape, via the `campanhas-chatifix` deploy flow.
        State the <5-min budget explicitly and that the procedure MUST be drilled in the HUMAN-UAT, not just written.

    (6) **## Required Env Vars** — a `| Var | App | Required | Gateway value | Direct value | Purpose |` table covering the per-app `*_BASE_URL` / `*_API_KEY` vars and both their gateway-vs-direct values.

    (7) **## Escalation** — 1st responder / escalation contact / per-severity comms (mirror RUNBOOK-FAILOVER.md lines 483-488).

    (8) **## Related Docs** — cross-link `.planning/phases/08-client-integration-converseai-chat-ifix/08-CONTEXT.md`, `scripts/integration-smoke/provision-tenants.sh`, `scripts/integration-smoke/smoke-converseai.py`, `scripts/integration-smoke/smoke-chat-ifix.py`, and `08-HUMAN-UAT.md`.
  </action>
  <verify>
    <automated>test -f gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md && grep -qi 'ROLLBACK' gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md && grep -qi '5 min\|5-min\|five min' gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md && grep -qi 'converseai-v4-dev' gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md && grep -qi 'campanhas-chatifix' gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md && grep -qiE 'Required Env Vars|BASE_URL' gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md` exists and follows the `RUNBOOK-FAILOVER.md` section skeleton (Read-this-when -> Mental Model -> Quick Diagnosis -> Symptom blocks -> ROLLBACK -> Required Env Vars -> Escalation -> Related Docs)
    - The ROLLBACK section has one numbered procedure per app (ConverseAI v4 via Portainer stack `converseai-v4-dev`; Chat Ifix via the `campanhas-chatifix` deploy flow), each with env-var diff + redeploy command + verify step, and states the <5-min budget + the must-be-drilled requirement
    - The Mental Model documents the env-var contract and the two-consumers-one-`converseai`-tenant model + the `chat-ifix` tenant on the `campanhas-chatifix` backend
    - There is a Required Env Vars table with per-app `*_BASE_URL` / `*_API_KEY` vars and their gateway-vs-direct values
    - The runbook cross-links the seed script + both smoke scripts + `08-HUMAN-UAT.md` in Related Docs
  </acceptance_criteria>
  <done>The client-integration runbook exists in `gateway/docs/`, mirrors the prior runbook structure, and its load-bearing ROLLBACK section gives a per-app numbered env-var-revert + redeploy + verify procedure within a stated <5-min budget.</done>
</task>

<task type="auto">
  <name>Task 2: Write 08-HUMAN-UAT.md scenario sheet + sign-off table</name>
  <read_first>
    - .planning/phases/06-auto-provisioning-emergency-pod-vast-ai/06-HUMAN-UAT.md (the closest produced-artifact analog — frontmatter, Prerequisites checkbox list, numbered UAT blocks, Sign-off table, Final Sign-off section)
    - .planning/phases/07-observability-dashboard-alerting/07-09-PLAN.md (the `passed_partial` fallback framing — credentials/deploy unavailable -> passed_partial, autonomous build still green)
    - .planning/phases/08-client-integration-converseai-chat-ifix/08-CONTEXT.md (the SC1-SC4 scope, the gpu-ifix-side-vs-operator-action boundary, the gateway-not-deployed gate)
    - .planning/phases/08-client-integration-converseai-chat-ifix/08-01-SUMMARY.md + 08-02-SUMMARY.md + 08-03-SUMMARY.md (the exact invocation of the seed script + both smoke scripts the UAT steps call)
    - gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md (Task 1's runbook — the UAT rollback drill executes its ROLLBACK procedure)
  </read_first>
  <action>
    Create `.planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md` mirroring `06-HUMAN-UAT.md`.

    YAML frontmatter: `status: pending`, `phase: 08-client-integration-converseai-chat-ifix`, `source: [08-CONTEXT.md, 08-04-PLAN.md Task 2, ROADMAP Phase 8 SC1-SC4]`, `started: ___`, `updated: ___`, `operator: ___________`, `date_executed: ___________`, `final_status: pending  # pass | partial | fail`.

    `## Prerequisites` — checkbox list: gateway deployed to the `ai-gateway-dev` Portainer stack (note this is currently blocked on Phase 6 emerg integration tests — a separate debug session); the two tenants provisioned via `scripts/integration-smoke/provision-tenants.sh --mint-keys` and the three raw keys captured; the `converseai` tenant key set as the gateway env var in the `converseai-v4-dev` Portainer stack + the `agents/` LangChain base_url switched; the `chat-ifix` tenant key + base_url set in the `campanhas-chatifix` deploy config; Python deps installed from `scripts/integration-smoke/requirements.txt`.

    `## Tests` — numbered `### N. UAT-N — <name> (SC-N)` blocks, each with **Pre-conditions / Steps (fenced bash) / Expected / pass-fail**:
      - **UAT-1 — ConverseAI env-var switch + smoke (SC1):** switch the converseai-v4 `apps/api` + `agents/` base_url + api_key env vars to the gateway, redeploy via Portainer; run `python scripts/integration-smoke/smoke-converseai.py --gateway-url <dev gateway> --api-key <converseai key> --out /tmp/converseai-report.json`. Expected: exit code 0, the report's `gates.all_passed` is `true` (chat / streaming / tool-call / embeddings all pass).
      - **UAT-2 — Chat Ifix transcription smoke +/-10% (SC2):** switch the campanhas-chatifix backend base_url + key to the gateway; run `python scripts/integration-smoke/smoke-chat-ifix.py --gateway-url <dev gateway> --api-key <chat-ifix key> --out /tmp/chat-ifix-report.json`. Expected: exit 0, `gates.all_passed` true — transcription quality (WER <= 0.10) AND latency (ratio <= 1.10) both within +/-10% of the committed baseline. Note: if `whatsapp-sample.baseline.json` `baseline_latency_s` was a conservative estimate, re-measure it against the real direct integration first and update the baseline.
      - **UAT-3 — Rollback drill, timed <5 min (SC3):** start a stopwatch; execute the `gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md` ROLLBACK procedure for BOTH apps (revert env vars, redeploy, verify); stop the stopwatch. Expected: both apps fully rolled back to direct providers, verified, in under 5 minutes; record the measured time.
      - **UAT-4 — Dashboard per-tenant cross-check (SC4):** open the Phase 7 dashboard; confirm `converseai` and `chat-ifix` appear as separate tenant rows with independent latency (P50/P95/P99) + cost panels populated from the UAT-1/UAT-2 traffic. This is verification of existing Phase 7 code, not new code.

    A **Sign-off table** — one row per UAT scenario with Result / Date / Operator / Notes columns, plus an overall phase-status line (`passed` / `passed_partial` / `human_needed`).

    A `passed_partial` fallback note — if the gateway is not deployed or credentials are unavailable, mark the affected scenarios `passed_partial`; the autonomous Phase 8 build (08-01..08-03) is already green and is NOT blocked. If a real defect is found (not a missing credential/deploy), describe it precisely so a `/gsd-plan-phase --gaps` pass can close it.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md && grep -qi 'Sign-off' .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md && grep -qi 'Prerequisites' .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md && grep -qi 'passed_partial' .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md && [ "$(grep -c 'UAT-' .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md)" -ge 4 ] && grep -q 'smoke-converseai' .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md && grep -q 'smoke-chat-ifix' .planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md` exists with YAML frontmatter (`status`, `operator`, `date_executed`, `final_status: pending`) and a `## Prerequisites` checkbox list including provisioning + the gateway-not-deployed-yet gate
    - It has at least 4 numbered UAT scenarios mapped to SC1/SC2/SC3/SC4, each with Pre-conditions / Steps (fenced bash) / Expected / pass-fail
    - UAT-1 + UAT-2 invoke `smoke-converseai.py` + `smoke-chat-ifix.py` with the tenant keys; UAT-3 drills the runbook ROLLBACK procedure and times it; UAT-4 cross-checks the Phase 7 dashboard
    - It has a Sign-off table (Result / Date / Operator / Notes) + an overall phase-status line
    - It documents the `passed_partial` path for an un-deployed gateway / unavailable credentials (autonomous build not blocked)
  </acceptance_criteria>
  <done>The HUMAN-UAT scenario sheet exists, mirrors 06-HUMAN-UAT.md, covers SC1-SC4 (converseai smoke / chat-ifix transcription / rollback drill / dashboard cross-check), lists the provisioning + deploy prerequisites, and has a sign-off table with the passed_partial fallback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute the live UAT scenarios + record sign-off</name>
  <what-built>
    The Phase 8 gpu-ifix-side artifacts are built and the autonomous plans (08-01..08-03) are green: the idempotent `provision-tenants.sh` seed script (wraps `gatewayctl tenant/key/admin-key create` for the `converseai` + `chat-ifix` tenants), `smoke-converseai.py` (INT-01 chat/streaming/tool-call/embedding smoke against the gateway with the tenant key), `smoke-chat-ifix.py` (INT-02 Whisper transcription smoke with +/-10% latency + WER quality gates vs a committed WhatsApp audio baseline), the committed audio fixture + baseline, and the `gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md` runbook with its <5-min ROLLBACK procedure. This task is the live-credential, live-traffic verification that cannot run autonomously.
  </what-built>
  <how-to-verify>
    1. Complete the **Prerequisites** in `08-HUMAN-UAT.md`: confirm the gateway is deployed to the `ai-gateway-dev` Portainer stack (this is currently blocked on Phase 6 emerg integration tests — a separate debug session; if still blocked, this whole task is `passed_partial`); run `scripts/integration-smoke/provision-tenants.sh --mint-keys` once to seed the two tenants + mint the keys; switch the `converseai-v4` `apps/api` + `agents/` base_url + key env vars in the `converseai-v4-dev` Portainer stack; switch the `campanhas-chatifix` backend base_url + key; install `scripts/integration-smoke/requirements.txt`.
    2. Execute UAT-1..UAT-4 in `08-HUMAN-UAT.md` in order. For each, record Result / Date / Operator / Notes in the sign-off table.
    3. If the gateway is not deployed or a credential is genuinely unavailable, mark the affected scenarios `passed_partial` and note the blocker — the autonomous build (08-01..08-03) is already green; live validation is the deferred-UAT pattern every prior phase used.
    4. Fill the overall phase-status line (`passed` if all live scenarios pass; `passed_partial` if some are deploy/credential-blocked; `human_needed` if a real defect is found).
    5. If a real defect is found (not a missing credential/deploy), describe it precisely so a `/gsd-plan-phase --gaps` pass can close it.
  </how-to-verify>
  <resume-signal>Type "approved" with the sign-off table filled, or describe the defects found for a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> deployed gateway + client-app Portainer stacks | The live UAT switches real client-app env vars and exercises the production-dev gateway deployment with real tenant credentials |
| client apps (converseai-v4, campanhas-chatifix) -> gateway `/v1/*` | Real production workloads cross the gateway auth + tenant-attribution boundary for the first time |
| raw tenant keys -> Portainer stack env vars | The keys minted by `provision-tenants.sh` are pasted into client-app Portainer stacks during the UAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Information Disclosure | the tenant + admin keys exposed in the runbook or the HUMAN-UAT sheet | mitigate | `RUNBOOK-CLIENT-INTEGRATION.md` and `08-HUMAN-UAT.md` reference env var NAMES and the seed-script invocation — never raw key VALUES. The keys are surfaced once by `provision-tenants.sh` stdout (T-08-01) and pasted directly into Portainer stack env vars by the operator; CLAUDE.md deploy pattern keeps stack env vars out of git. Mirrors 07-09-PLAN.md threat T-07-33. |
| T-08-15 | Repudiation | no attributable record of who ran the live integration UAT | mitigate | The `08-HUMAN-UAT.md` sign-off table records Operator + Date per scenario + a `date_executed` + `operator` frontmatter field — the live verification is attributable, mirroring the 06-HUMAN-UAT.md evidence pattern. |
| T-08-16 | Tampering | rolling back leaves a client app in a half-switched state (api on gateway, agents on direct) | mitigate | The runbook ROLLBACK procedure is per-app and per-consumer (it explicitly covers BOTH the converseai-v4 `apps/api` AND the `agents/` LangChain base_url), and UAT-3 drills the full procedure end-to-end with a verify step — a half-switched state is caught by the verify step before the stopwatch stops. |
| T-08-17 | Spoofing | a client app pointed at a wrong/malicious gateway URL | accept | The operator sets the gateway URL in the Portainer stack; TLS validation is on by default in the OpenAI SDK + httpx. A wrong host is caught by UAT-1/UAT-2 smoke failures and the UAT-4 dashboard cross-check (no traffic under the expected tenant). No new mitigation in scope — same posture as prior phases. |
| T-08-18 | Elevation of Privilege | the `converseai` tenant key (data_class normal) being used where a sensitive-class key is required | accept | Both Phase 8 tenants are `data_class: normal` per 08-CONTEXT.md `## Decisions`; the sensitive-class tenants (Telefonia, Cobranças, etc.) are Phase 9, behind LGPD review. No sensitive data flows through the Phase 8 integrations — the data-class boundary is exercised in Phase 9, not here. |
</threat_model>

<verification>
- `gateway/docs/RUNBOOK-CLIENT-INTEGRATION.md` and `.planning/phases/08-client-integration-converseai-chat-ifix/08-HUMAN-UAT.md` both exist and are committed.
- The Task 3 blocking checkpoint produces a filled sign-off table with an overall phase-status line.
- This plan is `autonomous: false` — it cannot reach COMPLETE without operator action, consistent with 03-08 / 04-09 / 06-11 / 07-09.
</verification>

<success_criteria>
- The client-integration runbook exists with a per-app <5-min ROLLBACK procedure.
- The HUMAN-UAT scenario sheet covers SC1 (converseai smoke), SC2 (chat-ifix transcription within +/-10%), SC3 (rollback drill timed <5 min), SC4 (dashboard per-tenant cross-check).
- An operator executes the scenarios and records a sign-off table with an overall phase-status line; `passed_partial` is the documented path when the gateway is not yet deployed or credentials are unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/08-client-integration-converseai-chat-ifix/08-04-SUMMARY.md` and `.planning/phases/08-client-integration-converseai-chat-ifix/08-VERIFICATION.md`
</output>
