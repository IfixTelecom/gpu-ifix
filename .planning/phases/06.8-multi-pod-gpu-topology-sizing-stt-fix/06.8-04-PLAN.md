---
phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md
autonomous: true
requirements: [LADDER]
must_haves:
  truths:
    - "An operator can read the runbook and know the exact env preset to apply for each shape (A 2×3090 / B 1×5090 / C placeholder) plus the force-down→env-swap→force-up transition steps"
    - "Each shape preset lists PRIMARY_GPU_NAME, PRIMARY_NUM_GPUS, PRIMARY_VAST_PRICE_CAP_DPH, allowlist seed, and approximate cost"
    - "The ladder is documented as a runbook + env presets — NOT a config-driven topology orchestrator (RESEARCH: that would be over-engineering for the single-primary gateway; broaden-search + cooldown already cover intra-shape unavailability)"
    - "Shape C (1pod-LLM + tiny-GPU services) is recorded as a Deferred placeholder dependent on dual-primary routing (also Deferred), not specced"
  artifacts:
    - path: "docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md"
      provides: "Operator runbook + per-shape env presets for the fallback topology ladder"
      contains: "2×RTX 3090"
  key_links:
    - from: "runbook preset table"
      to: "gateway env (PRIMARY_GPU_NAME/NUM_GPUS/PRICE_CAP/ALLOWLIST) + gatewayctl force-up"
      via: "operator swaps env preset then force-down/force-up to transition shapes"
      pattern: "PRIMARY_NUM_GPUS"
---

<objective>
Encode the fallback topology ladder as an operator runbook + per-shape env presets (RESEARCH recommendation: runbook over a config-driven orchestrator — the gateway is single-primary and already has allowlist→broaden + FilterBelowCap + cooldown for intra-shape resilience; iterating shapes is a rare operational decision, not a 1Hz loop). Shape transition is a manual, explicit env swap + force-up.

This is a docs-only plan (no code) and is independent of the STT/gateway work — it runs in Wave 1 in parallel with Plan 01.

Purpose: Give the operator a deterministic procedure to fall back from the preferred 2×3090 shape to a 5090 (and a placeholder for the degraded split-pod shape) when Vast inventory for the preferred shape dries up.
Output: `docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-CONTEXT.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-RESEARCH.md
@docs/RUNBOOK-PRIMARY-POD-TTS.md
@gateway/internal/config/config.go

<interfaces>
<!-- Per-shape preset table (RESEARCH §"Fallback Topology Ladder") + the env knobs they map to. -->

Gateway knobs (config.go) the presets set: PRIMARY_GPU_NAME (default "RTX 5090"),
  PRIMARY_NUM_GPUS (default 1), PRIMARY_VAST_PRICE_CAP_DPH (default 2.20),
  PRIMARY_VAST_MACHINE_ALLOWLIST (CSV machine_ids, default empty).

Preset table:
  Shape A (preferred) 2×RTX 3090 | PRIMARY_GPU_NAME=RTX 3090 | NUM_GPUS=2 | PRICE_CAP=0.60 | allowlist=43803 | ~$0.29-0.40/h
  Shape B (fallback)  1×RTX 5090 | PRIMARY_GPU_NAME=RTX 5090 | NUM_GPUS=1 | PRICE_CAP=2.20 | allowlist=(empty) | ~$0.74-2.00/h
  Shape C (degraded)  1pod-LLM + tiny-GPU services | DEFERRED (depends on dual-primary routing — also Deferred) | — | — | — | —

Transition procedure: change the env preset (Portainer stack UI / gateway .env) →
  `gatewayctl primary force-down` (if a pod is active on the old shape) → `gatewayctl primary force-up`.
  The reconciler's allowlist→broaden + cooldown handle intra-shape host unavailability automatically;
  the runbook covers only the rarer cross-shape switch.

Reference the live-UAT docs as the source of validated values:
  06.8-GW-2GPU-LIVE-UAT.md (allowlist machine_ids), 06.8-STT-LIVE-VALIDATION.md (image digest carrying the STT fix).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the GPU topology fallback runbook</name>
  <files>docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md</files>
  <action>Create docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md mirroring the structure/voice of the existing docs/RUNBOOK-PRIMARY-POD-TTS.md. Sections: (1) Purpose + why runbook-not-orchestrator (cite RESEARCH: single-primary; broaden-search + cooldown already cover intra-shape; a config-driven N-shape FSM is deferred to SEED-002 if/when emergency becomes full hot-standby). (2) Preset table for shapes A/B/C with columns PRIMARY_GPU_NAME, PRIMARY_NUM_GPUS, PRIMARY_VAST_PRICE_CAP_DPH, allowlist seed, approx cost (values from the interfaces block; pull validated allowlist machine_ids from 06.8-GW-2GPU-LIVE-UAT.md if available, else seed 43803). (3) Transition procedure: env swap → gatewayctl primary force-down → force-up; note force-up bypasses cooldown. (4) When to fall back: signal = repeated provisionLifecycle close with no_offers_below_cap despite force-up (preferred shape inventory dry). (5) Shape C explicitly marked DEFERRED — depends on dual-primary routing (Deferred per CONTEXT); placeholder only, not specced. (6) Note the PRIMARY_TEMPLATE_IMAGE must point at the image carrying the STT fix (digest from 06.8-STT-LIVE-VALIDATION.md) regardless of shape. Do NOT propose any config-driven topology-list code (RESEARCH Don't Hand-Roll).</action>
  <verify>
    <automated>test -f docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md && grep -q '2×RTX 3090' docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md && grep -q 'PRIMARY_NUM_GPUS' docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md && grep -Eiq 'deferred' docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md && grep -q 'force-up' docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md</automated>
  </verify>
  <done>Runbook exists with the 3-shape preset table (env knobs + cost), the force-down→swap→force-up transition, the fall-back signal, the runbook-not-orchestrator rationale, and Shape C marked Deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → gateway env | applies presets that drive Vast provisioning cost/shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.8-09 | Tampering | wrong price cap leaves an expensive shape running | mitigate | Runbook documents PRICE_CAP per shape + the MONTHLY_PRIMARY_BUDGET_BRL guard already caps spend; force-down is the explicit kill. |
| T-06.8-SC | Tampering | package installs | accept | Docs-only plan; no installs. |
</threat_model>

<verification>
- docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md exists with the preset table, transition steps, fall-back signal, and Shape C deferred.
- No config-driven orchestrator proposed (runbook + env only).
</verification>

<success_criteria>
- An operator can fall back from 2×3090 to 5090 using only the runbook + an env swap + gatewayctl, with no code change.
- Shape C documented as a deferred placeholder dependent on dual-primary routing.
</success_criteria>

<output>
Create `.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-04-SUMMARY.md` when done.
</output>
