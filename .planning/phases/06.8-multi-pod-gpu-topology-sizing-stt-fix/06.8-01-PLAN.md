---
phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pod/scripts/upload-weights.sh
  - pod/primary/supervisord.conf
autonomous: true
requirements: [STT-FIX]
must_haves:
  truths:
    - "upload-weights.sh produces a whisper tarball whose top-level entry is models--Systran--faster-whisper-large-v3/ (HF-hub-cache layout), NOT flat model.bin/config.json"
    - "The regenerated tarball contains snapshots/<hash>/README.md (model card) — without it speaches 500s MODEL_CARD_DOESNT_EXISTS"
    - "upload-weights.sh supports a CANDIDATE key path (via --weights-version v1.0.0-hf-cache-candidate → whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz) so Plan 02 can validate WITHOUT mutating the canonical whisper-large-v3/v1.0.0/model.tar.gz that other consumers depend on"
    - "supervisord.conf [program:speaches] sets HF_HUB_CACHE=\"/weights/whisper\" (absolute, matching onstart.go tar -C /weights/whisper)"
    - "snapshots/<hash>/ holds real file copies (not symlinks) so tar/untar cannot break the chain (Pitfall 2)"
    - "Each downloaded whisper file is sha256-integrity-checked (per-file) BEFORE bundling the tarball — a truncated/corrupt HF download aborts the script, never enters the artifact"
    - "These changes live in the LOCAL working tree ONLY; they are NOT committed/merged to develop in this plan — the commit is conditional on Plan 02's live validation passing (CLAUDE.md no-blind-commit-to-dev rule; develop auto-deploys via Actions+Portainer)"
  artifacts:
    - path: "pod/scripts/upload-weights.sh"
      provides: "Whisper weights tarball generation in HF-hub-cache layout (fix iii) + candidate-key path + per-file integrity checks"
      contains: "models--Systran--faster-whisper-large-v3"
    - path: "pod/primary/supervisord.conf"
      provides: "HF_HUB_CACHE env for the speaches STT child"
      contains: "HF_HUB_CACHE"
  key_links:
    - from: "upload-weights.sh whisper block"
      to: "onstart.go tar -xzf /weights/whisper/model.tar.gz -C /weights/whisper"
      via: "tarball top-level dir models--…/ extracts so HF_HUB_CACHE=/weights/whisper resolves it"
      pattern: "models--Systran--faster-whisper-large-v3"
    - from: "supervisord.conf [program:speaches] HF_HUB_CACHE"
      to: "speaches hf_utils.get_model_repo_path cache scan"
      via: "env honored by huggingface_hub.constants HF_HUB_CACHE"
      pattern: "HF_HUB_CACHE=\"/weights/whisper\""
---

<objective>
Prepare the STT fix at image/script level (fix iii from RESEARCH): regenerate the whisper weights tarball in HF-hub-cache layout and point the speaches child at it via HF_HUB_CACHE. This is the change-preparation step ONLY — it does NOT validate on a live pod (that is Plan 02, the mandatory gate per CLAUDE.md debugger rule).

**Critical handoff contract (review H1):** This plan produces LOCAL working-tree changes ONLY to `upload-weights.sh` + `supervisord.conf`. It does **NOT** commit or merge to `develop`. Merging to develop auto-deploys via Actions + Portainer, which would be a blind-commit-to-dev — prohibited by CLAUDE.md until the fix is proven live. The commit/merge to develop happens AFTER Plan 02's live-validation checkpoint approves (Plan 02 Task 3). The executor must leave the changes uncommitted on the working tree (or on a local-only validation branch — see Plan 02 M1) and explicitly NOT push to develop here.

Root cause (VERIFIED in pinned speaches v0.9.0-rc.3 source): `routers/stt.py` calls `get_model_card_data_or_raise(model)` BEFORE loading. That gate (`hf_utils.get_model_repo_path`) scans `HF_HUB_CACHE` (default `/root/.cache/huggingface/hub`, which does not exist → `CacheNotFound`) for a `models--Systran--faster-whisper-large-v3/snapshots/<hash>/README.md`. The current tarball extracts files FLAT into `/weights/whisper` (invisible to the scan; no README.md). Fix: tarball in cache layout + `HF_HUB_CACHE=/weights/whisper`.

Purpose: Make `/v1/audio/transcriptions` resolvable on every topology (it has been broken since 06.7 UAT). This blocks STT on the 2×3090 shape too.
Output: Modified `upload-weights.sh` (whisper block, with candidate-key support + per-file integrity checks) + `supervisord.conf` ([program:speaches] env), held LOCALLY. Image rebuild + candidate-tarball upload + live validation + the develop commit all happen in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-RESEARCH.md
@pod/scripts/upload-weights.sh
@pod/primary/supervisord.conf
@gateway/internal/primary/onstart.go

<interfaces>
<!-- Contracts the executor needs — extracted from codebase. No exploration required. -->

onstart.go extracts whisper into /weights/whisper (the HF_HUB_CACHE must be EXACTLY this absolute path):
  tar -xzf /weights/whisper/model.tar.gz -C /weights/whisper

The speaches resolution gate (speaches v0.9.0-rc.3) requires this on-disk layout inside HF_HUB_CACHE:
  models--Systran--faster-whisper-large-v3/
  ├── refs/main                       # content: <commit_hash>
  └── snapshots/<commit_hash>/
      ├── model.bin
      ├── config.json
      ├── tokenizer.json
      ├── preprocessor_config.json
      ├── vocabulary.json             # if present
      └── README.md                   # REQUIRED — model card; 500 without it

upload-weights.sh current whisper block (L80-91): downloads 5 flat files into ${WORKDIR}/whisper
and runs `tar -C "${WHISPER_DIR}" -czf "${WHISPER_TAR}" .` (flat). hf_download(repo,file,dest) helper
already exists (L50-59). upload_with_sidecar(local,key) computes+uploads sha (L61-70).
WEIGHTS_VERSION (L20, default v1.0.0) is the path segment in every MinIO key. Passing
`--weights-version v1.0.0-hf-cache-candidate` makes the whisper key become
`whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz` (the CANDIDATE key) WITHOUT touching the
canonical `whisper-large-v3/v1.0.0/model.tar.gz`. NOTE: Qwen + BGE keys would also pick up the
candidate version segment if --weights-version is changed — see Task 1 for keeping them on canonical.

supervisord.conf [program:speaches] (L35-59) today has NO `environment=` line. [program:chatterbox]
(L77) shows the established `environment=KEY="val",KEY2="val2"` syntax to mirror.

Consumer keys (config.go): PRIMARY_WHISPER_WEIGHTS_KEY + WEIGHTS_WHISPER_KEY both default
`whisper-large-v3/v1.0.0/model.tar.gz` and are env-overridable — Plan 02 overrides them to the
candidate key for the validation pod, then promotes (copies candidate → canonical) post-validation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate whisper tarball in HF-hub-cache layout + per-file integrity checks (upload-weights.sh)</name>
  <read_first>pod/scripts/upload-weights.sh (whisper block L80-91, hf_download L50-59, upload_with_sidecar L61-70, WEIGHTS_VERSION L20); gateway/internal/primary/onstart.go (tar -C /weights/whisper L159)</read_first>
  <files>pod/scripts/upload-weights.sh</files>
  <action>Rewrite the whisper block (currently L80-91). Define WHISPER_REPO="Systran/faster-whisper-large-v3" and WHISPER_HASH. Resolve the REAL HF commit hash of the repo's main branch (`curl -fsSL https://huggingface.co/api/models/Systran/faster-whisper-large-v3 | jq -r .sha`, or `git ls-remote https://huggingface.co/Systran/faster-whisper-large-v3 HEAD`) — per Assumption A1 the real hash is zero-risk; Plan 02 validates in-pod whether an arbitrary hash would also work. Build a stage dir WHISPER_STAGE with `models--Systran--faster-whisper-large-v3/refs/main` (file content = the hash, no trailing newline) and `models--Systran--faster-whisper-large-v3/snapshots/${WHISPER_HASH}/`. Download into that snapshot dir, via the existing hf_download helper, these files: model.bin config.json tokenizer.json vocabulary.json preprocessor_config.json README.md — README.md is MANDATORY (the script does not fetch it today; without it speaches 500s MODEL_CARD_DOESNT_EXISTS per Pitfall 1). Keep real file copies in snapshots/ (NOT symlinks — Pitfall 2). PER-FILE INTEGRITY (Gemini suggestion): after each hf_download into the snapshot dir, assert the file is non-empty and (where HF exposes it) verify against the upstream sha — fetch the expected sha from the HF LFS pointer / `huggingface.co/api/models/.../tree/main` size+oid, OR at minimum assert `[ -s "$dest" ]` (non-zero) AND that a final `sha256sum` over each file is logged; a truncated/corrupt download MUST abort the script (set -e already propagates, but add an explicit `|| { log "corrupt/empty $f"; exit 1; }` guard so a partial file never enters the tarball). Then `tar -C "${WHISPER_STAGE}" -czf "${WHISPER_TAR}" .` so the tarball top-level entry is `models--Systran--faster-whisper-large-v3/` (NOT flat). Reuse upload_with_sidecar to key `whisper-large-v3/${WEIGHTS_VERSION}/model.tar.gz`. CANDIDATE-KEY SUPPORT (review H2): because the key embeds ${WEIGHTS_VERSION}, running the script with `--weights-version v1.0.0-hf-cache-candidate` writes the candidate key `whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz` — this is what Plan 02 uses for live validation, NEVER mutating canonical v1.0.0 before the fix is proven. To keep Qwen/BGE pinned to canonical regardless of the whisper candidate version, gate them so they are SKIPPED (or keep their key segment fixed at v1.0.0) when WEIGHTS_VERSION != v1.0.0 — i.e. a candidate run only (re)produces the whisper artifact. Document this candidate-vs-canonical behavior in a header comment. Leave the faster-whisper pin untouched (UAT 17 sandwich).</action>
  <verify>
    <automated>bash -n pod/scripts/upload-weights.sh && grep -q 'models--Systran--faster-whisper-large-v3/snapshots' pod/scripts/upload-weights.sh && grep -q 'README.md' pod/scripts/upload-weights.sh && grep -Eq 'tar -C "\$\{WHISPER_STAGE\}"' pod/scripts/upload-weights.sh && grep -Eq 'hf-cache-candidate|WEIGHTS_VERSION' pod/scripts/upload-weights.sh && grep -Eq '\[ -s|corrupt|empty' pod/scripts/upload-weights.sh</automated>
  </verify>
  <acceptance_criteria>upload-weights.sh parses clean; whisper block builds the models--…/refs/main + snapshots/<hash>/ layout including README.md and tars from the stage root (not flat); per-file non-empty/integrity guard aborts on a corrupt/truncated download; candidate-key path works via --weights-version v1.0.0-hf-cache-candidate without touching canonical v1.0.0; Qwen/BGE not mutated on a candidate run; faster-whisper pin unchanged.</acceptance_criteria>
  <done>Whisper block emits HF-hub-cache layout with README.md + per-file integrity guard + candidate-key support; canonical Qwen/BGE untouched on candidate runs.</done>
</task>

<task type="auto">
  <name>Task 2: Add HF_HUB_CACHE to the speaches supervisord child</name>
  <read_first>pod/primary/supervisord.conf ([program:speaches] L35-59, comment block L52-54, [program:chatterbox] environment= syntax L77); gateway/internal/primary/onstart.go (tar -C /weights/whisper L159)</read_first>
  <files>pod/primary/supervisord.conf</files>
  <action>In `[program:speaches]`, add a line `environment=HF_HUB_CACHE="/weights/whisper"` (absolute path; must match onstart.go's `tar -xzf … -C /weights/whisper` exactly — Pitfall 4). Mirror the existing `environment=` syntax used by [program:chatterbox] (L77). Keep `directory=/opt/speaches-data` (required — UAT 11). Leave `WHISPER__INFERENCE_DEVICE` ABSENT (the comment block already explains speaches defaults inference_device=auto → picks CUDA; 2×3090 has 48GB headroom). Update the comment block (L52-54) noting that the HF cache is now populated by the pre-shaped tarball at /weights/whisper, superseding the old "populated via POST /v1/models/{id}" note. Do NOT add WHISPER__MODEL_DIR / SPEACHES__MODEL_DIR (UAT 15 confirmed they do not exist). This change is IMAGE-LEVEL → triggers an image rebuild (done in Plan 02). This change stays LOCAL — do NOT commit to develop here (review H1).</action>
  <verify>
    <automated>grep -A2 '\[program:speaches\]' pod/primary/supervisord.conf | grep -q 'HF_HUB_CACHE="/weights/whisper"' && ! grep -q 'WHISPER__MODEL_DIR\|SPEACHES__MODEL_DIR' pod/primary/supervisord.conf</automated>
  </verify>
  <acceptance_criteria>[program:speaches] carries environment=HF_HUB_CACHE="/weights/whisper"; no model-dir vars introduced; comment block updated to reflect the pre-shaped cache; change left uncommitted on the working tree.</acceptance_criteria>
  <done>[program:speaches] carries HF_HUB_CACHE=/weights/whisper; no model-dir vars; comment updated; not committed to develop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF (huggingface.co) → tarball | untrusted upstream model files baked into the weights artifact |
| MinIO → pod onstart | tarball fetched at boot; integrity gated by sha256sum -c |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.8-01 | Tampering | regenerated whisper tarball in MinIO | mitigate | onstart `sha256sum -c` gates the bundle; per-file non-empty/integrity guard (Task 1) blocks a truncated HF download from entering the tarball; the new SHA is propagated in Plan 02 (ordered promotion). Stale SHA fails pod boot loudly (fail-closed). |
| T-06.8-02 | Information disclosure | MinIO creds in upload-weights.sh / onstart | accept | onstart runs `set -euo` without xtrace; upload-weights.sh is operator-local (creds via env, not logged). No new surface added here. |
| T-06.8-10 | Tampering | premature commit to develop auto-deploys an unvalidated fix | mitigate | Plan 01 explicitly holds changes LOCAL (review H1); commit/merge to develop is gated on Plan 02's live checkpoint. |
| T-06.8-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — speaches/faster-whisper/huggingface_hub pins UNCHANGED (RESEARCH Package Legitimacy Audit: no new install; slopcheck N/A). |
</threat_model>

<verification>
- `bash -n pod/scripts/upload-weights.sh` parses.
- Whisper tarball block emits HF-hub-cache layout with README.md + per-file integrity guard.
- Candidate-key path (--weights-version v1.0.0-hf-cache-candidate) supported; canonical v1.0.0 untouched on candidate runs.
- [program:speaches] sets HF_HUB_CACHE=/weights/whisper.
- No change to Qwen/BGE blocks (canonical), faster-whisper pin, or model-dir vars.
- Changes remain on the local working tree — NOT committed to develop (H1).
NOTE: live correctness is NOT proven here — Plan 02 spins a pod and asserts transcription 200, then (only on pass) commits to develop. This plan is the prepared-change only.
</verification>

<success_criteria>
- upload-weights.sh, when run with --weights-version v1.0.0-hf-cache-candidate, produces a tarball at the CANDIDATE key that extracts to `/weights/whisper/models--Systran--faster-whisper-large-v3/snapshots/<hash>/{model.bin,config.json,tokenizer.json,preprocessor_config.json,README.md}`, with per-file integrity guards.
- supervisord.conf gives speaches `HF_HUB_CACHE=/weights/whisper`.
- Both changes are staged LOCALLY but NOT committed/merged to develop — the develop commit is conditional on Plan 02's live validation passing (H1).
</success_criteria>

<output>
Create `.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-01-SUMMARY.md` when done. Record: the WHISPER_REPO commit hash used (Plan 02 needs it for the A1 arbitrary-hash check), the candidate-key invocation (`--weights-version v1.0.0-hf-cache-candidate`), and an explicit note that the changes are UNCOMMITTED (held for Plan 02's post-validation commit).
</output>
