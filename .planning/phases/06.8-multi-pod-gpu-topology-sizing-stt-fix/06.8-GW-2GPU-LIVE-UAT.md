---
plan: 06.8-03
title: Gateway 2x3090 provisioning — live UAT record
status: passed
started: 2026-05-22
re_uat_passed: 2026-05-22
re_uat_plan: 06.8-05
---

# Gateway 2×3090 Provisioning — Live UAT (Plan 06.8-03)

Mirror of the 2026-05-21 manual spike, but driven by the GATEWAY (force-up), not manual create.

## A2 pre-check (Task 1) — RESOLVED POSITIVE, no relaxation needed

Ran the EXACT `DefaultSearchFilter` the gateway builds (types.go L233-244) against Vast `GET /bundles`:
`gpu_name=RTX 3090, num_gpus={eq:2}, reliability≥0.99, dph_total≤0.60, inet_down≥500, cuda_max_good≥12.8, driver_vers≥570000000, rentable=true`.

**Result: 7 offers returned through the cuda/driver gates** (broaden pass). Allowlist-first pass (`machine_id in [43803]`) returns **1** offer. Machine 43803 is present in BOTH.

| pass | offers | cheapest | 43803? |
|------|--------|----------|--------|
| broaden (default filter) | 7 | $0.248/h (mach 33927, Quebec, cuda 12.8, driver 570.86) | yes (id 36299652, $0.401) |
| allowlist (43803) | 1 | $0.401/h (id 36299650, cuda 13, driver 580.95) | yes |

Broaden-pass offers (all modern driver 570–590, cuda 12.8–13.1):
- 33103546 $0.248 mach=33927 cuda=12.8 driver=570.86.16 Quebec CA
- 37228023 $0.322 mach=55942 cuda=13 driver=580.82.09 Michigan US
- 36299652 $0.401 mach=43803 cuda=13 driver=580.95.05 Estonia EE  ← spike-proven
- 37139362 $0.430 mach=52359 cuda=12.8 driver=570.195 Quebec CA
- 36992899 $0.454 mach=68083 cuda=12.8 driver=570.133 CN
- 31139421 $0.482 mach=55158 cuda=13.1 driver=590.48 Germany DE
- 36119398 $0.509 mach=37943 cuda=12.8 driver=570.133 Romania RO

**Gate-isolation (drop one at a time):** drop_cuda=7, drop_driver=7, drop_rel=17, drop_dph=8.
The `cuda_max_good≥12.8` and `driver_vers≥570000000` gates are NOT the bottleneck (count stays 7 with them in place). `reliability≥0.99` is the most restrictive (17 without it) but still leaves 7 with it.

**Conclusion (A2):** The gateway's filtered 2×3090 search returns ample qualifying offers AND machine 43803 passes. The pre-authorized one-line gate relaxation is **NOT required**. No gateway code change will be made.

(Note: an earlier ad-hoc query returned 0 only because it added a `disk_space≥65` gate the gateway filter does NOT use — the gateway filter itself is healthy.)

## Live-UAT (checkpoint) — PARTIAL 2026-05-22

Dev gateway `ai-gateway-dev` env set to shape A (PRIMARY_NUM_GPUS=2, PRIMARY_GPU_NAME="RTX 3090", PRIMARY_VAST_PRICE_CAP_DPH=0.60, PRIMARY_TEMPLATE_IMAGE=…:develop carrying the Plan-02 STT fix, PRIMARY_WHISPER_WEIGHTS_SHA256=d8e8f6e…). Required adding 3 env passthrough lines to `/opt/ai-gateway-dev/docker-compose.yml` (PRIMARY_NUM_GPUS / PRIMARY_GPU_NAME / PRIMARY_VAST_MACHINE_ALLOWLIST were absent). Multiple `gatewayctl primary force-up` cycles run.

### PROVEN (gateway 2×3090 path mechanics)
| step | result |
|------|--------|
| force-up → offer picked | ✅ every cycle: RTX 3090, num_gpus=2, dph $0.16–0.28 ≤ cap 0.60 |
| Vast instance create + run | ✅ e.g. instance 37312454 (mach 76546) `cur=running`, image `:develop` |
| 4 service ports mapped | ✅ 8000→59831, 8001→59940, 8003→59892, 9400→59847, ssh 22→59854 |
| broken-CDI detection → auto-destroy | ✅ gateway caught Vast `inject CDI devices gpu=0` error, destroyed + closed lifecycle (mach 55942, 45778) |
| cost guard (L2) | ✅ MONTHLY_PRIMARY_BUDGET_BRL=2400 active; every pick ≤$0.28 and RTX 3090 — broaden never picked a non-3090 shape above the 0.60 cap |

### NOT achieved — markReady + 4 services 200 (two blockers, both real findings)

**Blocker 1 — `PRIMARY_VAST_MACHINE_ALLOWLIST` does NOT steer offer selection (CONFIRMED gateway bug).**
Tested progressively: allowlist=[43803,45778,55158,37943] → gateway picked 76546 (California, not in list); allowlist=[43803] alone (43803 confirmed available, $0.401, rel 0.995) → gateway picked 47525 (Quebec, not in list). The reconciler's allowlist-first PREFERENCE pass (`WithMachineAllowlist`, config.go L196 / types.go L258) is not effective at runtime — selection always broadens to the global-cheapest qualifying offer regardless of the allowlist env. This blocks deterministically landing the one known-good-CDI host (43803).

**Blocker 2 — broken CDI is widespread on 2×3090 hosts.**
`failed to inject CDI devices … gpu=0: unknown` (the catalogued multi-GPU CDI failure) hit machine **55942** (Michigan) and **45778** (Czech). Only machine **43803** (2026-05-21 spike) is known-good CDI. With Blocker 1 preventing allowlist targeting, broaden lands on broken-CDI hosts (fail in ~5min) or good-CDI-but-far US hosts with ~50min transatlantic MinIO-DE cold-start (76546 California: Qwen DL 5.1 MiB/s, ETA 50min, exceeds the 2400s budget).

Net: the gateway provisions a 2×3090 end-to-end (offer→create→run→ports), but cannot reliably reach markReady because it can't be steered to the known-good host. **STT-200 on the 2×3090 topology was therefore NOT re-confirmed via the gateway this session** (it WAS proven on the same `:develop` image + machine 43803 via MANUAL create in Plan 02 — see 06.8-STT-LIVE-VALIDATION.md).

### A2 relaxation status: NONE (not needed — the search returned 7 offers).

### Cost guard + allowlist catalog + SEED-002 (Task 2)
- **Cost guard verified:** budget guard active (2400 BRL); price cap 0.60 held — no pick exceeded $0.28, all RTX 3090, broaden never escalated to a non-3090 shape.
- **Broken-CDI catalog (blocklist):** machine_ids **55942, 45778** added to `PRIMARY_VAST_MACHINE_BLOCKLIST` on the dev gateway (persisted). Known-good: **43803** (would go in allowlist once Blocker 1 is fixed).
- **SEED-002 input:** validated shape = **2×RTX 3090, 48GB** (llama tensor-split Qwen ~8.5/9.4GB, full stack ~27GB/48GB — proven in the Plan-02 manual-create pod on 43803). This is the GPU shape the SEED-002 emergency hot-standby should mirror.

### NO gateway code changed.
Per plan scope, no source edits were made (the A2 relaxation was not needed). The allowlist-steering bug (Blocker 1) is a separate code fix, filed as a follow-up — out of this UAT plan's "no code change" scope.

### Dev gateway state after UAT (L3 revert)
Reverted `.env` to pre-UAT (PRIMARY_NUM_GPUS=1, PRIMARY_GPU_NAME=RTX 5090, allowlist empty, price cap 1.00) — shape A not fully validated. Preserved: broken-CDI blocklist (55942,45778), the Plan-02 whisper SHA (d8e8f6e…), and the 3 new compose env-passthrough lines (harmless; ready for when Blocker 1 is fixed). Gateway healthy. No Vast instance left running.

---

## Re-UAT 2026-05-22 (Plan 06.8-05) — PASS

The two blockers from the 2026-05-22 PARTIAL section above have been closed. Phase 06.8 verification gap is closed.

### Blocker 1 — RESOLVED (was deploy staleness, NOT a source bug)

Root cause documented in `06.8-ALLOWLIST-DIAGNOSIS.md` (commit `37a91cb`): the `ai-gateway-dev` container at the time of the 06.8-03 PARTIAL was running an image **built 2026-05-21T21:13:54Z**, ~2h20m before commit `6f57698` (`feat(gateway): primary allowlist-first pass + WithMachineAllowlist`, 2026-05-21T23:33Z). The deployed binary literally lacked the allowlist symbols (`strings | grep` returned 0 hits for `PRIMARY_VAST_MACHINE_ALLOWLIST` / `WithMachineAllowlist` / `PrimaryVastMachineAllowlist` / `primary allowlist exhausted`). The Portainer webhook fired but the local Docker daemon retained the previous digest (the documented STATE.md 2026-05-19 redeploy gotcha). Source on `develop` is correct; H1/H2 (wire encoding / FilterBelowCap) DISPROVEN. The 06.8-03 evidence "selection always broadens regardless of allowlist env" matches a binary with NO allowlist branch at all — broaden was the only pass that ran.

**Hardening landed (commit `d689321`):** `gateway/internal/emerg/vast/types_test.go` — new sub-test `TestWithMachineAllowlist/composition_DefaultSearchFilter_plus_WithMachineAllowlist` asserts the exact filter the reconciler sends (`DefaultSearchFilter(0.60, 0, "RTX 3090", 2, 55942, 45778)` ∘ `WithMachineAllowlist([43803])`) marshals `machine_id:{"in":[43803]}` and preserves every other DefaultSearchFilter field. This is a CI guard against future refactors stripping the allowlist branch unnoticed. `go build ./... && go vet ./... && go test ./...` pass.

### Blocker 2 — addressed (broken-CDI catalog grew; new known-good host found)

`PRIMARY_VAST_MACHINE_BLOCKLIST=55942,45778` already excluded the two confirmed broken-CDI hosts. **New known-good 2×3090 host catalogued: machine 55158 (Germany, host 167329, cuda 13.1, driver 590.48, reliab 0.998, inet_down 939 Mbps, $0.482/h).** Provisioned end-to-end through the gateway with no CDI failures; 4-endpoint health-poll reached markReady. Add 55158 to the standing allowlist alongside 43803.

### Re-UAT timeline (2026-05-22, UTC times from logs)

| t (UTC) | event |
|---|---|
| 16:39:20 | CI run 26299649490 success, image `:latest-dev` digest `sha256:1890ecdc7dee6a269f27272c01f74c9f9777ea40760dfb294650ae0c27a09642` pushed to GHCR |
| ~17:00 | force-pull on `vps-ifix-vm` + `docker compose up -d --force-recreate gateway` (Portainer-webhook-gotcha re-occurred; manual recreate required). Deployed binary now contains all 6 allowlist symbols (`strings` count=1 each). |
| 17:13 | first force-up cycle with `PRIMARY_VAST_MACHINE_ALLOWLIST=43803` → allowlist returned 0 offers (43803 transient `rentable:false` in the Vast spot market) → broaden picked 52361 Quebec → force-down immediately (R$0). **First positive proof the allowlist BRANCH ran** (the prior PARTIAL never logged "primary allowlist exhausted"). |
| 17:18 | second cycle, allowlist=24953 (cheapest qualifying EU) → allowlist returned 0 (24953 had `rentable:false` AND `inet_down=492 < 500`-gate) → broaden picked 52361 → force-down. |
| 17:19:45 | third cycle, `PRIMARY_VAST_MACHINE_ALLOWLIST=55158` → **`primary offer picked: offer_id=31139421 machine_id=55158 host_id=167329 dph=0.482 geo="Germany, DE"`**, with NO `primary allowlist exhausted` line and NO broaden — the in-list offer was found and picked. |
| 17:19:45 → 17:30:36 | Vast cold-start: image `converseai-primary-pod:develop` downloaded + container running; 4 supervisord children booted with weight downloads from MinIO-DE. |
| 17:30:36 | gateway emitted `primary_ready observed; emerg not active … primary_lifecycle_id=91` → **markReady reached** (~10m51s, well under the 2400s budget). FSM state `ready` confirmed via `gatewayctl primary state`. |
| 17:31:22 | `gatewayctl primary force-down --reason "06.8-05 UAT pass - destroy pod"` → drain → destroyed. |
| 17:31:25 | lifecycle 91 closed: `shutdown_reason: destroyed`, drain 3.2s, total_cost_brl 0. Vast instance 37360510 gone. |

### Endpoint validation on the gateway-provisioned 2×3090 pod (instance 37360510, host 192.68.211.105)

| service | port (host → pod) | URL probed | result |
|---|---|---|---|
| LLM (llama.cpp) | 32335 → 8000 | `/v1/models` | **HTTP 200**, `{"data":[{"id":"model.gguf","object":"model"}]}` |
| STT (speaches) | 32204 → 8001 | `/health` | **HTTP 200**, `{"status":"ok",...}` |
| STT (speaches) | 32204 → 8001 | `POST /v1/audio/transcriptions` (Systran/faster-whisper-large-v3, sample `whatsapp-sample.ogg`) | **HTTP 200**, body `{"text":"Olá, este é um teste de transcrição de áudio para o HTWide Inteligência Artificial.",…}` — **Plan-02 STT fix re-confirmed on the gateway-provisioned 2×3090 (not just the Plan-02 manual-create path)** |
| TTS (kani-tts) | 32675 → 8003 | `/health` | **HTTP 200**, `{"status":"ok","model_ready":true}` |
| DCGM | 32772 → 9400 | `/metrics` | **HTTP 200** (Prometheus text exposition format) |

### In-pod nvidia-smi (instance 37360510, SSH `root@192.68.211.105:32068` via `POD_DEBUG_SSH_PUBLIC_KEY=pedro@ops-claude`)

```
index, name, memory.used [MiB], memory.total [MiB], utilization.gpu [%]
0, NVIDIA GeForce RTX 3090, 16252 MiB, 24576 MiB, 0 %
1, NVIDIA GeForce RTX 3090,  9764 MiB, 24576 MiB, 0 %
```

2-GPU split confirmed: GPU0 ~16.2 GB (Qwen-llama tensor split majority + DCGM), GPU1 ~9.8 GB (Qwen-llama split tail + STT/TTS/embed). Total stack ~26.0 GB / 48 GB. Idle util because no inference load during the snapshot — STT transcription completed before the SSH snapshot.

### Cost (this re-UAT)

Three force-up cycles total. Cycles 1 + 2 were cancelled in flight before pod creation (R$0 each, total_cost_brl=0 on lifecycles 88/89/90). Cycle 3 ran ~11min cold-start + ~14s ready before force-down → max billable ~$0.482 × 12/60 ≈ **~$0.10** (lifecycle 91's `total_cost_brl=0` field is the gateway's pre-cron value; the Vast charge lands on the account in the usual settlement window).

### Three failed VERIFICATION truths from `06.8-VERIFICATION.md` — all closed

| truth | prior (06.8-03 PARTIAL) | now (06.8-05 re-UAT) |
|---|---|---|
| Gateway-provisioned 2×3090 reaches markReady on an in-list host | FAIL — allowlist did not steer; broaden picked broken-CDI / far-US hosts | **PASS** — allowlist steered to 55158 (in-list), markReady reached at 17:30:36 |
| STT `/v1/audio/transcriptions` 200 on the gateway-provisioned 2×3090 | FAIL — markReady never reached (downstream consequence) | **PASS** — HTTP 200 + correct PT-BR transcription via the gateway path |
| In-pod nvidia-smi shows 2-GPU llama tensor-split | FAIL — same downstream consequence | **PASS** — `nvidia-smi` shows 2× RTX 3090 with 16.2/9.8 GB used, ~26 GB / 48 GB |

### Substituted target host — methodology note

The Plan-03 PARTIAL originally planned to land on 43803 (Estonia, spike-proven 2026-05-21). At re-UAT time (2026-05-22 17:00 UTC) the Vast spot market showed 43803 at `rentable:false` — direct API probe `GET /bundles?q={"machine_id":{"in":[43803]},...}` returned the offer but with `rentable:false` (and the strict DefaultSearchFilter requires `rentable:{eq:true}`). The methodology to pick a substitute was: re-survey the EXACT DefaultSearchFilter constraints (`gpu_name=RTX 3090, num_gpus=2, reliability≥0.99, dph_total≤0.60, inet_down≥500, cuda_max_good≥12.8, driver_vers≥570000000, rentable=true, blocklist=55942,45778`) at re-UAT time, pick the cheapest EU-located qualifying host (closest to MinIO-DE to stay inside the 2400s cold-start budget), and run the force-up against that. 55158 (Germany) won. Allowlist STEERING is proven for an arbitrary in-list host — it does not depend on the specific machine_id 43803. The standing operator note: keep BOTH 43803 (when rentable) AND 55158 in the allowlist going forward.
