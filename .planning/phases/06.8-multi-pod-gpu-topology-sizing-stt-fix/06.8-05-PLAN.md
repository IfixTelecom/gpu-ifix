---
phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix
plan: 05
type: execute
wave: 4
depends_on: [06.8-03]
files_modified:
  - gateway/internal/emerg/vast/types.go
  - gateway/internal/primary/reconciler.go
  - gateway/internal/emerg/vast/client.go
  - gateway/internal/emerg/vast/types_test.go
  - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md
  - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md
autonomous: false
gap_closure: true
requirements: [GW-2GPU]
must_haves:
  truths:
    - "gatewayctl primary force-up with PRIMARY_NUM_GPUS=2 + PRIMARY_GPU_NAME=\"RTX 3090\" + PRIMARY_VAST_MACHINE_ALLOWLIST=43803 provisions a 2×3090 single-pod end-to-end via the GATEWAY path and the 4-endpoint health-poll reaches markReady (the allowlist actually steers selection to 43803, not a global-cheapest out-of-list host)"
    - "STT /v1/audio/transcriptions returns HTTP 200 on the GATEWAY-PROVISIONED 2×3090 pod (confirms the Plan 02 STT fix holds on this topology + the :develop image, via the gateway path not the manual create path)"
    - "In-pod nvidia-smi shows 2 GPUs with the llama tensor-split ~8.5GB GPU0 / ~9.4GB GPU1, full stack ~27GB/48GB, on the GATEWAY-PROVISIONED pod"
  artifacts:
    - path: ".planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md"
      provides: "Written root-cause statement naming the true failure mode of the allowlist-steering bug, with reproduction evidence (allowlist-pass vs broaden-pass offer sets + DphTotal vs cap + runtime config echo). No fix applied at diagnosis time."
      contains: "root cause"
    - path: "gateway/internal/emerg/vast/types_test.go"
      provides: "Go unit test proving the allowlist pass restricts the search to the listed machine_ids (extends TestWithMachineAllowlist or adds a reconciler-pass test)"
      contains: "machine_id"
    - path: ".planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md"
      provides: "PARTIAL UAT record updated to PASS: gateway picks 43803 → markReady → 4 endpoints 200 → nvidia-smi 2-GPU split"
      contains: "markReady"
  key_links:
    - from: "PRIMARY_VAST_MACHINE_ALLOWLIST=43803 (runtime env)"
      to: "reconciler.provisionLifecycle pickable[0] == an offer on machine_id 43803"
      via: "allowlist-first SearchOffers pass (reconciler.go L784-796) restricts offers to the listed machine_ids before broaden"
      pattern: "machine_id.*43803|primary offer picked.*43803"
    - from: "gatewayctl primary force-up"
      to: "reconciler waitForReadyOrDestroy markReady (4 endpoints 200)"
      via: "inst.Ports host-port health-poll on the 43803-provisioned 2×3090 pod"
      pattern: "markReady"
---

<objective>
Close the single gap blocking Phase 06.8 (verification 7/10): the gateway never reached **markReady** on a 2×3090 single-pod because `PRIMARY_VAST_MACHINE_ALLOWLIST` does not steer Vast offer selection at runtime. With `allowlist=[43803]` (known-good-CDI 2×3090, $0.401, rel 0.995, under the $0.60 cap), `gatewayctl primary force-up` still picks global-cheapest out-of-list hosts (47525 Quebec, 76546 California) — which land on broken-CDI hosts or hosts too far from MinIO-DE for the 2400s cold-start budget. The two other failed must-haves (STT 200 via gateway; in-pod nvidia-smi 2-GPU split) are downstream consequences of never reaching markReady — they get asserted automatically once the gateway lands on 43803.

This is a **debug-then-validate** gap closure, structured to honor the CLAUDE.md rule (NO speculative edits): DIAGNOSE the true root cause first with no source edit, gate on operator approval of the one-change fix, THEN apply the minimal repair + unit test, THEN re-run the Plan 03 live UAT against 43803.

**The crux to resolve (the code path LOOKS correct).** reconciler.go L784-796 builds `vast.WithMachineAllowlist(filter, allowlist)` → `SearchOffers` → `FilterBelowCap`; types.go L269-283 `WithMachineAllowlist` sets `machine_id:{in:[ids]}`; client.go L153-187 marshals the filter to `GET /bundles?q=<urlencoded-json>`. **A direct contradiction must be reconciled:** the Plan-03 A2 pre-check (recorded in 06.8-GW-2GPU-LIVE-UAT.md) ran the allowlist-pass filter directly against `/bundles` and got **1 offer (id 36299650, machine 43803)** — so the `in` operator DOES work at the Vast API level. Yet runtime force-up still picked out-of-list hosts. So the failure is NOT the Vast query language; it is somewhere in the runtime path (config wiring at runtime, FilterBelowCap eliminating the allowlist offers, or the pickable-selection branch). The diagnosis must isolate which.

Purpose: deterministic targeting of known-good-CDI 2×3090 hosts so the gateway can reach markReady end-to-end — the headline goal of Phase 06.8.
Output: 06.8-ALLOWLIST-DIAGNOSIS.md (root cause), the minimal source fix + unit test, 06.8-GW-2GPU-LIVE-UAT.md updated PARTIAL→PASS.
Cost: ~$0.29–0.40/h Vast 2×3090, per-second billing — force-down/destroy after the assertions pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-VERIFICATION.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-03-PLAN.md
@.planning/todos/pending/260522-allowlist-steering-bug.md
@gateway/internal/primary/reconciler.go
@gateway/internal/emerg/vast/types.go
@gateway/internal/emerg/vast/client.go

<interfaces>
<!-- Exact runtime path + identifiers the executor needs. No fenced code in tasks. -->

Allowlist-first pass (the suspect path):
  reconciler.go L769  filter := vast.DefaultSearchFilter(PrimaryVastPriceCapDPH, PrimaryHostID, PrimaryGPUName, PrimaryNumGPUs, PrimaryVastMachineBlocklist...)
  reconciler.go L784  if len(r.cfg.PrimaryVastMachineAllowlist) > 0 {
  reconciler.go L785    allowFilter := vast.WithMachineAllowlist(filter, r.cfg.PrimaryVastMachineAllowlist)
  reconciler.go L786    offers, err := r.deps.Vast.SearchOffers(ctx, allowFilter)
  reconciler.go L791    pickable = vastutil.FilterBelowCap(offers, r.cfg.PrimaryVastPriceCapDPH)
  reconciler.go L792    if len(pickable) == 0 { log "primary allowlist exhausted; broadening..." }
  reconciler.go L797  if len(pickable) == 0 { broaden: SearchOffers(filter) → FilterBelowCap }
  reconciler.go L809  offer := pickable[0]   ← selection

  types.go L269-283   WithMachineAllowlist: out["machine_id"] = {"in": ids}  (overwrites blocklist notin)
  types.go L253       DefaultSearchFilter sets machine_id:{notin: blocklist} ONLY when blocklist non-empty
  client.go L153-187  SearchOffers: json.Marshal(filter) → GET /bundles?q=url.QueryEscape(json) → decode {offers:[...]}
  vastutil/helpers.go:79  FilterBelowCap(offers, cap) — epsilon comparison on Offer.DphTotal (json dph_total, types.go L49)
  config.go L196+L370 PrimaryVastMachineAllowlist = parseInt64CSV(os.Getenv("PRIMARY_VAST_MACHINE_ALLOWLIST"))
  types.go L53        Offer.MachineID int64 `json:"machine_id"`

Root-cause hypotheses to TEST (do not assume — diagnose):
  H1  Vast `/bundles` honors machine_id:{in:[...]} ONLY in some serializations; the reconciler's
      WithMachineAllowlist output may differ from the A2 hand-built query (A2 got 1 offer for 43803,
      so the API DOES support `in` — reconcile WHY runtime differs). Compare the EXACT marshaled JSON
      the reconciler sends vs the A2 query that returned 43803.
  H2  FilterBelowCap eliminates the allowlisted offers at runtime (DphTotal field includes
      storage/bandwidth surcharge pushing 43803's dph_total over the cap epsilon) → pickable==0 →
      broaden fires → broaden's pickable[0] is the global-cheapest out-of-list host. (43803 A2 dph
      $0.401 is under cap 0.60 — so test whether the runtime Offer.DphTotal differs from the A2 dph.)
  H3  PrimaryVastMachineAllowlist is empty at runtime (env not passed through to the gateway
      container / parseInt64CSV got "") → the len()>0 branch is skipped entirely → only the broaden
      pass runs. (UAT-03 doc notes 3 env-passthrough lines had to be ADDED to the compose file —
      confirm PRIMARY_VAST_MACHINE_ALLOWLIST is one of them and is non-empty in the running container.)

Runtime env for the re-run UAT (shape A, 2×3090):
  PRIMARY_NUM_GPUS=2
  PRIMARY_GPU_NAME="RTX 3090"
  PRIMARY_VAST_MACHINE_ALLOWLIST=43803          # known-good-CDI host (Estonia, spike+STT proven)
  PRIMARY_VAST_PRICE_CAP_DPH=0.60               # bounds broaden-fallback to cheap 3090 shapes
  PRIMARY_VAST_MACHINE_BLOCKLIST=55942,45778    # broken-CDI 2×3090 hosts (carried from UAT-03)
  PRIMARY_TEMPLATE_IMAGE=…:develop              # carries the promoted Plan-02 STT fix
  PRIMARY_WHISPER_WEIGHTS_SHA256=d8e8f6e…       # promoted canonical SHA (Plan 02)

gatewayctl: `gatewayctl primary state`, `primary schedule`,
  `primary force-up --reason "06.8-05 allowlist-fix re-UAT"`, `primary force-down`,
  `primary lifecycles --format json`.

4-endpoint health ports (Vast host-port mapped): LLM 8000 /v1/models, STT 8001 /v1/audio/transcriptions,
  embed 8002 /embeddings, DCGM 9400 /metrics.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DIAGNOSE — isolar a causa-raiz do allowlist-steering bug (SEM editar source gateway)</name>
  <read_first>gateway/internal/primary/reconciler.go (provisionLifecycle L764-809, esp the allowlist-first branch L784-796); gateway/internal/emerg/vast/types.go (WithMachineAllowlist L269-283, DefaultSearchFilter L229-256); gateway/internal/emerg/vast/client.go (SearchOffers L153-187 serialization); gateway/internal/vastutil/helpers.go (FilterBelowCap L79); gateway/internal/config/config.go (L196 + L370 PrimaryVastMachineAllowlist parse); .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md (the A2 pre-check that got 1 offer for machine_id:{in:[43803]} — the contradiction to reconcile); .planning/todos/pending/260522-allowlist-steering-bug.md</read_first>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md</files>
  <action>Isolate the TRUE failure mode WITHOUT editing any gateway source (CLAUDE.md anti-speculative-edit rule). Reproduce the runtime path against the live Vast API using VAST_AI_API_KEY + the recipe in memory `vast-offer-search-method`. Test the three hypotheses H1/H2/H3 (see interfaces) and reconcile the central contradiction: the A2 pre-check already proved `machine_id:{in:[43803]}` returns 1 offer (id 36299650) from `/bundles`, so the Vast `in` operator works — yet runtime force-up picked out-of-list hosts. Concrete diagnostic actions: (a) reproduce the EXACT JSON the reconciler marshals for the allowlist pass — build the same SearchFilter that DefaultSearchFilter(cap=0.60, hostID, "RTX 3090", 2, 55942, 45778) + WithMachineAllowlist([43803]) produces, send it through the same `GET /bundles?q=<url.QueryEscape(json)>` serialization client.go L153-187 uses, and capture the returned offers + each Offer.DphTotal (json dph_total). Compare byte-for-byte against the A2 query that returned 43803 — note any difference (e.g. the blocklist notin overwrite, num_gpus eq vs the A2 query, the order/limit keys). (b) Run FilterBelowCap(offers, 0.60) semantics on the returned offers: confirm whether 43803's runtime dph_total survives the cap+epsilon (H2) or is eliminated. (c) Confirm PRIMARY_VAST_MACHINE_ALLOWLIST is actually non-empty in the running ai-gateway-dev container at runtime (echo/inspect the container env + confirm parseInt64CSV would yield [43803]; cross-check the UAT-03 note that env-passthrough lines were added to /opt/ai-gateway-dev/docker-compose.yml) (H3). Write 06.8-ALLOWLIST-DIAGNOSIS.md containing: the reproduction evidence for each hypothesis, the marshaled allowlist-pass JSON vs the A2 JSON, the returned offers + dph_total values vs the 0.60 cap, the runtime env confirmation, and a single explicit ROOT-CAUSE statement naming WHICH of H1/H2/H3 (or another isolated cause) is true and WHICH source line is responsible. Propose the minimal ONE-change fix targeting that line (do NOT apply it). This task MUST NOT edit any file under gateway/.</action>
  <verify>
    <automated>test -f .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md && grep -Eqi 'root[ -]?cause' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md && grep -Eq 'H1|H2|H3|FilterBelowCap|machine_id|WithMachineAllowlist' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md && grep -Eq 'dph_total|DphTotal|0\.60|43803' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md && git diff --quiet -- gateway/ && echo NO_GATEWAY_EDITS</automated>
  </verify>
  <acceptance_criteria>A written root-cause statement exists in 06.8-ALLOWLIST-DIAGNOSIS.md naming the TRUE failure mode (one of H1/H2/H3 or another isolated cause) and the responsible source line, supported by: the marshaled allowlist-pass JSON, the returned offers + their dph_total vs cap 0.60, and the runtime env confirmation of PRIMARY_VAST_MACHINE_ALLOWLIST. The A2-vs-runtime contradiction is reconciled. A proposed minimal one-change fix is named. `git diff --quiet -- gateway/` is clean (NO gateway source edited at diagnosis time).</acceptance_criteria>
  <done>Root cause isolated and documented; no gateway source edited; minimal fix proposed (not applied).</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve the isolated root cause + the proposed minimal one-change fix to the allowlist-first pass before any gateway source is edited (CLAUDE.md: validate the solution with the operator BEFORE editing files).</decision>
  <context>Task 1 isolated the true failure mode (H1/H2/H3 or another cause) and proposed a minimal one-change repair in 06.8-ALLOWLIST-DIAGNOSIS.md. Per CLAUDE.md, no edit goes to dev-prod (commit/push deploys to ai-gateway-dev) without operator validation. The fix target is one of: types.go WithMachineAllowlist serialization (L269-283), reconciler.go allowlist-first ordering / FilterBelowCap usage (L784-808), or client.go query encoding (L153-187) — exact file depends on the diagnosis.</context>
  <options>
    <option id="approve">
      <name>Approve the proposed fix</name>
      <pros>Unblocks Task 3 to apply the minimal repair + unit test, then the live re-UAT closes the gap.</pros>
      <cons>None if the diagnosis is sound; the fix is bounded to the one isolated cause.</cons>
    </option>
    <option id="revise">
      <name>Request more diagnosis / a different fix</name>
      <pros>Avoids a wrong edit reaching dev-prod if the root cause is not yet conclusive.</pros>
      <cons>Loops back to Task 1 for more evidence.</cons>
    </option>
  </options>
  <resume-signal>Type "approved: &lt;fix-target file:line&gt;" to authorize Task 3, or describe what additional diagnosis / alternative fix you want.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIX — aplicar a reparação mínima no allowlist-first pass + unit test que prova a restrição</name>
  <read_first>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md (the approved root cause + proposed fix); gateway/internal/emerg/vast/types.go (WithMachineAllowlist L269-283); gateway/internal/primary/reconciler.go (allowlist-first pass L784-808); gateway/internal/emerg/vast/client.go (SearchOffers L153-187); gateway/internal/emerg/vast/types_test.go (TestWithMachineAllowlist L114-139 + TestDefaultSearchFilter_NumGPUs L97 — mirror this structure)</read_first>
  <files>gateway/internal/emerg/vast/types.go, gateway/internal/primary/reconciler.go, gateway/internal/emerg/vast/client.go, gateway/internal/emerg/vast/types_test.go</files>
  <behavior>
    - The allowlist-first pass restricts the search to the listed machine_ids: given allowlist=[43803], the offers fed to FilterBelowCap/pickable are only offers whose Offer.MachineID is in the allowlist (no out-of-list host such as 47525/76546 can become pickable[0] while an in-list offer under the cap exists).
    - The fix is bounded to the SINGLE cause isolated + approved in Task 1 (do not refactor unrelated code).
    - Existing TestWithMachineAllowlist invariants still hold (empty allowlist no-op; input not mutated; `in` overwrites `notin`).
  </behavior>
  <action>Apply ONLY the approved minimal change to the file:line named in Task 1's root cause (likely one of: types.go WithMachineAllowlist serialization L269-283, reconciler.go allowlist-first ordering/FilterBelowCap L784-808, or client.go query encoding L153-187). Do not edit beyond the isolated cause. Add or extend a Go unit test that PROVES the allowlist pass restricts to the listed machine_ids — mirror the existing TestWithMachineAllowlist (types_test.go L114) and TestDefaultSearchFilter_NumGPUs (L97) structure. If the root cause is in WithMachineAllowlist serialization, the new sub-test asserts the marshaled filter the reconciler sends carries machine_id:{in:[ids]} after the full DefaultSearchFilter+WithMachineAllowlist composition the reconciler uses (not just the helper in isolation). If the root cause is FilterBelowCap eliminating in-list offers, add a vastutil test (or reconciler-level test with a fake Vast client returning a known-good in-list offer + out-of-list cheaper offers) asserting pickable[0].MachineID is the in-list host. Run `gofmt`/the project formatter, `go vet ./...`, `go build ./...`, and `go test ./...` from the gateway module — all must exit 0. Do NOT commit here; the executor's normal commit step handles it after the in-task verify passes.</action>
  <verify>
    <automated>cd gateway && go build ./... && go vet ./internal/emerg/vast/... ./internal/primary/... && go test ./internal/emerg/vast/... ./internal/primary/... ./internal/vastutil/... 2>&1 | grep -Ev '^(ok|---|=== RUN|=== PAUSE|=== CONT|PASS)' ; cd gateway && go test ./... >/dev/null 2>&1 && echo GO_TEST_ALL_PASS</automated>
  </verify>
  <acceptance_criteria>The minimal fix is applied to exactly the file:line approved in Task 2 (no unrelated edits). A new/extended unit test proves the allowlist pass restricts the offer set to the listed machine_ids (in-list host wins over a cheaper out-of-list host). `go build ./...`, `go vet`, and `go test ./...` all exit 0 (GO_TEST_ALL_PASS printed). Existing TestWithMachineAllowlist sub-tests still pass.</acceptance_criteria>
  <done>Allowlist-first pass repaired + regression test green; gateway builds and the full Go test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The allowlist-steering fix is committed to develop (Task 2) and the ai-gateway-dev stack has picked up the new gateway image (verify the running container digest changed — note the UAT-03 STATE.md gotcha: Portainer webhook may fire but the local image can stay stale; manual `docker pull` + `docker compose up -d gateway` may be needed). Dev env set to shape A: PRIMARY_NUM_GPUS=2, PRIMARY_GPU_NAME="RTX 3090", PRIMARY_VAST_MACHINE_ALLOWLIST=43803, PRIMARY_VAST_PRICE_CAP_DPH=0.60, PRIMARY_VAST_MACHINE_BLOCKLIST=55942,45778, PRIMARY_TEMPLATE_IMAGE=…:develop (Plan-02 STT fix), PRIMARY_WHISPER_WEIGHTS_SHA256=d8e8f6e…. Claude has prepared `gatewayctl primary force-up --reason "06.8-05 allowlist-fix re-UAT"`.</what-built>
  <how-to-verify>
    Re-run the Plan-03 2×3090 live-UAT against 43803 (Vast 2×3090 ~$0.29-0.40/h, per-second — force-down after):
    1. Confirm the gateway container is running the post-fix image digest (not the stale pre-fix one).
    2. Pre-flight: `gatewayctl primary schedule` + `gatewayctl primary state`.
    3. Trigger `gatewayctl primary force-up --reason "06.8-05 allowlist-fix re-UAT"`.
    4. THE GAP-CLOSER: reconciler logs show `primary offer picked` with **machine_id=43803** (the in-allowlist host) — NOT 47525/76546/any out-of-list host. dph ~$0.40, num_gpus=2, RTX 3090.
    5. `waitForReadyOrDestroy` reaches **markReady**; `gatewayctl primary state` / `primary lifecycles --format json` confirm Ports mapped for 8000/8001/8002/9400.
    6. Services through the mapped host ports return 200: LLM `/v1/models` (8000), STT `/v1/audio/transcriptions` (8001 — proves the Plan-02 STT fix holds on the gateway-provisioned 2×3090), embed `/embeddings` (8002), DCGM `/metrics` (9400).
    7. SSH into the pod: `nvidia-smi` shows 2 GPUs; llama tensor-split ~8.5GB GPU0 / ~9.4GB GPU1; full stack ~27GB/48GB.
    8. `gatewayctl primary force-down` → confirm Vast destroys the instance (billing stops, Vast instance count 0).
    IF force-up STILL picks an out-of-list host (fix ineffective): force-down + destroy any running pod, record the observed pick, and route back to Task 1 (re-diagnose) — do NOT leave a pod billing.
  </how-to-verify>
  <resume-signal>Type "approved" with: the picked machine_id (must be 43803), markReady reached (yes/no), the 4 service HTTP codes (esp STT 200 on 8001), and the nvidia-smi 2-GPU split — or describe the failure (routes back to Task 1).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: RECORD — atualizar 06.8-GW-2GPU-LIVE-UAT.md PARTIAL→PASS + fechar o todo do bug</name>
  <read_first>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md (the PARTIAL record + Blocker 1/Blocker 2 sections to update); .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md (root cause to cite); .planning/todos/pending/260522-allowlist-steering-bug.md (the bug to mark resolved)</read_first>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md, .planning/todos/pending/260522-allowlist-steering-bug.md</files>
  <action>Update 06.8-GW-2GPU-LIVE-UAT.md: change `status: in-progress` to `status: passed`; add a new "## Re-UAT 2026-05-22 (Plan 06.8-05) — PASS" section recording the checkpoint results: the picked machine_id=43803 (allowlist now steers selection — cite the root cause + fix commit from 06.8-ALLOWLIST-DIAGNOSIS.md), markReady reached, the 4 service HTTP codes (LLM 8000 / STT 8001 / embed 8002 / DCGM 9400 all 200, esp STT /v1/audio/transcriptions 200 proving the Plan-02 fix on the gateway-provisioned 2×3090), the in-pod nvidia-smi 2-GPU tensor-split (~8.5/9.4GB, stack ~27GB/48GB), and the force-down/destroy confirmation. Explicitly mark Blocker 1 (allowlist-steering) RESOLVED with the fix reference. Update the bug todo .planning/todos/pending/260522-allowlist-steering-bug.md: add a "## RESOLVED 2026-05-22" line citing the root cause (from the diagnosis doc), the fix commit, and the re-UAT PASS evidence (machine_id=43803 + markReady). Confirm the SEED-002 input section still states the validated shape (2×RTX 3090, 48GB) — now proven via the GATEWAY path, not just manual create.</action>
  <verify>
    <automated>grep -Eq 'status:\s*passed' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq 'markReady' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq '43803' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq '/v1/audio/transcriptions|STT.*200' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eqi 'nvidia-smi|2.GPU|tensor.split' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eqi 'resolved' .planning/todos/pending/260522-allowlist-steering-bug.md</automated>
  </verify>
  <acceptance_criteria>06.8-GW-2GPU-LIVE-UAT.md status=passed with a Re-UAT PASS section: machine_id=43803 picked, markReady reached, 4 endpoints 200 (STT /v1/audio/transcriptions 200), nvidia-smi 2-GPU split recorded, force-down confirmed. Blocker 1 marked RESOLVED with the fix reference. The bug todo marked RESOLVED with root cause + fix commit + re-UAT evidence. SEED-002 shape (2×3090 48GB) confirmed proven via the gateway path.</acceptance_criteria>
  <done>UAT record flipped PARTIAL→PASS closing all 3 failed VERIFICATION truths; bug todo resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → Vast API | provision/search/destroy via VAST_AI_API_KEY; offer-selection logic under repair |
| Vast spot host → primary pod | rented 2×3090 host; CDI may be broken (55942, 45778) or host far from MinIO-DE |
| operator → dev-prod (commit/push) | a wrong gateway edit deploys to ai-gateway-dev — guarded by diagnose-first + approval gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.8-15 | Tampering | speculative gateway edit reaches dev-prod via commit/push (CLAUDE.md rule) | mitigate | Task 1 diagnoses with NO source edit (verified by `git diff --quiet -- gateway/`); Task 2 fix gated behind blocking operator approval (checkpoint:decision); fix bounded to the single isolated cause. |
| T-06.8-16 | Tampering | the allowlist fix widens the search beyond the listed machine_ids (regression) | mitigate | Unit test asserts the allowlist pass restricts to listed machine_ids; existing TestWithMachineAllowlist no-op/no-mutate invariants must still pass; `go test ./...` exit 0. |
| T-06.8-13 | Tampering | broaden-fallback picks an expensive non-3090 shape, blowing budget | mitigate | PRIMARY_VAST_PRICE_CAP_DPH=0.60 bounds broaden (FilterBelowCap); MONTHLY_PRIMARY_BUDGET_BRL guard active; carried from Plan 03. |
| T-06.8-06 | Spoofing | broken/malicious Vast multi-GPU host | mitigate | allowlist=43803 (now steered) + blocklist 55942,45778 + reliability gte 0.99; broken-CDI auto-destroy already proven (UAT-03). |
| T-06.8-07 | Denial of service | spot preemption mid-UAT | accept | Resilience posture (CONTEXT): interruptible pods acceptable; force-down + re-bid; emergency tier covers gaps. |
| T-06.8-08 | Info disclosure | VAST_AI_API_KEY in gateway env | accept | Key in env/GH Secret only; gatewayctl publishes events via Redis, not the key. |
| T-06.8-SC | Tampering | package installs | accept | No package installs — Go source fix + config + docs only; no new dependencies. |
</threat_model>

<verification>
- Task 1: 06.8-ALLOWLIST-DIAGNOSIS.md exists with a root-cause statement + evidence; `git diff --quiet -- gateway/` clean (no edit at diagnosis time).
- Approval gate: operator authorizes the proposed one-change fix before any edit.
- Task 2: minimal fix applied to the approved file:line; unit test proves allowlist restriction; `go build ./...` + `go vet` + `go test ./...` all exit 0.
- Re-UAT checkpoint: force-up picks machine_id=43803 (allowlist steers) → markReady → 4 endpoints 200 (STT 8001 200) → nvidia-smi 2-GPU split → force-down/destroy.
- Task 3: UAT record PARTIAL→PASS; bug todo resolved; all 3 failed VERIFICATION truths re-asserted.
</verification>

<success_criteria>
- The gateway provisions a 2×3090 single-pod end-to-end via the GATEWAY path and reaches **markReady**, landing on the allowlisted known-good host 43803 (allowlist-steering bug FIXED, proven by unit test + live re-UAT).
- STT `/v1/audio/transcriptions` returns 200 on the gateway-provisioned 2×3090 pod (Plan-02 fix holds on this topology via the gateway path).
- In-pod nvidia-smi shows the 2-GPU llama tensor-split (~8.5/9.4GB, stack ~27GB/48GB) on the gateway-provisioned pod.
- No speculative gateway edit: diagnosis preceded any source change; the fix was operator-approved and bounded to the single isolated cause.
- Pod destroyed after the UAT; bug todo + UAT record updated to closed/PASS.
</success_criteria>

<output>
Create `.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-05-SUMMARY.md` when done. Surface: the isolated root cause + the one-change fix (file:line + commit), the re-UAT result (machine_id=43803 + markReady + 4 endpoints 200 + nvidia-smi split), and confirmation that all 3 failed VERIFICATION truths are now closed.
</output>
