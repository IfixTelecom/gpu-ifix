---
phase: 06.8
slug: multi-pod-gpu-topology-sizing-stt-fix
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-21
---

# Phase 06.8 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from RESEARCH.md `## Validation Architecture`. This phase's real gap is
> an inherently non-automatable live-UAT (Vast spot host + real GPU cost); unit
> coverage for the gateway search/allowlist already exists.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (gateway)** | Go `testing` (stdlib) — `gateway/internal/primary/*_test.go`, `gateway/internal/emerg/vast/types_test.go` |
| **Framework (pod smoke)** | Python `pod/smoke/smoke.py` (httpx async; report-schema.json) |
| **Config file** | none dedicated (go test + smoke script) |
| **Quick run command** | `go test ./gateway/internal/primary/... ./gateway/internal/emerg/vast/...` |
| **Full suite command** | `go test ./gateway/...` + smoke via `gh workflow run smoke.yml -f image_tag=develop` |
| **Estimated runtime** | ~30s unit; live-UAT minutes (pod boot) |

---

## Sampling Rate

- **After every task commit:** Run `go test ./gateway/internal/primary/... ./gateway/internal/emerg/vast/...`
- **After every plan wave:** Run `go test ./gateway/...`
- **Phase gate (before `/gsd:verify-work`):** live-UAT on a 2×3090 pod PASS (LLM+STT+TTS+DCGM)
- **Max feedback latency:** ~30s (unit); live-UAT is checkpoint-gated, not latency-bound

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.8-01-01 | 01 | 1 | STT-FIX | T-06.8-01 | tarball SHA gated by `sha256sum -c` (V6) | static | `bash -n pod/scripts/upload-weights.sh && grep -q 'models--Systran--faster-whisper-large-v3/snapshots' pod/scripts/upload-weights.sh` | ✅ | ⬜ pending |
| 06.8-01-02 | 01 | 1 | STT-FIX | T-06.8-01 | no creds in logs (V7) | static | `grep -A2 '\[program:speaches\]' pod/primary/supervisord.conf \| grep -q 'HF_HUB_CACHE="/weights/whisper"'` | ✅ | ⬜ pending |
| 06.8-02-* | 02 | 2 | STT-FIX | T-06.8-03/04/11/12 | candidate-key, SHA-to-pod, no canonical mutation pre-pass | manual (live pod) | live checklist: `/v1/audio/transcriptions` 200 + GPU offload + speaches `/health`/logs | ❌ manual — live pod (cost/spot) | ⬜ pending |
| 06.8-03-01 | 03 | 3 | GW-2GPU | T-06.8-06/07 | A2 search through cuda/driver gates | semi-auto | `pod/scripts/vast-ai.sh search` mirror of `DefaultSearchFilter` (types.go L233-244) | ✅ tooling | ⬜ pending |
| 06.8-03-* | 03 | 3 | GW-2GPU | T-06.8-08/14 | force-up 2×3090; one-line gate relax recorded if zero-offers; cost-cap holds on broaden | manual (live UAT) | `gatewayctl primary force-up` → 4-endpoint markReady; STT 200 on 2×3090 | ❌ manual — live pod (cost/spot) | ⬜ pending |
| 06.8-04-01 | 04 | 1 | LADDER | T-06.8-09/15 | runbook only; no orchestrator code | docs review | `test -f docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md && grep -q '2×RTX 3090' docs/RUNBOOK-GPU-TOPOLOGY-FALLBACK.md` | ❌ doc to create | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Unit for `DefaultSearchFilter` with `numGPUs=2` (beyond the landed coverage, if needed) — `gateway/internal/emerg/vast/types_test.go`
- [ ] STT smoke depends on a live pod carrying the fix — NO local fixture (CTranslate2 + GPU). Mark manual-only.

*Most test infra already exists; the real gap is the manual live-UAT, inherently non-automatable by cost/spot.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| 2×3090 provision end-to-end via gateway | GW-2GPU | Vast spot host + real GPU cost; not reproducible in CI | Plan 03 9-step live-UAT: `gatewayctl primary force-up` → 4-endpoint markReady → service HTTP codes → nvidia-smi split → force-down |
| `/v1/audio/transcriptions` resolves + 200 | STT-FIX | Needs a live pod with the rebuilt image + CTranslate2 GPU; no local fixture | Plan 02 checkpoint: POST transcriptions (smoke.py:262 sample) → 200; speaches `/health` + startup logs; GPU VRAM delta |
| Fallback ladder presets usable by operator | LADDER | Doc/runbook procedure, not code | Plan 04: read runbook, confirm per-shape env presets + force-down→swap→force-up transition |

---

## Validation Sign-Off

- [x] All tasks have `<automated>`/`<verify>` blocks or are documented manual-only (live-UAT)
- [x] Sampling continuity: no 3 consecutive automatable tasks without verify (manual live-UAT is inherently non-automatable, documented above)
- [x] Wave 0 covers the MISSING references (STT smoke marked manual-only; gateway unit landed)
- [x] No watch-mode flags
- [x] Feedback latency < 30s (unit); live-UAT checkpoint-gated
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-21
