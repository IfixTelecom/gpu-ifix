---
phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix
plan: 03
type: execute
wave: 3
depends_on: [06.8-02]
files_modified:
  - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md
autonomous: false
requirements: [GW-2GPU]
must_haves:
  truths:
    - "DefaultSearchFilter for RTX 3090 numGPUs=2 returns offers THROUGH the cuda_max_good gte 12.8 + driver_vers gte 570000000 gates (Open risk A2 — the spike used MANUAL create, not the search; this is the first live step)"
    - "gatewayctl primary force-up with PRIMARY_NUM_GPUS=2 + PRIMARY_GPU_NAME=\"RTX 3090\" + PRIMARY_VAST_MACHINE_ALLOWLIST=43803 provisions a 2×3090 single-pod end-to-end via the gateway path (not manual)"
    - "The 4-endpoint health-poll (LLM /v1/models, STT /health, TTS /health, DCGM /metrics) reaches markReady with 2 GPUs and the Vast host-port mapping (8000/8001/8003/9400) works"
    - "In-pod nvidia-smi shows 2 GPUs with the llama tensor-split ~8.5GB GPU0 / ~9.4GB GPU1, full stack ~27GB/48GB"
    - "STT /v1/audio/transcriptions returns 200 on the gateway-provisioned 2×3090 pod (confirms the Plan 02 STT fix holds on this topology + the promoted/develop image)"
    - "Cost guard holds: the MONTHLY_PRIMARY_BUDGET_BRL guard is active AND under shape-A env the broaden-fallback cannot pick an expensive non-3090 shape because PRIMARY_VAST_PRICE_CAP_DPH=0.60 bounds it — the price cap is asserted to hold on broaden (review L2)"
    - "No gateway CODE change was introduced EXCEPT — if and only if the A2 filtered search returns ZERO offers — an AUTHORIZED, bounded, ONE-LINE gate relaxation (operator pre-approved: drop cuda_max_good to 12.6 OR driver_vers to 565000000, whichever single gate isolates the zero-offers), with the exact diff + rationale recorded in 06.8-GW-2GPU-LIVE-UAT.md (operator chose Gemini's inline-relaxation remedy; Codex's separate-plan remedy M3 was NOT chosen)"
  artifacts:
    - path: ".planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md"
      provides: "Operator-signed live-UAT record: gateway provisions+health-polls a 2×3090 single-pod"
      contains: "force-up"
  key_links:
    - from: "PRIMARY_NUM_GPUS=2 + PRIMARY_GPU_NAME=RTX 3090 env"
      to: "DefaultSearchFilter num_gpus:{eq:2} + gpu_name:{eq:RTX 3090}"
      via: "SearchOffers returns 2×3090 offers past the cuda/driver gates (A2; inline one-line relaxation pre-authorized if zero-offers)"
      pattern: "RTX 3090.*num_gpus.*2"
    - from: "gatewayctl primary force-up"
      to: "reconciler waitForReadyOrDestroy markReady (4 endpoints)"
      via: "inst.Ports host-port health-poll, GPU-count-agnostic"
      pattern: "markReady|primary offer picked"
---

<objective>
Live-UAT the gateway 2×3090 provisioning path (mirror of the manual 2026-05-21 spike, but driven by the GATEWAY, not manual create). The gateway code is already GPU-count-agnostic and the knobs (PRIMARY_NUM_GPUS, PRIMARY_VAST_MACHINE_ALLOWLIST) landed in commit 6f57698 — RESEARCH proves there is NO code gap. The only gaps are RUNTIME config + the unvalidated assumption A2 (does the FILTERED search return 2×3090 offers through the cuda_max_good/driver gates — the spike used manual create which bypasses those gates).

This plan touches NO gateway source — it sets dev env, runs `gatewayctl primary force-up`, and records the live assertions — WITH ONE OPERATOR-AUTHORIZED EXCEPTION (A2 inline relaxation; see below). It uses the image validated+promoted in Plan 02 (so the STT-200 assertion holds on the 2×3090 topology).

**A2 gate-relaxation — OPERATOR DECISION (review convergent #2):** The operator chose Gemini's INLINE-RELAXATION remedy over Codex's separate-plan remedy (M3). So if the A2 pre-check (Task 1) finds the filtered 2×3090 search returns ZERO offers through the cuda_max_good>=12.8 / driver_vers>=570000000 gates, the executor is PRE-AUTHORIZED, inside the checkpoint, to make a SURGICAL ONE-LINE gate relaxation (drop cuda_max_good to 12.6 OR driver_vers to 565000000 — whichever SINGLE gate isolates the zero-offers; relax exactly one, the one Task 1 isolated). This is a bounded, authorized exception to the "no code change" scope. Constraints: it MUST be one line, isolated to the single offending gate (types.go L233-244), and the EXACT diff + rationale MUST be recorded in 06.8-GW-2GPU-LIVE-UAT.md. Do NOT spin off a separate plan.

Purpose: Prove the 2×3090 cost/availability sweet-spot is reachable via the gateway end-to-end, and decide/catalog the GPU shape the SEED-002 emergency hot-standby will mirror (this phase runs BEFORE SEED-002).
Output: `06.8-GW-2GPU-LIVE-UAT.md` with the 9-step UAT results; allowlist seeded with any newly-validated 2×3090 machine_id; the A2 relaxation diff if it was needed.
Cost: ~$0.29–0.40/h Vast 2×3090, per-second billing — force-down/destroy after the assertions pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-RESEARCH.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-02-SUMMARY.md
@gateway/internal/emerg/vast/types.go
@gateway/internal/primary/reconciler.go

<interfaces>
<!-- The gateway knobs to SET (config.go) + the live-UAT steps. No code change except the authorized A2 one-line relaxation. -->

Runtime env to set on the dev gateway (Portainer / .env) for shape A (2×3090):
  PRIMARY_NUM_GPUS=2
  PRIMARY_GPU_NAME="RTX 3090"
  PRIMARY_VAST_MACHINE_ALLOWLIST=43803          # spike-validated host (preference, not guarantee)
  PRIMARY_VAST_PRICE_CAP_DPH=0.60               # bounds broaden-fallback to cheap shapes (default 2.20 would allow a 5090)
  PRIMARY_TEMPLATE_IMAGE=<promoted image digest from Plan 02 — carries the STT fix>
  PRIMARY_WHISPER_WEIGHTS_SHA256=<new SHA from Plan 02 (already promoted to canonical + dev-stack env)>

Cost-guard knobs (config.go, review L2):
  MONTHLY_PRIMARY_BUDGET_BRL (default 800.0) — must be active; assert the guard is enforced.
  PRIMARY_VAST_PRICE_CAP_DPH=0.60 — assert broaden-fallback CANNOT pick a non-3090 shape above this cap
    (FilterBelowCap applies the cap+epsilon; a 5090 at ~$0.74-2.00/h is excluded under 0.60).

gatewayctl (repo root binary): `gatewayctl primary state`, `primary schedule`,
  `primary force-up --reason "06.8 2x3090 UAT"` (bypasses cooldown), `primary force-down`,
  `primary lifecycles --format json`.

DefaultSearchFilter gates that A2 must pass: cuda_max_good gte 12.8, driver_vers gte 570000000,
  reliability gte 0.99, num_gpus eq 2, gpu_name eq "RTX 3090". (types.go L233-244.)
  Spike confirmed host 43803 boots cu128 on Ampere — but via MANUAL create, NOT the search.
  If the filtered search returns 0 offers → AUTHORIZED one-line relaxation of the single isolated gate
  (cuda_max_good 12.8→12.6 OR driver_vers 570000000→565000000), diff recorded in the UAT doc.

9-step live-UAT (RESEARCH §"Live-UAT plan"):
  1. pre-flight: gatewayctl primary schedule + state
  2. set env (above)
  3. gatewayctl primary force-up --reason "06.8 2x3090 UAT"
  4. assert reconciler logs: "primary offer picked" machine_id (43803 or other 2×3090), num_gpus, dph ~$0.40
  5. assert 4-endpoint health → markReady; inst.Ports mapped 8000/8001/8003/9400
  6. assert services: LLM /v1/models 200 (~42 tok/s), STT /v1/audio/transcriptions 200, TTS /v1/audio/speech 200, DCGM /metrics 200
  7. assert in-pod (SSH): nvidia-smi 2 GPUs, llama split ~8.5/9.4GB, stack ~27GB/48GB
  8. assert cost guard (L2): MONTHLY_PRIMARY_BUDGET_BRL active; price cap 0.60 holds on broaden (no non-3090 picked)
  9. catalog: if a new 2×3090 host passed, add machine_id to allowlist env (manual — auto-add is Deferred)
  10. teardown: gatewayctl primary force-down → confirm destroy (Vast stops billing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A2 pre-check — confirm the filtered search returns 2×3090 offers; isolate the offending gate if zero</name>
  <read_first>gateway/internal/emerg/vast/types.go (DefaultSearchFilter L233-244 gate values); pod/scripts/vast-ai.sh (search); 06.8-02-SUMMARY.md (promoted image digest + SHA)</read_first>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md</files>
  <action>BEFORE provisioning, validate Open risk A2 (the first live step): confirm the gateway's DefaultSearchFilter for RTX 3090 numGPUs=2 returns offers THROUGH the cuda_max_good gte 12.8 + driver_vers gte 570000000 gates. Run the same q-filter the gateway builds against Vast `GET /bundles` (pod/scripts/vast-ai.sh search or a direct curl mirroring types.go L233-244: gpu_name eq "RTX 3090", num_gpus eq 2, reliability gte 0.99, dph_total lte 0.60, inet_down gte 500, cuda_max_good gte 12.8, driver_vers gte 570000000, rentable eq true). Record the offer count, cheapest dph, and whether machine 43803 appears. IF THE COUNT IS 0 despite known 2×3090 inventory → this is the UAT 18 regression class: run TEST queries dropping ONE gate at a time (cuda_max_good→12.6, then driver_vers→565000000) to ISOLATE the single gate that zeroes it. Record which single gate is responsible. Per the operator's pre-authorized inline-relaxation decision, the surgical one-line relaxation of THAT gate (and only that gate) is permitted in the checkpoint step — write the proposed exact one-line diff (types.go) into the A2 section now so the checkpoint can apply it. Write the A2 result as the first section of 06.8-GW-2GPU-LIVE-UAT.md.</action>
  <verify>
    <automated>grep -Eq 'A2|offers? (count|returned)|num_gpus.*2' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq 'cuda_max_good|driver_vers' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md</automated>
  </verify>
  <acceptance_criteria>A2 result recorded: the filtered 2×3090 search returns N offers (N>0 expected) past the cuda/driver gates, OR — if zero — the single offending gate is isolated and the exact one-line relaxation diff is pre-written. Cheapest dph + 43803 presence noted.</acceptance_criteria>
  <done>A2 outcome recorded; on zero-offers, the offending gate isolated and the authorized one-line relaxation diff staged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Gateway dev env set to shape A (PRIMARY_NUM_GPUS=2, PRIMARY_GPU_NAME="RTX 3090", PRIMARY_VAST_MACHINE_ALLOWLIST=43803, PRIMARY_VAST_PRICE_CAP_DPH=0.60, PRIMARY_TEMPLATE_IMAGE=Plan-02 promoted image, new whisper SHA). A2 pre-check done. If A2 returned zero offers, the pre-authorized one-line gate relaxation (single isolated gate, diff in the UAT doc) is applied to keep the UAT moving. Claude has prepared `gatewayctl primary force-up --reason "06.8 2x3090 UAT"`.</what-built>
  <how-to-verify>
    Run the live-UAT (Vast 2×3090 ~$0.29-0.40/h, per-second — force-down after):
    1-3. Pre-flight `gatewayctl primary schedule` + `state`; trigger `gatewayctl primary force-up --reason "06.8 2x3090 UAT"`.
    4. Reconciler logs show `primary offer picked` with a 2×3090 machine_id (43803 or other), num_gpus=2, dph ~$0.40.
    5. `waitForReadyOrDestroy` reaches markReady; `gatewayctl primary state` / `lifecycles` confirm Ports mapped for 8000/8001/8003/9400.
    6. Services through the mapped host ports: LLM `/v1/models` 200 (~42 tok/s on the split), STT `/v1/audio/transcriptions` 200 (proves the Plan 02 fix on 2×3090), TTS `/v1/audio/speech` 200, DCGM `/metrics` 200.
    7. SSH into the pod: `nvidia-smi` shows 2 GPUs; llama split ~8.5GB GPU0 / ~9.4GB GPU1; full stack ~27GB/48GB.
    8. Cost guard (L2): confirm MONTHLY_PRIMARY_BUDGET_BRL guard is active (gatewayctl/state or logs) AND that the broaden-search under PRICE_CAP_DPH=0.60 did NOT pick a non-3090 shape above the cap (inspect "primary offer picked" dph + gpu_name — must be ≤0.60 and RTX 3090).
    9. If a NEW 2×3090 host passed, note its machine_id to add to PRIMARY_VAST_MACHINE_ALLOWLIST (manual env catalog).
    10. `gatewayctl primary force-down` → confirm Vast destroys the instance (billing stops).
    IF A2 ZERO-OFFERS REQUIRED THE RELAXATION: confirm the applied diff is exactly one line on the single isolated gate, and that it now returns offers.
    IF UAT FAILS AFTER FORCE-UP (review L3): `gatewayctl primary force-down` + destroy the pod; if the A2 relaxation was applied to the working tree, REVERT that one-line change (it is NOT committed); record the failure. No allowlist/SEED-002 catalog on fail.
  </how-to-verify>
  <resume-signal>Type "approved" with: the picked machine_id + dph, the 4 service HTTP codes (esp STT 200), the nvidia-smi split, the cost-guard result, the A2 relaxation diff if applied, and any new allowlist machine_id — or describe the failure (triggers L3 revert).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record live-UAT results + cost guard + A2 relaxation + catalog allowlist + flag SEED-002 shape</name>
  <read_first>gateway/internal/emerg/vast/types.go (if a relaxation diff was applied); 06.8-GW-2GPU-LIVE-UAT.md (A2 section from Task 1)</read_first>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md</files>
  <action>Finalize 06.8-GW-2GPU-LIVE-UAT.md with all UAT results: picked machine_id + dph, 4-endpoint health outcome + port map, the 4 service HTTP codes, the in-pod nvidia-smi split + total VRAM, the cost-guard result (L2 — MONTHLY_PRIMARY_BUDGET_BRL active + price-cap-0.60 held on broaden, no non-3090 picked), and the destroy confirmation. If a new known-good 2×3090 host passed, append its machine_id to the documented allowlist seed (note the operator must set PRIMARY_VAST_MACHINE_ALLOWLIST=43803,<new> in the dev gateway env). Add a short "SEED-002 input" section stating the validated shape (2×RTX 3090, 48GB) is the GPU the emergency hot-standby will mirror (cross-phase output; SEED-002 runs next). Confirm explicitly in the doc that NO gateway code was changed — EXCEPT, if A2 required it, record the AUTHORIZED one-line gate relaxation (exact types.go diff + rationale: which gate, old→new value, why it isolates the zero-offers). If the UAT failed and the relaxation was reverted, document that too.</action>
  <verify>
    <automated>grep -Eq 'SEED-002|hot-standby|emergency' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq 'machine_id|allowlist' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq '/v1/audio/transcriptions|STT.*200' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq 'MONTHLY_PRIMARY_BUDGET_BRL|price cap|PRICE_CAP' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md</automated>
  </verify>
  <acceptance_criteria>UAT results recorded; cost-guard assertion (budget active + price-cap held on broaden) recorded; allowlist catalog updated with any new validated host; SEED-002 shape input documented (2×3090 48GB); explicit no-code-change confirmed OR the authorized one-line A2 relaxation diff (or its revert on fail) recorded.</acceptance_criteria>
  <done>Full UAT recorded incl. cost guard + A2 relaxation status; SEED-002 shape captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vast spot host → primary pod | rented GPU host; may be busy/preempted (spot) |
| gateway → Vast API | provision/destroy via VAST_AI_API_KEY |
| client → gateway endpoints | (out of scope — internal UAT only, no client traffic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.8-06 | Spoofing | malicious/broken Vast multi-GPU host | mitigate | allowlist (43803 + validated) + reliability gte 0.99 + machine blocklist for broken-CDI hosts; preference-pass + broaden keeps provisioning resilient. |
| T-06.8-07 | Denial of service | spot preemption mid-UAT | accept | Resilience posture (CONTEXT): interruptible pods acceptable because emergency/fallback covers gaps; allowlist is preference; broaden re-bids. |
| T-06.8-13 | Tampering | broaden-fallback picks an expensive non-3090 shape, blowing budget | mitigate | PRIMARY_VAST_PRICE_CAP_DPH=0.60 bounds broaden (FilterBelowCap); MONTHLY_PRIMARY_BUDGET_BRL guard active; both asserted live (L2). |
| T-06.8-14 | Tampering | the authorized A2 one-line relaxation widens the gate beyond intent | mitigate | Bounded to ONE line on the SINGLE isolated gate (12.8→12.6 OR 570000000→565000000); exact diff + rationale recorded; reverted (uncommitted) if the UAT fails. |
| T-06.8-08 | Info disclosure | VAST_AI_API_KEY in gateway env | accept | Key in env/GH Secret only; gatewayctl publishes events via Redis, not the key. |
| T-06.8-SC | Tampering | package installs | accept | No package installs — config-only plan; image is the Plan 02 promoted rebuild. |
</threat_model>

<verification>
- A2: filtered RTX-3090 numGPUs=2 search returns offers past the cuda/driver gates, OR the offending gate is isolated + the authorized one-line relaxation applied (Task 1 + checkpoint).
- Live force-up provisions a 2×3090 single-pod; 4-endpoint health markReady; ports mapped (checkpoint).
- LLM/STT/TTS/DCGM all 200; nvidia-smi 2-GPU split confirmed (checkpoint).
- Cost guard: budget guard active + price-cap-0.60 holds on broaden, no non-3090 picked (checkpoint + Task 2, L2).
- No gateway code change EXCEPT the authorized one-line A2 relaxation (Task 2); reverted if UAT fails (L3).
- Pod destroyed after UAT.
</verification>

<success_criteria>
- The gateway provisions + health-polls a 2×3090 single-pod end-to-end with `PRIMARY_NUM_GPUS=2` (GW-2GPU PROVEN, not assumed).
- STT 200 holds on the 2×3090 topology (cross-check of the Plan 02 promoted fix).
- A2 resolved: the filtered search returns 2×3090 offers, OR the authorized one-line gate relaxation (single isolated gate) is documented and unblocked the search.
- Cost guard verified: budget active + price cap 0.60 holds on broaden (no non-3090 shape) (L2).
- Validated shape (2×3090 48GB) recorded as the SEED-002 emergency-mirror input.
- No invented gateway code changes beyond the authorized one-line A2 relaxation; on UAT failure that relaxation is reverted and no shared state mutated (L3).
</success_criteria>

<output>
Create `.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-03-SUMMARY.md` when done. Surface the validated shape + allowlist for SEED-002, the cost-guard result, and the A2 relaxation status (none / applied one-line diff / reverted).
</output>
