---
phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix
plan: 03
type: execute
wave: 3
depends_on: [06.8-02]
files_modified:
  - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md
autonomous: false
requirements: [GW-2GPU]
must_haves:
  truths:
    - "DefaultSearchFilter for RTX 3090 numGPUs=2 returns offers THROUGH the cuda_max_good gte 12.8 + driver_vers gte 570000000 gates (Open risk A2 — the spike used MANUAL create, not the search; this is the first live step)"
    - "gatewayctl primary force-up with PRIMARY_NUM_GPUS=2 + PRIMARY_GPU_NAME=\"RTX 3090\" + PRIMARY_VAST_MACHINE_ALLOWLIST=43803 provisions a 2×3090 single-pod end-to-end via the gateway path (not manual)"
    - "The 4-endpoint health-poll (LLM /v1/models, STT /health, TTS /health, DCGM /metrics) reaches markReady with 2 GPUs and the Vast host-port mapping (8000/8001/8003/9400) works"
    - "In-pod nvidia-smi shows 2 GPUs with the llama tensor-split ~8.5GB GPU0 / ~9.4GB GPU1, full stack ~27GB/48GB"
    - "STT /v1/audio/transcriptions returns 200 on the gateway-provisioned 2×3090 pod (confirms the Plan 02 STT fix holds on this topology + image)"
    - "No gateway CODE change was introduced — only runtime env config (the path is GPU-count-agnostic per RESEARCH; inventing code changes is prohibited)"
  artifacts:
    - path: ".planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md"
      provides: "Operator-signed live-UAT record: gateway provisions+health-polls a 2×3090 single-pod"
      contains: "force-up"
  key_links:
    - from: "PRIMARY_NUM_GPUS=2 + PRIMARY_GPU_NAME=RTX 3090 env"
      to: "DefaultSearchFilter num_gpus:{eq:2} + gpu_name:{eq:RTX 3090}"
      via: "SearchOffers returns 2×3090 offers past the cuda/driver gates (A2)"
      pattern: "RTX 3090.*num_gpus.*2"
    - from: "gatewayctl primary force-up"
      to: "reconciler waitForReadyOrDestroy markReady (4 endpoints)"
      via: "inst.Ports host-port health-poll, GPU-count-agnostic"
      pattern: "markReady|primary offer picked"
---

<objective>
Live-UAT the gateway 2×3090 provisioning path (mirror of the manual 2026-05-21 spike, but driven by the GATEWAY, not manual create). The gateway code is already GPU-count-agnostic and the knobs (PRIMARY_NUM_GPUS, PRIMARY_VAST_MACHINE_ALLOWLIST) landed in commit 6f57698 — RESEARCH proves there is NO code gap. The only gaps are RUNTIME config + the unvalidated assumption A2 (does the FILTERED search return 2×3090 offers through the cuda_max_good/driver gates — the spike used manual create which bypasses those gates).

This plan touches NO gateway source — it sets dev env, runs `gatewayctl primary force-up`, and records the live assertions. It uses the image validated in Plan 02 (so the STT-200 assertion holds on the 2×3090 topology).

Purpose: Prove the 2×3090 cost/availability sweet-spot is reachable via the gateway end-to-end, and decide/catalog the GPU shape the SEED-002 emergency hot-standby will mirror (this phase runs BEFORE SEED-002).
Output: `06.8-GW-2GPU-LIVE-UAT.md` with the 9-step UAT results; allowlist seeded with any newly-validated 2×3090 machine_id.
Cost: ~$0.29–0.40/h Vast 2×3090, per-second billing — force-down/destroy after the assertions pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-RESEARCH.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-02-SUMMARY.md
@gateway/internal/emerg/vast/types.go
@gateway/internal/primary/reconciler.go

<interfaces>
<!-- The gateway knobs to SET (config.go) + the live-UAT steps. No code change. -->

Runtime env to set on the dev gateway (Portainer / .env) for shape A (2×3090):
  PRIMARY_NUM_GPUS=2
  PRIMARY_GPU_NAME="RTX 3090"
  PRIMARY_VAST_MACHINE_ALLOWLIST=43803          # spike-validated host (preference, not guarantee)
  PRIMARY_VAST_PRICE_CAP_DPH=0.60               # optional: avoids a pricey 5090 on broaden (default 2.20 also works)
  PRIMARY_TEMPLATE_IMAGE=<rebuilt image digest from Plan 02>   # carries the STT fix
  PRIMARY_WHISPER_WEIGHTS_SHA256=<new SHA from Plan 02>

gatewayctl (repo root binary): `gatewayctl primary state`, `primary schedule`,
  `primary force-up --reason "06.8 2x3090 UAT"` (bypasses cooldown), `primary force-down`,
  `primary lifecycles --format json`.

DefaultSearchFilter gates that A2 must pass: cuda_max_good gte 12.8, driver_vers gte 570000000,
  reliability gte 0.99, num_gpus eq 2, gpu_name eq "RTX 3090". (types.go L233-244.)
  Spike confirmed host 43803 boots cu128 on Ampere — but via MANUAL create, NOT the search.
  If the filtered search returns 0 offers despite inventory → relax cuda_max_good/driver surgically (Pitfall 3 / UAT 18 analogue).

9-step live-UAT (RESEARCH §"Live-UAT plan"):
  1. pre-flight: gatewayctl primary schedule + state
  2. set env (above)
  3. gatewayctl primary force-up --reason "06.8 2x3090 UAT"
  4. assert reconciler logs: "primary offer picked" machine_id (43803 or other 2×3090), num_gpus, dph ~$0.40
  5. assert 4-endpoint health → markReady; inst.Ports mapped 8000/8001/8003/9400
  6. assert services: LLM /v1/models 200 (~42 tok/s), STT /v1/audio/transcriptions 200, TTS /v1/audio/speech 200, DCGM /metrics 200
  7. assert in-pod (SSH): nvidia-smi 2 GPUs, llama split ~8.5/9.4GB, stack ~27GB/48GB
  8. catalog: if a new 2×3090 host passed, add machine_id to allowlist env (manual — auto-add is Deferred)
  9. teardown: gatewayctl primary force-down → confirm destroy (Vast stops billing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A2 pre-check — confirm the filtered search returns 2×3090 offers</name>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md</files>
  <action>BEFORE provisioning, validate Open risk A2 (the first live step): confirm the gateway's DefaultSearchFilter for RTX 3090 numGPUs=2 returns offers THROUGH the cuda_max_good gte 12.8 + driver_vers gte 570000000 gates. Run the same q-filter the gateway builds against Vast `GET /bundles` (use pod/scripts/vast-ai.sh search or a direct curl with the JSON filter mirroring types.go L233-244: gpu_name eq "RTX 3090", num_gpus eq 2, reliability gte 0.99, dph_total lte 0.60, inet_down gte 500, cuda_max_good gte 12.8, driver_vers gte 570000000, rentable eq true). Record the offer count, cheapest dph, and whether machine 43803 appears. If the count is 0 despite known 2×3090 inventory → this is the UAT 18 regression class: document which gate zeroes it (drop cuda_max_good to 12.6 or driver to 565000000 in a TEST query to isolate) and flag that the gateway default gate may need a surgical relaxation (note as a follow-up; do NOT change gateway code in this plan unless the checkpoint approves a one-line gate adjustment). Write the A2 result as the first section of 06.8-GW-2GPU-LIVE-UAT.md.</action>
  <verify>
    <automated>grep -Eq 'A2|offers? (count|returned)|num_gpus.*2' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq 'cuda_max_good|driver_vers' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md</automated>
  </verify>
  <done>A2 result recorded: the filtered 2×3090 search returns N offers (N>0 expected) past the cuda/driver gates, or the offending gate is isolated and a relaxation flagged. The cheapest dph and 43803 presence are noted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Gateway dev env set to shape A (PRIMARY_NUM_GPUS=2, PRIMARY_GPU_NAME="RTX 3090", PRIMARY_VAST_MACHINE_ALLOWLIST=43803, PRIMARY_TEMPLATE_IMAGE=Plan-02 image, new whisper SHA). A2 pre-check passed (search returns 2×3090 offers). Claude has prepared `gatewayctl primary force-up --reason "06.8 2x3090 UAT"`.</what-built>
  <how-to-verify>
    Run the 9-step live-UAT (Vast 2×3090 ~$0.29-0.40/h, per-second — force-down after):
    1-3. Pre-flight `gatewayctl primary schedule` + `state`; trigger `gatewayctl primary force-up --reason "06.8 2x3090 UAT"`.
    4. Reconciler logs show `primary offer picked` with a 2×3090 machine_id (43803 or other), num_gpus=2, dph ~$0.40.
    5. `waitForReadyOrDestroy` reaches markReady; `gatewayctl primary state` / `lifecycles` confirm Ports mapped for 8000/8001/8003/9400.
    6. Services through the mapped host ports: LLM `/v1/models` 200 (~42 tok/s on the split), STT `/v1/audio/transcriptions` 200 (proves the Plan 02 fix on 2×3090), TTS `/v1/audio/speech` 200, DCGM `/metrics` 200.
    7. SSH into the pod: `nvidia-smi` shows 2 GPUs; llama split ~8.5GB GPU0 / ~9.4GB GPU1; full stack ~27GB/48GB.
    8. If a NEW 2×3090 host passed, note its machine_id to add to PRIMARY_VAST_MACHINE_ALLOWLIST (manual env catalog).
    9. `gatewayctl primary force-down` → confirm Vast destroys the instance (billing stops).
  </how-to-verify>
  <resume-signal>Type "approved" with: the picked machine_id + dph, the 4 service HTTP codes (esp STT 200), the nvidia-smi split, and any new allowlist machine_id — or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record live-UAT results + catalog allowlist + flag SEED-002 shape</name>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md</files>
  <action>Finalize 06.8-GW-2GPU-LIVE-UAT.md with all 9-step results: picked machine_id + dph, 4-endpoint health outcome + port map, the 4 service HTTP codes, the in-pod nvidia-smi split + total VRAM, and the destroy confirmation. If a new known-good 2×3090 host passed, append its machine_id to the documented allowlist seed (and note the operator must set PRIMARY_VAST_MACHINE_ALLOWLIST=43803,<new> in the dev gateway env). Add a short "SEED-002 input" section stating the validated shape (2×RTX 3090, 48GB) is the GPU the emergency hot-standby will mirror — this is the cross-phase output (SEED-002 runs next). Confirm explicitly in the doc that NO gateway code was changed (only env), unless the A2 step required an approved one-line gate relaxation — in which case record that exact diff and rationale.</action>
  <verify>
    <automated>grep -Eq 'SEED-002|hot-standby|emergency' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq 'machine_id|allowlist' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md && grep -Eq '/v1/audio/transcriptions|STT.*200' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md</automated>
  </verify>
  <done>9-step results recorded; allowlist catalog updated with any new validated host; SEED-002 shape input documented (2×3090 48GB); explicit no-code-change (or the approved gate-relaxation diff) confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vast spot host → primary pod | rented GPU host; may be busy/preempted (spot) |
| gateway → Vast API | provision/destroy via VAST_AI_API_KEY |
| client → gateway endpoints | (out of scope — internal UAT only, no client traffic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.8-06 | Spoofing | malicious/broken Vast multi-GPU host | mitigate | allowlist (43803 + validated) + reliability gte 0.99 + machine blocklist for broken-CDI hosts; preference-pass + broaden keeps provisioning resilient. |
| T-06.8-07 | Denial of service | spot preemption mid-UAT | accept | Resilience posture (CONTEXT): interruptible pods acceptable because emergency/fallback covers gaps; allowlist is preference not guarantee; broaden-search re-bids. |
| T-06.8-08 | Info disclosure | VAST_AI_API_KEY in gateway env | accept | Key in env/GH Secret only; gatewayctl publishes events via Redis, not the key; no logging of the key. |
| T-06.8-SC | Tampering | package installs | accept | No package installs — config-only plan; image is the Plan 02 pinned rebuild. |
</threat_model>

<verification>
- A2: filtered RTX-3090 numGPUs=2 search returns offers past the cuda/driver gates (Task 1).
- Live force-up provisions a 2×3090 single-pod; 4-endpoint health markReady; ports mapped (checkpoint).
- LLM/STT/TTS/DCGM all 200 on the gateway-provisioned pod; nvidia-smi 2-GPU split confirmed (checkpoint).
- No gateway code change (or approved one-line gate relaxation documented) (Task 2).
- Pod destroyed after UAT.
</verification>

<success_criteria>
- The gateway provisions + health-polls a 2×3090 single-pod end-to-end with `PRIMARY_NUM_GPUS=2` (GW-2GPU PROVEN, not assumed).
- STT 200 holds on the 2×3090 topology (cross-check of the Plan 02 fix).
- A2 resolved: the filtered search returns 2×3090 offers (or the gate relaxation is documented).
- Validated shape (2×3090 48GB) recorded as the SEED-002 emergency-mirror input.
- No invented gateway code changes.
</success_criteria>

<output>
Create `.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-03-SUMMARY.md` when done. Surface the validated shape + allowlist for SEED-002.
</output>
