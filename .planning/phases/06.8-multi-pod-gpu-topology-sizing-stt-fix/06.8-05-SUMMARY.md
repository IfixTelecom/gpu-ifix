---
phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix
plan: 05
subsystem: infra
tags: [gateway, primary, allowlist, vast, deploy-staleness, ci-guard, 2x3090, gpu-topology]

# Dependency graph
requires:
  - phase: 06.8-03
    provides: PARTIAL UAT record + the false-positive "allowlist-steering bug" symptom that this plan diagnosed and resolved
  - phase: 06.8-02
    provides: promoted whisper SHA d8e8f6e… + :develop image carrying the supervisord HF_HUB_CACHE fix
  - phase: 06.6
    provides: Phase 6.6 primary reconciler with the allowlist-first SearchOffers pass + WithMachineAllowlist helper (commit 6f57698)
provides:
  - Diagnosis (06.8-ALLOWLIST-DIAGNOSIS.md) — proves the 06.8-03 PARTIAL was caused by stale deployed binary, NOT a Go-source bug; H1/H2 DISPROVEN with live evidence; H3 REFINED to deploy-staleness
  - CI hardening test (types_test.go composition sub-test) — guards against future refactors stripping the allowlist branch silently
  - Re-UAT PASS record on 06.8-GW-2GPU-LIVE-UAT.md — gateway-provisioned 2×3090 reaches markReady, 4 endpoints 200, STT transcription PT-BR, nvidia-smi 2-GPU split
  - Expanded known-good 2×3090 allowlist catalog: 43803 (Estonia, spike-proven 2026-05-21), 55158 (Germany, re-UAT-proven 2026-05-22)
  - Documented operational gotcha: `docker compose restart` does NOT reload .env; `docker compose up -d --force-recreate` is required for env propagation
affects: [phase-07-observability, phase-08-client-integration, phase-09-sensitive-clients, future-primary-pod-work, future-vast-host-selection]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Diagnose-before-edit gate: when a runtime symptom looks like a code bug, verify the deployed binary contains the expected symbols (strings + grep) BEFORE editing source — STATE.md 2026-05-19 Portainer redeploy gotcha can produce the same observable as a source-side bug"
    - "Composition unit test pattern for filter helpers: assert the full pipeline (DefaultSearchFilter + WithMachineAllowlist) marshals the exact JSON the consumer sends, not just the helper in isolation — catches accidental field-stripping in either function"

key-files:
  created:
    - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md
    - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-05-SUMMARY.md
  modified:
    - gateway/internal/emerg/vast/types_test.go (composition sub-test)
    - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md (PARTIAL → PASS + Re-UAT 2026-05-22 section)
    - .planning/todos/pending/260522-allowlist-steering-bug.md (RESOLVED frontmatter + resolution body)

key-decisions:
  - "Operator approved Option B (deploy + hardening test) over Option A (deploy-only) — accepted +1 CI build cycle to land a permanent regression guard against the deploy-staleness mechanic that produced this gap"
  - "Substituted re-UAT target 43803 → 55158 (Germany) when 43803 went transient rentable:false at re-UAT time. Allowlist STEERING is invariant of the specific machine_id; the substitution methodology (re-survey EXACT DefaultSearchFilter constraints, pick cheapest EU qualifying host within 2400s cold-start budget) is documented in the Re-UAT section as the standing operator playbook"
  - "Kept the bug todo in pending/ with `resolved:` frontmatter rather than moving it (no resolved/ dir in the repo). Frontmatter + body section + title prefix carry the resolution"
  - "Did NOT edit gateway/* source files — the diagnosis disproved every code-bug hypothesis. Only the test file changed under gateway/. `git diff --name-only develop~2 gateway/` returns ONLY types_test.go"

patterns-established:
  - "Pattern: dispatch gatewayctl via ephemeral docker run on the gateway's traefik-public network (debian:12-slim + /opt/ai-gateway-dev/.env env-file + mounted gatewayctl binary). Avoids needing to install Go on vps-ifix-vm and works around the distroless ai-gateway-dev container's lack of shell tooling. Reusable for any future operator-driven gateway RPC."
  - "Pattern: Portainer redeploy gotcha mitigation — after a CI build pushes a new :latest-dev tag, `docker compose up -d --force-recreate <service>` on the dev VM is the reliable way to land it. `docker compose restart` reuses the prior config and does not re-pull or re-read .env. STATE.md 2026-05-19 captured this; this phase re-confirmed it (twice in one re-UAT session)."

requirements-completed: [GW-2GPU]

# Metrics
duration: ~4h (orchestrator clock; includes operator-driven live-UAT cycles)
completed: 2026-05-22
---

# Plan 06.8-05: allowlist-steering gap closure — diagnose+fix+re-UAT — Summary

**Phase 06.8 verification gap (7/10) closed: the 06.8-03 PARTIAL "allowlist does not steer" was deploy staleness, not a source bug; CI hardening test landed; gateway-provisioned 2×3090 reached markReady + 4 endpoints 200 + 2-GPU split.**

## Performance

- **Duration:** ~4h orchestrator clock (multi-checkpoint plan with operator-driven live-UAT)
- **Started:** 2026-05-22 (orchestrator dispatch)
- **Completed:** 2026-05-22T17:31Z (force-down + lifecycle 91 destroyed)
- **Tasks:** 3 code tasks + 2 blocking operator checkpoints (decision + human-verify)
- **Files modified:** 4 (.planning artifacts + types_test.go); no production gateway source changed

## Accomplishments

- **Diagnosed the 06.8-03 PARTIAL as deploy staleness, not a Go-source bug.** `strings` on the deployed `ai-gateway-dev` binary at 06.8-03 UAT time returned 0 hits for `PRIMARY_VAST_MACHINE_ALLOWLIST` / `WithMachineAllowlist` / `PrimaryVastMachineAllowlist` / `primary allowlist exhausted`. The binary was built `2026-05-21T21:13:54Z`, ~2h20m BEFORE commit `6f57698` (the allowlist feature, 23:33Z). H1 (wire encoding) and H2 (FilterBelowCap eliminates) were DISPROVEN with live Vast API + offer-set reproduction. H3 (env empty) was REFINED to deploy-staleness — the env IS set, the binary code that reads it just was not deployed yet.
- **Landed a CI hardening test (`d689321`)** asserting the EXACT composition the reconciler sends — `DefaultSearchFilter(0.60, 0, "RTX 3090", 2, 55942, 45778) ∘ WithMachineAllowlist([43803])` must marshal `machine_id:{"in":[43803]}` AND preserve every other DefaultSearchFilter field. This guards against future refactors stripping the allowlist branch silently (the exact failure mode that produced the deploy staleness window).
- **Reached markReady end-to-end via the gateway path on a 2×3090 single-pod** (machine 55158, Germany; offer 31139421; instance 37360510). No `primary allowlist exhausted` log line, no broaden — the allowlist pass found the in-list offer and picked it. Cold-start 10m51s, well under the 2400s budget.
- **Re-confirmed Plan-02 STT fix on the gateway-provisioned 2×3090** — `POST /v1/audio/transcriptions` returned HTTP 200 with correct PT-BR transcription of `whatsapp-sample.ogg`. This proves the STT fix holds end-to-end through the gateway path (not just the Plan-02 manual-create path).
- **Confirmed 2-GPU llama tensor-split via in-pod nvidia-smi:** GPU0 ~16.2 GB, GPU1 ~9.8 GB, total stack ~26 GB / 48 GB. No CDI failures on 55158.
- **Expanded the known-good 2×3090 catalog:** 55158 (Germany, host 167329, $0.482/h, reliab 0.998, inet 939 Mbps, cuda 13.1, driver 590.48) joins 43803 (Estonia) as a re-UAT-proven safe-to-allowlist host.
- **All 3 failed VERIFICATION truths from `06.8-VERIFICATION.md` are now PASS** — see the "Three failed VERIFICATION truths — all closed" table in `06.8-GW-2GPU-LIVE-UAT.md`.

## Task Commits

Each task was committed atomically:

1. **Task 1: DIAGNOSE allowlist-steering root cause (no gateway/* edit)** — `37a91cb` (docs)
2. **Task 2 (refined): hardening composition sub-test** — `d689321` (test)
3. **Task 3: Re-UAT PARTIAL→PASS + close bug todo + SUMMARY** — this commit (docs)

_No TDD RED/GREEN cycle — Task 2 was reframed from "edit source + add test" to "add test only" after Task 1's diagnosis disproved every source-bug hypothesis. The composition sub-test passes against existing source on first run (which is the correct invariant for a defensive CI guard, not a regression-find-then-fix cycle)._

## Files Created/Modified

- `gateway/internal/emerg/vast/types_test.go` — new sub-test `TestWithMachineAllowlist/composition_DefaultSearchFilter_plus_WithMachineAllowlist`. Asserts the full filter the reconciler sends has `machine_id:{"in":[ids]}` AND every other DefaultSearchFilter field preserved. `go build ./... && go vet ./... && go test ./...` pass.
- `.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-ALLOWLIST-DIAGNOSIS.md` — root-cause statement + H1/H2/H3 evidence + timestamp chain proving deployed binary pre-dated the allowlist commit + fresh-pull verification.
- `.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-GW-2GPU-LIVE-UAT.md` — frontmatter `status: passed` + new `Re-UAT 2026-05-22 (Plan 06.8-05) — PASS` section with timeline, endpoint validations, nvidia-smi snapshot, methodology note for the 43803→55158 substitution.
- `.planning/todos/pending/260522-allowlist-steering-bug.md` — frontmatter `resolved: 2026-05-22` + title prefix `(RESOLVED)` + body section citing diagnosis doc + test commit + re-UAT evidence + updated catalog.

## Decisions Made

1. **Operator picked Option B (deploy + hardening test) over Option A (deploy-only) at the post-diagnosis checkpoint.** Rationale: the +1 CI build cycle is worth a permanent regression guard. The composition sub-test catches the exact mechanic that produced this gap (a future refactor stripping the allowlist branch from the marshaled filter would now be caught at CI, not at the next 2×3090 UAT).
2. **Substitute 43803 → 55158 at re-UAT time.** 43803 was transient `rentable:false` at re-UAT (visible offer but not currently rentable). The plan's truth #1 names 43803 specifically, but the underlying assertion is "the allowlist STEERS to the in-list host", which is invariant of which machine_id is in the list. Methodology to pick the substitute (re-survey EXACT DefaultSearchFilter constraints; pick cheapest EU qualifying host) is recorded in the Re-UAT section.
3. **No `gateway/*.go` production source edited.** `git diff --name-only develop~2 gateway/` shows ONLY `gateway/internal/emerg/vast/types_test.go`. The Task 1 acceptance criterion `git diff --quiet -- gateway/` was satisfied at diagnosis time (37a91cb adds no gateway/* file). The Task 2 plan-anticipated source edit was correctly reframed to a test-only edit after the diagnosis ruled out source bugs.
4. **Kept the bug todo in `.planning/todos/pending/`** rather than moving to a non-existent `resolved/` directory. Frontmatter `resolved:` field + title `(RESOLVED)` prefix + body `## RESOLVED` section carry the resolution unambiguously.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule: diagnose-first, no speculative edit] Task 2 reframed from source edit to test-only**
- **Found during:** Task 1 (DIAGNOSE)
- **Issue:** The plan's Task 2 anticipated a source edit to `types.go` / `reconciler.go` / `client.go` (whichever H1/H2/H3 isolated). The Task 1 diagnosis disproved every source-bug hypothesis with live evidence — the deployed binary literally lacked the allowlist symbols; the source on `develop` was already correct.
- **Fix:** Operator-approved reframing at the `checkpoint:decision` gate: Task 2 became "add the composition sub-test (CI guard) + deploy the existing develop tip" instead of "edit source + add regression test". Honors the plan's stated must_have truth ("The fix is bounded to the SINGLE cause isolated + approved in Task 1") — the isolated cause was deployment, so the deployment IS the fix; the test is the durable guard.
- **Files modified:** `gateway/internal/emerg/vast/types_test.go` only (no production source under gateway/)
- **Verification:** `git diff --name-only develop~2 gateway/` returns only `gateway/internal/emerg/vast/types_test.go`. `go build ./... && go vet ./... && go test ./...` exit 0.
- **Committed in:** `d689321`

**2. [Rule: empirical over planned] Re-UAT target host 43803 → 55158**
- **Found during:** Task ck2 (human-verify, three force-up cycles)
- **Issue:** Plan-05 frontmatter `truths[0]` names `PRIMARY_VAST_MACHINE_ALLOWLIST=43803` specifically. At re-UAT time 43803 was `rentable:false` in the Vast spot market (verified by direct `GET /bundles?q={"machine_id":{"in":[43803]},...}` probe). Two cycles with 43803 / 24953 confirmed allowlist BRANCH was executing (logged `primary allowlist exhausted; broadening` — a line the pre-fix binary NEVER emitted) but the in-list host was not available.
- **Fix:** Substituted 55158 (Germany — cheapest qualifying EU host under the EXACT DefaultSearchFilter at re-UAT time). The allowlist STEERING assertion is invariant of machine_id — 55158 demonstrates the same code path with a different in-list ID.
- **Files modified:** none (operator decision recorded in `06.8-GW-2GPU-LIVE-UAT.md` Re-UAT section)
- **Verification:** gateway picked offer 31139421 machine_id=55158 with NO `primary allowlist exhausted` line and NO broaden. lifecycle 91 reached markReady.
- **Committed in:** this commit (Re-UAT section in UAT-03 doc)

**Total deviations:** 2 (1 auto-fixed via operator-approved checkpoint reframe; 1 empirical substitution recorded in the UAT doc)
**Impact on plan:** Both deviations preserve the plan's INTENT (the must_haves truths). Truth #1 is met via 55158 instead of 43803 (allowlist STEERING proven); Truth #2 (STT 200) and Truth #3 (2-GPU nvidia-smi) are met on the gateway-provisioned 2×3090 as specified. The verification automated checks in the plan's task `<verify>` blocks all pass.

## Issues Encountered

1. **`docker compose restart` does NOT reload .env.** First re-UAT cycle (allowlist=24953 attempt) ran against the OLD env (still 43803) because the restart reused the prior container config. Resolved with `docker compose up -d --force-recreate gateway`. Updated patterns-established to capture this for future operator work.
2. **Vast spot market is transient.** 43803 was `rentable:true` at 06.8-03 PARTIAL morning + at the Plan-05 morning A2 pre-check, then went `rentable:false` by re-UAT afternoon. 24953 (cheapest qualifying EU at survey time) also flipped to `rentable:false` between survey and force-up (~30s window). Re-UATs against the Vast spot market must re-survey AT FORCE-UP TIME and may need to iterate on the in-list host.
3. **Local `gatewayctl` binary was stale** (pre-Phase 6.6, no `primary` subcommand). Rebuilt locally from `gateway/cmd/gatewayctl` and SCP'd to `/usr/local/bin/gatewayctl` on `vps-ifix-vm`. Dispatched via ephemeral `docker run --network traefik-public --env-file /opt/ai-gateway-dev/.env -v /usr/local/bin/gatewayctl:/gatewayctl:ro debian:12-slim /gatewayctl …` so the binary can resolve `infra-redis-1` on the docker network without requiring Go on the host or shell tools in the distroless ai-gateway-dev image. Pattern recorded.

## User Setup Required

None — no external service configuration required. The `.env` was restored to the pre-UAT state (`/opt/ai-gateway-dev/.env.bak-pre-06.8-05-20260522-134033` → `/opt/ai-gateway-dev/.env`); the dev gateway is back to shape pre-UAT defaults.

## Next Phase Readiness

- **Phase 06.8 verification gap is CLOSED.** All 3 failed truths from `06.8-VERIFICATION.md` are now PASS. The phase can be marked complete.
- **Standing operator allowlist for production-shape-A 2×3090:** `PRIMARY_VAST_MACHINE_ALLOWLIST=43803,55158`. Both proven on a live gateway-provisioned pod. Blocklist unchanged (`55942,45778`).
- **For Phase 07 (observability):** the markReady → ready FSM transition + `primary_ready observed` log + `dcgm_*` metrics from 9400 are confirmed working on a 2×3090 topology — dashboards and alerts built against these signals are validated.
- **For Phase 08/09 (client integration):** the Plan-02 STT fix is now triply confirmed (manual-create pod in Plan-02; gateway-provisioned 2×3090 in Plan 06.8-05). Clients can route STT to the primary path on a 2×3090 shape with confidence.
- **No blockers carried forward.**

---
*Phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix*
*Plan: 05*
*Completed: 2026-05-22*
