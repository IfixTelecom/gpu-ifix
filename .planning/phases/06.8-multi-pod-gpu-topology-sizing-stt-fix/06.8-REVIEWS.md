# Phase 06.8 — Cross-AI Plan Reviews

**Generated:** 2026-05-21
**Reviewers:** Gemini CLI, Codex CLI (Claude skipped — self, running as orchestrator)
**Plans reviewed:** 06.8-01..04

## Verdict summary

| Reviewer | Overall risk | HIGH concerns | MEDIUM | LOW |
|----------|--------------|---------------|--------|-----|
| Gemini | LOW | 0 | 1 | 2 |
| Codex | MEDIUM | 2 | 4 | 3 |

Both agree the technical direction is sound (STT fix = HF-hub-cache + `HF_HUB_CACHE`; gateway 2-GPU = runtime config, no new code; runbook ladder = right call). Concerns are **operational sequencing**, not architecture.

## Convergent concerns (both reviewers)

1. **SHA propagation timing / drift.** After re-uploading the whisper tarball, the new `PRIMARY_WHISPER_WEIGHTS_SHA256` must reach the gateway env (Portainer) before any auto-provision/smoke, else pods fail `sha256sum -c`. Gemini: pair with `gatewayctl primary force-down`. Codex: pass SHA explicitly to the validation pod.
2. **A2 gate relaxation handling.** If the 2×3090 search returns zero offers through `cuda_max_good`/`driver_vers` gates, plans only flag for follow-up. Gemini: authorize an immediate approved one-line relaxation to keep UAT moving. Codex: stop Plan 03 and open a separate gate-change plan with tests. (Divergent remedy — operator picks.)

## Codex — HIGH (actionable before execute)

- **H1: Plan 01 says changes are "committed" before live validation** → conflicts with the no-blind-commit-to-dev rule. Reword: Plan 01 prepares local patch only; commit/merge is conditional on Plan 02 passing.
- **H2: Plan 02 overwrites the existing MinIO whisper key (`whisper-large-v3/v1.0.0/model.tar.gz`) before validation.** Mutates a shared artifact. Fix: upload to a CANDIDATE key (e.g. `…/v1.0.0-hf-cache-candidate/model.tar.gz`), validate with explicit `WEIGHTS_WHISPER_KEY`, promote only after transcription 200.

## Codex — MEDIUM

- M1: Plan 02 builds image from `develop`, but Plan 01 may not be pushed → either workflow lacks `HF_HUB_CACHE`, or pushing-before-validation weakens the rule. Define a validation branch/image-tag path.
- M2: "Do NOT update GH Secrets yet" but the live pod needs the new SHA — state explicitly that the manual create passes `PRIMARY_WHISPER_WEIGHTS_SHA256` directly.
- M3: Plan 03 "no code changes" but allows a one-line gate relaxation — breaks scope; prefer a stop + follow-up plan.
- M4: Plan 04 (wave 1) references Plan 03 (wave 3) outputs (`06.8-GW-2GPU-LIVE-UAT.md`) that won't exist yet — seed with 43803 + amend later; call it out.

## Codex — LOW

- L1: STT validation should also capture speaches startup/restart logs (delayed model-load) + `/health`.
- L2: Cost-guard validation only implicit — confirm budget guard + that broaden-fallback can't pick an expensive non-3090 shape under the same env.
- L3: Cleanup/revert path underspecified if Plan 02/03 validation fails after upload/build.

## Gemini — MEDIUM / LOW

- [MED] SHA propagation race (see convergent #1).
- [LOW] A2 gate relaxation stall (see convergent #2).
- [LOW] `upload-weights.sh` re-downloads whole whisper model — acceptable for a one-time migration.

## Gemini — suggestions

- Pair SHA propagation with `gatewayctl primary force-down`.
- Authorize proactive one-line gate relaxation in Plan 03 if A2 zero-offers.
- Add per-file `sha256sum` checks in `upload-weights.sh` before bundling.

## Recommended action

Replan to fold in the HIGH + convergent items: `/gsd:plan-phase --reviews 06.8`.
Priority edits: H2 candidate-key (don't mutate shared artifact), H1 wording (no commit before Plan 02 passes), M1 validation-branch image, SHA-propagation step, A2 decision (relax-inline vs stop+follow-up — operator choice).

---

## Raw — Gemini

(see `/tmp/gsd-review-gemini-06.8.md`)

## Raw — Codex

(see `/tmp/gsd-review-codex-06.8.md`)
