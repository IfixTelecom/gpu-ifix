---
phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix
plan: 02
type: execute
wave: 2
depends_on: [06.8-01]
files_modified:
  - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md
autonomous: false
requirements: [STT-FIX]
must_haves:
  truths:
    - "The candidate whisper tarball is uploaded to the CANDIDATE MinIO key whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz — the canonical whisper-large-v3/v1.0.0/model.tar.gz is NOT mutated until the live transcription passes (review H2)"
    - "The validation image carries Plan 01's supervisord.conf HF_HUB_CACHE change WITHOUT merging to develop first — built from a short-lived validation branch (validate/06.8-stt) pushed only for the build, tagged on GHCR (e.g. :stt-validate-<sha>) and consumed by the pod via PRIMARY_TEMPLATE_IMAGE (review M1)"
    - "A live pod boots the validation image and speaches resolves the local whisper model — no CacheNotFound, no MODEL_CARD_DOESNT_EXISTS, no 404 not-installed"
    - "POST /v1/audio/transcriptions with model=Systran/faster-whisper-large-v3 returns 200 + transcribed text on the live pod"
    - "whisper loads on GPU (not CPU) — confirmed via dcgm/nvidia-smi VRAM delta during transcription"
    - "The validation pod receives the candidate tarball SHA DIRECTLY on the create command (PRIMARY_WHISPER_WEIGHTS_SHA256 + PRIMARY_WHISPER_WEIGHTS_KEY=candidate via env-file), NOT via GH Secrets (review M2). GH Secrets + dev-stack env are updated only in the post-validation promotion step."
    - "PROMOTION (candidate→canonical) happens ONLY after the checkpoint passes, as an ordered sequence: (a) copy candidate key→canonical key, (b) update GH Secrets PRIMARY_WHISPER_WEIGHTS_SHA256 + WEIGHTS_WHISPER_SHA256, (c) update dev-stack gateway Portainer env PRIMARY_WHISPER_WEIGHTS_SHA256, (d) gatewayctl primary force-down so the next provision picks up the new SHA cleanly (Convergent #1)"
    - "ONLY after the checkpoint passes is the Plan 01 working-tree change committed/merged to develop (review H1)"
    - "Whether an arbitrary snapshot <commit_hash> is accepted (A1) is recorded empirically; if real hash required, that is documented"
    - "silero v5 VAD assets (vendored ONNX, Dockerfile L177-182) confirmed present — transcription depends on them (UAT 15)"
    - "If validation FAILS after upload/build, the cleanup/revert path leaves NO poisoned shared state: candidate key stays unpromoted, validation branch/image is discarded, pod destroyed, develop never receives the commit (review L3)"
  artifacts:
    - path: ".planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md"
      provides: "Operator-signed live-pod validation record for the STT fix (the CLAUDE.md anti-blind-commit gate)"
      contains: "transcriptions"
  key_links:
    - from: "validation image (validate/06.8-stt) + candidate tarball"
      to: "live pod /v1/audio/transcriptions 200"
      via: "HF_HUB_CACHE resolves /weights/whisper/models--…/snapshots/<hash>/"
      pattern: "200"
    - from: "candidate tarball SHA"
      to: "validation pod create env-file (PRIMARY_WHISPER_WEIGHTS_SHA256 + PRIMARY_WHISPER_WEIGHTS_KEY=candidate)"
      via: "SHA passed directly on create, NOT GH Secrets (M2)"
      pattern: "PRIMARY_WHISPER_WEIGHTS_SHA256"
    - from: "promotion sequence (a→d)"
      to: "canonical key + GH Secrets + dev-stack env + force-down"
      via: "ordered post-validation steps so onstart sha256sum -c never drifts (Convergent #1)"
      pattern: "force-down"
---

<objective>
The MANDATORY live-pod validation gate for the STT fix (CLAUDE.md debugger rule: image/script-level fixes MUST be validated on a live pod before committing to dev — no blind speculative edits to prod). Upload the regenerated tarball to a CANDIDATE key, build a validation image carrying Plan 01's supervisord change WITHOUT touching develop, spin ONE live pod, prove `/v1/audio/transcriptions` works end-to-end, then — and only then — PROMOTE (candidate→canonical), propagate the SHA in order, and commit Plan 01 to develop.

This plan touches NO source code — it executes the operator pipeline (candidate upload, validation-branch image build, SHA-to-pod, promotion, develop commit) and records the live results.

**Review-driven sequencing (H1/H2/M1/M2/Convergent #1):**
- Plan 01's changes are still LOCAL (uncommitted). This plan builds + validates them off-develop, then commits to develop ONLY on pass.
- The candidate tarball goes to `whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz`; canonical `…/v1.0.0/model.tar.gz` is mutated ONLY in the post-pass promotion step.
- The SHA reaches the validation pod DIRECTLY on create (env-file), not GH Secrets.

Purpose: De-risk the STT fix on real hardware (resolves Open Q1 / A1 snapshot-hash; A3 HF_HUB_CACHE honored) without poisoning any shared artifact or auto-deploying an unproven fix.
Output: `06.8-STT-LIVE-VALIDATION.md` with the live assertions + new SHA + promotion log; canonical key + GH Secrets + dev-stack env updated only post-pass; develop commit only post-pass.
Cost: ~$0.13–0.40/h Vast pod, per-second billing — destroy immediately after the assertions pass (or fail — see L3 cleanup).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-RESEARCH.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-01-SUMMARY.md
@pod/scripts/upload-weights.sh
@pod/scripts/vast-ai.sh
@.github/workflows/build-primary-pod.yml

<interfaces>
<!-- Operator tooling + the live-validation checklist (RESEARCH "What the executor DEVE validar AO VIVO"). -->

MinIO creds (CLAUDE.md): MINIO_ENDPOINT=https://s3.ifixtelecom.com.br MINIO_BUCKET=ai-gateway
  MINIO_ACCESS_KEY/MINIO_SECRET_KEY in CLAUDE.md.
  Candidate upload: ./pod/scripts/upload-weights.sh --weights-version v1.0.0-hf-cache-candidate
    → writes candidate key whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz, prints WEIGHTS_WHISPER_SHA256.
  Canonical key (DO NOT overwrite until promotion): whisper-large-v3/v1.0.0/model.tar.gz.
  Promote (post-pass): mc cp ifix/ai-gateway/whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz \
    ifix/ai-gateway/whisper-large-v3/v1.0.0/model.tar.gz (and the .sha256 sidecar).
Vast: VAST_AI_API_KEY in CLAUDE.md / GH Secret. ./pod/scripts/vast-ai.sh search|create|destroy.
  create takes --offer-id --image --env-file --onstart → the candidate SHA + candidate KEY go in --env-file
  (PRIMARY_WHISPER_WEIGHTS_SHA256=<candidate sha>, PRIMARY_WHISPER_WEIGHTS_KEY=whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz),
  NOT GH Secrets (M2).
Validation image (M1): create a short-lived branch validate/06.8-stt off the local Plan-01 working tree,
  push ONLY to trigger build-primary-pod.yml, which builds+pushes ghcr.io/ifixtelecom/converseai-primary-pod.
  Tag/capture the pushed digest (or a candidate tag :stt-validate-<sha>) and pass it as PRIMARY_TEMPLATE_IMAGE
  on create. develop is NOT touched. After validation, delete the validation branch.
Pod SSH: set POD_DEBUG_SSH_PUBLIC_KEY env on create → SSH via public_ipaddr + mapped host port for container 22.

Live validation checklist (the asserts the executor MUST run on the pod, RESEARCH §"O que o executor DEVE validar" + review L1):
  1. ls /weights/whisper/models--Systran--faster-whisper-large-v3/snapshots/*/ → 4-5 files + README.md
  2. curl localhost:8001/v1/models → whisper listed as local model
  3. POST /v1/audio/transcriptions (model=Systran/faster-whisper-large-v3) → 200 + text (use pod/smoke/smoke.py:262 sample)
  4. dcgm/nvidia-smi → whisper VRAM on GPU (not CPU)
  5. silero v5 ONNX assets present (Dockerfile L177-182)
  6. record whether the arbitrary <commit_hash> was accepted, or the real HF hash was required (A1)
  7. (L1) capture speaches startup/restart logs (supervisord [program:speaches] — delayed model-load) + curl localhost:8001/health
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upload candidate tarball + build validation image off-develop</name>
  <read_first>pod/scripts/upload-weights.sh (candidate-key support from Plan 01); pod/scripts/vast-ai.sh (create --image --env-file); .github/workflows/build-primary-pod.yml (push trigger + image name); 06.8-01-SUMMARY.md (commit hash + uncommitted-changes note)</read_first>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md</files>
  <action>(1) CANDIDATE UPLOAD (H2): run `./pod/scripts/upload-weights.sh --weights-version v1.0.0-hf-cache-candidate` (MinIO creds from CLAUDE.md). This writes the CANDIDATE key `whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz` and prints WEIGHTS_WHISPER_SHA256 (the candidate SHA). The canonical `whisper-large-v3/v1.0.0/model.tar.gz` is NOT touched. Capture the candidate SHA. (2) VALIDATION IMAGE (M1): create a short-lived branch `validate/06.8-stt` from the current local working tree (which carries Plan 01's uncommitted supervisord.conf HF_HUB_CACHE change + upload-weights.sh change), commit the Plan-01 changes onto THAT branch only, and push `validate/06.8-stt` (NOT develop) to trigger build-primary-pod.yml. Wait for the run (`gh run watch`), capture the pushed image digest (or tag it :stt-validate-<sha>). develop receives NOTHING here. (3) Record both the candidate SHA and the validation image digest in 06.8-STT-LIVE-VALIDATION.md under a "Build artifacts" section, plus the validation branch name (so cleanup can delete it). Do NOT update GH Secrets or canonical key — promotion is Task 3, post-pass.</action>
  <verify>
    <automated>grep -q 'WEIGHTS_WHISPER_SHA256' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md && grep -q 'v1.0.0-hf-cache-candidate' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md && grep -Eq 'converseai-primary-pod@sha256:|stt-validate-|image digest' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md && grep -q 'validate/06.8-stt' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>Candidate tarball uploaded to the candidate key (canonical untouched, candidate SHA recorded); validation image built+pushed from validate/06.8-stt (develop untouched); candidate SHA + image digest + branch name captured in the doc.</acceptance_criteria>
  <done>Candidate key + validation image exist off-develop; no canonical mutation, no develop commit, no GH Secret change yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Candidate HF-cache-layout whisper tarball at whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz + validation image (from validate/06.8-stt) carrying HF_HUB_CACHE=/weights/whisper. Claude has prepared the create command: ./pod/scripts/vast-ai.sh create with PRIMARY_TEMPLATE_IMAGE=<validation digest>, and an --env-file passing PRIMARY_WHISPER_WEIGHTS_KEY=whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz + PRIMARY_WHISPER_WEIGHTS_SHA256=<candidate SHA> DIRECTLY (M2 — not GH Secrets), MinIO env, POD_DEBUG_SSH_PUBLIC_KEY for in-pod inspection.</what-built>
  <how-to-verify>
    Spin ONE pod (Claude runs `./pod/scripts/vast-ai.sh create …` against a 2×3090 or any qualifying GPU; per-second billing ~$0.13-0.40/h — destroy after). Once the 4 services boot, run the live checklist and confirm each:
    1. SSH in: `ls /weights/whisper/models--Systran--faster-whisper-large-v3/snapshots/*/` shows model.bin/config.json/tokenizer.json/preprocessor_config.json + README.md.
    2. `curl localhost:8001/v1/models` lists the whisper model (local resolution worked → no CacheNotFound).
    3. `POST localhost:8001/v1/audio/transcriptions` with model=Systran/faster-whisper-large-v3 (smoke.py:262 sample WAV) returns HTTP 200 + transcribed text — NOT 404 "not installed", NOT 500 card-missing, NOT CacheNotFound.
    4. `nvidia-smi` / dcgm shows whisper VRAM on a GPU during the transcription (GPU offload, not CPU fallback).
    5. silero v5 VAD ONNX assets present in the image (Dockerfile L177-182 paths).
    6. Note whether the arbitrary <commit_hash> was accepted, or speaches/HF demanded the real hash (A1 — record the empirical answer).
    7. (L1) Capture the speaches startup/restart logs (`supervisorctl tail -f speaches` or /var/log) — confirm the model-load completed (delayed load) — and `curl localhost:8001/health` returns healthy.
    DESTROY the pod (`./pod/scripts/vast-ai.sh destroy <id>`) the moment the checklist passes — Vast stops billing on destroy.
    IF ANY ASSERT FAILS: do NOT promote. Trigger the L3 cleanup (Task 3 rollback branch): leave candidate key unpromoted, delete the validate/06.8-stt branch + image tag, destroy the pod, develop stays untouched.
  </how-to-verify>
  <resume-signal>Type "approved" with the results (esp. transcription HTTP code, /health, speaches model-load log, the A1 hash answer), or describe what failed (triggers the L3 cleanup path).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Promote candidate→canonical (ordered), propagate SHA, commit Plan 01 to develop — OR run cleanup on fail</name>
  <read_first>pod/scripts/upload-weights.sh; 06.8-01-SUMMARY.md (uncommitted Plan-01 changes); CLAUDE.md (GH PAT for develop push, gh secret set, no-blind-commit rule)</read_first>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md</files>
  <action>BRANCH ON THE CHECKPOINT OUTCOME.

  ON PASS — run the ordered promotion sequence (Convergent #1, exact order matters):
  (a) PROMOTE the candidate to canonical: `mc cp ifix/ai-gateway/whisper-large-v3/v1.0.0-hf-cache-candidate/model.tar.gz ifix/ai-gateway/whisper-large-v3/v1.0.0/model.tar.gz` AND copy the .sha256 sidecar so canonical now serves the validated tarball.
  (b) Update GH Secrets to the new SHA: `gh secret set PRIMARY_WHISPER_WEIGHTS_SHA256 -R IfixTelecom/gpu-ifix` and `gh secret set WEIGHTS_WHISPER_SHA256 -R IfixTelecom/gpu-ifix` (the smoke.yml consumer).
  (c) Update the dev-stack gateway Portainer env `PRIMARY_WHISPER_WEIGHTS_SHA256` to the new SHA (record as the operator action if Portainer access is not available from here — it MUST happen before the next provision/smoke or onstart sha256sum -c fails closed).
  (d) `gatewayctl primary force-down` so the next provision picks up the new SHA cleanly (Gemini suggestion — avoids a running pod racing the env update).
  THEN commit Plan 01 to develop (H1): merge/cherry-pick the validate/06.8-stt commit (upload-weights.sh + supervisord.conf) into develop and push (this is the now-validated, allowed commit; Actions+Portainer will redeploy the dev stack with the fix). Delete the validate/06.8-stt branch.
  Finalize 06.8-STT-LIVE-VALIDATION.md: the checklist results, HTTP codes, /health + speaches model-load log, the A1 hash answer, the candidate SHA, the canonical-promotion confirmation, the (a)-(d) ordered-step log, the develop commit SHA, and the destroyed pod id.

  ON FAIL — run the L3 cleanup/revert (leave NO poisoned shared state):
  - Do NOT promote: canonical `whisper-large-v3/v1.0.0/model.tar.gz` is left as-is (unmutated); the candidate key may be left in place or deleted (`mc rm`) — it is NOT a consumer default, so it harms nothing.
  - Discard the validation image/branch: delete the `validate/06.8-stt` branch (and the :stt-validate-<sha> tag if pushed).
  - Destroy the validation pod (`./pod/scripts/vast-ai.sh destroy <id>`).
  - Do NOT touch GH Secrets, dev-stack env, or develop.
  - Record the failure mode in 06.8-STT-LIVE-VALIDATION.md and stop (re-plan needed before retry).</action>
  <verify>
    <automated>grep -Eq '/v1/audio/transcriptions.*200|transcription.*200|VALIDATION FAILED|cleanup' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md && grep -Eq 'A1|commit_hash|arbitrary hash' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md && grep -Eq 'promote|canonical|force-down|cleanup|unpromoted' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>ON PASS: candidate promoted to canonical, GH Secrets + dev-stack env updated, force-down issued, Plan 01 committed to develop, doc records the ordered (a)-(d) steps + transcription 200 + A1. ON FAIL: canonical unmutated, validation branch/image discarded, pod destroyed, develop/Secrets/env untouched, failure recorded.</acceptance_criteria>
  <done>Exactly one branch executed cleanly; no SHA drift on pass; no poisoned shared state on fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator host → MinIO/GHCR | uploads candidate tarball + pushes validation image (creds in env) |
| gateway/onstart → MinIO | pod fetches tarball; sha256sum -c gates integrity |
| Vast host → pod | rented spot host runs the inference stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.8-03 | Tampering | SHA drift between tarball and env | mitigate | Ordered promotion (a) canonical copy → (b) GH Secrets → (c) dev-stack env → (d) force-down ensures the env SHA is updated before the next provision; onstart sha256sum -c fails closed on any mismatch (Convergent #1). |
| T-06.8-11 | Tampering | mutating the shared canonical whisper key before the fix is proven | mitigate | Candidate key isolates the unvalidated artifact (H2); canonical is copied only post-pass; consumers keep serving the old valid tarball until promotion. |
| T-06.8-12 | Tampering | unvalidated fix auto-deployed via a premature develop commit | mitigate | Validation image built from validate/06.8-stt (M1); develop commit only post-pass (H1); on fail, develop is never touched (L3). |
| T-06.8-04 | Info disclosure | MinIO/Vast/GH creds during operator run | accept | Creds via env only; upload-weights.sh logs no creds; vast-ai.sh masks; gh secret set does not echo values; SHA passed via env-file not Secrets pre-pass (M2). |
| T-06.8-05 | Spoofing | malicious Vast host | accept | Single-use validation pod, destroyed immediately; no client traffic. |
| T-06.8-SC | Tampering | package installs | accept | No new package installs (image rebuild uses pinned speaches/faster-whisper/llama.cpp digests). |
</threat_model>

<verification>
- Candidate tarball uploaded to candidate key; canonical untouched (Task 1).
- Validation image built from validate/06.8-stt; develop untouched (Task 1).
- Live pod: cache layout, /v1/models lists whisper, transcriptions 200, GPU offload, silero present, /health + speaches model-load log, A1 answered, SHA passed via env-file (checkpoint).
- ON PASS: ordered promotion (a)-(d) + develop commit (Task 3). ON FAIL: L3 cleanup, no shared-state mutation (Task 3).
</verification>

<success_criteria>
- `/v1/audio/transcriptions` returns 200 on a live pod running the validation image — the STT fix is PROVEN, not assumed.
- The shared canonical whisper key is mutated ONLY after the live pass (H2); develop is committed ONLY after the live pass (H1).
- New whisper SHA propagated in order so sha256sum -c never drifts (Convergent #1).
- The SHA reached the validation pod via env-file, not GH Secrets (M2).
- A1 (snapshot hash) resolved empirically and documented.
- On failure, no poisoned shared state (L3); pod destroyed (no lingering Vast charges).
</success_criteria>

<output>
Create `.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-02-SUMMARY.md` when done. Surface: new whisper SHA, validation image digest, the transcription HTTP result, the A1 hash finding, the promotion/cleanup branch taken, and (on pass) the develop commit SHA (Plan 03's live-UAT asserts STT 200 against the promoted image).
</output>
