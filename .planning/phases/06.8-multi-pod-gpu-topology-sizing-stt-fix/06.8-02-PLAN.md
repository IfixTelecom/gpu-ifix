---
phase: 06.8-multi-pod-gpu-topology-sizing-stt-fix
plan: 02
type: execute
wave: 2
depends_on: [06.8-01]
files_modified:
  - .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md
autonomous: false
requirements: [STT-FIX]
must_haves:
  truths:
    - "A live pod boots the rebuilt converseai-primary-pod image (with the HF_HUB_CACHE fix) and speaches resolves the local whisper model — no CacheNotFound, no MODEL_CARD_DOESNT_EXISTS, no 404 not-installed"
    - "POST /v1/audio/transcriptions with model=Systran/faster-whisper-large-v3 returns 200 + transcribed text on the live pod"
    - "whisper loads on GPU (not CPU) — confirmed via dcgm/nvidia-smi VRAM delta during transcription"
    - "The new whisper tarball SHA is propagated to PRIMARY_WHISPER_WEIGHTS_SHA256 + WEIGHTS_WHISPER_SHA256 (GH Secrets) so onstart sha256sum -c passes"
    - "Whether an arbitrary snapshot <commit_hash> is accepted (A1) is recorded empirically; if real hash required, that is documented"
    - "silero v5 VAD assets (vendored ONNX, Dockerfile L177-182) confirmed present — transcription depends on them (UAT 15)"
  artifacts:
    - path: ".planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md"
      provides: "Operator-signed live-pod validation record for the STT fix (the CLAUDE.md anti-blind-commit gate)"
      contains: "transcriptions"
  key_links:
    - from: "rebuilt image + re-uploaded tarball"
      to: "live pod /v1/audio/transcriptions 200"
      via: "HF_HUB_CACHE resolves /weights/whisper/models--…/snapshots/<hash>/"
      pattern: "200"
    - from: "upload-weights.sh new WHISPER_SHA"
      to: "PRIMARY_WHISPER_WEIGHTS_SHA256 + WEIGHTS_WHISPER_SHA256 GH Secrets"
      via: "operator updates secrets; onstart sha256sum -c gate passes"
      pattern: "WEIGHTS_WHISPER_SHA256"
---

<objective>
The MANDATORY live-pod validation gate for the STT fix (CLAUDE.md debugger rule: image/script-level fixes MUST be validated on a live pod before committing to dev — no blind speculative edits to prod). Re-upload the regenerated tarball, rebuild + repush the primary-pod image, spin ONE live pod, prove `/v1/audio/transcriptions` works end-to-end, then propagate the new SHA. Only after this passes is the Plan 01 change trusted for the dev stack.

This plan touches NO source code — it executes the operator pipeline (upload-weights.sh re-run, image rebuild, SHA propagation) and records the live results in a validation doc.

Purpose: De-risk the STT fix on real hardware (resolves Open Q1 / Assumption A1 — snapshot hash; A3 — HF_HUB_CACHE honored).
Output: `06.8-STT-LIVE-VALIDATION.md` with the 6 live assertions + new SHA recorded; GH Secrets updated.
Cost: ~$0.13–0.40/h Vast pod, per-second billing — destroy immediately after the assertions pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-RESEARCH.md
@.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-01-SUMMARY.md
@pod/scripts/upload-weights.sh
@pod/scripts/vast-ai.sh
@.github/workflows/build-primary-pod.yml

<interfaces>
<!-- Operator tooling + the live-validation checklist (RESEARCH "What the executor DEVE validar AO VIVO"). -->

MinIO creds (CLAUDE.md): MINIO_ENDPOINT=https://s3.ifixtelecom.com.br MINIO_BUCKET=ai-gateway
  MINIO_ACCESS_KEY/MINIO_SECRET_KEY in CLAUDE.md. Re-run: ./pod/scripts/upload-weights.sh prints WEIGHTS_WHISPER_SHA256.
Vast: VAST_AI_API_KEY in CLAUDE.md / GH Secret. ./pod/scripts/vast-ai.sh search|create|destroy.
Image: ghcr.io/ifixtelecom/converseai-primary-pod — rebuild via `gh workflow run build-primary-pod.yml`
  (or the workflow's push trigger on pod/primary/** + pod/scripts/**). Tag the rebuilt SHA for PRIMARY_TEMPLATE_IMAGE.
Pod SSH: set POD_DEBUG_SSH_PUBLIC_KEY env on create → SSH via public_ipaddr + mapped host port for container 22.

Live validation checklist (the 6 asserts the executor MUST run on the pod, RESEARCH §"O que o executor DEVE validar"):
  1. ls /weights/whisper/models--Systran--faster-whisper-large-v3/snapshots/*/ → 4-5 files + README.md
  2. curl localhost:8001/v1/models → whisper listed as local model
  3. POST /v1/audio/transcriptions (model=Systran/faster-whisper-large-v3) → 200 + text (use pod/smoke/smoke.py:262 sample)
  4. dcgm/nvidia-smi → whisper VRAM on GPU (not CPU)
  5. silero v5 ONNX assets present (Dockerfile L177-182)
  6. record whether the arbitrary <commit_hash> was accepted, or the real HF hash was required (A1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-upload regenerated whisper tarball + rebuild/repush primary-pod image</name>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md</files>
  <action>Run `./pod/scripts/upload-weights.sh` (MinIO creds from CLAUDE.md, --weights-version v1.0.0 so the key matches WEIGHTS_WHISPER_KEY default `whisper-large-v3/v1.0.0/model.tar.gz`). Capture the printed WEIGHTS_WHISPER_SHA256 (the new tarball's SHA). Trigger the image rebuild: `gh workflow run build-primary-pod.yml` (the supervisord.conf change from Plan 01 is on develop; the workflow builds + pushes ghcr.io/ifixtelecom/converseai-primary-pod). Wait for the run to succeed (`gh run watch`), capture the pushed image digest. Record both (new SHA, image digest) in 06.8-STT-LIVE-VALIDATION.md under a "Build artifacts" section. Do NOT yet update GH Secrets — that happens after the pod passes (Task 3), so a bad tarball cannot poison the dev stack mid-validation.</action>
  <verify>
    <automated>grep -q 'WEIGHTS_WHISPER_SHA256' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md && grep -Eq 'converseai-primary-pod@sha256:|image digest' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md</automated>
  </verify>
  <done>New whisper tarball uploaded (same key, new SHA recorded); primary-pod image rebuilt+pushed with the HF_HUB_CACHE change; both the new SHA and image digest captured in the validation doc.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Re-uploaded HF-cache-layout whisper tarball + rebuilt converseai-primary-pod image carrying HF_HUB_CACHE=/weights/whisper. Claude has prepared the create command (vast-ai.sh create with the rebuilt image digest as PRIMARY_TEMPLATE_IMAGE, PRIMARY_WHISPER_WEIGHTS_SHA256=<new SHA>, MinIO env, POD_DEBUG_SSH_PUBLIC_KEY for in-pod inspection).</what-built>
  <how-to-verify>
    Spin ONE pod (Claude runs `./pod/scripts/vast-ai.sh create …` against a 2×3090 or any qualifying GPU; per-second billing ~$0.13-0.40/h — destroy after). Once the 4 services boot, run the 6-item live checklist and confirm each:
    1. SSH in: `ls /weights/whisper/models--Systran--faster-whisper-large-v3/snapshots/*/` shows model.bin/config.json/tokenizer.json/preprocessor_config.json + README.md.
    2. `curl localhost:8001/v1/models` lists the whisper model (local resolution worked → no CacheNotFound).
    3. `POST localhost:8001/v1/audio/transcriptions` with model=Systran/faster-whisper-large-v3 (smoke.py:262 sample WAV) returns HTTP 200 + transcribed text — NOT 404 "not installed", NOT 500 card-missing, NOT CacheNotFound.
    4. `nvidia-smi` / dcgm shows whisper VRAM on a GPU during the transcription (GPU offload, not CPU fallback).
    5. silero v5 VAD ONNX assets present in the image (Dockerfile L177-182 paths).
    6. Note whether the arbitrary <commit_hash> was accepted, or speaches/HF demanded the real hash (A1 — record the empirical answer).
    DESTROY the pod (`./pod/scripts/vast-ai.sh destroy <id>`) the moment the checklist passes — Vast stops billing on destroy.
  </how-to-verify>
  <resume-signal>Type "approved" with the 6 results (esp. transcription HTTP code + the A1 hash answer), or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Propagate new SHA + record validation results</name>
  <files>.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md</files>
  <action>ONLY after the checkpoint passes: update the GH Secrets `PRIMARY_WHISPER_WEIGHTS_SHA256` and `WEIGHTS_WHISPER_SHA256` to the new SHA (`gh secret set PRIMARY_WHISPER_WEIGHTS_SHA256 -R IfixTelecom/gpu-ifix`, same for WEIGHTS_WHISPER_SHA256 used by smoke.yml). Note the dev-stack gateway env `PRIMARY_WHISPER_WEIGHTS_SHA256` (Portainer) ALSO needs the new value — record this as an operator action in the doc (do not assume Portainer access from here unless available). Finalize 06.8-STT-LIVE-VALIDATION.md: the 6 checklist results, the HTTP codes observed, the A1 hash answer, the new SHA, the image digest used, and the destroyed pod id. If A1 said the real hash was required, note that upload-weights.sh already used the real hash (Plan 01) so no rework — but document it.</action>
  <verify>
    <automated>grep -Eq '/v1/audio/transcriptions.*200|transcription.*200' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md && grep -Eq 'A1|commit_hash|arbitrary hash' .planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-STT-LIVE-VALIDATION.md</automated>
  </verify>
  <done>GH Secrets updated to the new whisper SHA; dev-stack env update flagged as operator action; validation doc records all 6 results incl. transcription 200 and the A1 hash finding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator host → MinIO/GHCR | re-uploads tarball + pushes image (creds in env) |
| gateway/onstart → MinIO | pod fetches tarball; sha256sum -c gates integrity |
| Vast host → pod | rented spot host runs the inference stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.8-03 | Tampering | SHA drift between tarball and env | mitigate | New SHA propagated to BOTH PRIMARY_WHISPER_WEIGHTS_SHA256 and WEIGHTS_WHISPER_SHA256 AFTER the live pod passes; onstart sha256sum -c fails closed if mismatched. |
| T-06.8-04 | Info disclosure | MinIO/Vast/GH creds during operator run | accept | Creds via env only; upload-weights.sh logs no creds; vast-ai.sh masks; gh secret set does not echo values. |
| T-06.8-05 | Spoofing | malicious Vast host | accept | Single-use validation pod, destroyed immediately; no client traffic; allowlist + reliability gate apply for production provision (Plan 03). |
| T-06.8-SC | Tampering | package installs | accept | No new package installs (image rebuild uses pinned speaches/faster-whisper/llama.cpp digests). |
</threat_model>

<verification>
- New whisper SHA captured and image rebuilt (Task 1).
- Live pod: ls cache layout, /v1/models lists whisper, transcriptions 200, GPU offload, silero present, A1 answered (checkpoint).
- GH Secrets updated to new SHA; dev-stack env update flagged (Task 3).
</verification>

<success_criteria>
- `/v1/audio/transcriptions` returns 200 on a live pod running the rebuilt image — the STT fix is PROVEN, not assumed.
- New whisper SHA propagated; sha256sum -c will pass on subsequent boots.
- A1 (snapshot hash) resolved empirically and documented.
- Pod destroyed (no lingering Vast charges).
</success_criteria>

<output>
Create `.planning/phases/06.8-multi-pod-gpu-topology-sizing-stt-fix/06.8-02-SUMMARY.md` when done. Surface: new whisper SHA, image digest, the transcription HTTP result, and the A1 hash finding (Plan 03's live-UAT asserts STT 200 against this same image).
</output>
