---
phase: 06.6
plan: 03
type: execute
wave: 2
depends_on: [06.6-01, 06.6-02]
files_modified:
  - gateway/internal/config/config.go
  - gateway/internal/config/config_test.go
autonomous: true
requirements: []
tags: [config, wave-2, env-vars, primary-pod, schedule, wave0-locked, custom-image]

must_haves:
  truths:
    - "gateway/internal/config/config.go expõe 24 novos campos PrimaryXxx + PrimaryPodScheduleXxx (D-08.1 + Decisions Resolved #5 RESEARCH.md:656-681 + ProvisionFailureCooldownSeconds parity emerg per checker revision + PrimaryPodScheduleProvisionLeadSeconds per reviews 2026-05-17)"
    - "**Wave 0 LOCKED defaults** (per 06.6-WAVE0-GATES.md Decisions 1+3+5+6) — image SHA pins enforce: `PrimaryTemplateImage` default = `ghcr.io/ggml-org/llama.cpp:server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3` (UPGRADED from b9128 — empirically validated b9128 fails Qwen3.6 SSM tensor load per 06.6-SPIKE-qwen3.6-jinja.md Round 3); `PrimarySpeachesImage` default contains `@sha256:5c6206a349e90b9a6782342917e72f84fc7cb60e8afd540f6aa625831ac1fd0f`; `PrimaryInfinityImage` default contains `@sha256:11e8b3921b9f1a58965afaad4a844c435c9807cbc82c51e47cb147b7d977fc88`; `PrimaryDCGMImage` default contains `@sha256:60d3b00ac80b4ae77f94dae2f943685605585ad9e92fdccda3154d009ae317cc`"
    - "**ZERO DinD-related env vars** (DOCKERD_*, INNER_DOCKER_*, DIND_*, etc) — Wave 0 REJECTED DinD; orchestration is supervisord (Plan 04) via custom multi-stage image. No new env required to express supervisord — it is an implementation detail of the custom image (Plan 04 `pod/primary/Dockerfile` + `supervisord.conf`)"
    - "PRIMARY_WHISPER_WEIGHTS_SHA256 + PRIMARY_BGEM3_WEIGHTS_SHA256 são FAIL-FAST se empty quando primary lifecycle build invocado (per reviews consensus action #6); este plan grava empty defaults — Plan 06.6-04 buildPrimaryCreateRequest REJECTS empty values explicitly"
    - "PrimaryQwenJinjaKey + PrimaryQwenJinjaSHA256 default EMPTY (B1 GGUF-embedded LOCKED per WAVE0-GATES Decision 3). `--jinja` flag alone extracts PEG-native parser from GGUF chat_template — no MinIO Jinja fetch. Override via env vars allowed for future B2 fallback."
    - "PrimaryLlamaArgs é csvOr default nil — empty CSV → lifecycle.go usa hardcoded primaryLlamaArgsDefault const (mesma convention que EmergencyLlamaArgs Phase 6); default args MUST NOT include `--chat-template-file` (B1 embedded LOCKED)"
    - "PrimaryProvisionFailureCooldownSeconds: default 300 (5min); mirror emerg's ProvisionFailureCooldownSeconds. Plan 06.6-06a reconciler.evaluateAsleep enforces."
    - "PrimaryProvisionColdStartBudgetSeconds: default 2400 (40min) per WAVE0-GATES Decision 6 — generous margin accommodating slow inet hosts + multi-stage image pull + aria2c weight download + 4-service supervisord startup"
    - "PrimaryPodScheduleProvisionLeadSeconds: default 1800 (30min); reconciler provisions `lead_seconds` ANTES de UpHour para honor schedule semantics with 25-30min cold-start reality (per reviews consensus action #8)"
    - "PrimaryPodScheduleDisabled: default `true` per WAVE0-GATES Decision 5 (soak gate) — operator manual flip to `false` only after Plan 06.6-11 Live UAT GREEN"
    - "Helpers envOr/atoiOr/csvOr/boolOr/floatOr existentes em config.go:340-396 são REUTILIZADOS — zero novo helper criado"
  artifacts:
    - path: "gateway/internal/config/config.go"
      provides: "24 new env var fields under Phase 6.6 header block"
      contains: "PrimaryTemplateImage|PrimarySpeachesImage|PrimaryInfinityImage|PrimaryDCGMImage|PrimaryQwenWeightsKey|PrimaryQwenWeightsSHA256|PrimaryQwenJinjaKey|PrimaryQwenJinjaSHA256|PrimaryLlamaArgs|PrimaryWhisperWeightsKey|PrimaryWhisperWeightsSHA256|PrimaryBGEM3WeightsKey|PrimaryBGEM3WeightsSHA256|PrimaryVastPriceCapDPH|PrimaryProvisionColdStartBudgetSeconds|PrimaryProvisionFailureCooldownSeconds|MonthlyPrimaryBudgetBRL|PrimaryPodScheduleTimezone|PrimaryPodScheduleUpHour|PrimaryPodScheduleDownHour|PrimaryPodScheduleDays|PrimaryPodScheduleGraceRampDownSeconds|PrimaryPodScheduleDisabled|PrimaryPodScheduleProvisionLeadSeconds"
    - path: "gateway/internal/config/config_test.go"
      provides: "Coverage for 24 new fields — defaults + override-via-env + Wave 0 SHA pin enforcement"
      contains: "TestConfig_PrimaryPod_DefaultsLoaded|TestConfig_PrimaryPod_EnvOverride|TestConfig_PrimaryPod_FailureCooldownDefault|TestConfig_PrimaryPod_ProvisionLeadDefault|TestConfig_PrimaryPod_TemplateImageIsB9191SHAPinned|TestConfig_PrimaryPod_DisabledDefaultsTrue_SoakGate|TestConfig_PrimaryPod_JinjaDefaultsEmpty_B1Embedded"
  key_links:
    - from: "config.go Load() block (new lines)"
      to: "primary/lifecycle.go buildPrimaryCreateRequest (Wave 2 Plan 06.6-04 consumer)"
      via: "cfg.PrimaryXxx field reads"
      pattern: "cfg\\.Primary[A-Z]"
    - from: "config.go PrimaryProvisionFailureCooldownSeconds field"
      to: "primary/reconciler.go evaluateAsleep cooldown check (Plan 06.6-06a consumer)"
      via: "cfg.PrimaryProvisionFailureCooldownSeconds read"
      pattern: "cfg\\.PrimaryProvisionFailureCooldownSeconds"
    - from: "config.go PrimaryPodScheduleProvisionLeadSeconds field"
      to: "primary/schedule.go ShouldBeProvisioned (Plan 06.6-05 consumer)"
      via: "cfg.PrimaryPodScheduleProvisionLeadSeconds read"
      pattern: "cfg\\.PrimaryPodScheduleProvisionLeadSeconds"

threat_refs: [T-06.6-03, T-06.6-04, T-06.6-SC]
---

<objective>
Adicionar 24 novos campos de configuração ao gateway/internal/config/config.go com defaults LOCKED pelo 06.6-WAVE0-GATES.md (Wave 0 empirical findings 2026-05-17). Image tag default mudou de Phase 6's b9128 → b9191 (Qwen3.6 SSM support per Wave 0 spike Round 3). All 4 upstream image defaults are SHA-pinned. B1 GGUF-embedded Jinja default (empty key + sha256). Soak gate default (Disabled=true). Cold-start budget 2400s (40min).

Purpose: Plans Wave 2+ (lifecycle.go, schedule.go, reconciler.go) leem cfg.PrimaryXxx via dependency injection. Plan 04 (custom multi-stage Dockerfile) consumes `PrimaryTemplateImage` + 3 other upstream image refs (multi-stage FROM lines). Plan 05 consumes schedule fields. Plan 06a consumes PrimaryProvisionFailureCooldownSeconds + PrimaryProvisionColdStartBudgetSeconds.

Output: 24 fields no struct Config + 24 linhas no Load() block + coverage tests verifying defaults match WAVE0-GATES.md SHA pins literally + soak gate + B1 empty Jinja defaults.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md
@gateway/internal/config/config.go

<interfaces>
<!-- Existing Phase 6 emergency block at config.go lines 119-149 (struct fields) + 252-288 (Load reads). Pattern to mirror VERBATIM, renaming Emergency→Primary. -->

Helpers already exist in config.go:340-396:
- `envOr(k, def string) string`
- `atoiOr(s string, def int) int`
- `csvOr(s string, def []string) []string`
- `boolOr(s string, def bool) bool`
- `floatOr(s string, def float64) float64`

Existing emerg analog (line 141): `ProvisionFailureCooldownSeconds int // PROVISION_FAILURE_COOLDOWN_SECONDS (default 60 ...)` — Phase 6.6 primary mirrors with `PrimaryProvisionFailureCooldownSeconds` (default 300 — scaled-up for schedule cadence).

24 new env var defaults (verbatim from RESEARCH.md §Decisions Resolved #5, with WAVE0-GATES.md overrides applied to image + schedule fields):

| Env | Type | Default (WAVE0-locked where noted) |
|-----|------|-------------------------------------|
| PRIMARY_POD_SCHEDULE_TIMEZONE | string | "America/Sao_Paulo" |
| PRIMARY_POD_SCHEDULE_UP_HOUR | int | 8 |
| PRIMARY_POD_SCHEDULE_DOWN_HOUR | int | 22 |
| PRIMARY_POD_SCHEDULE_DAYS | []string | ["mon","tue","wed","thu","fri"] |
| PRIMARY_POD_SCHEDULE_GRACE_RAMP_DOWN_SECONDS | int | 300 |
| PRIMARY_POD_SCHEDULE_DISABLED | bool | **true** (WAVE0-GATES Decision 5 soak gate) |
| PRIMARY_POD_SCHEDULE_PROVISION_LEAD_SECONDS | int | 1800 (reviews #8 pre-warm offset) |
| PRIMARY_TEMPLATE_IMAGE | string | **"ghcr.io/ggml-org/llama.cpp:server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3"** (WAVE0-GATES Decision 1 — UPGRADED from b9128; Plan 04 custom image FROM this base) |
| PRIMARY_SPEACHES_IMAGE | string | **"ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3@sha256:5c6206a349e90b9a6782342917e72f84fc7cb60e8afd540f6aa625831ac1fd0f"** (WAVE0-GATES Decision 1; Plan 04 multi-stage source) |
| PRIMARY_INFINITY_IMAGE | string | **"michaelf34/infinity:0.0.77@sha256:11e8b3921b9f1a58965afaad4a844c435c9807cbc82c51e47cb147b7d977fc88"** (WAVE0-GATES Decision 1; Plan 04 multi-stage source) |
| PRIMARY_DCGM_IMAGE | string | **"nvcr.io/nvidia/k8s/dcgm-exporter:4.5.3-4.8.2-distroless@sha256:60d3b00ac80b4ae77f94dae2f943685605585ad9e92fdccda3154d009ae317cc"** (WAVE0-GATES Decision 1; Plan 04 multi-stage source) |
| PRIMARY_QWEN_WEIGHTS_KEY | string | "qwen3.6-27b-Q4_K_M/v1.0.0/model.gguf" (CONTEXT.md D-04) |
| PRIMARY_QWEN_WEIGHTS_SHA256 | string | "a7cbd3ecc0e3f9b333edee61ae66bc87ed713c5d49587a8355814722ed329e0f" (CONTEXT.md D-04, Wave 0 verified) |
| PRIMARY_QWEN_JINJA_KEY | string | **""** (WAVE0-GATES Decision 3 B1 GGUF-embedded LOCKED) |
| PRIMARY_QWEN_JINJA_SHA256 | string | **""** (WAVE0-GATES Decision 3 B1 GGUF-embedded LOCKED) |
| PRIMARY_LLAMA_ARGS | []string | nil (lifecycle.go uses hardcoded const) |
| PRIMARY_WHISPER_WEIGHTS_KEY | string | "whisper-large-v3/v1.0.0/model.tar.gz" (Phase 1 default) |
| PRIMARY_WHISPER_WEIGHTS_SHA256 | string | "" — empty default permitted in config layer; Plan 06.6-04 buildPrimaryCreateRequest REJECTS empty per reviews #6 (fail-fast). Operator MUST set this env before deploy. |
| PRIMARY_BGEM3_WEIGHTS_KEY | string | "bge-m3/v1.0.0/model.tar.gz" (Phase 1 default) |
| PRIMARY_BGEM3_WEIGHTS_SHA256 | string | "" — same fail-fast policy as Whisper SHA per reviews #6 |
| PRIMARY_VAST_PRICE_CAP_DPH | float64 | 0.40 |
| PRIMARY_PROVISION_COLDSTART_BUDGET_SECONDS | int | **2400** (WAVE0-GATES Decision 6 — 40min generous margin) |
| PRIMARY_PROVISION_FAILURE_COOLDOWN_SECONDS | int | 300 (per checker revision; mirror emerg's PROVISION_FAILURE_COOLDOWN_SECONDS=60 scaled-up; consumer = Plan 06.6-06a reconciler.evaluateAsleep) |
| MONTHLY_PRIMARY_BUDGET_BRL | float64 | 800.0 (Pitfall #12 separate from emergency) |

POD_DEBUG_SSH_PUBLIC_KEY: REUSE existing field (config.go:137 — verified existing). No new field needed per D-10.
MINIO_*, VAST_AI_API_KEY, USD_TO_BRL_RATE: REUSE existing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 24 Primary* struct fields + Load() block to gateway/internal/config/config.go with Wave 0 LOCKED defaults</name>
  <files>gateway/internal/config/config.go</files>
  <read_first>
    - gateway/internal/config/config.go lines 119-149 (Phase 6 emergency block — pattern to mirror; specifically line 141 `ProvisionFailureCooldownSeconds int` field + line 270 `ProvisionFailureCooldownSeconds: atoiOr(os.Getenv("PROVISION_FAILURE_COOLDOWN_SECONDS"), 60)`) + lines 252-288 (existing Phase 6 Load() reads — pattern to mirror) + lines 340-396 (envOr/atoiOr/csvOr/boolOr/floatOr helpers — reuse, do not redefine)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md Decisions 1+3+5+6 (operator-locked defaults — image SHA pins, B1 Jinja empty, soak gate, cold-start budget)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md `<canonical_refs>` block (4 SHA-pinned image references — literal strings to embed)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 681-721 (MODIFIED config.go section spec)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md lines 196-244 (Recommended Pre-execution Actions #6 SHA fail-fast + #8 pre-warm offset)
  </read_first>
  <action>Localizar fim do bloco Phase 6 emergency em config.go (~line 149 do struct + ~line 288 do Load). Adicionar SOB header comment `// Phase 6.6 — primary pod auto-provisioning (schedule-driven; D-08 + Decisions Resolved #5 + WAVE0-GATES.md Decisions 1+3+5+6 + Reviews 2026-05-17 #6 #8)`: (1) 24 struct fields no order da tabela <interfaces>, each one-line `FieldName Type // ENV_VAR_NAME (default ...; brief purpose)`. Tipos:  são `string`; PrimaryPodScheduleUpHour/PrimaryPodScheduleDownHour/PrimaryPodScheduleGraceRampDownSeconds/PrimaryProvisionColdStartBudgetSeconds/PrimaryProvisionFailureCooldownSeconds/PrimaryPodScheduleProvisionLeadSeconds são `int`; PrimaryPodScheduleDays/PrimaryLlamaArgs são `[]string`; PrimaryPodScheduleDisabled é `bool`; PrimaryVastPriceCapDPH/MonthlyPrimaryBudgetBRL são `float64`. (2) 24 Load() reads no order da tabela. **CRITICAL — image defaults literal strings**: `PrimaryTemplateImage: envOr(os.Getenv("PRIMARY_TEMPLATE_IMAGE"), "ghcr.io/ggml-org/llama.cpp:server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3")` — note `b9191` NOT `b9128` (Wave 0 LOCKED); same SHA-pinned literal form para Speaches/Infinity/DCGM defaults using digests from WAVE0-GATES Decision 1 table. (3) `PrimaryPodScheduleDisabled: boolOr(os.Getenv("PRIMARY_POD_SCHEDULE_DISABLED"), true)` — note `true` default per WAVE0-GATES Decision 5 soak gate (inline comment `// default true per WAVE0-GATES Decision 5 soak gate — operator manual flip to false after Plan 06.6-11 Live UAT GREEN`). (4) `PrimaryProvisionColdStartBudgetSeconds: atoiOr(os.Getenv("PRIMARY_PROVISION_COLDSTART_BUDGET_SECONDS"), 2400)` per WAVE0-GATES Decision 6. (5) `PrimaryQwenJinjaKey: envOr(os.Getenv("PRIMARY_QWEN_JINJA_KEY"), "")` + same for SHA256 — empty defaults per WAVE0-GATES Decision 3 B1 GGUF-embedded (inline comment `// default empty per WAVE0-GATES Decision 3 — B1 GGUF-embedded Jinja LOCKED; --jinja flag alone extracts PEG-native parser from Qwen3.6 GGUF chat_template; override via env if B2 MinIO fallback needed`). (6) `PrimaryQwenWeightsKey: envOr(os.Getenv("PRIMARY_QWEN_WEIGHTS_KEY"), "qwen3.6-27b-Q4_K_M/v1.0.0/model.gguf")` + `PrimaryQwenWeightsSHA256: envOr(os.Getenv("PRIMARY_QWEN_WEIGHTS_SHA256"), "a7cbd3ecc0e3f9b333edee61ae66bc87ed713c5d49587a8355814722ed329e0f")`. (7) `PrimaryPodScheduleProvisionLeadSeconds: atoiOr(os.Getenv("PRIMARY_POD_SCHEDULE_PROVISION_LEAD_SECONDS"), 1800)` (reviews #8). (8) Whisper/BGE SHA fields use direct `os.Getenv("PRIMARY_WHISPER_WEIGHTS_SHA256")` (no envOr default, empty string passthrough); comentário inline `// FAIL-FAST policy per reviews consensus action #6 — Plan 06.6-04 buildPrimaryCreateRequest REJECTS empty value at build time with ErrMissingWhisperSHA`. **CRITICAL — NO DinD env vars added** (DOCKERD_*, INNER_DOCKER_*, DIND_*, NESTED_*). Wave 0 REJECTED DinD; orchestration is supervisord as PID 1 inside custom multi-stage image (Plan 04 details). Supervisord doesn't need user-facing env config — it's baked into pod/primary/Dockerfile + pod/primary/supervisord.conf.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./... && grep -q 'PrimaryProvisionFailureCooldownSeconds' gateway/internal/config/config.go && grep -q 'PrimaryPodScheduleProvisionLeadSeconds' gateway/internal/config/config.go && grep -q 'server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3' gateway/internal/config/config.go && grep -c 'DOCKERD_\|INNER_DOCKER_\|DIND_' gateway/internal/config/config.go | awk '$1 == 0 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds
    - 24 fields exported (capitalized) no Config struct, incluindo PrimaryProvisionFailureCooldownSeconds e PrimaryPodScheduleProvisionLeadSeconds
    - `grep -c 'server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3' gateway/internal/config/config.go == 1` (literal b9191 SHA-pinned default, NOT b9128)
    - `grep -c '@sha256:' gateway/internal/config/config.go` >= 4 (all 4 image defaults SHA-pinned)
    - `grep -c 'DOCKERD_\|INNER_DOCKER_\|DIND_\|NESTED_' gateway/internal/config/config.go == 0` (NO DinD env vars introduced — Wave 0 REJECTED DinD)
    - `grep -q 'PrimaryProvisionColdStartBudgetSeconds.*2400' gateway/internal/config/config.go` (40min budget per WAVE0-GATES Decision 6)
    - `grep -q 'PrimaryPodScheduleDisabled.*true' gateway/internal/config/config.go` (soak gate per WAVE0-GATES Decision 5)
  </acceptance_criteria>
  <done>Build succeeds. 24 fields exportados. Image defaults match WAVE0-GATES Decision 1 SHA digests literally. b9191 (not b9128). Disabled=true (soak gate). Cold-start budget 2400s. Jinja empty (B1 embedded). NO DinD env vars present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config_test.go — coverage for 24 Primary defaults + env override + Wave 0 LOCKED defaults assertions</name>
  <files>gateway/internal/config/config_test.go</files>
  <behavior>
    - TestConfig_PrimaryPod_DefaultsLoaded: com env limpo (todos PRIMARY_* unset, exceto Whisper/BGE SHAs que ficam empty), Load() retorna cfg com 24 fields populados com defaults expected (timezone=America/Sao_Paulo, UpHour=8, DownHour=22, Days=mon..fri, GraceRampDown=300, Disabled=true [soak gate per WAVE0 #5], TemplateImage contém 'server-cuda-b9191@sha256:cb37', ColdStartBudget=2400 [WAVE0 #6], PrimaryWhisperWeightsSHA256=="" e PrimaryBGEM3WeightsSHA256==""; PrimaryPodScheduleProvisionLeadSeconds==1800; PrimaryQwenJinjaKey=="" e PrimaryQwenJinjaSHA256=="" [WAVE0 #3 B1 embedded])
    - TestConfig_PrimaryPod_EnvOverride: t.Setenv("PRIMARY_POD_SCHEDULE_UP_HOUR", "7") + t.Setenv("PRIMARY_POD_SCHEDULE_DISABLED", "false") + t.Setenv("PRIMARY_VAST_PRICE_CAP_DPH", "0.50") + t.Setenv("PRIMARY_LLAMA_ARGS", "--host,0.0.0.0,--port,8000") → Load retorna UpHour=7, Disabled=false, PriceCap=0.50, LlamaArgs=["--host","0.0.0.0","--port","8000"]
    - TestConfig_PrimaryPod_DaysCSVParse: t.Setenv("PRIMARY_POD_SCHEDULE_DAYS", "tue,wed,thu") → cfg.PrimaryPodScheduleDays == ["tue","wed","thu"]
    - TestConfig_PrimaryPod_WhisperSHANoDefault: env limpo → cfg.PrimaryWhisperWeightsSHA256 == "" (operator must set explicitly; Plan 06.6-04 build rejects empty)
    - TestConfig_PrimaryPod_BGEM3SHANoDefault: env limpo → cfg.PrimaryBGEM3WeightsSHA256 == "" (same fail-fast policy)
    - TestConfig_PrimaryPod_FailureCooldownDefault: env limpo → cfg.PrimaryProvisionFailureCooldownSeconds == 300 (scaled-up vs emerg 60s); t.Setenv("PRIMARY_PROVISION_FAILURE_COOLDOWN_SECONDS", "120") → Load retorna 120
    - TestConfig_PrimaryPod_ProvisionLeadDefault: env limpo → cfg.PrimaryPodScheduleProvisionLeadSeconds == 1800 (30min pre-warm); t.Setenv("PRIMARY_POD_SCHEDULE_PROVISION_LEAD_SECONDS", "2400") → Load retorna 2400
    - TestConfig_PrimaryPod_ColdStartBudget40MinDefault: env limpo → cfg.PrimaryProvisionColdStartBudgetSeconds == 2400 (WAVE0-GATES Decision 6)
    - **TestConfig_PrimaryPod_TemplateImageIsB9191SHAPinned (NEW Wave 0)**: require.Contains(t, cfg.PrimaryTemplateImage, "server-cuda-b9191") AND require.Contains(t, cfg.PrimaryTemplateImage, "@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3") AND require.NotContains(t, cfg.PrimaryTemplateImage, "b9128") — proves Wave 0 image upgrade from b9128 → b9191 is enforced at config layer
    - **TestConfig_PrimaryPod_AllUpstreamImagesSHAPinned (NEW Wave 0)**: defaults → require.Contains(t, cfg.PrimaryTemplateImage, "@sha256:cb37"); require.Contains(t, cfg.PrimarySpeachesImage, "@sha256:5c62"); require.Contains(t, cfg.PrimaryInfinityImage, "@sha256:11e8"); require.Contains(t, cfg.PrimaryDCGMImage, "@sha256:60d3") — matches all 4 WAVE0-GATES Decision 1 digest prefixes
    - **TestConfig_PrimaryPod_DisabledDefaultsTrue_SoakGate (NEW Wave 0)**: env limpo → cfg.PrimaryPodScheduleDisabled == true (WAVE0-GATES Decision 5 soak gate enforced at config layer; operator-explicit flip required to enable scheduling in production)
    - **TestConfig_PrimaryPod_JinjaDefaultsEmpty_B1Embedded (NEW Wave 0)**: env limpo → cfg.PrimaryQwenJinjaKey == "" AND cfg.PrimaryQwenJinjaSHA256 == "" (WAVE0-GATES Decision 3 B1 GGUF-embedded Jinja LOCKED — no MinIO fetch required)
  </behavior>
  <read_first>
    - gateway/internal/config/config_test.go (existing structure — Phase 6 emergency tests para mirror pattern)
    - gateway/internal/config/config.go (Task 1 output — confirm field names + types + Wave 0 default literals)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md (operator-locked digests for assertion strings)
  </read_first>
  <action>Adicionar 12 testes no fim de config_test.go (após bloco Phase 6). Cada test usa `t.Setenv` para isolar env mutations (Go 1.17+ auto-cleanup). Para TestConfig_PrimaryPod_TemplateImageIsB9191SHAPinned usar literal digest strings (não variables — defaults têm que MATCH WAVE0-GATES literally): `require.Contains(t, cfg.PrimaryTemplateImage, "server-cuda-b9191")`; `require.Contains(t, cfg.PrimaryTemplateImage, "@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3")`; `require.NotContains(t, cfg.PrimaryTemplateImage, "b9128", "Wave 0 spike (06.6-SPIKE-qwen3.6-jinja.md Round 3) proved b9128 fails Qwen3.6 SSM tensor load — must use b9191+")`. TestConfig_PrimaryPod_AllUpstreamImagesSHAPinned cobre os 4 image defaults via 4 require.Contains checks (one per image field, matching digest prefix). TestConfig_PrimaryPod_DisabledDefaultsTrue_SoakGate usa `require.True(t, cfg.PrimaryPodScheduleDisabled, "WAVE0-GATES Decision 5 soak gate requires Disabled=true default; operator manual flip after UAT GREEN")`. TestConfig_PrimaryPod_JinjaDefaultsEmpty_B1Embedded usa `require.Equal(t, "", cfg.PrimaryQwenJinjaKey)` + same para SHA — comentário citando WAVE0-GATES Decision 3.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/config/... -count=1 -race -run TestConfig_PrimaryPod</automated>
  </verify>
  <acceptance_criteria>
    - All 12+ TestConfig_PrimaryPod_* tests pass under -race
    - TestConfig_PrimaryPod_TemplateImageIsB9191SHAPinned asserts the literal b9191 digest from WAVE0-GATES Decision 1
    - TestConfig_PrimaryPod_DisabledDefaultsTrue_SoakGate enforces WAVE0-GATES Decision 5
    - TestConfig_PrimaryPod_JinjaDefaultsEmpty_B1Embedded enforces WAVE0-GATES Decision 3
    - TestConfig_PrimaryPod_ColdStartBudget40MinDefault enforces WAVE0-GATES Decision 6
    - TestConfig_PrimaryPod_AllUpstreamImagesSHAPinned enforces all 4 WAVE0-GATES Decision 1 digests
  </acceptance_criteria>
  <done>12+ tests passam. Confirma defaults batem com WAVE0-GATES.md decisions LITERALLY + override path funciona + soak gate enforced + cold-start budget 2400s + B1 Jinja empty default enforced + b9191 (NOT b9128) enforced + 4 SHA pin digests asserted by their first-8-char prefixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Portainer stack env UI → gateway process env vars | Operator-controlled; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-03 | Information Disclosure | POD_DEBUG_SSH_PUBLIC_KEY accidental commit | mitigate | Field REUSED from Phase 6 (no new field — confirmed config.go line 137 already exposes PodDebugSSHPublicKey) — existing CI check (Phase 6 19a69a3 commit) protects against git accidental commit. Production env empty enforced by ops runbook. |
| T-06.6-04 | DoS | Schedule env-var DoS (operator sets bad UpHour/DownHour causing rapid up/down loop) | mitigate | Reconciler enforces min 5-min between provisions via cfg.PrimaryProvisionFailureCooldownSeconds (default 300s — added in this plan; consumed by Plan 06.6-06a); operator kill-switch via PRIMARY_POD_SCHEDULE_DISABLED=true (default true per soak gate Wave 0 Decision 5). Pre-warm offset via PRIMARY_POD_SCHEDULE_PROVISION_LEAD_SECONDS prevents oscillation at hour-boundary edge. Config layer only validates types (atoiOr falls back to default on parse error) — no business logic gates here. |
| T-06.6-SC | Tampering | OCI image references | mitigate | Image defaults include `@sha256:<digest>` SHA-pinned suffix per WAVE0-GATES Decision 1. TestConfig_PrimaryPod_TemplateImageIsB9191SHAPinned + TestConfig_PrimaryPod_AllUpstreamImagesSHAPinned enforce literal digest match. Bumps require WAVE0-GATES.md amendment + operator sign-off. |
</threat_model>

<verification>
- `cd gateway && go build ./...` succeeds
- `cd gateway && go test ./internal/config/... -count=1 -race` all green (existing + 12 new)
- `cd gateway && grep -c 'server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3' gateway/internal/config/config.go == 1` (literal b9191 SHA-pinned default)
- `cd gateway && grep -c 'b9128' gateway/internal/config/config.go == 0` (NO references to old image tag)
- `cd gateway && grep -c 'DOCKERD_\|INNER_DOCKER_\|DIND_\|NESTED_DOCKER' gateway/internal/config/config.go == 0` (NO DinD env vars — Wave 0 REJECTED DinD)
- `cd gateway && grep -q 'PrimaryProvisionFailureCooldownSeconds' gateway/internal/config/config.go` (cooldown field present)
- `cd gateway && grep -q 'PrimaryPodScheduleProvisionLeadSeconds' gateway/internal/config/config.go` (lead field present per reviews #8)
</verification>

<success_criteria>
- 24 fields exposed in Config struct (22 from D-08.1 + 1 new PrimaryProvisionFailureCooldownSeconds + 1 new PrimaryPodScheduleProvisionLeadSeconds)
- Load() populates all 24 from env or Wave 0 LOCKED defaults
- **Wave 0 fidelity**: `PrimaryTemplateImage` default contains exact `server-cuda-b9191@sha256:cb375311f4170bb1aa18840e946f64f99e6094b90bde69dcb6e0a62a183d7ba3` literal (NOT b9128); 4 image defaults SHA-pinned per WAVE0-GATES Decision 1; `PrimaryPodScheduleDisabled` defaults `true` per Decision 5 (soak gate); `PrimaryProvisionColdStartBudgetSeconds` defaults `2400` per Decision 6; `PrimaryQwenJinjaKey` + `PrimaryQwenJinjaSHA256` default empty per Decision 3 (B1 embedded)
- **NO DinD env vars** — DOCKERD_*, INNER_DOCKER_*, DIND_*, NESTED_* completely absent (Wave 0 REJECTED DinD; supervisord lives inside custom multi-stage image per Plan 04, no user-facing config)
- D-08.1 + Decisions Resolved #5 fully implemented
- Reviews consensus actions #6 (SHA fail-fast policy declared) + #8 (pre-warm offset field added) addressed
- Image defaults SHA-pinned per WAVE0-GATES Decision 1 (literal digest match enforced by test)
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-03-SUMMARY.md`.
</output>
