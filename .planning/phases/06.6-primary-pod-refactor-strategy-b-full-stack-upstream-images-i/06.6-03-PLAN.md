---
phase: 06.6
plan: 03
type: execute
wave: 2
depends_on: [06.6-01, 06.6-02]
files_modified:
  - gateway/internal/config/config.go
  - gateway/internal/config/config_test.go
autonomous: true
requirements: []
tags: [config, wave-2, env-vars, primary-pod, schedule]

must_haves:
  truths:
    - "gateway/internal/config/config.go expõe 24 novos campos PrimaryXxx + PrimaryPodScheduleXxx (D-08.1 + Decisions Resolved #5 RESEARCH.md:656-681 + ProvisionFailureCooldownSeconds parity emerg per checker revision + PrimaryPodScheduleProvisionLeadSeconds per reviews 2026-05-17)"
    - "Defaults dos 24 fields correspondem exatamente aos valores LOCKED em 06.6-WAVE0-GATES.md (Decisions 1, 3, 4, 6) — image SHA pins de Decision 1 com `@sha256:<digest>` SUFFIX (per D-02 upstream tag-pinned policy + reviews Codex LOW concern: defaults ARE SHA-pinned, not tag-only)"
    - "PRIMARY_WHISPER_WEIGHTS_SHA256 + PRIMARY_BGEM3_WEIGHTS_SHA256 são FAIL-FAST se empty quando primary lifecycle build invocado (per reviews consensus action #6); este plan grava defaults shipped via WAVE0-GATES sha256 lookup OR leaves empty mas Plan 06.6-04 buildCreateRequest REJECTS empty values explicitly"
    - "PrimaryQwenJinjaKey + PrimaryQwenJinjaSHA256 default empty SE WAVE0-GATES Decision 3 = B1 (GGUF-embedded); ELSE default = MinIO key + sha256 do upload Wave 0 Task 2 (B2 fallback)"
    - "PrimaryLlamaArgs é csvOr default nil — empty CSV → lifecycle.go usa hardcoded primaryLlamaArgsDefault const (mesma convention que EmergencyLlamaArgs Phase 6)"
    - "PrimaryProvisionFailureCooldownSeconds: default 300 (5min); mirror emerg's ProvisionFailureCooldownSeconds. Plan 06.6-06a reconciler.evaluateAsleep enforces."
    - "PrimaryPodScheduleProvisionLeadSeconds: default 1800 (30min); semantics — reconciler provisions `lead_seconds` ANTES de UpHour para honor schedule semantics with 25-30min cold-start reality (per reviews consensus action #8). With UpHour=8 + default 1800s → reconciler kicks off provision at 07:30 so pod is Ready by ~08:00."
    - "Helpers envOr/atoiOr/csvOr/boolOr/floatOr existentes em config.go:340-396 são REUTILIZADOS — zero novo helper criado"
  artifacts:
    - path: "gateway/internal/config/config.go"
      provides: "24 new env var fields under Phase 6.6 header block"
      contains: "PrimaryTemplateImage|PrimarySpeachesImage|PrimaryInfinityImage|PrimaryDCGMImage|PrimaryQwenWeightsKey|PrimaryQwenWeightsSHA256|PrimaryQwenJinjaKey|PrimaryQwenJinjaSHA256|PrimaryLlamaArgs|PrimaryWhisperWeightsKey|PrimaryWhisperWeightsSHA256|PrimaryBGEM3WeightsKey|PrimaryBGEM3WeightsSHA256|PrimaryVastPriceCapDPH|PrimaryProvisionColdStartBudgetSeconds|PrimaryProvisionFailureCooldownSeconds|MonthlyPrimaryBudgetBRL|PrimaryPodScheduleTimezone|PrimaryPodScheduleUpHour|PrimaryPodScheduleDownHour|PrimaryPodScheduleDays|PrimaryPodScheduleGraceRampDownSeconds|PrimaryPodScheduleDisabled|PrimaryPodScheduleProvisionLeadSeconds"
    - path: "gateway/internal/config/config_test.go"
      provides: "Coverage for 24 new fields — defaults + override-via-env"
      contains: "TestConfig_PrimaryPod_DefaultsLoaded|TestConfig_PrimaryPod_EnvOverride|TestConfig_PrimaryPod_FailureCooldownDefault|TestConfig_PrimaryPod_ProvisionLeadDefault"
  key_links:
    - from: "config.go Load() block (new lines)"
      to: "primary/lifecycle.go buildCreateRequest (Wave 2 Plan 06.6-04 consumer)"
      via: "cfg.PrimaryXxx field reads"
      pattern: "cfg\\.Primary[A-Z]"
    - from: "config.go PrimaryProvisionFailureCooldownSeconds field"
      to: "primary/reconciler.go evaluateAsleep cooldown check (Plan 06.6-06a consumer)"
      via: "cfg.PrimaryProvisionFailureCooldownSeconds read"
      pattern: "cfg\\.PrimaryProvisionFailureCooldownSeconds"
    - from: "config.go PrimaryPodScheduleProvisionLeadSeconds field"
      to: "primary/schedule.go ShouldBeProvisioned (Plan 06.6-05 consumer)"
      via: "cfg.PrimaryPodScheduleProvisionLeadSeconds read"
      pattern: "cfg\\.PrimaryPodScheduleProvisionLeadSeconds"

threat_refs: [T-06.6-03, T-06.6-04, T-06.6-SC]
---

<objective>
Adicionar 24 novos campos de configuração ao gateway/internal/config/config.go correspondentes às decisões D-08.1 (env vars schedule) + Decisions Resolved #5 (env var naming) + Plan 06 threat_model T-06.6-04 mitigation (PrimaryProvisionFailureCooldownSeconds — checker revision) + reviews consensus action #8 (PrimaryPodScheduleProvisionLeadSeconds pre-warm offset, default 1800s). Os defaults são determinados por 06.6-WAVE0-GATES.md (Wave 0) — image SHA pins, Jinja path strategy, soak gate.

Purpose: Plans Wave 2+ (lifecycle.go, schedule.go, reconciler.go) leem cfg.PrimaryXxx via dependency injection — config é o ponto único de wiring entre operator env vars e código. Sem este plan, todos os Wave 2 plans seriam blocked. PrimaryProvisionFailureCooldownSeconds é referenciado por Plan 06.6-06a threat_model T-06.6-04 mitigation. PrimaryPodScheduleProvisionLeadSeconds é referenciado por Plan 06.6-05 IsInPeak extension e Plan 06.6-09 gatewayctl primary schedule output.

Output: 24 fields no struct Config + 24 linhas no Load() block + coverage test verificando defaults batem com WAVE0-GATES.md + new lead-seconds default 1800.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md
@gateway/internal/config/config.go

<interfaces>
<!-- Existing Phase 6 emergency block at config.go lines 119-149 (struct fields) + 252-288 (Load reads). Pattern to mirror VERBATIM, renaming Emergency→Primary. -->

Helpers already exist in config.go:340-396:
- `envOr(k, def string) string`
- `atoiOr(s string, def int) int`
- `csvOr(s string, def []string) []string`
- `boolOr(s string, def bool) bool`
- `floatOr(s string, def float64) float64`

Existing emerg analog (line 141): `ProvisionFailureCooldownSeconds int // PROVISION_FAILURE_COOLDOWN_SECONDS (default 60 ...)` — Phase 6.6 primary mirrors with `PrimaryProvisionFailureCooldownSeconds` (default 300 — scaled-up for schedule cadence).

24 new env var defaults (verbatim from RESEARCH.md §Decisions Resolved #5, lines 656-681, plus PrimaryProvisionFailureCooldownSeconds per checker revision + PrimaryPodScheduleProvisionLeadSeconds per reviews consensus action #8):
| Env | Type | Default |
|-----|------|---------|
| PRIMARY_POD_SCHEDULE_TIMEZONE | string | "America/Sao_Paulo" |
| PRIMARY_POD_SCHEDULE_UP_HOUR | int | 8 |
| PRIMARY_POD_SCHEDULE_DOWN_HOUR | int | 22 |
| PRIMARY_POD_SCHEDULE_DAYS | []string | ["mon","tue","wed","thu","fri"] |
| PRIMARY_POD_SCHEDULE_GRACE_RAMP_DOWN_SECONDS | int | 300 |
| PRIMARY_POD_SCHEDULE_DISABLED | bool | true (per WAVE0-GATES Decision 5 soak gate) |
| PRIMARY_POD_SCHEDULE_PROVISION_LEAD_SECONDS | int | 1800 (NEW per reviews consensus action #8 — pre-warm offset honoring 25-30min cold-start; with UpHour=8 + 1800s, reconciler provisions at 07:30 so pod Ready by ~08:00) |
| PRIMARY_TEMPLATE_IMAGE | string | "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128@sha256:<digest from WAVE0-GATES Decision 1>" |
| PRIMARY_SPEACHES_IMAGE | string | "ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3@sha256:<digest from WAVE0-GATES Decision 1>" |
| PRIMARY_INFINITY_IMAGE | string | "michaelf34/infinity:0.0.77@sha256:<digest from WAVE0-GATES Decision 1>" |
| PRIMARY_DCGM_IMAGE | string | "nvcr.io/nvidia/k8s/dcgm-exporter:4.5.3-4.8.2-distroless@sha256:<digest from WAVE0-GATES Decision 1>" |
| PRIMARY_QWEN_WEIGHTS_KEY | string | "qwen3.6-27b-Q4_K_M/v1.0.0/model.gguf" (CONTEXT.md D-04) |
| PRIMARY_QWEN_WEIGHTS_SHA256 | string | "a7cbd3ecc0e3f9b333edee61ae66bc87ed713c5d49587a8355814722ed329e0f" (CONTEXT.md D-04) |
| PRIMARY_QWEN_JINJA_KEY | string | "" (B1) OR MinIO key (B2 fallback) per WAVE0-GATES Decision 3 |
| PRIMARY_QWEN_JINJA_SHA256 | string | "" (B1) OR sha256 (B2 fallback) per WAVE0-GATES Decision 3 |
| PRIMARY_LLAMA_ARGS | []string | nil (lifecycle.go uses hardcoded const) |
| PRIMARY_WHISPER_WEIGHTS_KEY | string | "whisper-large-v3/v1.0.0/model.tar.gz" (Phase 1 default) |
| PRIMARY_WHISPER_WEIGHTS_SHA256 | string | "" — empty default permitted in config layer; Plan 06.6-04 buildCreateRequest REJECTS empty per reviews consensus action #6 (fail-fast). Operator MUST set this env before deploy. |
| PRIMARY_BGEM3_WEIGHTS_KEY | string | "bge-m3/v1.0.0/model.tar.gz" (Phase 1 default) |
| PRIMARY_BGEM3_WEIGHTS_SHA256 | string | "" — same fail-fast policy as Whisper SHA per reviews consensus action #6 |
| PRIMARY_VAST_PRICE_CAP_DPH | float64 | 0.40 |
| PRIMARY_PROVISION_COLDSTART_BUDGET_SECONDS | int | 2400 (WAVE0-GATES Decision 6) |
| PRIMARY_PROVISION_FAILURE_COOLDOWN_SECONDS | int | 300 (per checker revision; mirror emerg's PROVISION_FAILURE_COOLDOWN_SECONDS=60 scaled-up because primary cadence is hours-scale vs emerg minutes-scale; consumer = Plan 06.6-06a reconciler.evaluateAsleep) |
| MONTHLY_PRIMARY_BUDGET_BRL | float64 | 800.0 (Pitfall #12 separate from emergency) |

POD_DEBUG_SSH_PUBLIC_KEY: REUSE existing field (config.go:137 — verified existing). No new field needed per D-10.
MINIO_*, VAST_AI_API_KEY, USD_TO_BRL_RATE: REUSE existing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 24 Primary* struct fields + Load() block to gateway/internal/config/config.go</name>
  <files>gateway/internal/config/config.go</files>
  <read_first>
    - gateway/internal/config/config.go lines 119-149 (Phase 6 emergency block — pattern to mirror; specifically line 141 `ProvisionFailureCooldownSeconds int` field + line 270 `ProvisionFailureCooldownSeconds: atoiOr(os.Getenv("PROVISION_FAILURE_COOLDOWN_SECONDS"), 60)`) + lines 252-288 (existing Phase 6 Load() reads — pattern to mirror) + lines 340-396 (envOr/atoiOr/csvOr/boolOr/floatOr helpers — reuse, do not redefine)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md (operator-locked defaults — Decision 1, 3, 5, 6 — extract SHA pin digests for image fields)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 681-721 (MODIFIED config.go section spec)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md lines 196-244 (Recommended Pre-execution Actions #6 SHA fail-fast + #8 pre-warm offset)
  </read_first>
  <action>Localizar fim do bloco Phase 6 emergency em config.go (~line 149 do struct + ~line 288 do Load). Adicionar SOB header comment `// Phase 6.6 — primary pod auto-provisioning (schedule-driven; D-08 + Decisions Resolved #5 + Reviews 2026-05-17 #6 #8)`: (1) 24 struct fields no order da tabela <interfaces>, each one-line `FieldName Type // ENV_VAR_NAME (default ...; brief purpose)`. Tipos:  são `string`; PrimaryPodScheduleUpHour/PrimaryPodScheduleDownHour/PrimaryPodScheduleGraceRampDownSeconds/PrimaryProvisionColdStartBudgetSeconds/PrimaryProvisionFailureCooldownSeconds/PrimaryPodScheduleProvisionLeadSeconds são `int`; PrimaryPodScheduleDays/PrimaryLlamaArgs são `[]string`; PrimaryPodScheduleDisabled é `bool`; PrimaryVastPriceCapDPH/MonthlyPrimaryBudgetBRL são `float64`. (2) 24 Load() reads no order da tabela usando helpers existentes. Image defaults DEVEM usar formato SHA-pinned `<tag>@sha256:<digest>` (per reviews Codex LOW concern + RESEARCH.md SHA pinning policy). SHA digests literais extraídos do WAVE0-GATES.md Decision 1 — verificar `docker manifest inspect <ref>` output presente naquela doc. Para `PrimaryPodScheduleProvisionLeadSeconds`: `PrimaryPodScheduleProvisionLeadSeconds: atoiOr(os.Getenv("PRIMARY_POD_SCHEDULE_PROVISION_LEAD_SECONDS"), 1800)` — comentário inline `// NEW per reviews consensus action #8 — pre-warm offset (seconds before UpHour to trigger provisioning); default 1800s = 30min honors 25-30min cold-start reality. Consumer = Plan 06.6-05 schedule.go ShouldBeProvisioned`. Whisper/BGE SHA fields use direct `os.Getenv("PRIMARY_WHISPER_WEIGHTS_SHA256")` (no envOr default, empty string passthrough); comentário inline `// FAIL-FAST policy per reviews consensus action #6 — Plan 06.6-04 buildCreateRequest REJECTS empty value at build time`. Comentar a primeira linha do bloco com `// Phase 6.6 (06.6-CONTEXT.md D-08 + 06.6-RESEARCH.md §Decisions Resolved #5 + 06.6-WAVE0-GATES.md Decision 5 soak-gate + 06.6-REVIEWS.md actions #6 #8)`.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./... && grep -q 'PrimaryProvisionFailureCooldownSeconds' gateway/internal/config/config.go && grep -q 'PrimaryPodScheduleProvisionLeadSeconds' gateway/internal/config/config.go && grep -E '@sha256:' gateway/internal/config/config.go | grep -c 'Primary' | awk '$1 >= 4 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>Build succeeds. 24 fields exportados (capitalized) no Config struct, incluindo PrimaryProvisionFailureCooldownSeconds e PrimaryPodScheduleProvisionLeadSeconds. Load() retorna struct populado pelos defaults. 4 image fields têm `@sha256:` suffix (SHA-pinned per reviews + RESEARCH policy).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config_test.go — coverage for 24 Primary defaults + env override + new lead-seconds field</name>
  <files>gateway/internal/config/config_test.go</files>
  <behavior>
    - TestConfig_PrimaryPod_DefaultsLoaded: com env limpo (todos PRIMARY_* unset, exceto Whisper/BGE SHAs que ficam empty), Load() retorna cfg com 24 fields populados com defaults expected (timezone=America/Sao_Paulo, UpHour=8, DownHour=22, Days=mon..fri, GraceRampDown=300, Disabled=true [soak gate], TemplateImage contém '@sha256:', demais conforme WAVE0-GATES; PrimaryWhisperWeightsSHA256=="" e PrimaryBGEM3WeightsSHA256==""; PrimaryPodScheduleProvisionLeadSeconds==1800)
    - TestConfig_PrimaryPod_EnvOverride: t.Setenv("PRIMARY_POD_SCHEDULE_UP_HOUR", "7") + t.Setenv("PRIMARY_POD_SCHEDULE_DISABLED", "false") + t.Setenv("PRIMARY_VAST_PRICE_CAP_DPH", "0.50") + t.Setenv("PRIMARY_LLAMA_ARGS", "--host,0.0.0.0,--port,8000") → Load retorna UpHour=7, Disabled=false, PriceCap=0.50, LlamaArgs=["--host","0.0.0.0","--port","8000"]
    - TestConfig_PrimaryPod_DaysCSVParse: t.Setenv("PRIMARY_POD_SCHEDULE_DAYS", "tue,wed,thu") → cfg.PrimaryPodScheduleDays == ["tue","wed","thu"]
    - TestConfig_PrimaryPod_WhisperSHANoDefault: env limpo → cfg.PrimaryWhisperWeightsSHA256 == "" (operator must set explicitly; Plan 06.6-04 build rejects empty)
    - TestConfig_PrimaryPod_BGEM3SHANoDefault: env limpo → cfg.PrimaryBGEM3WeightsSHA256 == "" (same fail-fast policy)
    - TestConfig_PrimaryPod_FailureCooldownDefault: env limpo → cfg.PrimaryProvisionFailureCooldownSeconds == 300 (scaled-up vs emerg 60s); t.Setenv("PRIMARY_PROVISION_FAILURE_COOLDOWN_SECONDS", "120") → Load retorna 120
    - TestConfig_PrimaryPod_ProvisionLeadDefault: env limpo → cfg.PrimaryPodScheduleProvisionLeadSeconds == 1800 (30min pre-warm); t.Setenv("PRIMARY_POD_SCHEDULE_PROVISION_LEAD_SECONDS", "2400") → Load retorna 2400
    - TestConfig_PrimaryPod_ImagesAreSHAPinned: defaults → cfg.PrimaryTemplateImage contém '@sha256:'; same para PrimarySpeachesImage, PrimaryInfinityImage, PrimaryDCGMImage (per reviews Codex LOW concern resolved as SHA-pinned)
  </behavior>
  <read_first>
    - gateway/internal/config/config_test.go (existing structure — Phase 6 emergency tests para mirror pattern)
    - gateway/internal/config/config.go (Task 1 output — confirm field names + types)
  </read_first>
  <action>Adicionar 8 testes no fim de config_test.go (após bloco Phase 6). Cada test usa `t.Setenv` para isolar env mutations (Go 1.17+ auto-cleanup). Para TestConfig_PrimaryPod_ImagesAreSHAPinned usar `require.Contains(t, cfg.PrimaryTemplateImage, "@sha256:")` × 4. TestConfig_PrimaryPod_ProvisionLeadDefault cobre o NOVO field per reviews consensus action #8.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/config/... -count=1 -race -run TestConfig_PrimaryPod</automated>
  </verify>
  <done>8+ tests passam. Confirma defaults batem com WAVE0-GATES.md decisions + override path funciona + PrimaryProvisionFailureCooldownSeconds default 300s coberto + PrimaryPodScheduleProvisionLeadSeconds default 1800s coberto + image SHA pinning enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Portainer stack env UI → gateway process env vars | Operator-controlled; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-03 | Information Disclosure | POD_DEBUG_SSH_PUBLIC_KEY accidental commit | mitigate | Field REUSED from Phase 6 (no new field — confirmed config.go line 137 already exposes PodDebugSSHPublicKey) — existing CI check (Phase 6 19a69a3 commit) protects against git accidental commit. Production env empty enforced by ops runbook. |
| T-06.6-04 | DoS | Schedule env-var DoS (operator sets bad UpHour/DownHour causing rapid up/down loop) | mitigate | Reconciler enforces min 5-min between provisions via cfg.PrimaryProvisionFailureCooldownSeconds (default 300s — added in this plan; consumed by Plan 06.6-06a); operator kill-switch via PRIMARY_POD_SCHEDULE_DISABLED=true (default true per soak gate). Pre-warm offset via PRIMARY_POD_SCHEDULE_PROVISION_LEAD_SECONDS prevents oscillation at hour-boundary edge. Config layer only validates types (atoiOr falls back to default on parse error) — no business logic gates here. |
| T-06.6-SC | Tampering | OCI image references | mitigate | Image defaults include `@sha256:<digest>` SHA-pinned suffix per WAVE0-GATES Decision 1 + reviews Codex LOW concern (defaults ARE SHA-pinned, not tag-only). TestConfig_PrimaryPod_ImagesAreSHAPinned enforces. |
</threat_model>

<verification>
- `cd gateway && go build ./...` succeeds
- `cd gateway && go test ./internal/config/... -count=1 -race` all green (existing + 8 new)
- `cd gateway && grep -c 'Primary[A-Z]\\w*\\s\\+\\(string\\|int\\|bool\\|float64\\|\\[\\]string\\)' gateway/internal/config/config.go >= 24` (24 fields present)
- `cd gateway && grep -q 'PrimaryProvisionFailureCooldownSeconds' gateway/internal/config/config.go` (cooldown field present)
- `cd gateway && grep -q 'PrimaryPodScheduleProvisionLeadSeconds' gateway/internal/config/config.go` (lead field present per reviews #8)
</verification>

<success_criteria>
- 24 fields exposed in Config struct (22 from D-08.1 + 1 new PrimaryProvisionFailureCooldownSeconds per checker revision + 1 new PrimaryPodScheduleProvisionLeadSeconds per reviews #8)
- Load() populates all 24 from env or defaults
- WAVE0-GATES.md decisions are the ONLY authoritative source for defaults (no code-side override of operator decisions)
- D-08.1 + Decisions Resolved #5 fully implemented
- Reviews consensus actions #6 (SHA fail-fast policy declared) + #8 (pre-warm offset field added) addressed
- Image defaults SHA-pinned per reviews Codex LOW concern
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-03-SUMMARY.md`.
</output>
