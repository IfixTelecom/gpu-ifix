---
phase: 06.6
slug: primary-pod-refactor-strategy-b-full-stack-upstream-images-i
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-17
---

# Phase 06.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | go test (gateway/) + bash spike scripts (Wave 0) |
| **Config file** | gateway/go.mod (existing); no extra config |
| **Quick run command** | `cd gateway && go test ./internal/primary/... ./internal/vastutil/...` |
| **Full suite command** | `cd gateway && go test ./...` |
| **Estimated runtime** | ~25 seconds (unit) + ~5 min (live Vast.ai UAT on Wave 4) |

---

## Sampling Rate

- **After every task commit:** Run `cd gateway && go test ./internal/primary/... ./internal/vastutil/...`
- **After every plan wave:** Run `cd gateway && go test ./...`
- **Before `/gsd:verify-work`:** Full suite must be green + live UAT lifecycle GREEN (Wave 4)
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

Populated by planner per task. Template row:

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.6-01-01 | 01 | 0 | TBD | — | privileged DinD spike succeeds OR fallback E adopted | spike | `bash pod/scripts/spike-dind.sh` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `pod/scripts/spike-dind.sh` — Vast.ai privileged DinD viability spike (~$0.05)
- [ ] `pod/scripts/spike-qwen36-jinja.sh` — Qwen3.6 chat_template embedded vs sidecar spike (~$0.20)
- [ ] `06.6-WAVE0-GATES.md` — operator-locked SHA pins for 4 upstream images + schedule env defaults + soak-gate decision
- [ ] `06.6-SPIKE-dind-privileged.md` — empirical spike report
- [ ] `gateway/internal/vastutil/` — extracted shared helpers (FilterBelowCap, ExcludeHost, MustEventJSON, PgInt8, PgNumericFromFloat, CaptureBreadcrumb, BestEffortDestroy) — preq for primary lifecycle

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live Vast.ai primary pod lifecycle (provision → 4-service ready → /health probes GREEN → drain → destroy) | Phase 6.6 goal | Real GPU spend (~$0.40), can't run in CI | `./gatewayctl primary provision` + observe `gateway logs` + `curl http://<vast_host>:8000/health` + `./gatewayctl primary drain` |
| Schedule trigger end-to-end (set `PRIMARY_POD_SCHEDULE_UP_HOUR=$(date +%H)` and observe reconciler provisions within 1 min) | D-08 | Live reconciler + Vast.ai | Manual smoke during PR2 UAT |
| Operator SSH debug works (POD_DEBUG_SSH_PUBLIC_KEY set → port 22 mapped → ssh root@<vast_host> succeeds) | D-10 | Real SSH, real Vast.ai network | `ssh -i ~/.ssh/pod_debug root@<vast_host>` |
| Emerg + primary coexistence race resolved (primary Ready event → emerg force-destroys within 30s) | Pitfall #11 (RESEARCH) | Requires both pods alive simultaneously | Simulate during Wave 4 UAT by manually triggering breaker OPEN before primary schedule UP |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (DinD spike + Qwen3.6 Jinja spike + image SHA pins + vastutil extraction)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
