---
phase: 06.6
plan: 11
type: execute
wave: 5
depends_on: [06.6-08, 06.6-09, 06.6-10]
files_modified:
  - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md
  - gateway/docs/RUNBOOK-PRIMARY-POD.md
autonomous: false
requirements: []
tags: [live-uat, wave-5, vast-ai, schedule, drain, runbook, human-verify, wave0-supervisord]

must_haves:
  truths:
    - "06.6-HUMAN-UAT.md script com ≥6 cenários ordenados: (1) PRIMARY_POD_SCHEDULE_DISABLED=false + force-up provision → 4 services GREEN; (2) /v1/chat/completions com tools= retorna correct tool_calls (Qwen3.6 Jinja validation); (3) /v1/audio/transcriptions com audio fixture retorna text (Speaches functional); (4) /v1/embeddings com 8 sentences retorna 1024-dim embeddings (Infinity functional); (5) schedule out-of-window simulation → drain + destroy → fallback chain serves requests com EXPLICIT log-line assertion (BLOCKER 2 revision); (6) Pitfall #11 coexist test: trigger emerg via local-llm breaker OPEN → emerg provisions → primary fires schedule UP → emerg force-destroyed dentro 30s (com pre-flight verify de gatewayctl emerg force-provision existence per WARNING 5 revision)"
    - "**Wave 0 LOCKED: RUNBOOK Architecture section MUST describe supervisord (NOT DinD).** Architecture section explicitly documents: (a) custom multi-stage image `ghcr.io/ifixtelecom/converseai-primary-pod:vX@sha256:...` built by .github/workflows/build-primary-pod.yml; (b) supervisord as PID 1 inside container; (c) 4 child processes (llama-server b9191 + speaches + infinity + dcgm-exporter) sharing container's network namespace + GPU + filesystem; (d) NO DinD/nested docker — DinD design REJECTED per Spike Task 1 (06.6-SPIKE-dind-privileged.md overlayfs fail)"
    - "D-05 traceability (per WARNING 1 revision): RUNBOOK-PRIMARY-POD.md documenta explicitly que health-bridge service Phase 1 foi DROPPED — gateway Phase 3 probe loop (ProbeIntervalSeconds=10) é o substituto. RUNBOOK Architecture section calls out the dropped component."
    - "gateway/docs/RUNBOOK-PRIMARY-POD.md operator-facing doc com seções: Overview, Architecture (supervisord 4-service single-container model + custom image build), Environment Variables (24 PRIMARY_* + reuse list — inclui PrimaryProvisionFailureCooldownSeconds per Plan 06.6-03 revision), Image Build & Bump Policy (NEW Wave 0 — references .github/workflows/build-primary-pod.yml + pod/primary/Dockerfile + WAVE0-GATES Decision 1 SHA pins), Schedule Behavior, Operator CLI (gatewayctl primary subcommands), Troubleshooting (cold-start gap, schedule won't fire, drain hangs, emerg precedence, supervisord child crash diagnostics), Cost Budget Monitoring"
    - "RUNBOOK-PRIMARY-POD.md documenta production-mode deployment checklist: env vars in Portainer stack UI, sha256 SHAs operator-set, soak gate sequence (deploy → manual flip PRIMARY_POD_SCHEDULE_DISABLED=false após UAT GREEN)"
    - "06.6-HUMAN-UAT.md operator sign-off section deve ser preenchida ANTES de marcar Phase 6.6 COMPLETE — analog Phase 6/6.5 pattern"
  artifacts:
    - path: ".planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md"
      provides: "Live Vast.ai UAT script com 6+ scenarios + explicit drain log assertion + supervisord 4-service health probes"
      contains: "Scenario 1|Scenario 2|Sign-off|Cost actual|schedule_window_exited|ready.*draining|supervisord"
    - path: "gateway/docs/RUNBOOK-PRIMARY-POD.md"
      provides: "Operator runbook for primary pod ops — Wave 0 supervisord + custom image architecture documented"
      contains: "Overview|Architecture|Environment Variables|Schedule Behavior|Troubleshooting|Cost Budget|health-bridge.*DROPPED|supervisord|converseai-primary-pod|server-cuda-b9191"
  key_links:
    - from: "06.6-HUMAN-UAT.md Scenario 1"
      to: "gatewayctl primary force-up"
      via: "operator command execution"
      pattern: "gatewayctl primary force-up"
    - from: "06.6-HUMAN-UAT.md Scenario 5"
      to: "gateway logs (transition ready→draining)"
      via: "explicit grep assertion (BLOCKER 2 revision)"
      pattern: "transition.*ready.*draining.*schedule_window_exited"
    - from: "06.6-HUMAN-UAT.md Scenario 6 pre-flight"
      to: "gatewayctl emerg force-provision (Phase 6 deliverable)"
      via: "command existence verification (WARNING 5 revision)"
      pattern: "gatewayctl emerg force-provision"
    - from: "RUNBOOK-PRIMARY-POD.md Architecture section"
      to: "pod/primary/Dockerfile + supervisord.conf + .github/workflows/build-primary-pod.yml (Plan 04)"
      via: "explicit links"
      pattern: "pod/primary/Dockerfile|build-primary-pod.yml"
    - from: "RUNBOOK-PRIMARY-POD.md Troubleshooting"
      to: "STATE.md tech debt entries (cold-start, schedule, drain)"
      via: "operator reference docs"

threat_refs: [T-06.6-04]
---

<objective>
Live Vast.ai UAT + operator runbook. Este é o último gate before Phase 6.6 COMPLETE. Custo estimado Vast.ai live spend: ~$0.50-1.00 (3 short pod lifecycles + 1 longer for schedule simulation).

Purpose: Mechanical proof + human-witnessed validation que (a) o full stack (4 services supervisord co-located) sobe em Vast.ai real, (b) tool-calling Qwen3.6 funciona, (c) STT e embed retornam respostas válidas, (d) schedule trigger/drain ciclo completo funciona COM ASSERTION EXPLÍCITA das transições FSM (BLOCKER 2), (e) Pitfall #11 emerg coexistence é safely resolved (com pre-flight check de Phase 6 dependencies per WARNING 5). SEM este UAT, o código pode estar logicamente correto mas falhar contra real-world Vast.ai surprises (network, supervisord timing, GPU hand-off race).

**Wave 0 LOCKED**: RUNBOOK Architecture documents the supervisord-as-PID-1 4-child-process model (NOT DinD — DinD rejected per Spike Task 1). RUNBOOK references the custom multi-stage image build pipeline (pod/primary/Dockerfile + .github/workflows/build-primary-pod.yml + 4 SHA-pinned upstream sources per WAVE0-GATES.md Decision 1).

T-06.6-05 (DinD escape) REMOVED from threat_refs per Wave 0 — DinD not implemented. Supervisord children share existing container namespace (already trusted).

Output: 2 docs novos. Operator sign-off é blocking checkpoint (autonomous=false) — Phase 6.6 COMPLETE só após Task 3 operator approves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-SPIKE-dind-privileged.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-SPIKE-qwen3.6-jinja.md
@.planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md
@gateway/docs/RUNBOOK-EMERGENCY-POD.md
@gateway/cmd/gatewayctl/emerg.go
@pod/smoke
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write gateway/docs/RUNBOOK-PRIMARY-POD.md (operator runbook) — Wave 0 supervisord architecture + custom image build docs</name>
  <files>gateway/docs/RUNBOOK-PRIMARY-POD.md</files>
  <read_first>
    - gateway/docs/RUNBOOK-EMERGENCY-POD.md (analog runbook — full content; mirror structure)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Service Endpoint Matrix (lines 893-905) — service ports + health endpoints + weight files; §Schedule Reconciler Design (lines 907-982) — state diagram + env var contract
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md (operator-locked decisions to surface in runbook — supervisord LOCKED + 4 SHA pin digests + b9191 upgrade)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-SPIKE-dind-privileged.md (DinD rejection evidence — runbook should cite this)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md (D-03/D-05/D-06.1/D-07 decisions to surface)
    - STATE.md Open Todos (operator-visible tech debts to call out in Troubleshooting)
  </read_first>
  <action>Criar gateway/docs/RUNBOOK-PRIMARY-POD.md com seções: (1) **Overview** — purpose, relationship to emergency pod (precedence per Pitfall #11), schedule-driven vs event-driven, cost estimate ~$130/mo at default schedule. (2) **Architecture** (Wave 0 LOCKED — see WARNING 1 revision):
- Custom multi-stage image `ghcr.io/ifixtelecom/converseai-primary-pod:vX@sha256:...` built by `.github/workflows/build-primary-pod.yml` from `pod/primary/Dockerfile`. Image bundles 4 service binaries extracted from 4 SHA-pinned upstream images (per WAVE0-GATES.md Decision 1):
  - `ghcr.io/ggml-org/llama.cpp:server-cuda-b9191@sha256:cb37...` (UPGRADED from b9128 — Qwen3.6 SSM support)
  - `ghcr.io/speaches-ai/speaches:0.9.0-rc.3-cuda-12.6.3@sha256:5c62...`
  - `michaelf34/infinity:0.0.77@sha256:11e8...`
  - `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.3-4.8.2-distroless@sha256:60d3...`
- At runtime: **supervisord as PID 1** (NOT DinD — DinD design REJECTED per 06.6-SPIKE-dind-privileged.md: overlayfs in nested namespace fails on Vast.ai). 4 child processes spawn via `/etc/supervisor/conf.d/services.conf`:
  - `[program:llama]` exec `/app/llama-server` on port 8000 (Qwen3.6 + --jinja)
  - `[program:speaches]` exec speaches on port 8001 (Whisper-Large-v3)
  - `[program:infinity]` exec infinity_emb on port 8002 (BGE-M3)
  - `[program:dcgm]` exec dcgm-exporter on port 9400
- All 4 services share container's network namespace, GPU device, filesystem. Gateway only sees 4 HTTP endpoints exposed via Vast.ai host port mapping.
- **Phase 1 health-bridge service was DROPPED per CONTEXT.md D-05** — gateway Phase 3 probe loop (ProbeIntervalSeconds=10 default) substituiu, eliminating the custom Go health-bridge sidecar entirely (D-05 traceability per WARNING 1 revision).

(3) **Image Build & Bump Policy** (NEW Wave 0):
- Image bumps require WAVE0-GATES.md amendment + operator sign-off
- Workflow `.github/workflows/build-primary-pod.yml` triggers on `develop` push (path-filtered `pod/primary/**`) + `v*` tags + workflow_dispatch
- Published as `ghcr.io/ifixtelecom/converseai-primary-pod:vX.Y.Z@sha256:...`
- To bump upstream LLM (e.g., b9191 → b9234): update FROM line in `pod/primary/Dockerfile` + run `docker manifest inspect` to capture new digest + update WAVE0-GATES.md Decision 1 + bump semver tag + push

(4) **Environment Variables** — tabular list of 24 PRIMARY_* (incluindo PrimaryProvisionFailureCooldownSeconds added Plan 06.6-03 + PrimaryPodScheduleProvisionLeadSeconds added per reviews #8) + 6 reused (MINIO_*, VAST_AI_API_KEY, POD_DEBUG_SSH_PUBLIC_KEY, USD_TO_BRL_RATE); for each: env name, type, default, description, change requires restart yes/no. Document Wave 0 defaults: PrimaryTemplateImage = `server-cuda-b9191@sha256:cb37...` (NOT b9128); PrimaryPodScheduleDisabled default `true` (soak gate per Decision 5); PrimaryProvisionColdStartBudgetSeconds default `2400` (40min per Decision 6); PrimaryQwenJinjaKey default `""` (B1 GGUF embedded per Decision 3).

(5) **Schedule Behavior** — IsInPeak semantics, overnight wrap (reviews #5 fix documented), DST-immune (Brazil), kill-switch via PRIMARY_POD_SCHEDULE_DISABLED, hot-reload NOT supported (gateway restart required); explicitly document FSM transition log format: `gateway emits log line "transition <from>→<to> reason=<reason>" on every FSM transition; key transitions to grep for: provisioning_started, all_probes_passed (ready), schedule_window_exited (start of drain), drain_complete (start of destroy), destroyed`. Document pre-warm offset (PrimaryPodScheduleProvisionLeadSeconds, default 1800s = 30min — reviews #8) so operator understands provisioning starts BEFORE UpHour.

(6) **Operator CLI** — `gatewayctl primary state|force-up|force-down|schedule|lifecycles` examples with sample output + cross-reference to `gatewayctl emerg force-provision|force-destroy` (Phase 6 deliverable; preserved). Include sample `gatewayctl primary schedule` output showing ProvisionLeadSeconds line per reviews #8.

(7) **Deployment Checklist** — Portainer stack env update sequence, sha256 ops upload via pod/scripts/upload-weights.sh, soak gate (PRIMARY_POD_SCHEDULE_DISABLED=true pós-deploy; operator manual flip false após UAT GREEN per WAVE0-GATES Decision 5).

(8) **Troubleshooting** — Cold-start gap ~25-30min (SC-2 inherited from Phase 6 — known limitation; 40min budget per Decision 6); schedule won't fire (check PRIMARY_POD_SCHEDULE_DISABLED + cfg.timezone parse + redsync leader lock contention + PrimaryProvisionFailureCooldownSeconds cooldown gate from prior failure); drain hangs (max wait = grace_s, then force destroy); emerg precedence (primary takes over via Pub/Sub event); **supervisord child crash diagnostics** (NEW Wave 0): SSH into pod (if POD_DEBUG_SSH_PUBLIC_KEY set), check `/var/log/supervisord.log` + `/var/log/llama.log` / `/var/log/speaches.log` / `/var/log/infinity.log` / `/var/log/dcgm.log`. supervisord autorestarts crashed children but logs preserve forensics. Pod-level GPU silently fallback to CPU = real risk — gateway P95 breaker detects, fallback chain redirects.

(9) **Cost Budget Monitoring** — MonthlyPrimaryBudgetBRL (default 800 BRL ~$160), audit_log event_kind=primary_lifecycle_close, Sentry alert dedupe 24h.

(10) **Security Notes** — POD_DEBUG_SSH_PUBLIC_KEY production = empty; supervisord co-located children share container namespace (NOT nested DinD — that was rejected per Spike Task 1); operator MUST NEVER commit env file with SSH key.

(11) **Phase 6.6 References** — links to CONTEXT.md / RESEARCH.md / PATTERNS.md / WAVE0-GATES.md / SPIKE-dind-privileged.md / SPIKE-qwen3.6-jinja.md. Footer with "Last updated: 2026-05-17 (Phase 6.6 plan 06.6-11; revised per checker + Wave 0 supervisord lock 2026-05-17)".</action>
  <verify>
    <automated>ls /home/pedro/projetos/pedro/gpu-ifix/gateway/docs/RUNBOOK-PRIMARY-POD.md && grep -c '^## ' /home/pedro/projetos/pedro/gpu-ifix/gateway/docs/RUNBOOK-PRIMARY-POD.md | awk '$1 >= 11 { exit 0 } { exit 1 }' && grep -q 'health-bridge.*DROPPED\|D-05' /home/pedro/projetos/pedro/gpu-ifix/gateway/docs/RUNBOOK-PRIMARY-POD.md && grep -q 'supervisord' /home/pedro/projetos/pedro/gpu-ifix/gateway/docs/RUNBOOK-PRIMARY-POD.md && grep -q 'server-cuda-b9191' /home/pedro/projetos/pedro/gpu-ifix/gateway/docs/RUNBOOK-PRIMARY-POD.md && grep -q 'converseai-primary-pod' /home/pedro/projetos/pedro/gpu-ifix/gateway/docs/RUNBOOK-PRIMARY-POD.md && ! grep -q 'docker run -d.*--gpus all' /home/pedro/projetos/pedro/gpu-ifix/gateway/docs/RUNBOOK-PRIMARY-POD.md</automated>
  </verify>
  <acceptance_criteria>
    - RUNBOOK exists with 11+ H2 sections
    - D-05 (health-bridge dropped) explicitly documented per WARNING 1
    - supervisord architecture documented (4 program blocks, PID 1)
    - b9191 (NOT b9128) referenced as engine LLM image
    - Custom image build pipeline references pod/primary/Dockerfile + .github/workflows/build-primary-pod.yml
    - NO references to DinD/dockerd/nested-docker-run as architecture (only as "rejected per Spike Task 1")
    - Sample gatewayctl primary schedule output shows ProvisionLeadSeconds
  </acceptance_criteria>
  <done>RUNBOOK-PRIMARY-POD.md existe com 11+ seções H2. D-05 (health-bridge dropped) explicitly documented per WARNING 1 revision. Architecture section describes supervisord 4-service single-container model + custom image build pipeline (Wave 0 LOCKED). b9191 referenced (NOT b9128). Operator pode usar este doc como referência única para ops dia-a-dia.</done>
</task>

<task type="auto">
  <name>Task 2: Write 06.6-HUMAN-UAT.md (live Vast.ai UAT script) — com explicit drain assertion (BLOCKER 2) + force-provision pre-flight (WARNING 5) + Wave 0 supervisord 4-service probes</name>
  <files>.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06-emergency-pod-template-refactor/06-HUMAN-UAT.md (analog format; 6 cenários LIVE)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Validation Architecture (lines 1009-1070) — req→test map; §Open Question 8 (lines 1164-1166) — STT + embed live coverage requirement
    - pod/smoke (Phase 1 smoke fixtures — reuse for VRAM POD-04 + chat 8k + Whisper longa)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md (Decision 5 soak gate sequence)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-SPIKE-qwen3.6-jinja.md (validates tool-calling shape that Scenario 2 will assert)
    - gateway/cmd/gatewayctl/emerg.go (verify force-provision + force-destroy subcommands exist — confirmed Phase 6 deliverable per 06-06-SUMMARY.md and line 9/18 comments in emerg.go)
  </read_first>
  <action>Criar 06.6-HUMAN-UAT.md com header: title, Phase, Author, Date, Estimated Cost ~$0.50-1.00, Estimated Time 90min. **Prerequisites section (per WARNING 5)**: (a) gateway dev stack deployed via Portainer (post-Phase 6.6 PRs merged); (b) env vars set per RUNBOOK Deployment Checklist; (c) PRIMARY_POD_SCHEDULE_DISABLED=true initially; (d) **Pre-flight CLI existence check**: `gatewayctl emerg force-provision --help 2>&1` exits 0 and prints usage — proves Phase 6 dependency present. If this check fails, STOP UAT and verify Phase 6 PRs are deployed; (e) **Pre-flight CLI existence check (primary)**: `gatewayctl primary --help 2>&1` exits 2 and prints "Usage: gatewayctl primary state|force-up|force-down|schedule|lifecycles" — proves Plan 06.6-09 deployed; (f) **Pre-flight image build check** (NEW Wave 0): `docker pull ghcr.io/ifixtelecom/converseai-primary-pod:dev-{sha} || gh run list --workflow=build-primary-pod.yml --limit=1` — proves Plan 04 image is published to ghcr.io.

Seção **6 Scenarios** numbered + each with: (a) operator action steps, (b) expected outcome, (c) cleanup, (d) pass/fail checkbox.

**Scenario 1: Force provision + 4 services GREEN (Wave 0 supervisord)** — operator runs `gatewayctl primary force-up`; expect Vast pod created within 30s using `ghcr.io/ifixtelecom/converseai-primary-pod:vX@sha256:...` image (verify in Vast UI instance details); wait ~25-30min cold-start (aria2c weight downloads + supervisord init); then `curl http://<pod-ip>:<host-8000>/v1/models` returns 200 + Qwen3.6 model in list (llama-server child); `curl :<host-8001>/health` Speaches 200 (speaches child); `:<host-8002>/health` Infinity 200 (infinity child); `:<host-9400>/metrics` DCGM 200 with Prometheus format (dcgm-exporter child). **Wave 0 ASSERTION**: all 4 endpoints respond from SAME pod IP (different host ports) — proves supervisord 4-service single-container model. If pod SSH key set, additionally verify `ssh root@<pod-ip> supervisorctl status` shows 4 running programs (llama, speaches, infinity, dcgm) all "RUNNING".

**Scenario 2: Tool-calling /v1/chat/completions** — POST with tools=[get_weather]; assert response has tool_calls[0].function.name=="get_weather" and arguments.location parseable; validates D-04 + WAVE0-GATES Decision 3 B1 GGUF-embedded Jinja in production (matches Wave 0 spike Round 3 PASS result).

**Scenario 3: Speaches /v1/audio/transcriptions** — use a 30s WhatsApp audio fixture from converseai-v4 or pod/smoke; assert response.text non-empty + duration matches.

**Scenario 4: Infinity /v1/embeddings** — POST com 8 input sentences; assert response.data length 8 + each embedding has 1024 floats.

**Scenario 5 (per BLOCKER 2): Schedule out-of-window drain — com EXPLICIT log assertion**: (a) set PRIMARY_POD_SCHEDULE_DOWN_HOUR to current hour - 1 (simulate out-of-window) via Portainer env update + restart gateway; (b) gateway boots → reconciler reads new rule; (c) **EXPLICIT ASSERTION**: dentro de 90s do restart, gateway logs MUST emit a line matching `transition.*ready.*draining.*schedule_window_exited` — operator runs `docker logs converseai-dev-gateway --tail 500 2>&1 | grep -E "transition.*ready.*draining.*schedule_window_exited"` and verifies at least 1 match (exit code 0); if zero matches dentro de 90s, FAIL Scenario 5 imediatamente (drain logic não disparou); (d) primary FSM transitions Ready→Draining; concurrent test requests (5 LLM completions) should still succeed during grace period; (e) **SECOND EXPLICIT ASSERTION**: após cfg.PrimaryPodScheduleGraceRampDownSeconds (300s default) elapsed, operator runs `docker logs converseai-dev-gateway --tail 500 2>&1 | grep -E "transition.*draining.*destroying.*drain_complete"` and verifies match; (f) **THIRD ASSERTION**: operator runs `gatewayctl primary state` and verifies state == "asleep" (FSM reached terminal state of drain cycle); (g) fallback chain (OpenRouter LLM) takes over for new requests.

**Scenario 6: Pitfall #11 emerg coexist (per WARNING 5)** — pre-flight: confirmed `gatewayctl emerg force-provision` exists per Prerequisites step (d); manually trigger emerg via `gatewayctl emerg force-provision --reason "uat_pitfall11_test"`; emerg provisions Vast pod; observe `gatewayctl emerg state` shows emerg in StateEmergencyActive; THEN flip PRIMARY_POD_SCHEDULE_DISABLED=false + run `gatewayctl primary force-up`; observe gateway logs: primary pod cold-starts (supervisord brings up 4 children); quando primary marks Ready (emit primary_ready event), emerg subscriber fires; emerg pod destroyed dentro 30s; verify via Vast UI both Vast instances respectively created/destroyed; verify gateway routes resume primary; assert no >30s window of dual pod billing.

Seção **Cleanup**: destroy all Vast pods via `gatewayctl primary force-down` + `gatewayctl emerg force-destroy`; verify via Vast UI zero active instances; revert PRIMARY_POD_SCHEDULE_DISABLED=true.

Seção **Sign-off**: signoff date, signer (operator), all 6 scenarios pass/fail, cost actual ($), notes for follow-ups. Final note: results feed 06.6-VERIFICATION.md (created by /gsd:verify-work) — operator should track Sentry post-UAT for 24h.</action>
  <verify>
    <automated>ls /home/pedro/projetos/pedro/gpu-ifix/.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md && grep -c '^### Scenario' /home/pedro/projetos/pedro/gpu-ifix/.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md | awk '$1 >= 6 { exit 0 } { exit 1 }' && grep -q 'schedule_window_exited' /home/pedro/projetos/pedro/gpu-ifix/.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md && grep -q 'gatewayctl emerg force-provision' /home/pedro/projetos/pedro/gpu-ifix/.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md && grep -q 'supervisord\|supervisorctl' /home/pedro/projetos/pedro/gpu-ifix/.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md && grep -q 'converseai-primary-pod' /home/pedro/projetos/pedro/gpu-ifix/.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 06.6-HUMAN-UAT.md exists with 6+ Scenario blocks + Sign-off section
    - EXPLICIT drain log assertions in Scenario 5 (BLOCKER 2 resolved)
    - force-provision pre-flight in Prerequisites (WARNING 5 resolved)
    - Wave 0 supervisord 4-service probes in Scenario 1 (supervisorctl status check + 4-endpoint reachability assertion)
    - References ghcr.io/ifixtelecom/converseai-primary-pod custom image in Prerequisites pre-flight
  </acceptance_criteria>
  <done>06.6-HUMAN-UAT.md existe com 6+ scenarios + Sign-off section + EXPLICIT drain log assertions (BLOCKER 2 resolved) + force-provision pre-flight check (WARNING 5 resolved) + Wave 0 supervisord 4-service probes (Scenario 1) + custom image pre-flight check (Wave 0). Operator pode executar script direto sem ambiguidade.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Execute live Vast.ai UAT (operator)</name>
  <files>.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-HUMAN-UAT.md</files>
  <what-built>Plans 06.6-01..06.6-10 shipped: vastutil + primary package + config (24 fields incl cooldown + lead) + DB + redisx + main.go wiring + emerg coexist + gatewayctl CLI + 10 integration tests (including primary_supervisord_test.go per Wave 0 + primary_lifecycles_integration_test.go per BLOCKER 3) all GREEN + custom multi-stage image `ghcr.io/ifixtelecom/converseai-primary-pod` built and pushed to ghcr.io via .github/workflows/build-primary-pod.yml. Phase 6.6 logic complete; needs live Vast.ai validation against real GPU + real network + real supervisord + 4 service binaries baked into custom image.</what-built>
  <how-to-verify>
    1. Verificar `cd gateway && go test ./... -count=1 -race -tags=integration -timeout=15m` ALL GREEN antes de gastar $$
    2. Confirmar PR1 + PR2 merged em develop, Portainer stack ai-gateway-dev recreated com env vars per RUNBOOK-PRIMARY-POD.md
    3. Confirmar PRIMARY_POD_SCHEDULE_DISABLED=true (soak gate inicial)
    4. **Executar Prerequisites pre-flight checks** (incluindo gatewayctl emerg force-provision --help + gatewayctl primary --help + docker pull converseai-primary-pod image OR gh run list build-primary-pod.yml) — se qualquer um falhar, STOP
    5. Abrir 06.6-HUMAN-UAT.md
    6. Executar Scenarios 1-6 em ordem; preencher checkboxes; capturar logs gateway + Vast.ai UI screenshots
    7. **Para Scenario 1 Wave 0 supervisord assertion**: se POD_DEBUG_SSH_PUBLIC_KEY set, execute `ssh root@<pod-ip> supervisorctl status` AND record the 4 program lines (llama, speaches, infinity, dcgm all RUNNING) in Sign-off section
    8. **Para Scenario 5**: explicitly run the 3 log-grep assertions (ready→draining + draining→destroying + primary state asleep); record the matching log lines in Sign-off section (proof of drain execution)
    9. Após Scenario 6: verificar Vast UI ZERO active instances + Sentry sem CRITICAL events
    10. Preencher Sign-off section: data, operator name, pass/fail por scenario, cost real ($), notas
    11. Type "approved" se todos 6 scenarios PASS, ou descrever quais failed + propor follow-up
  </how-to-verify>
  <resume-signal>Type "approved" + commit 06.6-HUMAN-UAT.md sign-off, OR describe failed scenarios + propose follow-up plan (gap closure via /gsd:plan-phase --gaps OR new tech debt entry in STATE.md)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → Portainer (dev stack env update) | Phase 6.6 needs env vars before primary reconciler can do anything; operator-controlled |
| operator workstation → Vast.ai console (visual inspection during UAT) | Manual validation; outside automation surface |
| ghcr.io → Vast.ai pod (custom image pull) | OIDC-authenticated GHA pushed image; Vast pod pulls via standard docker pull |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-04 | DoS | Operator misconfigures schedule env vars in Portainer leading to provisioning loop | mitigate | UAT Scenario 5 explicitly tests schedule out-of-window drain — operator MUST observe drain working in production env before going live VIA EXPLICIT LOG-GREP ASSERTIONS (BLOCKER 2 revision) confirming gateway emits transition ready→draining log line + transition draining→destroying line + FSM reaches asleep. Soak gate (DISABLED=true) is the safety net. |
| T-06.6-SC | Tampering | Custom converseai-primary-pod image | mitigate | Image built by GHA workflow with GITHUB_TOKEN auth + GHA cache. Image SHA pin enforced at gateway-side via cfg.PrimaryTemplateImage default (Plan 03 test enforces b9191@sha256:cb37...). Operator can override env to a different SHA-pinned ref for rollback. |
</threat_model>

<verification>
- gateway/docs/RUNBOOK-PRIMARY-POD.md exists with 11+ H2 sections + D-05 health-bridge drop documented + supervisord architecture + custom image build pipeline + b9191 references
- 06.6-HUMAN-UAT.md exists with 6+ Scenario blocks + explicit drain log assertions in Scenario 5 + force-provision pre-flight in Prerequisites + supervisord 4-service probes in Scenario 1
- All 6 UAT scenarios PASS (operator sign-off) — autonomous=false until operator approves
</verification>

<success_criteria>
- Phase 6.6 production-ready: live UAT GREEN + RUNBOOK published
- Pitfall #11 emerg coexistence resolved in production
- Schedule-driven lifecycle functional — provado mecanicamente via log-grep assertions, não inferido pela operator (BLOCKER 2 resolved)
- 4 services (LLM + STT + Embed + DCGM) all healthy on Vast.ai real pod (supervisord 4-child-process model validated)
- Tool-calling validated end-to-end (D-04 + WAVE0-GATES Decision 3 B1 embedded Jinja)
- D-05 (health-bridge dropped) traceability surfaced in RUNBOOK (WARNING 1 resolved)
- Phase 6 CLI dependency (gatewayctl emerg force-provision) explicitly pre-flight checked before Scenario 6 (WARNING 5 resolved)
- Wave 0 supervisord architecture documented in RUNBOOK + asserted in UAT Scenario 1
- Custom image (converseai-primary-pod) build pipeline operational + verified in Prerequisites
- Operator has runbook + CLI tools for ops day-to-day
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-11-SUMMARY.md` after Task 3 sign-off received.
</output>
