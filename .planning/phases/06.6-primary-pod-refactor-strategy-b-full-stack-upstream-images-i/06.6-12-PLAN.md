---
phase: 06.6
plan: 12
type: execute
wave: 6
depends_on: [06.6-11]
files_modified:
  - pod/onstart.sh
  - pod/docker-compose.yml
  - pod/health-bridge
  - pod/scripts/download-weights.sh
  - pod/README.md
  - .github/workflows/smoke.yml
  - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md
autonomous: true
requirements: []
tags: [cleanup, pr3, wave-6, delete, post-uat, wave0-supervisord]

must_haves:
  truths:
    - "pod/onstart.sh (137 lines) DELETED — substituído por gateway/internal/primary/onstart.go primaryOnstartHead raw-string Go const (Plan 06.6-04 Task 3)"
    - "pod/docker-compose.yml (183 lines) DELETED — substituído por **supervisord** (Wave 0 LOCKED) dentro do custom multi-stage image (pod/primary/Dockerfile + pod/primary/supervisord.conf via Plan 06.6-04 Tasks 1-2)"
    - "pod/health-bridge/ subdir (entire tree) DELETED — droped per D-05; gateway Phase 3 probe loop (ProbeIntervalSeconds=10) substituiu — D-05 traceability: health-bridge service no longer exists in repo"
    - "pod/scripts/download-weights.sh DELETED — consumido apenas pelo pod/onstart.sh (deletado); inline `aria2c download_with_verify` agora vive em gateway/internal/primary/onstart.go primaryOnstartHead (Plan 04)"
    - "pod/README.md REWRITE para 1 paragraph pointer redirecting para gateway/docs/RUNBOOK-PRIMARY-POD.md + lista files preserved (incl. **NEW pod/primary/Dockerfile + pod/primary/supervisord.conf** per Wave 0) + CI history showing what was deleted (smoke.yml in Phase 6.6 PR3, build-pod.yml in Phase 6 PR2)"
    - ".github/workflows/smoke.yml DELETED — Phase 1 vestige tied to deleted artifacts. Strategy B-revised primary pod is now reconciler-driven (gateway/internal/primary + gatewayctl primary force-up CLI) + image-built via .github/workflows/build-primary-pod.yml (Plan 04 Task 2 NEW); the GHA-driven smoke flow cannot survive deletion of its dependencies. UAT moves to Plan 06.6-11 live operator-run script."
    - "**Files KEPT (preserved)** (with Wave 0 additions): pod/scripts/upload-weights.sh (operator weight rotation pipeline ainda usado), pod/templates/qwen3.5-27b-tool-calling.jinja (still consumed by emerg pod), pod/smoke/ (Phase 1 smoke fixtures reused for UAT POD-07 VRAM validation), pod/scripts/vast-ai.sh (operator manual provisioning fallback), **pod/primary/Dockerfile (NEW Plan 04 Task 1 — Wave 0 custom multi-stage image source)**, **pod/primary/supervisord.conf (NEW Plan 04 Task 1 — Wave 0 supervisord 4-program config baked into custom image)**, **.github/workflows/build-primary-pod.yml (NEW Plan 04 Task 2 — Wave 0 GHA build pipeline)**"
    - "Already-deleted files (Phase 6 PR2 commit 064702e): pod/Dockerfile (verified absent at plan time), .github/workflows/build-pod.yml (verified absent at plan time) — no action needed in this plan for these"
    - "git diff stats: ~700-900 lines deleted (PATTERNS.md:895 estimate) + ~290 lines from smoke.yml; zero gateway/ regression (verified by Plan 06.6-10 integration test suite green)"
  artifacts:
    - path: "pod/README.md"
      provides: "Redirect pointer to RUNBOOK-PRIMARY-POD.md + list of preserved files including Wave 0 new artifacts"
      contains: "RUNBOOK-PRIMARY-POD.md|gateway/docs|pod/primary/Dockerfile|supervisord.conf|build-primary-pod.yml"
  key_links:
    - from: "pod/README.md"
      to: "gateway/docs/RUNBOOK-PRIMARY-POD.md"
      via: "markdown link"
      pattern: "gateway/docs/RUNBOOK-PRIMARY-POD\\.md"
    - from: "pod/README.md"
      to: "pod/primary/Dockerfile + .github/workflows/build-primary-pod.yml (Wave 0 NEW artifacts from Plan 04)"
      via: "markdown links in Files Preserved section"
      pattern: "pod/primary/Dockerfile|build-primary-pod\\.yml"

threat_refs: []
---

<objective>
PR3 cleanup — burnt-bridge final delete dos artefatos do Phase 1 primary pod arch (HOST-mode docker-compose + custom onstart bash + health-bridge Go service + GHA smoke workflow) que foram substituídos pela Phase 6.6 Strategy B-revised architecture (supervisord-based custom multi-stage image — Wave 0 LOCKED). Este plan corre APENAS APÓS Plan 06.6-11 live UAT GREEN, mirror Phase 6 PR2 sequence (PR1 inline → PR2 delete pós-UAT).

**Wave 0 LOCKED**: legacy `pod/Dockerfile` + `.github/workflows/build-pod.yml` já foram deletados em Phase 6 PR2 commit 064702e — verified absent at plan time. This plan only deletes the remaining Phase 1 vestiges. **NEW preserved artifacts** (Plan 04 Wave 0 outputs): `pod/primary/Dockerfile` + `pod/primary/supervisord.conf` + `.github/workflows/build-primary-pod.yml` — these are the active build pipeline for the converseai-primary-pod image and MUST be preserved.

Purpose: Sem este cleanup, o repo carrega dual-implementation arch (legacy pod/ + new gateway/internal/primary/ + pod/primary/) + um workflow `.github/workflows/smoke.yml` que referencia arquivos deletados (CI quebra silenciosamente quando alguém tentar workflow_dispatch). Phase 6.6 termina apenas quando legacy é removido E quebra de CI é prevenida E new Wave 0 artifacts (pod/primary/) preserved + documented in pod/README.md.

Output: 5 files/subdirs deleted (4 pod/ + 1 .github/workflows/) + 1 file rewritten (pod/README.md) + 1 CONTEXT.md amendment (deferred follow-up). ~1000-1200 lines deleted no diff. ZERO impact runtime (Plan 06.6-08 main.go já é o único path em produção; UAT Plan 06.6-11 confirma).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md
@pod/onstart.sh
@pod/docker-compose.yml
@pod/health-bridge
@pod/scripts/download-weights.sh
@pod/scripts/upload-weights.sh
@pod/README.md
@.github/workflows/smoke.yml
@gateway/docs/RUNBOOK-PRIMARY-POD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete pod/onstart.sh + pod/docker-compose.yml + pod/health-bridge/ + pod/scripts/download-weights.sh + .github/workflows/smoke.yml (preserve Wave 0 new artifacts in pod/primary/)</name>
  <files>pod/onstart.sh, pod/docker-compose.yml, pod/health-bridge, pod/scripts/download-weights.sh, .github/workflows/smoke.yml</files>
  <read_first>
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 67-77 (DELETED files table — confirm what stays vs goes)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-11-SUMMARY.md (verify Live UAT signed off GREEN before destruction)
    - pod/primary/Dockerfile (Plan 04 Task 1 — verify exists; Wave 0 LOCKED, MUST NOT delete)
    - pod/primary/supervisord.conf (Plan 04 Task 1 — verify exists; MUST NOT delete)
    - .github/workflows/build-primary-pod.yml (Plan 04 Task 2 — verify exists; MUST NOT delete)
    - pod/scripts/upload-weights.sh (verify this is KEPT — operator pipeline still in use)
    - .github/workflows/smoke.yml lines 155-215 (confirm hard refs ao pod/onstart.sh + pod/docker-compose.yml + pod/scripts/download-weights.sh — these prove the workflow cannot survive without the deleted artifacts)
    - .github/workflows/ (ls remaining workflows to confirm CI surface after delete — build-pod.yml já deleted Phase 6 PR2; smoke.yml é o último Phase 1 vestige; build-primary-pod.yml NEW Plan 04 stays)
  </read_first>
  <action>**Pre-flight assertions** antes de deletar:
- ASSERT Plan 06.6-11 UAT sign-off é GREEN (read 06.6-11-SUMMARY.md). Se FAIL or pending, STOP.
- ASSERT Wave 0 NEW artifacts exist: `[ -f pod/primary/Dockerfile ] && [ -f pod/primary/supervisord.conf ] && [ -f .github/workflows/build-primary-pod.yml ]`. If any missing, STOP (Plan 04 incomplete — fix before cleanup).
- ASSERT already-deleted Phase 6 PR2 files stay deleted: `[ ! -f pod/Dockerfile ] && [ ! -f .github/workflows/build-pod.yml ]`. If either exists, investigate (Phase 6 PR2 partial revert?).

Caso contrário: (1) `git rm pod/onstart.sh`; (2) `git rm pod/docker-compose.yml`; (3) `git rm -r pod/health-bridge/` (entire subdir incluindo .go files + Dockerfile se houver + go.mod local se houver); (4) `git rm pod/scripts/download-weights.sh`; (5) `git rm .github/workflows/smoke.yml`.

Decisão DELETE (não rewrite) para smoke.yml: o workflow é Phase 1 GHA-driven smoke baseado em `pod/scripts/vast-ai.sh create --onstart pod/onstart.sh` + scp do compose + onstart re-invoke; com Strategy B-revised (Wave 0 supervisord), primary pod é provisioned pelo reconciler (gateway/internal/primary) com custom multi-stage image (ghcr.io/ifixtelecom/converseai-primary-pod) e validado por Plan 06.6-11 live operator script (06.6-HUMAN-UAT.md) que cobre os mesmos cenários (4 services GREEN, tool-calling, STT, embed, schedule, Pitfall #11 coexist). Rewrite would require parallel maintenance burden sem ganho — operator UAT é mais aderente à realidade Strategy B-revised.

NÃO deletar (preserved): `pod/scripts/upload-weights.sh`, `pod/scripts/vast-ai.sh`, `pod/templates/qwen3.5-27b-tool-calling.jinja`, `pod/templates/qwen3.5-27b-tool-calling.jinja.sha256`, `pod/smoke/`, `pod/weights/.gitkeep`, **pod/primary/Dockerfile (Plan 04 — Wave 0)**, **pod/primary/supervisord.conf (Plan 04 — Wave 0)**, **.github/workflows/build-primary-pod.yml (Plan 04 — Wave 0)**.

Verificar `git status` shows exatamente 5 deletes. Commitar com mensagem `chore(06.6): PR3 — delete pod/ legacy + smoke.yml (Phase 1 vestiges; Wave 0 supervisord supersedes via pod/primary/Dockerfile + supervisord.conf)` — referencia 06.6-11-SUMMARY.md sign-off no body + cita linhas 163/197/199/200/213 do smoke.yml como prova das refs quebradas.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix && [ ! -f pod/onstart.sh ] && [ ! -f pod/docker-compose.yml ] && [ ! -d pod/health-bridge ] && [ ! -f pod/scripts/download-weights.sh ] && [ ! -f .github/workflows/smoke.yml ] && [ -f pod/scripts/upload-weights.sh ] && [ -d pod/smoke ] && [ -f pod/primary/Dockerfile ] && [ -f pod/primary/supervisord.conf ] && [ -f .github/workflows/build-primary-pod.yml ] && ! grep -rE "pod/(onstart\.sh|docker-compose\.yml|scripts/download-weights\.sh)" .github/ 2>/dev/null && echo "delete-set OK"</automated>
  </verify>
  <acceptance_criteria>
    - 5 legacy paths deleted (pod/onstart.sh, pod/docker-compose.yml, pod/health-bridge/, pod/scripts/download-weights.sh, .github/workflows/smoke.yml)
    - KEPT files preserved (pod/scripts/upload-weights.sh, pod/smoke/, pod/templates/, pod/scripts/vast-ai.sh)
    - **Wave 0 NEW artifacts preserved**: pod/primary/Dockerfile, pod/primary/supervisord.conf, .github/workflows/build-primary-pod.yml
    - .github/ has zero refs to deleted pod/ artifacts (workflow scans clean)
    - git diff stats show ~1000-1200 lines removed
  </acceptance_criteria>
  <done>5 paths deleted. KEPT files preserved including Wave 0 NEW artifacts (pod/primary/Dockerfile, supervisord.conf, build-primary-pod.yml). .github/ has zero refs aos pod/ artifacts deletados. git diff stats show ~1000-1200 lines removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite pod/README.md as pointer to gateway/docs/RUNBOOK-PRIMARY-POD.md (list Wave 0 NEW preserved artifacts)</name>
  <files>pod/README.md</files>
  <read_first>
    - pod/README.md current content (legacy references to Dockerfile + build-pod.yml + onstart.sh — all obsolete)
    - gateway/docs/RUNBOOK-PRIMARY-POD.md (Plan 06.6-11 Task 1 output — the new canonical doc)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md line 76 (rewrite spec)
    - pod/primary/Dockerfile (Plan 04 — confirm exists for the file listing)
  </read_first>
  <action>Substituir TODO o conteúdo de pod/README.md por:

`# pod/` (H1) + 1 parágrafo:

`Esta pasta contém artefatos operacionais de provisioning do primary pod ifix-ai-gateway (custom multi-stage image build pipeline, weights upload pipeline, manual Vast.ai provisioning fallback, smoke-test fixtures Phase 1). A arquitetura de runtime do pod (Strategy B-revised — supervisord-co-located 4 services per WAVE0-GATES.md Decision 2 LOCKED) vive em \`gateway/internal/primary/\` e é orchestrada via \`gateway/cmd/gatewayctl/primary*\` CLI. Documentação operator-facing canonical: [gateway/docs/RUNBOOK-PRIMARY-POD.md](../gateway/docs/RUNBOOK-PRIMARY-POD.md).`

Seção `## Files (Preserved)`:
- `pod/primary/Dockerfile` — **NEW Wave 0 (Plan 04 Task 1)** Multi-stage Docker build extracting 4 SHA-pinned upstream binaries (llama.cpp b9191 + speaches + infinity + dcgm-exporter) + supervisord + aria2 into a single `ghcr.io/ifixtelecom/converseai-primary-pod:vX` image. See [WAVE0-GATES.md Decision 1](../.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md) for SHA pin policy.
- `pod/primary/supervisord.conf` — **NEW Wave 0 (Plan 04 Task 1)** 4-program supervisord config (llama / speaches / infinity / dcgm) — PID 1 inside the custom image. WAVE0-GATES.md Decision 2: supervisord LOCKED (DinD REJECTED).
- `pod/scripts/upload-weights.sh` — operator weight rotation pipeline (Qwen3.6 GGUF + Whisper + BGE-M3 → MinIO).
- `pod/scripts/vast-ai.sh` — operator manual provisioning fallback (rarely used since reconciler-driven primary pod is the default path).
- `pod/templates/qwen3.5-27b-tool-calling.jinja` — Qwen3.5 Jinja consumed by emerg pod (Phase 6 path). Qwen3.6 default uses GGUF-embedded Jinja per WAVE0-GATES.md Decision 3 (B1 LOCKED); B2 MinIO fallback via PRIMARY_QWEN_JINJA_KEY override.
- `pod/smoke/` — Phase 1 smoke-test fixtures (audio clips, embeddings sentences) reused para UAT POD-04 VRAM + Plan 06.6-11 Scenarios 2-4.
- `pod/weights/.gitkeep` — placeholder for weights directory.

Seção `## Files Deleted in Phase 6.6 PR3` (Plan 06.6-12):
- `pod/onstart.sh` (Phase 1 5-service compose bootstrap; replaced by gateway/internal/primary/onstart.go primaryOnstartHead — Plan 04 Task 3).
- `pod/docker-compose.yml` (Phase 1 5-service HOST-mode compose; replaced by pod/primary/supervisord.conf inside custom image — Plan 04 Tasks 1-2).
- `pod/health-bridge/` (Phase 1 Go service; DROPPED per CONTEXT.md D-05 — gateway Phase 3 probe loop substituiu).
- `pod/scripts/download-weights.sh` (Phase 1 weight download script; replaced by inline `aria2c download_with_verify` in primaryOnstartHead — Plan 04 Task 3).
- `.github/workflows/smoke.yml` (Phase 1 GHA workflow tied to deleted artifacts; UAT migrated to operator-run live script `06.6-HUMAN-UAT.md` — Plan 11).

Seção `## See Also`:
- [.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/](../.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/) — Phase 6.6 planning artifacts
- [Phase 1 history](../.planning/phases/01-gpu-pod-image-smoke-test/) — original primary pod arch (5-service compose, deleted)
- [Phase 6 history](../.planning/phases/06-emergency-pod-template-refactor/) — emerg pod refactor (precedes Phase 6.6)

Seção `## CI Workflow History`:
- `.github/workflows/build-primary-pod.yml` (NEW Wave 0 Plan 04 Task 2) — builds + pushes `ghcr.io/ifixtelecom/converseai-primary-pod` image on develop/v* tags
- `.github/workflows/smoke.yml` REMOVED in Phase 6.6 PR3 (Plan 06.6-12) — UAT migrated para operator-run live script em 06.6-HUMAN-UAT.md
- `.github/workflows/build-pod.yml` REMOVED in Phase 6 PR2 (commit 064702e) — Phase 1 custom image build superseded by Phase 6 Strategy B (upstream image direct) for emerg, then by Phase 6.6 Strategy B-revised (custom multi-stage image) for primary

Footer com `_Last updated: 2026-05-17 — Phase 6.6 PR3 cleanup (06.6-12-PLAN.md; Wave 0 supervisord LOCKED 2026-05-17)_`.</action>
  <verify>
    <automated>grep -q "RUNBOOK-PRIMARY-POD.md" /home/pedro/projetos/pedro/gpu-ifix/pod/README.md && grep -q "gateway/internal/primary" /home/pedro/projetos/pedro/gpu-ifix/pod/README.md && grep -q "smoke.yml" /home/pedro/projetos/pedro/gpu-ifix/pod/README.md && grep -q "pod/primary/Dockerfile" /home/pedro/projetos/pedro/gpu-ifix/pod/README.md && grep -q "supervisord.conf" /home/pedro/projetos/pedro/gpu-ifix/pod/README.md && grep -q "build-primary-pod.yml" /home/pedro/projetos/pedro/gpu-ifix/pod/README.md && grep -q 'WAVE0-GATES' /home/pedro/projetos/pedro/gpu-ifix/pod/README.md</automated>
  </verify>
  <acceptance_criteria>
    - pod/README.md rewritten with RUNBOOK-PRIMARY-POD.md redirect
    - Wave 0 NEW preserved artifacts listed: pod/primary/Dockerfile, supervisord.conf, build-primary-pod.yml
    - CI history transparent (smoke.yml removed in PR3, build-pod.yml removed in Phase 6 PR2, build-primary-pod.yml added in Plan 04)
    - WAVE0-GATES.md cross-referenced for SHA pin + supervisord policy
  </acceptance_criteria>
  <done>pod/README.md reescrito. Operator pode chegar via README e ser redirected à canonical documentação em gateway/docs/. Wave 0 NEW artifacts (pod/primary/Dockerfile, supervisord.conf, build-primary-pod.yml) explicitly listed. CI history transparent.</done>
</task>

<task type="auto">
  <name>Task 3: Final regression check — full build + test suite must remain GREEN after deletes</name>
  <files>(verification-only, no files modified)</files>
  <read_first>
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-VALIDATION.md (Quick run + Full suite commands)
  </read_first>
  <action>Executar comandos de regression check: (1) `cd gateway && go build ./...` — sucesso obrigatório; (2) `cd gateway && go test ./... -count=1 -race -timeout=10m` — todos green; (3) `cd gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=15m` — todos green incluindo primary_emerg_coexist_test.go (Pitfall #11) + primary_supervisord_test.go (Wave 0 4-endpoint) + primary_restart_recovery_test.go (reviews #4) + primary_overnight_schedule_test.go (reviews #5) + primary_disabled_force_up_test.go (reviews #2); (4) `find .github/ -type f -name '*.yml' -o -name '*.yaml' | xargs grep -lE "pod/(onstart\\.sh|docker-compose\\.yml|scripts/download-weights\\.sh|health-bridge)" || true` — output deve ser empty (zero stale refs); (5) confirm new artifacts still present: `ls pod/primary/Dockerfile pod/primary/supervisord.conf .github/workflows/build-primary-pod.yml`. Documentar exit codes e wall-clock time em commit message. Se algum falhar: STOP, criar 06.6-12-FAILURE-NOTE.md descrevendo qual file delete causou regression + propor restoration; NÃO PROCEED com Phase 6.6 close.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./... && go test ./... -count=1 -race -timeout=10m && cd /home/pedro/projetos/pedro/gpu-ifix && ! grep -rE "pod/(onstart\.sh|docker-compose\.yml|scripts/download-weights\.sh|health-bridge)" .github/ 2>/dev/null && [ -f pod/primary/Dockerfile ] && [ -f pod/primary/supervisord.conf ] && [ -f .github/workflows/build-primary-pod.yml ]</automated>
  </verify>
  <acceptance_criteria>
    - go build green
    - go test (unit + race) green
    - Zero stale refs to deleted files in .github/
    - Wave 0 NEW artifacts still present (pod/primary/ + build-primary-pod.yml)
  </acceptance_criteria>
  <done>Build + unit tests green pós-deletes. Zero stale refs em .github/. Wave 0 NEW artifacts preserved. Confirma que zero código gateway depende dos arquivos pod/ deletados E nenhum CI workflow ficou órfão. Phase 6.6 código está auto-suficiente.</done>
</task>

<task type="auto">
  <name>Task 4: Add follow-up note in CONTEXT.md Deferred Ideas for replacement gateway-driven smoke workflow (reviews suggestion #12)</name>
  <files>.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md</files>
  <read_first>
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md (existing Deferred Ideas section)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md (Gemini suggestion + Codex LOW concern about smoke.yml deletion losing automated coverage)
  </read_first>
  <action>Append a NEW bullet to .planning/phases/.../06.6-CONTEXT.md `## Deferred Ideas` section (do NOT touch existing bullets — only append): `- **Gateway-driven Strategy B-revised smoke workflow** — replacement for the deleted .github/workflows/smoke.yml (Phase 1 GHA workflow). Reviews 2026-05-17 flagged this gap: deleting smoke.yml removes automated non-GPU bootstrap validation. Follow-up phase should create a new .github/workflows/strategy-b-smoke.yml that (a) pulls latest ghcr.io/ifixtelecom/converseai-primary-pod image, (b) runs it locally on a self-hosted GPU runner OR a controlled dev environment, (c) invokes `gatewayctl primary force-up` against a controlled dev gateway, (d) asserts `gatewayctl primary state` reaches `ready` within a time budget. Track as standalone phase post-6.6 cleanup — no blocking dependency on 6.6 completion since live UAT (Plan 06.6-11) provides equivalent operator-witnessed coverage in the short term. Note: Wave 0 LOCKED supervisord 4-service model means smoke would need to assert all 4 endpoint health (not just LLM), which is more substantial than Phase 1's single-service smoke — justifies separate phase.`. Edit ONLY the existing CONTEXT.md — do NOT create a new doc.</action>
  <verify>
    <automated>grep -q "Gateway-driven Strategy B-revised smoke workflow" /home/pedro/projetos/pedro/gpu-ifix/.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTEXT.md Deferred Ideas section has new bullet for gateway-driven Strategy B-revised smoke workflow
    - Reviews suggestion #12 tracked as deferred follow-up
    - No phase-blocking dependency
  </acceptance_criteria>
  <done>CONTEXT.md Deferred Ideas section has new bullet for gateway-driven Strategy B-revised smoke workflow. Reviews suggestion #12 tracked as deferred follow-up. No phase-blocking dependency.</done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

(Cleanup plan; only deletes files. No new trust boundaries.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none new) | — | — | — | Cleanup is delete-only; reduces attack surface (less custom code to audit; one fewer CI workflow with stale broken refs). Wave 0 NEW artifacts (pod/primary/Dockerfile + supervisord.conf + build-primary-pod.yml) ADD attack surface but are reviewed by Plan 04 + automated by GHA OIDC; net surface change is small. |
</threat_model>

<verification>
- `[ ! -f pod/onstart.sh ] && [ ! -f pod/docker-compose.yml ] && [ ! -d pod/health-bridge ] && [ ! -f pod/scripts/download-weights.sh ] && [ ! -f .github/workflows/smoke.yml ]` (5 paths deleted)
- `[ -f pod/scripts/upload-weights.sh ] && [ -d pod/smoke ] && [ -f pod/templates/qwen3.5-27b-tool-calling.jinja ]` (3 legacy KEPT preserved)
- `[ -f pod/primary/Dockerfile ] && [ -f pod/primary/supervisord.conf ] && [ -f .github/workflows/build-primary-pod.yml ]` (3 Wave 0 NEW preserved)
- `grep -q "RUNBOOK-PRIMARY-POD.md" pod/README.md && grep -q "pod/primary/Dockerfile" pod/README.md && grep -q "supervisord" pod/README.md` (redirect + Wave 0 artifacts documented)
- `! grep -rE "pod/(onstart\.sh|docker-compose\.yml|scripts/download-weights\.sh|health-bridge)" .github/` (zero stale CI refs)
- `cd gateway && go build ./... && go test ./... -count=1 -race -timeout=10m` all green
- `grep -q "Gateway-driven Strategy B-revised smoke workflow" .planning/phases/06.6-.../06.6-CONTEXT.md` (deferred follow-up tracked)
</verification>

<success_criteria>
- Phase 1 dual-implementation eliminated; only Strategy B-revised (Wave 0 supervisord custom image) remains
- ~1000-1200 lines deleted (PATTERNS.md estimate + smoke.yml ~290 lines)
- README updates make navigation operator-friendly + Wave 0 NEW artifacts (pod/primary/) documented
- Regression check confirms gateway code is independent of deleted pod/ artifacts
- CI surface clean: no broken refs to deleted artifacts; UAT scope owned by Plan 06.6-11 operator script; build-primary-pod.yml (Plan 04) preserves active build pipeline
- Wave 0 NEW artifacts (pod/primary/Dockerfile + supervisord.conf + build-primary-pod.yml) preserved + listed in pod/README.md
- Deferred follow-up tracked (gateway-driven Strategy B-revised smoke workflow in CONTEXT.md)
- Phase 6.6 ready for SUMMARY.md + VERIFICATION.md + ROADMAP close
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-12-SUMMARY.md`. After this plan completes, the orchestrator should run `/gsd:verify-work 06.6` to produce 06.6-VERIFICATION.md + close Phase 6.6 in ROADMAP.md + update STATE.md.
</output>
