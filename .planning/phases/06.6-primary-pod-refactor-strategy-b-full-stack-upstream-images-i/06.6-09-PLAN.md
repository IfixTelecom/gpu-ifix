---
phase: 06.6
plan: 09
type: execute
wave: 4
depends_on: [06.6-06a, 06.6-06b, 06.6-07]
files_modified:
  - gateway/cmd/gatewayctl/primary.go
  - gateway/cmd/gatewayctl/primary_test.go
  - gateway/cmd/gatewayctl/main.go
autonomous: true
requirements: []
tags: [cli, wave-4, gatewayctl, primary, operator]

must_haves:
  truths:
    - "gateway/cmd/gatewayctl/primary.go expõe subcommand router `gatewayctl primary [state|force-up|force-down|schedule|lifecycles]` espelhando emerg.go pattern + new `schedule` subcommand sem analog emerg"
    - "gatewayctl primary state — lê redisx.PrimaryStateKey() HGETALL + imprime state, lifecycle_id, pod_url, instance_id, entered_at em formato human-readable"
    - "gatewayctl primary force-up — emite redisx.PublishPrimaryEvent type=force_up_request; **REAL CONSUMER per reviews #3** = primary.Reconciler.handleForceUpRequest (Plan 06.6-06a)"
    - "gatewayctl primary force-down — emite redisx.PublishPrimaryEvent type=force_down_request; **REAL CONSUMER** = primary.Reconciler.handleForceDownRequest (Plan 06.6-06a)"
    - "**gatewayctl primary schedule** — chama primary.ParseScheduleEnv(cfg) + imprime ScheduleRule fields **INCLUINDO PROVISION_LEAD_SECONDS per reviews consensus action #8** (operator pre-flight visibility para pre-warm offset config) + ScheduleRule.NextTransition(time.Now()) timestamp"
    - "gatewayctl primary lifecycles [--limit N] — SELECT FROM primary_lifecycles ORDER BY started_at DESC LIMIT N + imprime tabela (parity emerg lifecycles command)"
    - "main.go gatewayctl dispatcher adiciona case \"primary\" chamando runPrimary"
  artifacts:
    - path: "gateway/cmd/gatewayctl/primary.go"
      provides: "5 subcommands: state | force-up | force-down | schedule (surfaces provision_lead_seconds per reviews #8) | lifecycles"
      contains: "runPrimary|runPrimaryState|runPrimaryForceUp|runPrimaryForceDown|runPrimarySchedule|runPrimaryLifecycles|ProvisionLeadS|provision_lead_seconds"
    - path: "gateway/cmd/gatewayctl/primary_test.go"
      provides: "6 unit tests cobrindo cada subcommand happy path + new schedule lead-seconds output assertion"
      contains: "TestRunPrimaryState|TestRunPrimaryForceUp|TestRunPrimaryForceDown|TestRunPrimarySchedule|TestRunPrimarySchedule_ShowsProvisionLeadSeconds|TestRunPrimary_NoArgs|TestRunPrimary_UnknownSubcommand"
    - path: "gateway/cmd/gatewayctl/main.go"
      provides: "Updated subcommand dispatcher with primary case"
      contains: "case \"primary\":"
  key_links:
    - from: "gatewayctl primary force-up"
      to: "primary.Reconciler.handleForceUpRequest (Plan 06.6-06a — REAL CONSUMER per reviews #3)"
      via: "redisx.PublishPrimaryEvent"
      pattern: "force_up_request"
    - from: "gatewayctl primary force-down"
      to: "primary.Reconciler.handleForceDownRequest (Plan 06.6-06a)"
      via: "redisx.PublishPrimaryEvent"
      pattern: "force_down_request"
    - from: "gatewayctl primary schedule show"
      to: "primary.ParseScheduleEnv + ShouldBeProvisioned + NextTransition"
      via: "shared primary package"
      pattern: "primary\\.(ParseScheduleEnv|NextTransition)|ProvisionLeadS"

threat_refs: []
---

<objective>
Adicionar CLI operacional `gatewayctl primary <subcommand>` para dar ao operator controle manual sobre o primary pod fora do schedule. 5 subcomandos com 1 mudança per reviews consensus action #8: `schedule` subcommand surface o NOVO `PRIMARY_POD_SCHEDULE_PROVISION_LEAD_SECONDS` config para operator pre-flight verify.

Depends_on updated to [06.6-06a, 06.6-06b, 06.6-07] per reviews #1 split — force-up/force-down events have real consumers in Plan 06.6-06a (handleForceUpRequest/handleForceDownRequest); no orphan publishes.

Purpose: Operator precisa ferramentas para (a) verificar estado, (b) forçar provision/destroy (real consumers in 06.6-06a), (c) inspecionar schedule + pre-warm offset, (d) inspecionar histórico.

Output: 1 novo arquivo Go (primary.go) + 1 test file + 1-line dispatcher addition em main.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md
@gateway/cmd/gatewayctl/emerg.go
@gateway/cmd/gatewayctl/emerg_test.go
@gateway/cmd/gatewayctl/main.go

<interfaces>
<!-- runPrimarySchedule output spec (per reviews #8) -->

```
$ gatewayctl primary schedule
Timezone:                  America/Sao_Paulo
UpHour:                    8
DownHour:                  22
Days:                      [mon tue wed thu fri]
GraceRampDownSeconds:      300
ProvisionLeadSeconds:      1800             # NEW per reviews #8 — kicks off provisioning 30min before UpHour
Disabled:                  true              # soak gate
Next transition:           2026-05-18T08:00:00-03:00 (up)
Should be provisioned now: false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gateway/cmd/gatewayctl/primary.go with 5 subcommands (schedule surfaces ProvisionLeadSeconds per reviews #8)</name>
  <files>gateway/cmd/gatewayctl/primary.go, gateway/cmd/gatewayctl/main.go</files>
  <read_first>
    - gateway/cmd/gatewayctl/emerg.go full file (verbatim pattern for runEmergState/ForceProvision/ForceDestroy/Lifecycles)
    - gateway/cmd/gatewayctl/main.go (dispatcher pattern — find switch on cmd; insert "primary" case)
    - gateway/internal/primary/schedule.go (Plan 06.6-05 — ParseScheduleEnv + ShouldBeProvisioned + NextTransition; ProvisionLeadS field per reviews #8)
    - gateway/internal/redisx/primary.go (Plan 06.6-07 — PublishPrimaryEvent + PrimaryStateKey; PrimaryEvent.Type force_up_request/force_down_request per reviews #3)
    - gateway/db/queries/primary_lifecycles.sql (Plan 06.6-07 sqlc — for SELECT lifecycles)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md (actions #3 real consumers + #8 surface lead seconds)
  </read_first>
  <action>(a) gatewayctl/primary.go: `package main`. Imports: stdlib + config + redisx + primary + db sqlc queries + go-redis + pgx. Implementar `runPrimary` dispatcher. 5 subcommands: (1) runPrimaryState: HGetAll redisx.PrimaryStateKey() + print "State: <state>\nLifecycle ID: <id>\nPod URL: <url>\nInstance: <id>\nEntered at: <unix→time>"; (2) runPrimaryForceUp: PublishPrimaryEvent(Type:"force_up_request", Reason:"manual_gatewayctl", ReplicaID:"gatewayctl-cli", SinceUnix:time.Now().Unix()) — note: REAL CONSUMER is Plan 06.6-06a primary.Reconciler.handleForceUpRequest (added per reviews #3); print "force-up request published — consumer will pick up at next leader tick"; (3) runPrimaryForceDown: análogo com Type:"force_down_request"; (4) **runPrimarySchedule (REVISED per reviews #8)**: chama primary.ParseScheduleEnv(cfg); se err != nil print error + return 1; **print INCLUI rule.ProvisionLeadS** linha (NEW per reviews #8): `fmt.Printf("ProvisionLeadSeconds:      %d             # kicks off provisioning %d seconds before UpHour (pre-warm offset)\n", rule.ProvisionLeadS, rule.ProvisionLeadS)`. Print all fields: Timezone, UpHour, DownHour, Days, GraceRampDownSeconds, **ProvisionLeadSeconds**, Disabled; then NextTransition(time.Now()) timestamp + kind; finally print `Should be provisioned now: <true|false>` via rule.ShouldBeProvisioned(time.Now()); (5) runPrimaryLifecycles: pgxpool + SELECT * ORDER BY started_at DESC LIMIT $limit; tab-separated table with headers; `--limit N` flag via flag.IntVar default 20. (b) gatewayctl/main.go: localizar dispatcher switch + adicionar `case "primary": return runPrimary(ctx, args[1:], log)`.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./cmd/gatewayctl/...</automated>
  </verify>
  <done>gatewayctl primary binary compila. 5 subcommands disponíveis. runPrimarySchedule surfaces ProvisionLeadSeconds per reviews #8. runPrimaryForceUp/ForceDown publish events that have real consumers in Plan 06.6-06a per reviews #3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gateway/cmd/gatewayctl/primary_test.go — 7 unit tests covering each subcommand (incl new lead-seconds assertion + DB-fetch lifecycles deferred to Plan 06.6-10)</name>
  <files>gateway/cmd/gatewayctl/primary_test.go</files>
  <behavior>
    - TestRunPrimary_NoArgs_UsageHelp: runPrimary(ctx, []string{}, log) → exit code 2 + stderr contains "Usage:"
    - TestRunPrimary_UnknownSubcommand: runPrimary(ctx, []string{"xxx"}, log) → exit code 2
    - TestRunPrimaryState_HappyPath: miniredis seed → runPrimaryState → stdout contains "ready" + "42"
    - TestRunPrimaryForceUp_PublishesEvent: miniredis + Subscribe gw:primary:events; chamar runPrimaryForceUp → received event has Type="force_up_request"
    - TestRunPrimaryForceDown_PublishesEvent: análogo
    - TestRunPrimarySchedule_PrintsResolvedRule: cfg defaults → runPrimarySchedule → stdout contains "America/Sao_Paulo", "UpHour: 8", "DownHour: 22", "Disabled: true"
    - **TestRunPrimarySchedule_ShowsProvisionLeadSeconds (NEW per reviews #8)**: cfg defaults → runPrimarySchedule → stdout contains "ProvisionLeadSeconds" AND "1800" (the default value from Plan 06.6-03 PrimaryPodScheduleProvisionLeadSeconds) — proves operator pre-flight visibility into pre-warm offset config
    - TestRunPrimaryLifecycles_FetchesFromDB: t.Skip with explicit pointer to Plan 06.6-10 — integration coverage lives in gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go
  </behavior>
  <read_first>
    - gateway/cmd/gatewayctl/emerg_test.go (analog test harness with miniredis + capturing stdout/stderr; lines 14-19 deferral pattern)
    - gateway/cmd/gatewayctl/primary.go (Task 1 output — confirm function signatures)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md (action #8 lead-seconds surface)
  </read_first>
  <action>Criar primary_test.go com `package main`. Imports: stdlib + miniredis + testify/require + bytes + io. Module-level doc comment espelhando emerg_test.go:1-19 pattern. Helper redirect os.Stdout via os.Pipe trick. Implementar 8 tests da tabela <behavior>. **CRITICAL — TestRunPrimaryLifecycles_FetchesFromDB**: explicit t.Skip with msg `t.Skip("integration test — see gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go (Plan 06.6-10 Task 3 closes BLOCKER 3 coverage gap)")`. TestRunPrimarySchedule_ShowsProvisionLeadSeconds (reviews #8): setup config.Config with PrimaryPodScheduleProvisionLeadSeconds=1800; runPrimarySchedule(ctx, nil, log); captured stdout MUST contain BOTH "ProvisionLeadSeconds" (label) AND "1800" (value).</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./cmd/gatewayctl/... -count=1 -race -run TestRunPrimary</automated>
  </verify>
  <done>7 tests verdes (TestRunPrimaryLifecycles_FetchesFromDB t.Skip aponta para Plan 10). TestRunPrimarySchedule_ShowsProvisionLeadSeconds confirma reviews #8 visibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation (gatewayctl binary) → Redis (gw:primary:events) | Outbound; operator-controlled; force-up/force-down require Redis access (cluster-internal in production) |
| operator workstation (gatewayctl binary) → Postgres (primary_lifecycles SELECT) | Outbound; read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (no new threats) | — | — | — | gatewayctl CLI runs only on ops-claude host with operator credentials; same trust model as gatewayctl emerg subcommand. Real consumers in Plan 06.6-06a ensure operator commands have effect (reviews #3 closure). |
</threat_model>

<verification>
- `cd gateway && go build ./cmd/gatewayctl/...` succeeds
- `cd gateway && go test ./cmd/gatewayctl/... -count=1 -race -run TestRunPrimary` all green (7 tests; 1 skip with pointer to Plan 10)
- `cd gateway && grep -q 'ProvisionLeadSeconds\\|ProvisionLeadS' gateway/cmd/gatewayctl/primary.go` (reviews #8 surfaced)
- `cd gateway && grep -q 'force_up_request' gateway/cmd/gatewayctl/primary.go` (real consumer in 06.6-06a)
</verification>

<success_criteria>
- Operator has CLI tools to verify state, force lifecycle, inspect schedule + pre-warm offset (reviews #8), audit history
- force_up_request / force_down_request published events have REAL consumers in Plan 06.6-06a per reviews #3 (no orphan publishes)
- runPrimarySchedule surfaces PROVISION_LEAD_SECONDS for operator pre-flight verify per reviews #8
- Tests cover all 5 subcommands at unit level
- depends_on chain updated to [06.6-06a, 06.6-06b, 06.6-07] per reviews #1 split
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-09-SUMMARY.md`.
</output>
