---
phase: 06.6
plan: 04
type: execute
wave: 2
depends_on: [06.6-02, 06.6-03]
files_modified:
  - gateway/internal/primary/onstart.go
  - gateway/internal/primary/lifecycle.go
  - gateway/internal/primary/lifecycle_test.go
  - gateway/internal/primary/doc.go
autonomous: true
requirements: []
tags: [primary-pod, wave-2, lifecycle, onstart, dind, qwen3.6, strategy-b]

must_haves:
  truths:
    - "gateway/internal/primary/onstart.go expõe primaryOnstartHead raw-string Go const (~3.3KB inline bash) com ZERO fmt.Sprintf (Pitfall #9 enforced) e termina com exec /app/llama-server (Pitfall #2 PID 1 invariant); 4 sidecars (LLM + STT + Embed + DCGM) all from upstream tag-pinned images per D-01 + D-02; 3 parallel weight downloads per lifecycle per D-09"
    - "Shell hardening per reviews consensus action #7: primaryOnstartHead uses `set -euo pipefail` (NO `x` — prevents secret leakage of MinIO creds in pod logs); ALL env var expansions are quoted (`\"$VAR\"` not `$VAR`); pre-flight precondition checks reject empty required env vars via `: \"${VAR:?required}\"` guards"
    - "D-03 traceability: cfg.PrimaryTemplateImage default == \"ghcr.io/ggml-org/llama.cpp:server-cuda-b9128@sha256:<digest>\" (SHA-pinned engine LLM upstream image — locked decision per CONTEXT.md D-03 + WAVE0-GATES Decision 1 + reviews Codex LOW concern); enforced via TestBuildPrimaryCreateRequest_D03_EngineImagePinned asserting req.Image equals this SHA-pinned form when cfg has defaults"
    - "D-07 traceability: cfg.PrimaryDCGMImage default SHA-pinned + onstart bash includes `-p 9400:9400` port mapping + DCGM sidecar docker run block uses `\"$PRIMARY_DCGM_IMAGE\"` (quoted env expansion); enforced via TestPrimaryOnstart_DCGMPort9400Preserved"
    - "Script inline cobre: optional sshd (D-10), mc install (curl, no apt — Pitfall hot-fix 4896004), 3 parallel weight downloads + sha256 verify (Qwen + Whisper + BGE-M3), optional Jinja fetch se PRIMARY_QWEN_JINJA_KEY non-empty, dockerd start + 30s wait loop (DinD per WAVE0-GATES Decision 2), 3 sidecar docker run -d (speaches + infinity + dcgm), exec llama-server final line"
    - "buildPrimaryCreateRequest produz vast.CreateRequest com Image=cfg.PrimaryTemplateImage (SHA-pinned), Runtype=\"args\", Onstart=\"/bin/bash\", Args=[\"-c\", onstart], Disk=50, Label=ifix-primary-lifecycle-<id>, TargetState=\"running\""
    - "buildPrimaryCreateRequest precondition gate per reviews consensus action #6: REJECTS build with explicit error if cfg.PrimaryQwenWeightsSHA256 == \"\" OR cfg.PrimaryWhisperWeightsSHA256 == \"\" OR cfg.PrimaryBGEM3WeightsSHA256 == \"\" — returns error so reconciler can transition to Cooldown + emit forensics event (not silently passthrough empty SHA into pod onstart)"
    - "Env map inclui 4 port forwards (-p 8000:8000, -p 8001:8001, -p 8002:8002, -p 9400:9400 per Pitfall #8) + 4 MINIO_* (Endpoint, Bucket, AccessKey, SecretKey — ALL four must be non-empty per WARNING 2 revision) + 3 PRIMARY_*_WEIGHTS_KEY/SHA256 pairs + 3 PRIMARY_*_IMAGE refs + condicional POD_DEBUG_SSH_PUBLIC_KEY + -p 22:22 quando key non-empty + condicional PRIMARY_QWEN_JINJA_KEY/SHA256 quando key non-empty"
    - "buildPrimaryOnstart(llamaArgs []string, jinjaPath string) string retorna primaryOnstartHead + \"exec /app/llama-server \" + strings.Join(llamaArgs, \" \") + \"\\n\" (último-line invariant)"
    - "primaryLlamaArgsDefault var contém slice com --host 0.0.0.0 --port 8000 -m /weights/qwen/model.gguf -ngl 99 -np 2 --ctx-size 16384 --jinja (sem --chat-template-file se WAVE0-GATES Decision 3 = B1)"
    - "Quando cfg.PrimaryQwenJinjaKey != \"\", onstart bash conditional fetch Jinja para /app/templates/qwen3.6.jinja com sha256 verify; llamaArgs append --chat-template-file /app/templates/qwen3.6.jinja"
  artifacts:
    - path: "gateway/internal/primary/doc.go"
      provides: "Package-level doc comment for gateway/internal/primary"
      contains: "Package primary"
    - path: "gateway/internal/primary/onstart.go"
      provides: "primaryOnstartHead raw-string const + buildPrimaryOnstart + primaryLlamaArgsDefault"
      contains: "primaryOnstartHead|buildPrimaryOnstart|primaryLlamaArgsDefault|exec /app/llama-server|dockerd|set -euo pipefail"
    - path: "gateway/internal/primary/lifecycle.go"
      provides: "buildPrimaryCreateRequest + Reconciler skeleton (struct fields only; methods minimally stubbed for compile) + SHA precondition gate"
      contains: "buildCreateRequest|primaryPodURLs|Reconciler|ErrMissingWhisperSHA|ErrMissingBGEM3SHA"
    - path: "gateway/internal/primary/lifecycle_test.go"
      provides: "14+ unit tests covering buildCreateRequest assembly + onstart shell hardening + bash content invariants + D-03/D-07 traceability + MinIO non-empty + SHA fail-fast"
      contains: "TestBuildPrimaryCreateRequest_DinD_4Services|TestPrimaryOnstartLengthBelowLimit|TestPrimaryOnstart_StartsWithSetEuo|TestPrimaryOnstart_NoSetX_NoSecretLeak|TestPrimaryOnstart_NoLegacyImage|TestPrimaryOnstart_ExecLlamaServerIsLastLine|TestBuildPrimaryCreateRequest_Has4PortMappings|TestPrimaryOnstart_QwenWeightsInjected|TestBuildPrimaryCreateRequest_JinjaEmbeddedMode|TestBuildPrimaryCreateRequest_JinjaMinIOFallback|TestBuildPrimaryCreateRequest_D03_EngineImagePinnedSHA|TestPrimaryOnstart_DCGMPort9400Preserved|TestBuildPrimaryCreateRequest_MinIOCredentialsNotEmpty|TestBuildPrimaryCreateRequest_RejectsEmptyWhisperSHA|TestBuildPrimaryCreateRequest_RejectsEmptyBGEM3SHA|TestPrimaryOnstart_HasRequiredEnvGuards|TestPrimaryOnstart_QuotedEnvExpansions"
  key_links:
    - from: "primary.buildPrimaryCreateRequest"
      to: "vast.CreateRequest DTO (gateway/internal/emerg/vast/types.go)"
      via: "DTO shape"
      pattern: "vast\\.CreateRequest\\{"
    - from: "primary.buildPrimaryCreateRequest Env map"
      to: "primaryOnstartHead bash expansion (quoted $VAR references)"
      via: "Vast.ai env-var injection"
      pattern: "\"\\$(MINIO_|PRIMARY_|POD_DEBUG_)"

threat_refs: [T-06.6-01, T-06.6-02, T-06.6-SC]
---

<objective>
Criar pacote gateway/internal/primary/ com 3 artefatos centrais: (1) `onstart.go` raw-string Go const com bash inline ~3.3KB (4 sidecars via DinD + 3 paralelos weight downloads) com SHELL HARDENING per reviews consensus action #7 (`set -euo pipefail` SEM `x`, quoted env expansions, required-env guards); (2) `lifecycle.go` com `buildCreateRequest` Strategy B payload + Reconciler struct skeleton + SHA FAIL-FAST PRECONDITION GATE per reviews consensus action #6 (rejects build with explicit error if Whisper/BGE SHA empty); (3) `lifecycle_test.go` com 17+ testes invariants incluindo shell hardening + SHA fail-fast.

Purpose: Plans 06.6-05/06.6-06a/06.6-06b (schedule + reconciler + adapters) consomem este lifecycle.go. Plan 06.6-08 (main.go wiring) precisa do Reconciler struct público. Sem este plan, demais Wave 2+ plans não compilam.

Output: 3 arquivos go novos + 1 doc.go. Build verde; 17+ testes verdes. Behavior diferenças vs emerg: Image upstream SHA-pinned (não custom GHCR), 4 services (não 1), Disk=50 (não 40), Label prefix ifix-primary-lifecycle, hardened shell (no `set -x` para evitar MinIO cred leak nos pod logs), SHA fail-fast precondition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-CONTEXT.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md
@gateway/internal/emerg/lifecycle.go
@gateway/internal/emerg/lifecycle_test.go
@gateway/internal/emerg/vast/types.go
@gateway/internal/config/config.go

<interfaces>
<!-- vast.CreateRequest DTO (consumed) — from gateway/internal/emerg/vast/types.go lines 130-160 -->

```go
type CreateRequest struct {
    ClientID    string            `json:"client_id"`
    Image       string            `json:"image"`
    Env         map[string]string `json:"env"`
    Onstart     string            `json:"onstart_cmd"`     // "/bin/bash" for Strategy B
    Runtype     string            `json:"runtype"`         // "args" for Strategy B
    Args        []string          `json:"args"`            // []string{"-c", <onstart_script>}
    Disk        int               `json:"disk"`
    Label       string            `json:"label"`
    TargetState string            `json:"target_state"`
}
```

<!-- Shell hardening template (NEW per reviews #7) — onstart head MUST start with these guards -->

```bash
#!/bin/bash
set -euo pipefail                          # NO 'x' — would leak MinIO creds in pod logs

# Required env precondition guards (reject empty critical values explicitly):
: "${MINIO_ENDPOINT:?required}"
: "${MINIO_BUCKET:?required}"
: "${MINIO_ACCESS_KEY:?required}"
: "${MINIO_SECRET_KEY:?required}"
: "${PRIMARY_QWEN_WEIGHTS_KEY:?required}"
: "${PRIMARY_QWEN_WEIGHTS_SHA256:?required}"
: "${PRIMARY_WHISPER_WEIGHTS_KEY:?required}"
: "${PRIMARY_WHISPER_WEIGHTS_SHA256:?required}"
: "${PRIMARY_BGEM3_WEIGHTS_KEY:?required}"
: "${PRIMARY_BGEM3_WEIGHTS_SHA256:?required}"

# All env expansions inside the script body MUST be quoted: "$VAR" not $VAR
mc alias set ifix "$MINIO_ENDPOINT" "$MINIO_ACCESS_KEY" "$MINIO_SECRET_KEY"
mc cp "ifix/$MINIO_BUCKET/$PRIMARY_QWEN_WEIGHTS_KEY" /weights/qwen/model.gguf
echo "$PRIMARY_QWEN_WEIGHTS_SHA256  /weights/qwen/model.gguf" | sha256sum -c -
```

<!-- SHA fail-fast precondition (NEW per reviews #6) — buildCreateRequest signature CHANGED to return error -->

```go
// Before reviews: func (r *Reconciler) buildCreateRequest(offer vast.Offer, lifecycleID int64) vast.CreateRequest
// After reviews:  func (r *Reconciler) buildCreateRequest(offer vast.Offer, lifecycleID int64) (vast.CreateRequest, error)
//
// Errors returned:
//   ErrMissingQwenSHA   (defensive — Qwen SHA HAS default, but check defends against operator override-to-empty)
//   ErrMissingWhisperSHA
//   ErrMissingBGEM3SHA

var (
    ErrMissingQwenSHA    = errors.New("primary: PRIMARY_QWEN_WEIGHTS_SHA256 is empty — refusing to build pod request (sha256 verification would silently no-op)")
    ErrMissingWhisperSHA = errors.New("primary: PRIMARY_WHISPER_WEIGHTS_SHA256 is empty — operator must set this env var explicitly (no default shipped)")
    ErrMissingBGEM3SHA   = errors.New("primary: PRIMARY_BGEM3_WEIGHTS_SHA256 is empty — operator must set this env var explicitly (no default shipped)")
)
```

<!-- primaryPodURLs new struct (consumed later by reconciler in Plan 06.6-06a) -->
```go
type primaryPodURLs struct {
    LLM   string // http://<vast_ip>:<8000-host-port>
    STT   string
    Embed string
    DCGM  string
}
```

<!-- Reconciler struct fields needed for buildCreateRequest call site — minimal viable shape -->
```go
type Reconciler struct {
    deps          Deps                                 // config + log + vast client + DB pool + loader + dcgm + dispatcher + fsm
    activePodURLs atomic.Pointer[primaryPodURLs]      // populated on Ready
    rule          ScheduleRule                         // resolved by Plan 06.6-05 schedule.go
    drainStartedAt atomic.Pointer[time.Time]
    cfg           config.Config
}

type Deps struct {
    Cfg         config.Config
    Log         *slog.Logger
    Vast        vastutil.VastDestroyer // minimal interface; full vast.Client wired in main.go
    // Loader, DCGMScraper, Dispatcher, FSM, DB pool — fields wired in Plan 06.6-08 main.go;
    // for THIS plan, declare them as interface{} or specific types as needed (placeholders OK
    // — Plan 06.6-06a will refine when reconciler.go methods land)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gateway/internal/primary/doc.go + onstart.go (raw-string Go const + buildPrimaryOnstart) with shell hardening per reviews #7</name>
  <files>gateway/internal/primary/doc.go, gateway/internal/primary/onstart.go</files>
  <read_first>
    - gateway/internal/emerg/lifecycle.go lines 650-742 (emergencyLlamaArgsDefault + emergencyOnstartHead + buildEmergencyOnstart — analog shape; note emerg uses `set -euxo pipefail` — primary uses `set -euo pipefail` per reviews #7 to avoid MinIO cred leak)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Onstart Script Anatomy (lines 772-892) — full 7-section inline bash spec
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 180-258 (onstart.go pattern spec with raw-string + buildPrimaryOnstart signature)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md Decision 3 (Jinja strategy B1 vs B2 — defines whether --chat-template-file appears)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md lines 196-244 (Recommended Pre-execution Actions #7 shell hardening)
  </read_first>
  <action>Criar gateway/internal/primary/doc.go com block comment `// Package primary implements the primary pod lifecycle for ifix-ai-gateway. // Phase 6.6 introduces a schedule-driven 5-state FSM (Asleep|Provisioning|Ready|Draining|Destroying) // distinct from the emergency-event-driven emerg package. See // .planning/phases/06.6-.../06.6-CONTEXT.md and 06.6-RESEARCH.md.` + `package primary`. Criar gateway/internal/primary/onstart.go com `package primary` + import "strings". Definir `var primaryLlamaArgsDefault = []string{"--host", "0.0.0.0", "--port", "8000", "-m", "/weights/qwen/model.gguf", "-ngl", "99", "-np", "2", "--ctx-size", "16384", "--jinja"}`. Definir `const primaryOnstartHead = \`...\`` raw-string com EXATAMENTE estas 7 seções (reviews #7 hardening enforced): (1) header `#!/bin/bash` + `set -euo pipefail` (NÃO `set -euxo` — `x` faria `mc alias set` printar MinIO credentials no pod log) + `export DEBIAN_FRONTEND=noninteractive` + `mkdir -p /weights/qwen /weights/whisper /weights/bge-m3 /app/templates /run/sshd /root/.ssh`; **PRECONDITION GUARDS** após mkdir, ANTES de qualquer download: `: "${MINIO_ENDPOINT:?required}"; : "${MINIO_BUCKET:?required}"; : "${MINIO_ACCESS_KEY:?required}"; : "${MINIO_SECRET_KEY:?required}"; : "${PRIMARY_QWEN_WEIGHTS_KEY:?required}"; : "${PRIMARY_QWEN_WEIGHTS_SHA256:?required}"; : "${PRIMARY_WHISPER_WEIGHTS_KEY:?required}"; : "${PRIMARY_WHISPER_WEIGHTS_SHA256:?required}"; : "${PRIMARY_BGEM3_WEIGHTS_KEY:?required}"; : "${PRIMARY_BGEM3_WEIGHTS_SHA256:?required}"; : "${PRIMARY_TEMPLATE_IMAGE:?required}"; : "${PRIMARY_SPEACHES_IMAGE:?required}"; : "${PRIMARY_INFINITY_IMAGE:?required}"; : "${PRIMARY_DCGM_IMAGE:?required}"` — script will exit immediately with non-zero status if any required env missing; (2) optional sshd if POD_DEBUG_SSH_PUBLIC_KEY set (mesma block que emergencyOnstartHead:702-712 mas com `"$POD_DEBUG_SSH_PUBLIC_KEY"` quoted); (3) mc curl install if not present; (4) **all env expansions QUOTED**: `mc alias set ifix "$MINIO_ENDPOINT" "$MINIO_ACCESS_KEY" "$MINIO_SECRET_KEY"` (NOT `$MINIO_ENDPOINT`); 3 parallel `( ... ) &` weight download blocks com `mc cp "ifix/$MINIO_BUCKET/$PRIMARY_QWEN_WEIGHTS_KEY" /weights/qwen/model.gguf` + sha256sum -c verify using quoted `echo "$PRIMARY_QWEN_WEIGHTS_SHA256 /weights/qwen/model.gguf" | sha256sum -c -`, capturing 3 PIDs, optional Jinja fetch conditional `if [ -n "${PRIMARY_QWEN_JINJA_KEY:-}" ]; then`, then `wait $QWEN_PID $WHISPER_PID $BGE_PID`; (5) dockerd start `dockerd --host=unix:///var/run/docker.sock &>/var/log/dockerd.log &` + 30s wait loop; (6) 3 sidecar `docker run -d` blocks com all env-var refs quoted: `docker run -d --name speaches --gpus all -p 8001:8001 -v /weights/whisper:/weights "$PRIMARY_SPEACHES_IMAGE"` etc; DCGM block exposes `-p 9400:9400 "$PRIMARY_DCGM_IMAGE"`; (7) **NO final exec line aqui** — buildPrimaryOnstart appende dinamicamente. Definir `func buildPrimaryOnstart(llamaArgs []string, jinjaPath string) string { args := strings.Join(llamaArgs, " "); if jinjaPath != "" { args += " --chat-template-file " + jinjaPath }; return primaryOnstartHead + "exec /app/llama-server " + args + "\n" }`. ZERO fmt.Sprintf no raw-string ou no exec append — strings.Join é safe. Comentário sobre Pitfall #9 + reviews #7 acima da const.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./internal/primary/...</automated>
  </verify>
  <done>onstart.go + doc.go compilam standalone. primaryOnstartHead é const começa com `set -euo pipefail` (NÃO `set -euxo` — no `x` per reviews #7). Conteúdo bash inline tem ZERO fmt.Sprintf, todas env expansions quoted (`"$VAR"`), todos required env vars têm `: "${VAR:?required}"` guards, termina sem exec (exec adicionado por buildPrimaryOnstart).</done>
</task>

<task type="auto">
  <name>Task 2: Create gateway/internal/primary/lifecycle.go — buildPrimaryCreateRequest with SHA fail-fast precondition per reviews #6 + Reconciler skeleton</name>
  <files>gateway/internal/primary/lifecycle.go</files>
  <read_first>
    - gateway/internal/emerg/lifecycle.go lines 1-22 (imports), 70-105 (VastAPI + HealthChecker + const block), 770-826 (buildCreateRequest pattern)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 262-357 (lifecycle.go pattern spec with full buildCreateRequest body)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Pitfall #8 (lines 494-525) — 4 port mappings invariant
    - gateway/internal/config/config.go (Plan 06.6-03 output — confirm 24 Primary* field names including PrimaryProvisionFailureCooldownSeconds + PrimaryPodScheduleProvisionLeadSeconds)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md lines 196-244 (Recommended Pre-execution Actions #6 SHA fail-fast)
  </read_first>
  <action>Criar gateway/internal/primary/lifecycle.go com `package primary`. Imports: "context", "errors", "fmt", "log/slog", "sync/atomic", "time", "github.com/ifixtelecom/gpu-ifix/gateway/internal/config", "github.com/ifixtelecom/gpu-ifix/gateway/internal/emerg/vast", "github.com/ifixtelecom/gpu-ifix/gateway/internal/vastutil". Definir struct `primaryPodURLs { LLM, STT, Embed, DCGM string }`. Definir struct `Deps { Cfg config.Config; Log *slog.Logger; Vast vastutil.VastDestroyer; HealthCheck func(ctx context.Context, url string) bool }` (campos mínimos para THIS plan; Plan 06.6-06a/06b adicionam Loader/DCGMScraper/Inflight/FSM/DB). Definir `Reconciler` struct + constructor `NewReconciler(deps Deps) *Reconciler`. **NOVO per reviews #6 — sentinel errors**: `var ( ErrMissingQwenSHA = errors.New("primary: PRIMARY_QWEN_WEIGHTS_SHA256 is empty — refusing to build pod request"); ErrMissingWhisperSHA = errors.New("primary: PRIMARY_WHISPER_WEIGHTS_SHA256 is empty — operator must set this env var explicitly (no default shipped)"); ErrMissingBGEM3SHA = errors.New("primary: PRIMARY_BGEM3_WEIGHTS_SHA256 is empty — operator must set this env var explicitly (no default shipped)") )`. **Função SIGNATURE MUDA** per reviews #6: `func (r *Reconciler) buildCreateRequest(offer vast.Offer, lifecycleID int64) (vast.CreateRequest, error)` — retorna error. **Primeira ação dentro da função**: SHA precondition gate: `if r.cfg.PrimaryQwenWeightsSHA256 == "" { return vast.CreateRequest{}, ErrMissingQwenSHA }; if r.cfg.PrimaryWhisperWeightsSHA256 == "" { return vast.CreateRequest{}, ErrMissingWhisperSHA }; if r.cfg.PrimaryBGEM3WeightsSHA256 == "" { return vast.CreateRequest{}, ErrMissingBGEM3SHA }` — comentário explicativo cita reviews consensus action #6. Resto do corpo segue PATTERNS.md:267-313 com renames Emergency→Primary, Disk=50 (WAVE0-GATES Decision 4), Label `fmt.Sprintf("ifix-primary-lifecycle-%d", lifecycleID)`. Build env map: 4 port forwards `"-p 8000:8000": "1"`, `"-p 8001:8001": "1"`, `"-p 8002:8002": "1"`, `"-p 9400:9400": "1"`, 4 MINIO_*, 3 PRIMARY_*_WEIGHTS_KEY + 3 PRIMARY_*_WEIGHTS_SHA256, 3 PRIMARY_*_IMAGE, condicional Jinja env, condicional POD_DEBUG_SSH_PUBLIC_KEY. Selecionar llamaArgs + jinjaPath conforme antes. onstart := buildPrimaryOnstart(llamaArgs, jinjaPath). Return `vast.CreateRequest{...}, nil`. Adicionar helpers stub `podLLMURL/podSTTURL/podEmbedURL/podDCGMURL`. Adicionar `ActivePodURLs()` getter.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./internal/primary/...</automated>
  </verify>
  <done>lifecycle.go compila. Reconciler struct + buildCreateRequest (com SHA fail-fast gate retornando explicit errors) exportadas. 3 sentinel errors definidos. Build agreement com config.go field names verificada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lifecycle_test.go — 17+ unit tests covering buildCreateRequest + onstart shell hardening + SHA fail-fast + D-03/D-07 traceability + MinIO non-empty</name>
  <files>gateway/internal/primary/lifecycle_test.go</files>
  <behavior>
    - TestBuildPrimaryCreateRequest_DinD_4Services: cfg com defaults completos (INCLUI WhisperSHA + BGEM3SHA non-empty test values) → req.Image == cfg.PrimaryTemplateImage (SHA-pinned form); req.Runtype == "args"; req.Onstart == "/bin/bash"; require.Len(req.Args, 2); req.Args[1] contém "exec /app/llama-server", "--jinja", "/weights/qwen/model.gguf", "dockerd", "speaches", "infinity", "dcgm"; req.Disk == 50; req.Label == "ifix-primary-lifecycle-7"; req.TargetState == "running"; require.NoError(err); 4 MINIO env non-empty asserts
    - TestBuildPrimaryCreateRequest_Has4PortMappings: 4 keys "-p 8000:8000", "-p 8001:8001", "-p 8002:8002", "-p 9400:9400" presentes em req.Env com valor "1"
    - TestBuildPrimaryCreateRequest_MinIOCredentialsNotEmpty: cfg com 4 MINIO_* set → all 4 keys present + values match cfg fields
    - TestBuildPrimaryCreateRequest_JinjaEmbeddedMode: cfg.PrimaryQwenJinjaKey="" → req.Args[1] NÃO contém "--chat-template-file" + req.Env não tem PRIMARY_QWEN_JINJA_KEY
    - TestBuildPrimaryCreateRequest_JinjaMinIOFallback: cfg.PrimaryQwenJinjaKey="qwen-templates/...jinja" + cfg.PrimaryQwenJinjaSHA256="abc..." → req.Args[1] contém "--chat-template-file /app/templates/qwen3.6.jinja" + req.Env tem ambos PRIMARY_QWEN_JINJA_KEY e PRIMARY_QWEN_JINJA_SHA256
    - TestBuildPrimaryCreateRequest_LlamaArgsOverride: cfg.PrimaryLlamaArgs=["--host","127.0.0.1","--port","9000"] → req.Args[1] termina com "exec /app/llama-server --host 127.0.0.1 --port 9000\n"
    - TestPrimaryOnstartLengthBelowLimit: len(req.Args[1]) < 14000 (Vast 16KB hard limit margin per Pitfall #1)
    - TestPrimaryOnstart_StartsWithSetEuo (per reviews #7): req.Args[1] (first 200 chars) contains `set -euo pipefail` — explicitly NOT `set -euxo`; `require.NotContains(t, req.Args[1][:200], "set -euxo", "set -x must NOT appear per reviews #7 — leaks MinIO creds in pod logs")`
    - TestPrimaryOnstart_NoSetX_NoSecretLeak (NEW per reviews #7): grep entire req.Args[1] for occurrences of `set -x` or `set -euxo` — assert ZERO occurrences (entire script must run without xtrace)
    - TestPrimaryOnstart_HasRequiredEnvGuards (NEW per reviews #7): req.Args[1] contains all required env guards in the form `: "${MINIO_ENDPOINT:?required}"`, `: "${MINIO_ACCESS_KEY:?required}"`, `: "${MINIO_SECRET_KEY:?required}"`, `: "${MINIO_BUCKET:?required}"`, `: "${PRIMARY_QWEN_WEIGHTS_SHA256:?required}"`, `: "${PRIMARY_WHISPER_WEIGHTS_SHA256:?required}"`, `: "${PRIMARY_BGEM3_WEIGHTS_SHA256:?required}"`, `: "${PRIMARY_TEMPLATE_IMAGE:?required}"` (8 mandatory guards minimum)
    - TestPrimaryOnstart_QuotedEnvExpansions (NEW per reviews #7): grep req.Args[1] for unquoted MinIO/Primary env expansions; assert `mc alias set` line uses `"$MINIO_ENDPOINT"` quoted form; assert NO occurrences of `$MINIO_ACCESS_KEY` outside quotes (regex: `[^"]\\$MINIO_ACCESS_KEY[^"]`)
    - TestPrimaryOnstart_NoLegacyImage: req.Args[1] NÃO contém substring "ifix-ai-pod"
    - TestPrimaryOnstart_ExecLlamaServerIsLastLine: strings.TrimSpace(req.Args[1]) termina com "/app/llama-server <flags>"
    - TestPrimaryOnstart_QwenWeightsInjected: req.Args[1] contém `"$PRIMARY_QWEN_WEIGHTS_SHA256"` (quoted) AND `"$PRIMARY_QWEN_WEIGHTS_KEY"`
    - TestBuildPrimaryCreateRequest_SSHDebugConditional: cfg.PodDebugSSHPublicKey="ssh-ed25519 AAA..." → req.Env tem POD_DEBUG_SSH_PUBLIC_KEY + "-p 22:22"; cfg empty → ambos ausentes
    - TestBuildPrimaryCreateRequest_D03_EngineImagePinnedSHA (revised per reviews Codex LOW): cfg com defaults → req.Image == "ghcr.io/ggml-org/llama.cpp:server-cuda-b9128@sha256:..." (contains `@sha256:` suffix — SHA-pinned, NOT tag-only — D-03 + WAVE0-GATES Decision 1)
    - TestPrimaryOnstart_DCGMPort9400Preserved: req.Args[1] contém "9400:9400" AND `"$PRIMARY_DCGM_IMAGE"` (quoted env expansion)
    - TestBuildPrimaryCreateRequest_RejectsEmptyWhisperSHA (NEW per reviews #6): cfg with PrimaryWhisperWeightsSHA256="" → req, err := r.buildCreateRequest(offer, 1); require.ErrorIs(t, err, ErrMissingWhisperSHA); require.Zero(t, req)
    - TestBuildPrimaryCreateRequest_RejectsEmptyBGEM3SHA (NEW per reviews #6): cfg with PrimaryBGEM3WeightsSHA256="" → require.ErrorIs(t, err, ErrMissingBGEM3SHA)
    - TestBuildPrimaryCreateRequest_RejectsEmptyQwenSHA (defensive, NEW per reviews #6): cfg with PrimaryQwenWeightsSHA256="" (defensive — Qwen HAS default but operator could override-to-empty) → require.ErrorIs(t, err, ErrMissingQwenSHA)
  </behavior>
  <read_first>
    - gateway/internal/emerg/lifecycle_test.go lines 226-417 (TestBuildCreateRequest_StrategyB_args + variants — pattern to mirror)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md lines 361-415 (lifecycle_test pattern spec with test naming table)
    - gateway/internal/primary/lifecycle.go (Task 2 output — confirm public/private symbols + sentinel errors)
    - gateway/internal/primary/onstart.go (Task 1 output — confirm const + builder + shell hardening)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md lines 196-244 (Recommended actions #6 #7 SHA fail-fast + shell hardening + #14 SHA-pinned image tests)
  </read_first>
  <action>Criar gateway/internal/primary/lifecycle_test.go com `package primary`. Imports: "testing", "strings", "regexp", "github.com/stretchr/testify/require", "github.com/ifixtelecom/gpu-ifix/gateway/internal/config", "github.com/ifixtelecom/gpu-ifix/gateway/internal/emerg/vast". Helper `cfgWithDefaults`: **AGORA INCLUI** `PrimaryWhisperWeightsSHA256: "wh1sp3r5h4..." (test value), PrimaryBGEM3WeightsSHA256: "bg3m35h4..."` (test values — per reviews #6 tests need non-empty SHAs to reach the rest of buildCreateRequest). Image fields use SHA-pinned form `"ghcr.io/ggml-org/llama.cpp:server-cuda-b9128@sha256:abc123..."` etc (per reviews #14 — tests assert SHA-pinned form). Escrever 19 testes da tabela <behavior>. Para TestPrimaryOnstart_NoSetX_NoSecretLeak usar `require.False(t, strings.Contains(req.Args[1], "set -x"), "set -x must not appear — reviews #7 secret leak hardening"); require.False(t, strings.Contains(req.Args[1], "set -euxo"), "set -euxo (with x) must not appear — reviews #7")`. Para TestPrimaryOnstart_HasRequiredEnvGuards loop sobre lista de 8 required env names: `for _, env := range []string{"MINIO_ENDPOINT","MINIO_BUCKET","MINIO_ACCESS_KEY","MINIO_SECRET_KEY","PRIMARY_QWEN_WEIGHTS_SHA256","PRIMARY_WHISPER_WEIGHTS_SHA256","PRIMARY_BGEM3_WEIGHTS_SHA256","PRIMARY_TEMPLATE_IMAGE"} { require.Contains(t, req.Args[1], `: "${`+env+`:?required}"`, "env guard missing for "+env) }`. Para TestPrimaryOnstart_QuotedEnvExpansions: regex `unquoted := regexp.MustCompile(`[^"]\$(MINIO|PRIMARY)_[A-Z_]+[^"}]`); matches := unquoted.FindAllString(req.Args[1], -1); require.Empty(t, matches, "found unquoted env expansions: %v — per reviews #7 ALL env expansions MUST be quoted", matches)`. Para SHA fail-fast tests: copy cfgWithDefaults + set específico SHA field to "" + assert ErrMissingXxxSHA returned.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test ./internal/primary/... -count=1 -race -run TestBuildPrimary -run TestPrimaryOnstart</automated>
  </verify>
  <done>19 testes verdes. Invariants enforced: PID 1 last-line, length < 14KB, 4 port mappings, Jinja conditional, no legacy image refs, D-03 engine SHA-pinned, D-07 DCGM port preservation, MinIO 4 credentials non-empty, shell hardening (set -euo + required env guards + quoted env expansions per reviews #7), SHA fail-fast precondition (Whisper + BGE + Qwen empty → explicit error per reviews #6).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway process → Vast.ai API | CreateInstance payload travels operator-controlled cfg values (no untrusted input) |
| Vast.ai pod (runtime) → MinIO (`s3.ifixtelecom.com.br`) | Pod onstart bash uses MINIO_* env (operator-supplied); sha256 verify on each download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-01 | Information Disclosure | MinIO credentials leaked in pod logs via `set -x` xtrace | mitigate | Reviews #7: primaryOnstartHead uses `set -euo pipefail` (NO `x`). TestPrimaryOnstart_NoSetX_NoSecretLeak enforces. Additionally: TestPrimaryOnstart_QuotedEnvExpansions ensures no unquoted `$VAR` that could behave unpredictably under whitespace/special-char values. |
| T-06.6-02 | Tampering | MinIO weight download (GGUF tampering) via missing SHA verification | mitigate | Reviews #6: buildCreateRequest precondition gate REJECTS build if Whisper/BGE/Qwen SHA empty (explicit sentinel errors ErrMissingWhisperSHA/BGEM3SHA/QwenSHA). Bash precondition guards `: "${VAR:?required}"` fail fast inside pod if SHA missing at runtime. sha256sum -c verify on every download. |
| T-06.6-SC | Tampering | OCI image references | mitigate | cfg.Primary*Image defaults are SHA-pinned (`tag@sha256:<digest>` form from WAVE0-GATES.md Decision 1). TestBuildPrimaryCreateRequest_D03_EngineImagePinnedSHA enforces. |
</threat_model>

<verification>
- `cd gateway && go build ./internal/primary/...` succeeds
- `cd gateway && go test ./internal/primary/... -count=1 -race` all green (19 tests)
- `cd gateway && grep -c 'fmt\\.Sprintf' gateway/internal/primary/onstart.go == 0` (Pitfall #9 invariant)
- `cd gateway && grep -c 'set -euxo' gateway/internal/primary/onstart.go == 0` (reviews #7 — no xtrace)
- `cd gateway && grep -c 'set -euo pipefail' gateway/internal/primary/onstart.go == 1` (reviews #7 — correct guard)
- `cd gateway && grep -c ':?required' gateway/internal/primary/onstart.go >= 8` (reviews #7 — env guards present)
- `cd gateway && grep -c 'ifix-ai-pod' gateway/internal/primary/onstart.go == 0` (no legacy image)
- `cd gateway && grep -c '\"exec /app/llama-server \"' gateway/internal/primary/onstart.go == 1` (PID 1 invariant via builder)
- `cd gateway && grep -c '9400:9400' gateway/internal/primary/onstart.go >= 1` (D-07 DCGM port preserved)
- `cd gateway && grep -c 'ErrMissingWhisperSHA\\|ErrMissingBGEM3SHA\\|ErrMissingQwenSHA' gateway/internal/primary/lifecycle.go == 3` (reviews #6 — 3 fail-fast sentinels)
</verification>

<success_criteria>
- gateway/internal/primary/ package is the foundation for schedule.go (06.6-05) + reconciler.go (06.6-06a) + adapters (06.6-06b)
- buildCreateRequest produces a Strategy B payload structurally identical to Phase 6 emergency BUT with hardened shell (no xtrace, quoted env, required guards) + SHA fail-fast precondition
- 4-service DinD orchestration embedded in inline bash (per WAVE0-GATES Decision 2)
- Jinja strategy flexible (B1 default, B2 fallback) via cfg.PrimaryQwenJinjaKey conditional
- Reviews consensus actions addressed: #6 (SHA fail-fast — Whisper/BGE/Qwen empty → ErrMissingXxxSHA explicit error), #7 (shell hardening — no set -x, quoted env, required guards), #14 (SHA-pinned image defaults asserted)
- D-03 engine image SHA-pinned + asserted
- D-07 DCGM port preserved + asserted
- MinIO 4 credentials non-empty asserted
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-04-SUMMARY.md`.
</output>
