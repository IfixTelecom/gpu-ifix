---
phase: 06.6
plan: 10
type: execute
wave: 4
depends_on: [06.6-06a, 06.6-06b, 06.6-07, 06.6-08, 06.6-09]
files_modified:
  - gateway/internal/integration_test/primary_probe_test.go
  - gateway/internal/integration_test/primary_supervisord_test.go
  - gateway/internal/integration_test/primary_leader_test.go
  - gateway/internal/integration_test/primary_cancel_inflight_test.go
  - gateway/internal/integration_test/primary_emerg_coexist_test.go
  - gateway/internal/integration_test/primary_disabled_force_up_test.go
  - gateway/internal/integration_test/primary_overnight_schedule_test.go
  - gateway/internal/integration_test/primary_restart_recovery_test.go
  - gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go
  - gateway/internal/primary/export_test.go
autonomous: true
requirements: []
tags: [integration, wave-4, tests, primary-pod, pitfall-11, leader-election, cli-db-coverage, reviews, wave0-supervisord]

must_haves:
  truths:
    - "9 arquivos de integration test em gateway/internal/integration_test/ (8) + gateway/cmd/gatewayctl/ (1) usando build tag `//go:build integration` (parity emerg_*_test.go pattern)"
    - "**primary_supervisord_test.go (NEW Wave 0 — REPLACES any DinD test)** cobre supervisord-multi-process invariants: (a) markReady triggers OverrideTier0 for 3 roles (llm/stt/embed) + DCGMScraper.SetURL when 4 endpoint probes pass; (b) one-endpoint-unhealthy → markReady NOT called (FSM stays Provisioning); (c) post-Ready, if any endpoint goes unhealthy during evaluateReady, breaker logic detects + can trigger drain. Tests use mock HealthCheck per-URL to simulate supervisord child process crash scenarios (we don't run real supervisord in CI — that's Live UAT scope in Plan 11)."
    - "primary_probe_test.go cobre markReady + 3-role OverrideTier0 + DCGMScraper.SetURL via testcontainers Postgres + miniredis — **updated per Wave 0 to assert 4-endpoint reachability on container localhost ports** (NOT 4 separate sidecar runtime endpoints)"
    - "primary_leader_test.go cobre 2-replicas-1-leader scenario via redsync"
    - "primary_cancel_inflight_test.go cobre Layer 1+2+3 cancel-in-flight triple-layer"
    - "primary_emerg_coexist_test.go cobre Pitfall #11"
    - "**primary_disabled_force_up_test.go (reviews #2)** cobre `disabled+force_up_request → provisions` AND `disabled+no_event → stays asleep`"
    - "**primary_overnight_schedule_test.go (reviews #5)** cobre overnight wrap day-filter semantics"
    - "**primary_restart_recovery_test.go (reviews #4)** cobre Reconciler.recoverOpenLifecycle: healthy instance → restore FSM=Ready + tier-0 + DCGM URL; orphan instance → close lifecycle with shutdown_reason=gateway_restart_orphan + FSM stays Asleep"
    - "primary_lifecycles_integration_test.go cobre gatewayctl primary lifecycles DB fetch path"
    - "export_test.go expõe test-only setter SetScheduleRuleForTest"
    - "**Wave 0: NO DinD-specific tests.** DinD design REJECTED per Spike Task 1 (06.6-SPIKE-dind-privileged.md overlayfs fail). All tests assume supervisord-co-located 4-service single-container model. Any pre-Wave-0 test referencing DinD/dockerd/nested-docker-run is OUT OF SCOPE for Phase 6.6."
    - "Todos rodam via `go test -tags=integration ./internal/integration_test/... ./cmd/gatewayctl/...`"
  artifacts:
    - path: "gateway/internal/integration_test/primary_probe_test.go"
      provides: "markReady + 3-role tier-0 override + DCGM SetURL E2E + 4-endpoint reachability assertion (Wave 0 supervisord single-container)"
      contains: "OverrideTier0|primaryPodURLs|DCGMScraper\\.SetURL"
    - path: "gateway/internal/integration_test/primary_supervisord_test.go"
      provides: "Wave 0 supervisord multi-process invariants (replaces any DinD test)"
      contains: "TestSupervisord_4ServicesReachableOnLocalhost|TestSupervisord_OneEndpointDown_DoesNotPromoteToReady|TestSupervisord_AutorestartSimulated_RecoveryAfterTransientFailure"
    - path: "gateway/internal/integration_test/primary_leader_test.go"
      provides: "Leader-election 2-replicas-1-leader"
      contains: "redsync|isLeader|NewPrimaryRedsync"
    - path: "gateway/internal/integration_test/primary_cancel_inflight_test.go"
      provides: "Triple-layer cancel-in-flight coverage"
      contains: "cancelActiveLifecycle|cancel_in_flight|BestEffortDestroy|SetScheduleRuleForTest"
    - path: "gateway/internal/integration_test/primary_emerg_coexist_test.go"
      provides: "Pitfall #11 coexistence"
      contains: "primary_ready|StateEmergencyActive|primary_took_over"
    - path: "gateway/internal/integration_test/primary_disabled_force_up_test.go"
      provides: "Reviews #2 — DISABLED+force_up flow integration coverage"
      contains: "PrimaryPodScheduleDisabled|force_up_request|handleForceUpRequest"
    - path: "gateway/internal/integration_test/primary_overnight_schedule_test.go"
      provides: "Reviews #5 — overnight schedule day-filter wrap semantics"
      contains: "UpHour:\\s*22|DownHour:\\s*8|Days.*time\\.Monday|IsInPeak"
    - path: "gateway/internal/integration_test/primary_restart_recovery_test.go"
      provides: "Reviews #4 — restart recovery integration coverage"
      contains: "recoverOpenLifecycle|gateway_restart_orphan|GetOpenPrimaryLifecycle"
    - path: "gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go"
      provides: "gatewayctl primary lifecycles DB-fetch CLI coverage (BLOCKER 3)"
      contains: "TestRunPrimaryLifecycles_FetchesFromDB|testcontainers|primary_lifecycles"
    - path: "gateway/internal/primary/export_test.go"
      provides: "Test-only ScheduleRule setter"
      contains: "SetScheduleRuleForTest"
  key_links:
    - from: "primary_supervisord_test.go (Wave 0)"
      to: "primary.Reconciler.evaluateProvisioning 4-endpoint health check (Plan 06.6-06a)"
      via: "fake HealthCheck per-URL"
      pattern: "HealthCheck|evaluateProvisioning"
    - from: "primary_emerg_coexist_test.go"
      to: "emerg.Reconciler.SubscribePrimaryEvents (Plan 06.6-08)"
      via: "miniredis Pub/Sub"
      pattern: "PrimaryEvent\\{.*Type:.*primary_ready"
    - from: "primary_disabled_force_up_test.go (reviews #2)"
      to: "primary.Reconciler.handleForceUpRequest (Plan 06.6-06a) + Start() unconditional"
      via: "PublishPrimaryEvent type=force_up_request"
      pattern: "force_up_request"
    - from: "primary_overnight_schedule_test.go (reviews #5)"
      to: "primary.ScheduleRule.IsInPeak overnight-wrap day-filter (Plan 06.6-05)"
      via: "IsInPeak(tue02h00) with Days={mon} or Days={tue}"
      pattern: "Days\\[time\\.Monday\\]"
    - from: "primary_restart_recovery_test.go (reviews #4)"
      to: "primary.Reconciler.recoverOpenLifecycle (Plan 06.6-06a) + GetOpenPrimaryLifecycle SQL (Plan 06.6-07)"
      via: "DB seed open row + Vast mock + healthcheck mock"
      pattern: "recoverOpenLifecycle|gateway_restart_orphan"

threat_refs: [T-06.6-04]
---

<objective>
Adicionar 9 integration tests + 1 export_test.go cobrindo paths que unit tests não conseguem (cross-process, multi-replica, Pub/Sub, CLI DB-fetch, restart recovery, Wave 0 supervisord 4-service invariants).

**Wave 0 material change**: any pre-Wave-0 DinD-specific test (`primary_dind_test.go`, `primary_nested_docker_test.go`, etc.) is OUT OF SCOPE — DinD design rejected per 06.6-SPIKE-dind-privileged.md. REPLACED by `primary_supervisord_test.go` covering single-container 4-service supervisord model (markReady + autorestart sim + 1-endpoint-down detection). Pre-Wave-0 cancel-inflight test stays (cancel triple-layer is orthogonal to orchestration).

Plan extended per reviews consensus actions: #2 (disabled+force_up integration test), #4 (restart recovery integration test), #5 (overnight schedule day-filter integration test).

Per BLOCKER 3 revision: o 5º teste (primary_lifecycles_integration_test.go) fecha o gap zero-coverage da DB-fetch CLI path. Per WARNING 4 revision: export_test.go adicionado para safely mutate ScheduleRule em integration tests.

Output: 10 arquivos novos (9 integration tests + 1 export_test.go). Plan 06.6-11 (Live UAT) depende destes para regression confidence ANTES de gastar Vast.ai $$.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-PATTERNS.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md
@.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md
@gateway/internal/integration_test
@gateway/internal/primary/reconciler.go
@gateway/internal/emerg/subscribe.go
@gateway/internal/redisx/primary.go
@gateway/internal/redisx/emerg.go
@gateway/cmd/gatewayctl/primary.go
@gateway/cmd/gatewayctl/emerg_test.go

<interfaces>
<!-- Build tag pattern (first 2 lines of each integration test) -->
```go
//go:build integration
// +build integration
```

<!-- 9 new test files specs -->

1. primary_probe_test.go — markReady + 3-role tier-0 override + DCGM SetURL + Wave 0 4-endpoint reachability assertion via testcontainers + miniredis (1 test: TestPrimaryProbe_MarkReady_OverridesTier03Roles_4EndpointsReachable)
2. **primary_supervisord_test.go (NEW Wave 0)** — 3 tests covering supervisord single-container 4-service invariants:
   - TestSupervisord_4ServicesReachableOnLocalhost: setup Reconciler with fake VastAPI.GetInstance returning Ports map {8000:33000, 8001:33001, 8002:33002, 9400:33400}; fake HealthCheck returns true for all 4 derived URLs (LLM/STT/Embed/DCGM); trigger evaluateProvisioning poll iteration; assert markReady called → activePodURLs populated with non-empty LLM+STT+Embed+DCGM strings
   - TestSupervisord_OneEndpointDown_DoesNotPromoteToReady: same setup, BUT fake HealthCheck returns true for LLM/STT/DCGM and FALSE for Embed; assert markReady NOT called; FSM stays StateProvisioning until budget or all-healthy
   - TestSupervisord_AutorestartSimulated_RecoveryAfterTransientFailure: simulate supervisord child autorestart — first HealthCheck call returns false for Embed, second call returns true (autorestart completes); poll budget allows 2 attempts; assert second attempt promotes to Ready
3. primary_leader_test.go — 2 reconcilers + redsync (1 test: TestPrimaryLeader_OnlyOneActiveLeader)
4. primary_cancel_inflight_test.go — Layer 1+2+3 cancel-in-flight; uses SetScheduleRuleForTest atomic replacement (1 test)
5. primary_emerg_coexist_test.go — Pitfall #11 (1 test: TestEmergCoexist_PrimaryReadyForceDestroysEmerg)
6. **primary_disabled_force_up_test.go (reviews #2)** — 2 tests:
   - TestPrimaryDisabled_ForceUpRequest_Provisions: cfg.PrimaryPodScheduleDisabled=true; start Reconciler; publish force_up_request via miniredis; assert FSM transitions Asleep→Provisioning within 5s
   - TestPrimaryDisabled_NoEvent_StaysAsleep: cfg.PrimaryPodScheduleDisabled=true; start Reconciler; wait 10s; assert FSM stays Asleep (schedule ticks skipped)
7. **primary_overnight_schedule_test.go (reviews #5)** — 4 tests:
   - TestOvernightSchedule_MonOnly_TueEarly_IsInPeak: rule UP=22 DOWN=8, Days={mon: true}, now=Tuesday 02:00 BRT → IsInPeak=true
   - TestOvernightSchedule_TueOnly_TueEarly_IsNotInPeak: same now, Days={tue: true}; require.False
   - TestOvernightSchedule_MonOnly_TueLate_IsNotInPeak: now=Tue 23:00, Days={mon:true} → require.False
   - TestOvernightSchedule_TueOnly_TueLate_IsInPeak: now=Tue 23:00, Days={tue:true} → require.True
8. **primary_restart_recovery_test.go (reviews #4)** — 4 tests covering healthy/orphan/unhealthy/no-row branches
9. primary_lifecycles_integration_test.go (BLOCKER 3) — gatewayctl primary lifecycles DB-fetch (3 tests)
10. export_test.go — package primary + SetScheduleRuleForTest test-only setter
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: export_test.go + primary_probe_test.go (Wave 0 4-endpoint reachability) + primary_supervisord_test.go (Wave 0 NEW) + primary_leader_test.go + primary_disabled_force_up_test.go (reviews #2)</name>
  <files>gateway/internal/primary/export_test.go, gateway/internal/integration_test/primary_probe_test.go, gateway/internal/integration_test/primary_supervisord_test.go, gateway/internal/integration_test/primary_leader_test.go, gateway/internal/integration_test/primary_disabled_force_up_test.go</files>
  <behavior>
    - export_test.go: package primary + `func SetScheduleRuleForTest(t *testing.T, r *Reconciler, rule ScheduleRule) { t.Helper(); r.rule = rule }`
    - primary_probe_test.go (1 test): TestPrimaryProbe_MarkReady_OverridesTier03Roles_4EndpointsReachable — setup full stack with fake VastAPI returning Ports {8000:33000, 8001:33001, 8002:33002, 9400:33400}; fake HealthCheck returns true for all 4 derived URLs; call reconciler.markReady; assert 3 OverrideTier0 + DCGMScraper.SetURL + DB row marked healthy + FSM=Ready
    - **primary_supervisord_test.go (3 tests per Wave 0)**:
      - TestSupervisord_4ServicesReachableOnLocalhost: setup Reconciler with fake VastAPI.GetInstance returning Ports map with all 4 host ports; fake HealthCheck returns true for all 4 URLs derived from those ports; call evaluateProvisioning poll iteration (export wrapper if needed); assert markReady invoked + activePodURLs has non-empty LLM+STT+Embed+DCGM strings; assert Loader.OverrideTier0 called 3 times (llm, stt, embed) + DCGMScraper.SetURL called with DCGM URL
      - TestSupervisord_OneEndpointDown_DoesNotPromoteToReady: same setup with one HealthCheck returning false (per-URL routing); assert markReady NOT called; FSM stays Provisioning
      - TestSupervisord_AutorestartSimulated_RecoveryAfterTransientFailure: fake HealthCheck that returns false on first call for Embed and true on subsequent calls; advance poll loop 2 iterations (15s ticker, use shorter test interval); assert second iteration promotes to Ready
    - primary_leader_test.go (1 test): TestPrimaryLeader_OnlyOneActiveLeader — 2 reconcilers; Start both; within 5s assert exactly 1 isLeader
    - **primary_disabled_force_up_test.go (2 tests per reviews #2)**:
      - TestPrimaryDisabled_ForceUpRequest_Provisions: setup minimal stack with cfg.PrimaryPodScheduleDisabled=true + cfg.PrimaryProvisionFailureCooldownSeconds=0; create Reconciler + go r.Start(ctx); after 100ms, publish PrimaryEvent{Type:"force_up_request", Reason:"reviews_2_uat", ReplicaID:"test"} via miniredis publish; require.Eventually(func() bool { return r.deps.FSM.State() == StateProvisioning }, 5s, 100ms) — proves event subscriber runs even under DISABLED AND force-up bypasses schedule gate
      - TestPrimaryDisabled_NoEvent_StaysAsleep: setup minimal stack with cfg.PrimaryPodScheduleDisabled=true + rule s.t. ShouldBeProvisioned would return true under normal conditions; Start reconciler; wait 10s; require.Equal(t, StateAsleep, r.deps.FSM.State()) — proves schedule loop respects DISABLED at tick-decision-point
  </behavior>
  <read_first>
    - gateway/internal/integration_test/emerg_provision_happy_test.go (testcontainers + miniredis harness pattern)
    - gateway/internal/integration_test/emerg_leader_test.go (redsync leader-election pattern)
    - gateway/internal/primary/reconciler.go (Plan 06.6-06a — markReady signature; Start; isLeader field; AND confirm r.rule field type for export_test.go setter; AND confirm handleForceUpRequest path; confirm evaluateProvisioning 4-endpoint poll logic)
    - gateway/internal/redisx/primary.go (Plan 06.6-07 — PublishPrimaryEvent + SubscribePrimaryEvents)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md (action #2 disabled+force_up)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-WAVE0-GATES.md Decision 2 (supervisord 4-service)
  </read_first>
  <action>(a) Criar export_test.go com `package primary` + import "testing" + `func SetScheduleRuleForTest(t *testing.T, r *Reconciler, rule ScheduleRule) { t.Helper(); r.rule = rule }`. Comment: `// SetScheduleRuleForTest é test-only — ScheduleRule é immutable em produção. Per WARNING 4 revision.`. (b) Criar primary_probe_test.go com build tag header + `package integration_test`. Helper `setupPrimaryTestStack` returns Reconciler with all 11 Deps wired via fakes (including fakeVastAPI with Ports map + fakeHealthCheck per-URL configurable). Test usa helper + invoca markReady. (c) **NEW Wave 0 — Criar primary_supervisord_test.go** com 3 tests covering supervisord 4-endpoint invariants. Reuse setupPrimaryTestStack helper. Critical: each test must wire fakeHealthCheck `func(ctx, url string) bool` with per-URL behavior (e.g., return true if strings.Contains(url, ":33000") || ":33001") || ":33002") || ":33400")). For autorestart sim: use atomic counter inside fakeHealthCheck closure — return false on first call for Embed URL, true on subsequent. (d) Criar primary_leader_test.go análogo emerg_leader_test.go com 2 Reconciler. (e) **NEW per reviews #2 — Criar primary_disabled_force_up_test.go** com 2 tests: setup minimal stack via testcontainers Postgres + miniredis + fake Vast (CreateInstance returns 999, HealthCheck always true); construct Reconciler com cfg.PrimaryPodScheduleDisabled=true; `go r.Start(ctx)`; for ForceUpRequest test publish via `redisClient.Publish(ctx, redisx.PrimaryEventsChannel, jsonEvent)`; for NoEvent test just wait + assert FSM stays Asleep.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go build ./internal/primary/... && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=5m -run 'TestPrimaryProbe|TestSupervisord|TestPrimaryLeader|TestPrimaryDisabled'</automated>
  </verify>
  <acceptance_criteria>
    - export_test.go compiles
    - 7 integration tests pass (1 probe + 3 supervisord + 1 leader + 2 disabled-force-up per reviews #2)
    - Wave 0 supervisord 4-service invariants mechanically proven via primary_supervisord_test.go
    - Reviews #2 semantic resolution mechanically proven
  </acceptance_criteria>
  <done>7 integration tests verdes + export_test.go compila. Wave 0 supervisord 4-service model proved via primary_supervisord_test.go. Reviews #2 semantic resolution mechanically proved via primary_disabled_force_up_test.go.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: primary_cancel_inflight_test.go + primary_emerg_coexist_test.go (Pitfall #11) + primary_overnight_schedule_test.go (reviews #5)</name>
  <files>gateway/internal/integration_test/primary_cancel_inflight_test.go, gateway/internal/integration_test/primary_emerg_coexist_test.go, gateway/internal/integration_test/primary_overnight_schedule_test.go</files>
  <behavior>
    - primary_cancel_inflight_test.go (1 test): TestPrimaryCancelInflight_TripleLayer — Setup with fakeVast CreateInstance=999, HealthCheck always false, short cold-start budget; advance FSM Asleep→Provisioning; after 1s construct **fresh ScheduleRule via value-copy + mutation of COPY** (NOT mutation of original — ScheduleRule immutable) + `primary.SetScheduleRuleForTest(t, r, freshRule)` → trigger evaluateTick → assert (a) ctx.Done fires (Layer 1), (b) 1 Pub/Sub event with Type="cancel_in_flight" (Layer 2), (c) fakeVast.destroyed contains 999 (Layer 3)
    - primary_emerg_coexist_test.go (1 test): TestEmergCoexist_PrimaryReadyForceDestroysEmerg — setup 2 reconcilers; emerg seeded with FSM=StateEmergencyActive + fakeVast.activeID=888; spawn emerg.SubscribePrimaryEvents; primary publishes PrimaryEvent{Type:"primary_ready"}; require.Eventually(emerg.FSM.State() != StateEmergencyActive && contains(fakeVast.destroyed, 888), 30s)
    - **primary_overnight_schedule_test.go (4 tests per reviews #5)**:
      - TestOvernightSchedule_MonOnly_TueEarly_IsInPeak: rule := ScheduleRule{Timezone: brt, UpHour: 22, DownHour: 8, Days: map[time.Weekday]bool{time.Monday: true}}; now := time.Date(2026, 5, 12, 2, 0, 0, 0, brt) // Tue 02:00; require.True(t, rule.IsInPeak(now))
      - TestOvernightSchedule_TueOnly_TueEarly_IsNotInPeak: same now, Days={tue:true}; require.False
      - TestOvernightSchedule_MonOnly_TueLate_IsNotInPeak: now=Tue 23:00, Days={mon:true} → require.False (Tue's bit applies for hour>=UpHour; tue NOT enabled)
      - TestOvernightSchedule_TueOnly_TueLate_IsInPeak: now=Tue 23:00, Days={tue:true} → require.True
  </behavior>
  <read_first>
    - gateway/internal/integration_test/emerg_cancel_post_create_test.go (analog cancel-in-flight)
    - gateway/internal/primary/reconciler.go (cancelActiveLifecycle)
    - gateway/internal/primary/export_test.go (Task 1 — SetScheduleRuleForTest)
    - gateway/internal/primary/schedule.go (Plan 06.6-05 — IsInPeak overnight wrap day-filter per reviews #5)
    - gateway/internal/emerg/subscribe.go (Plan 06.6-08 — SubscribePrimaryEvents)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-RESEARCH.md §Pitfall #11
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md (action #5 overnight wrap)
  </read_first>
  <action>Criar primary_cancel_inflight_test.go — value-copy ScheduleRule + SetScheduleRuleForTest replace. Assert 3 channels. Criar primary_emerg_coexist_test.go (mesmo conteúdo anterior — sem mudanças reviews). **NEW per reviews #5** — Criar primary_overnight_schedule_test.go com build tag + `package integration_test`. NÃO precisa testcontainers — pure schedule.IsInPeak tests, mas mantém build tag para keep them grouped with other primary integration tests. brt := time.FixedZone("BRT", -3*3600). 4 tests construindo ScheduleRule manually e calling IsInPeak.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -tags=integration ./internal/integration_test/... -count=1 -timeout=10m -run 'TestPrimaryCancelInflight|TestEmergCoexist|TestOvernightSchedule'</automated>
  </verify>
  <acceptance_criteria>
    - 6 integration tests pass (1 cancel + 1 coexist + 4 overnight schedule)
    - Reviews #5 overnight wrap day-filter semantics mechanically proven
  </acceptance_criteria>
  <done>3 integration test files (6 tests total: 1 cancel + 1 coexist + 4 overnight schedule) verdes. Reviews #5 overnight wrap day-filter semantics mechanically proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: primary_restart_recovery_test.go (reviews #4) + primary_lifecycles_integration_test.go (BLOCKER 3)</name>
  <files>gateway/internal/integration_test/primary_restart_recovery_test.go, gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go</files>
  <behavior>
    - **primary_restart_recovery_test.go (4 tests per reviews #4)**:
      - TestRestartRecovery_HealthyInstanceRestoresReady: testcontainers Postgres + migration applied + INSERT primary_lifecycles row (id=42, vast_instance_id=777, started_at=now-1h, ended_at=NULL, first_health_pass_at=now-50min, trigger_reason="schedule_up"); fake VastAPI returns inst{ID:777, ActualStatus:"running", Ports: {8000:33000, 8001:33001, 8002:33002, 9400:33400}}; fake HealthCheck returns true for all 4; construct Reconciler with all fakes wired; call `r.recoverOpenLifecycle(ctx)` directly (export wrapper via export_test.go if needed); assert: (a) r.activeLifecycleID.Load() == 42, (b) r.activeInstanceID.Load() == 777, (c) Loader.OverrideTier0 called 3 times with roles llm/stt/embed, (d) DCGMScraper.SetURL called with the DCGM URL, (e) r.deps.FSM.State() == StateReady, (f) no rows in primary_lifecycles have ended_at != NULL (lifecycle still open)
      - TestRestartRecovery_OrphanInstance_ClosesLifecycle: same DB seed but fake VastAPI returns ErrNotFound for GetInstance(777); call recoverOpenLifecycle; assert: (a) row id=42 has ended_at!=NULL + shutdown_reason="gateway_restart_orphan", (b) FSM stays StateAsleep, (c) Loader.OverrideTier0 NOT called
      - TestRestartRecovery_UnhealthyInstance_ClosesLifecycle: same DB seed; fake VastAPI returns running; fake HealthCheck returns FALSE for STT endpoint; call recoverOpenLifecycle; assert row closed with shutdown_reason containing "gateway_restart_orphan" or "unhealthy"
      - TestRestartRecovery_NoOpenRow_NoOp: empty primary_lifecycles table; call recoverOpenLifecycle; assert no error + FSM stays Asleep + no OverrideTier0 calls
    - primary_lifecycles_integration_test.go (3 tests, BLOCKER 3):
      - TestRunPrimaryLifecycles_FetchesFromDB: seed 3 rows + invoke runPrimaryLifecycles; assert 3 rows in stdout ordered DESC
      - TestRunPrimaryLifecycles_RespectsLimitFlag: seed 5 rows + invoke with --limit 2; assert 2 rows
      - TestRunPrimaryLifecycles_EmptyTable_NoRows: empty table; assert headers-only or "no lifecycles" message
  </behavior>
  <read_first>
    - gateway/cmd/gatewayctl/emerg_test.go (full file — lines 14-19 deferral pattern)
    - gateway/cmd/gatewayctl/primary.go (Plan 06.6-09 Task 1 — runPrimaryLifecycles signature)
    - gateway/db/migrations/*primary_lifecycles.sql (Plan 06.6-07 computed-name file — schema to apply)
    - gateway/internal/db/sqlc/primary_lifecycles.sql.go (Plan 06.6-07 generated — GetOpenPrimaryLifecycle binding for reviews #4)
    - gateway/internal/primary/reconciler.go (Plan 06.6-06a — recoverOpenLifecycle implementation per reviews #4)
    - gateway/internal/integration_test/emerg_provision_happy_test.go (testcontainers Postgres harness pattern)
    - .planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-REVIEWS.md (action #4 restart recovery integration coverage)
  </read_first>
  <action>(a) **NEW per reviews #4** — Criar primary_restart_recovery_test.go com build tag + `package integration_test`. Imports: testcontainers postgres + pgxpool + miniredis + testify + primary + redisx + fakes. Helper `setupRestartRecoveryStack(t *testing.T) (*pgxpool.Pool, *primary.Reconciler, *fakes)`: testcontainers postgres started; apply migration via raw SQL exec (load `gateway/db/migrations/*primary_lifecycles.sql` content); pgxpool to test container; construct Reconciler with fake Loader/DCGMScraper/Inflight/VastAPI/HealthCheck/FSM + real pgxpool. Helper `seedOpenLifecycle(t *testing.T, pool *pgxpool.Pool, id int64, instanceID int64)` INSERT row. Write 4 tests from <behavior>. For TestRestartRecovery_HealthyInstanceRestoresReady fakeVast.GetInstanceFunc returns inst with Ports map; fakeHealthCheck always true; call `r.recoverOpenLifecycle(ctx)`; assert via fakeLoader.overrides + fakeDCGM.url + FSM state. (b) Criar primary_lifecycles_integration_test.go (BLOCKER 3) — sem mudanças reviews; 3 tests usando testcontainers + capture stdout.</action>
  <verify>
    <automated>cd /home/pedro/projetos/pedro/gpu-ifix/gateway && go test -tags=integration ./internal/integration_test/... ./cmd/gatewayctl/... -count=1 -timeout=15m -run 'TestRestartRecovery|TestRunPrimaryLifecycles'</automated>
  </verify>
  <acceptance_criteria>
    - 7 integration tests pass (4 restart recovery per reviews #4 + 3 lifecycles BLOCKER 3)
    - Restart recovery integration coverage mechanically proven
    - DB-fetch CLI coverage closes BLOCKER 3
  </acceptance_criteria>
  <done>7 integration tests verdes (4 restart recovery per reviews #4 + 3 lifecycles BLOCKER 3). Restart recovery integration coverage mechanically proven. DB-fetch CLI coverage closes BLOCKER 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(Integration tests run in CI; same trust boundaries as unit tests; no new prod surface.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-04 | DoS | emerg + primary double-provision race | mitigate | primary_emerg_coexist_test.go provides mechanical proof of Pitfall #11 mitigation. primary_disabled_force_up_test.go (reviews #2) proves operator can override soak-gate via force-up event. primary_restart_recovery_test.go (reviews #4) proves restart doesn't orphan or double-provision. primary_overnight_schedule_test.go (reviews #5) proves overnight wrap correctness — no rogue Tuesday 02:00 provisioning when Monday disabled. primary_supervisord_test.go (Wave 0) proves 4-endpoint health check requires ALL 4 supervisord children healthy before promotion to Ready. |
</threat_model>

<verification>
- `cd gateway && go test -tags=integration ./internal/integration_test/... ./cmd/gatewayctl/... -count=1 -timeout=15m` all green
- `cd gateway && grep -c '//go:build integration' gateway/internal/integration_test/primary_*.go == 8` (all 8 integration test files have build tag)
- `cd gateway && grep -q 'SetScheduleRuleForTest' gateway/internal/primary/export_test.go`
- `cd gateway && grep -q '//go:build integration' gateway/cmd/gatewayctl/primary_lifecycles_integration_test.go`
- `cd gateway && ls gateway/internal/integration_test/primary_supervisord_test.go` (Wave 0 supervisord test file present)
- `cd gateway && ls gateway/internal/integration_test/primary_disabled_force_up_test.go` (reviews #2 file present)
- `cd gateway && ls gateway/internal/integration_test/primary_overnight_schedule_test.go` (reviews #5 file present)
- `cd gateway && ls gateway/internal/integration_test/primary_restart_recovery_test.go` (reviews #4 file present)
- `cd gateway && grep -c 'primary_dind_test\|primary_nested_docker_test\|primary_inner_docker_test' gateway/internal/integration_test/*.go 2>/dev/null` returns 0 (NO DinD-specific tests — Wave 0)
- `cd gateway && grep -q 'gateway_restart_orphan' gateway/internal/integration_test/primary_restart_recovery_test.go`
</verification>

<success_criteria>
- 9 integration tests cover paths that unit tests can't
- **primary_supervisord_test.go (Wave 0) replaces any pre-Wave-0 DinD test** — proves 4-endpoint supervisord single-container model
- Pitfall #11 mechanically proven (primary_emerg_coexist)
- Cancel-in-flight triple-layer fully covered WITHOUT violating ScheduleRule immutability (WARNING 4)
- Leader-election prevents double-provision
- DB-fetch CLI covered (BLOCKER 3)
- **Reviews #2 (disabled+force_up) mechanically proven via primary_disabled_force_up_test.go**
- **Reviews #4 (restart recovery) mechanically proven via primary_restart_recovery_test.go**
- **Reviews #5 (overnight wrap day-filter) mechanically proven via primary_overnight_schedule_test.go**
- Plan 06.6-11 live UAT proceeds with confidence
- depends_on updated to [06.6-06a, 06.6-06b, 06.6-07, 06.6-08, 06.6-09] per reviews #1 split
- ZERO DinD-specific integration tests (Wave 0 REJECTED DinD)
</success_criteria>

<output>
Create `.planning/phases/06.6-primary-pod-refactor-strategy-b-full-stack-upstream-images-i/06.6-10-SUMMARY.md`.
</output>
