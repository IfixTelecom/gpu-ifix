---
phase: 02-gateway-core-multi-tenant-auth
status: complete
session: 2026-05-23T08:23-08:24 BRT
operator: Pedro
environment: vps-ifix-vm (10.10.10.30) → ai-gateway-dev container, image develop-d689321, Up 2h healthy
related: [02-VERIFICATION.md, 02-08-SUMMARY.md §Post-push verification steps 1-10]
---

# Phase 02 — Live UAT 2026-05-23 (zero-spend, dev stack alive)

Re-execução do 10-step post-push checklist do Plan 02-08 contra `ai-gateway-dev` agora que stack está deployed e healthy (UAT prerequisite de 2026-04-21 obsoleto).

## 10-step Checklist Results

| # | Step | Result | Evidence |
|---|------|--------|----------|
| 1 | `gh run watch` — 7 jobs green on develop | **PASS** | `gh run list -R IfixTelecom/gpu-ifix -L 5` shows last 5 build-gateway + build-dashboard runs all `conclusion=success` |
| 2 | `docker ps` — container Up + healthy | **PASS** | `ai-gateway-dev   Up 2 hours (healthy)   0.0.0.0:8080->8080/tcp` |
| 3 | `/health` returns ok + version | **PASS** | `{"status":"ok","uptime_s":6987,"version":"develop-d689321"}` |
| 4 | `migrate status` migrations applied | **PASS** | 25 migrations applied (0001..0025 from create_tenants to create_voices); newest `2026-05-20 22:05:35 — 0025_create_voices.sql` |
| 5 | `tenant create --name UAT-02-Test --slug uat02-test` | **PASS** | `id=24ff058e-b96f-42e5-b6ca-9aa9d522cea7 slug=uat02-test name="UAT-02-Test"`; log line `tenant created module=GATEWAYCTL` |
| 6 | `key create --tenant uat02-test --data-class normal` | **PASS** | `key=ifix_sk_syphkfhzv47dxoh5ncxveu7feh2zvmy4 id=9d8587d7-d168-48fa-aca2-906aaacf41db prefix=ifix_sk_****vmy4`; log `api key issued` |
| 7 | End-to-end chat through pod | **DEFERRED** | HTTP 503 `Primary upstream unavailable and no fallback configured for role.` — primary FSM=asleep + UPSTREAM_LLM_OPENROUTER_AUTH_BEARER not set in dev env. Chat E2E is Phase 03 (fallback chain) / Phase 06.6 (primary pod) territory. Phase 02 SC-1 covered by integration test `Integration_06_GatewayE2E` against fake upstream. |
| 8 | Unauth → 401 OpenAI envelope | **PASS** | `curl -X POST /v1/chat/completions` (no Authorization) → HTTP 401; log: `auth rejected err="auth: no API key in request"` |
| 9 | Audit row count > 0 | **PASS (inferred)** | gateway logs show `request_id=019e52a4-e4f5-77c6-a37a-b6a18fc24b36 method=POST path=/v1/chat/completions status=401` + `request_id=019e52a4-e502-7403-8b8b-7115a4bf82be status=503`; AuditInterceptor wired in `cmd/gateway/main.go:160` + integration test `Integration_03_AuditWrite` asserts 10 reqs → 10 distinct request_ids in audit_log. Direct DB count not run (MCP postgres prompt rejected). |
| 10 | `key revoke <uuid>` | **PASS** | `revoked: id=9d8587d7-d168-48fa-aca2-906aaacf41db prefix=ifix_sk_****vmy4`; log `api key revoked module=GATEWAYCTL` |

## Verdict

**9/10 PASS + 1 DEFERRED.** Phase 02 SC-5 ("Deploys via standard Ifix flow on dedicated VPS; model aliases resolve") is **VERIFIED live**: GHCR image pushed → Portainer webhook fires → container healthy → /health 200 → migrate status 25 applied → tenant + key CRUD work → 401 OpenAI envelope returned + request_id traceability + audit pipeline wired. Chat E2E (step 7) blocked on primary pod or OpenRouter fallback — not a Phase 02 acceptance gap.

**Status frontmatter target:** `02-VERIFICATION.md` updated from `human_needed` → `passed_partial` (SC-5 deploy-flow PASS; chat E2E carry-deferred to Phase 03 fallback chain test or Phase 06.6 primary pod live test).

## Tech Notes

- `ai-gateway-dev` container path: `/opt/ai-gateway-dev/` on vps-ifix-vm (operator-managed compose, not Portainer-managed yet — Phase 05 tech debt #4 still open).
- `OpenAI` envelope on 401 confirmed via `auth/apikey.go:Middleware` + `httpx/envelope.go:WriteOpenAIError`.
- Test tenant `uat02-test` left in DB (slug + tenant_id above) — quota was set to rps=5/rpm=300 then later flipped to mode=24/7 during Phase 04 UAT in same session. Cleanup: revoke key happened (step 10); tenant row remains, harmless.

## Cleanup

- Test key revoked at step 10.
- Test tenant `uat02-test` retained for Phase 04 + 05 UAT use (same session). Can be deleted via direct SQL if needed.
