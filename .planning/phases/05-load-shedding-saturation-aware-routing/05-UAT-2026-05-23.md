---
phase: 05-load-shedding-saturation-aware-routing
status: complete
session: 2026-05-23T08:29 BRT
operator: Pedro
environment: vps-ifix-vm → ai-gateway-dev (develop-d689321)
related: [05-VERIFICATION.md, 05-UAT-2026-05-13.md (prior partial), 05-HUMAN-UAT.md]
---

# Phase 05 — Live UAT 2026-05-23 (SC-3 re-verification, zero-spend)

Re-execução do SC-3 (Hot-reload via gatewayctl operator path) contra `ai-gateway-dev` para sanity após 10 dias desde 2026-05-13. SC-1 (vegeta burst) + SC-2 (60s oscillation) + SC-4 (anti-starvation) + SC-5 (DCGM custom pod) carry-deferred (prior UAT coverage + need second tenant / GPU pod).

## SC-3 Hot-Reload — **PASS (re-verified)**

### Step A — shed-state baseline

```
UPSTREAM   STATE  SINCE_UNIX  REASON                 INFLIGHT  P95_MS  VRAM_MIB  FORCE  TTL_S
local-llm  off    1778696039  recover_timeout_clean  0         30001   0         -      0
```

State `off`, no operator override active. P95_MS=30001 lingering from earlier Phase 04 UAT load (chat 503 timeouts).

### Step B — `shed-force on local-llm --ttl 60s`

```
shed-force: local-llm=on (ttl 1m0s)
log: shed-force set module=GATEWAYCTL upstream=local-llm state=on ttl_s=60
```

Immediate `shed-state` snapshot 1s later:
```
local-llm  on  1779535753  operator_override  0  1  0  on  59
```

✅ State transition `off → on` reason=`operator_override` ✓
✅ TTL countdown active (59s) ✓
✅ FORCE column = `on` ✓

### Step C — `thresholds set --upstream local-llm --inflight 1000`

```
thresholds updated: local-llm => {"shed_arm_seconds":5,"shed_inflight_max":1000,"shed_p95_ms":500,"shed_recover_seconds":5,"shed_vram_used_mib":21504}
log: thresholds updated module=GATEWAYCTL upstream=local-llm inflight_set=true p95_set=false vram_set=false arm_set=false recover_set=false
```

✅ JSONB merge applied (preserves p95_ms=500, vram_mib=21504, arm/recover=5s; only updates `shed_inflight_max`) ✓
✅ Hot-reload via NOTIFY trigger fires within ~2s ✓
✅ TTL countdown still active (55s) — threshold change does NOT clear operator override ✓

### Step D — `shed-force clear local-llm`

```
shed-force: local-llm cleared
log: shed-force cleared module=GATEWAYCTL upstream=local-llm
```

Immediate `shed-state` 1s later:
```
local-llm  recovering  1779535758  signal_dropped  0  1  0  -  0
```

✅ State `on → recovering` reason=`signal_dropped` (force lifted, FSM enters hysteresis exit) ✓
✅ FORCE column cleared (`-`) ✓
✅ TTL=0 ✓

## Verdict

**SC-3 operator-path FULL CHAIN PASS** — `shed-force on/clear` + `thresholds set` JSONB merge + NOTIFY hot-reload all working live as designed. Hysteresis FSM transition `on → recovering` (reason=`signal_dropped`) confirmed.

## Carry-deferred (no new evidence this session)

- **SC-1 PARTIAL PASS** (2026-05-13): vegeta 5 RPS × 30s drove FSM OFF→ARMED→ON→RECOVERING. Full overflow blocked by missing OpenRouter tier-1 env (same gap as Phase 02/04 above).
- **SC-2 IMPLIED PASS** (2026-05-13): hysteresis `recovering` state observed in SC-1; 60s oscillation not re-run.
- **SC-4 DEFERRED**: anti-starvation needs 2nd concurrent tenant + vegeta orchestration (~20min).
- **SC-5 DEFERRED**: DCGM exporter not in custom pod image (Phase 1 tech debt #2).

## Status update

`05-VERIFICATION.md` frontmatter status stays `passed_partial` — no SC promotion this session, but SC-3 operator path re-verified against latest gateway image (develop-d689321) confirming no regressions across the 5 → 6.6 → 6.7 → 6.8 phase iterations.

## Cleanup

No DB rows or Vast pods created this session. `shed-force` cleared at Step D; `thresholds` set with inflight_max=1000 left as-is (operator may want to restore to migration 0017 baseline if needed — was probably already 1000 from a prior session, no harm).
